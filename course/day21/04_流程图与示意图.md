# Day 21 流程图与示意图

**需求**：ZL-NA-REQ-021  
**用途**：课堂投屏、学员笔记配图

---

## 1. 今日总览：周测 + 整合

```mermaid
flowchart TB
    subgraph morning[上午 周测]
        QZ[sprint3_quiz.py 10题]
        REV[讲评 Day15-20]
    end
    subgraph afternoon[下午 整合]
        TR[ToolRegistry]
        TE[ToolExecutor]
        CO[ChatOrchestrator]
        DEMO[integrated_assistant_demo]
    end
    morning --> afternoon
    TR --> TE --> CO --> DEMO
```

## 2. Sprint 3 能力汇入编排器

```mermaid
flowchart LR
    D15[Token] --> T4[estimate_tokens]
    D17[Prompt] --> QZ[周测]
    D18[Intent] --> T3[intent_classify]
    D19[RAG] --> T2[rag_search]
    D20[FAQ+Emb] --> T1[faq_lookup]
    T1 --> ORCH[ChatOrchestrator]
    T2 --> ORCH
    T3 --> ORCH
    T4 --> ORCH
    ORCH --> OUT[统一用户回复]
```

## 3. 用户消息决策树

```mermaid
flowchart TD
    A[用户输入] --> B{"空?"}
    B -->|是| Z[请输入有效内容]
    B -->|否| C{"FAQ直答开启?"}
    C -->|否| G[chat_turn]
    C -->|是| D{faq_matcher.match}
    D -->|无匹配| G
    D -->|有| E{"score >= threshold?"}
    E -->|是| F["[FAQ直答] answer"]
    E -->|否| G
    G --> H{auto_route}
    H --> I[IntentRouter + RAG + LLM]
    I --> J[助手回复]
```

## 4. 四工具与底层服务映射

```mermaid
graph TB
    subgraph tools[ToolRegistry]
        FL[faq_lookup]
        RS[rag_search]
        IC[intent_classify]
        ET[estimate_tokens]
    end
    subgraph svc[服务]
        FAQ[SimilarQuestionMatcher]
        RAG[RAGContextService.retrieve_context]
        IR[IntentRouter.classify]
        TC[TokenCounter.estimate_text]
    end
    FL --> FAQ
    RS --> RAG
    IC --> IR
    ET --> TC
```

## 5. CLI 命令全景（Sprint 3 累计）

```mermaid
mindmap
  root((ChatAssistant命令))
    基础
      /help
      /exit
      /clear
    Day15-16
      /tokens
    Day18-20
      /intent
      /retrieve
      /similar
    Day21
      /tool list
      /tool name args
```

## 6. 测试金字塔

```mermaid
flowchart BT
    E2E[integrated_assistant_demo 脚本]
    INT[test_sprint3 编排与工具]
    UNIT[各服务单测 Day15-20]
    E2E --> INT --> UNIT
```

## 7. Day 21 → Day 22 衔接

```mermaid
sequenceDiagram
    participant Browser as 静态页 Day22
    participant API as FastAPI Day23
    participant Orch as ChatOrchestrator Day21

  Note over Browser,Orch: Day21 交付稳定 handle_message
    Browser->> "API: POST /chat"
    API->>Orch: handle_message
    Orch-->>API: reply
    API-->>Browser: JSON
```

## 8. 周测知识覆盖图

| 题号 | 知识点 | 对应 Day |
|------|--------|----------|
| 1 | Token 估算 | 15 |
| 2 | SSE 流式 | 16 |
| 3 | Prompt ConfigError | 17 |
| 4 | query_context_provider | 18-19 |
| 5 | chunk overlap | 19 |
| 6 | Embedding vs Keyword | 20 |
| 7 | cosine_similarity 范围 | 20 |
| 8 | /similar | 20 |
| 9 | use_embedding | 20 |
| 10 | Day21 整合 | 21 |

---

## 9. 上午周测流程（运营视角）

```mermaid
flowchart TD
    A[学员入场] --> B[发放纸质试卷]
    B --> C[09:00-09:45 闭卷作答]
    C --> D[机房 sprint3_quiz.py]
    D --> E{"得分>=60?"}
    E -->|是| F[下午 Lab 席位]
    E -->|否| G[晚自习补测名单]
    F --> H[09:45 讲评错题]
    G --> H
```

赵岩在流程图旁标注：周测不是为难学员，而是**对齐 Sprint 3 前半程共同语言**。若大量学员第 4、6 题错误，说明 Day 19–20 检索课件需加练，而非加快 Day 22 前端进度。

---

## 10. tool_demos 执行时序

```mermaid
sequenceDiagram
    participant Main as tool_demos.main
    participant BNT as build_nexus_tools
    participant TE as ToolExecutor
    participant FAQ as faq_matcher
    participant RAG as rag_service

    Main->>BNT: 注入四服务
    BNT-->>Main: ToolRegistry
    Main->>TE: new executor
    Main->>TE: execute faq_lookup
    TE->>FAQ: match
    FAQ-->>TE: FaqMatch
    TE-->>Main: ToolResult
    Main->>TE: execute rag_search
    TE->>RAG: retrieve_context
    RAG-->>TE: context str
```

林晓在 Lab 报告中需能手绘此图，标明**哪一步发生 fit 语料**（rag_service 首次 retrieve 时若已 index 则跳过）。

---

## 11. FAQ 直答与工具调用路径对比

| 路径 | 触发 | 调 LLM | 典型前缀 |
|------|------|--------|----------|
| handle_message 直答 | 自然语言 | 否 | [FAQ 直答·] |
| /tool faq_lookup | 命令 | 否 | [faq_lookup] |
| /similar | 命令 | 否 | sim= 摘要 |
| chat_turn | 自然语言 | 是 | 助手正文 |

```mermaid
flowchart LR
    subgraph 省钱路径
        A[高置信FAQ] --> B[直答]
    end
    subgraph 能力路径
        C[复杂问题] --> D[RAG+LLM]
    end
    subgraph 调试路径
        E["/tool"] --> F[单能力预览]
    end
```

---

## 12. Sprint 3 测试依赖图

```mermaid
graph BT
    T21[test_sprint3.py]
    T20[test_embedding.py]
    T19[test_rag.py]
    T21 --> T20
    T21 --> T19
    T20 --> T19
```

周航要求合并 CI 时按依赖顺序跑；任一日回归失败应阻塞发布。Day 21 测试**不 mock** FAQ 与 RAG 核心逻辑，仅 LLM 走 mock transport。

---

## 13. 智链科技团队沟通图（非技术）

```mermaid
flowchart TB
    赵岩[产品赵岩] -->|阈值需求| 陈默[架构陈默]
    陈默 -->|接口冻结| 林晓[开发林晓]
    林晓 -->|PR| 周航[DevOps周航]
    周航 -->|CI绿| 赵岩
```

今日站会新增沟通项：`handle_message` 返回字符串格式是否满足客服系统对接——结论：暂保持纯文本，Day 23 再议 JSON 包装。

---

## 14. 学员一日学习时间线

| 时刻 | 活动 | 产出 |
|------|------|------|
| 09:00 | 周测 | 分数 |
| 10:45 | 工具理论 | 笔记 |
| 14:00 | tool_demos | 终端日志 |
| 15:45 | integrated demo | 观察报告 |
| 18:30 | 晚自习 pytest | 12 passed |

此时间线供讲师控制节奏；若周测超时，压缩 10:45 理论，保证下午上机不少于 120 分钟。
