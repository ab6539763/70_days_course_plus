# Day 9 授课实录（节选）

**陈默**：抽象类不是语法炫技，是**防呆设计**。忘了写 validate，Python 在实例化子类时就会 TypeError——也比线上脏数据强。

**学员**：ChatMessage 要改很多吗？  
**陈默**：加一行继承，pytest 绿了就对。这就是有测试的好处。

**学员**：ModelConfig 和 Day 5 的 model 字段什么关系？  
**赵岩**：Day 5 读响应里的 model；ModelConfig 是发请求时的参数。方向相反，字段同名。

**林晓**（实操课）：写 Tag 模型时先写 validate 再写 to_dict，顺序反过来容易漏字段。

**陈默**（收尾）：明天 Day 10 整理 import，models 目录不要再往 day08 塞新类。今晚把 polymorphism_demo 跑通。

---

*整理：培训组*
