# Day 9 课后作业

## 题目一：概念题（20 分）

解释继承、抽象类、多态各一句话，并各举一个 NexusAgent 中的例子。

---

## 题目二：实现 MiniModel（25 分）

在 `exercises/day09/` 创建继承 `BaseModel` 的 `Tag` 类：字段 `name: str`，`validate` 要求非空且长度 ≤ 20。

---

## 题目三：ModelConfig 边界测试（20 分）

为 `ModelConfig` 补充 pytest：`temperature=2.1` 失败、`max_tokens=0` 失败。

---

## 题目四：阅读 polymorphism_demo（20 分）

说明 `ModelRegistry.register` 为何参数类型是 `BaseModel` 而非 `ChatMessage`。

---

## 题目五：迁移说明（15 分）

`Contact` 从 day08 迁到 models 后，`contact_class.py` 为何保留？不保留会怎样？

---

## 加分题（+10 分）

为 `BaseModel` 添加 `def __eq__` 默认实现：同类型且 `to_dict()` 相等则相等（Contact 已手写，思考 ChatMessage 是否冲突）。

---

*答案：[09_作业答案.md](09_作业答案.md)*
