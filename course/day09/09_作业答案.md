# Day 9 作业答案

## 题目一

- **继承**：子类获得父类能力；`ChatMessage(BaseModel)`
- **抽象类**：定义接口不许直接实例化；`BaseModel`
- **多态**：父类引用调用子类方法；`ModelRegistry.register(any BaseModel)`

## 题目二：Tag 参考

```python
from models.llm_base import BaseModel

class Tag(BaseModel):
    def __init__(self, name: str):
        self.name = (name or "").strip()

    def validate(self) -> str | None:
        if not self.name:
            return "标签名不能为空"
        if len(self.name) > 20:
            return "标签名不能超过 20 字"
        return None

    def to_dict(self) -> dict:
        return {"name": self.name}

    @classmethod
    def from_dict(cls, data: dict) -> Tag:
        return cls(data.get("name", ""))
```

## 题目三

```python
def test_temperature_too_high():
    assert ModelConfig(temperature=2.1).validate() is not None

def test_max_tokens_zero():
    assert ModelConfig(max_tokens=0).validate() is not None
```

## 题目四

Registry 要同时容纳 ChatMessage、Contact、ModelConfig 等多种模型；注解 BaseModel 是**最小公共接口**，体现多态扩展性。

## 题目五

保留 `contact_class.py` re-export 是为了**向后兼容** Day 8 作业与测试路径；不保留则 `from day08.contact_class import Contact` 的代码全部断裂。

---

*加分题：默认 __eq__ 可能与 created_at 等字段比较策略冲突，子类可覆盖。*
