# Day 18 授课实录（节选）

**日期**：2026-07-23  
**讲师**：陈默  
**助教**：智链 NexusAgent 团队

---

## 开场（09:05）

陈默：「昨天我们把 Prompt 做成资产，今天解决**谁来选资产**。用户不会记 `/template rag_qa`，但会说『根据说明书』。」

林晓举手：「能不能让 LLM 自己选？」

陈默：「可以，Day 19。今天先用规则——**可测试、零成本、可解释**。金融产品里可解释性常常是硬需求。」

---

## 白板：分类流水线（09:40）

陈默画出：

```
text → strip → for each intent count keywords
     → max score → tie break by INTENT_PRIORITY
     → IntentMatch → template_name
```

赵岩：「合规和文档同时出现怎么办？」

陈默：「我们写了 `test_priority_compliance_over_rag`。产品拍板：合规优先。」

---

## Live Coding：intent_demos（10:20）

陈默现场改 `SAMPLES` 加一句「内部纪要请归纳」。

运行：

```bash
python3 src/day18/intent_demos.py
```

学员看到 `doc_summary`，`matched_keywords` 含「归纳」。

陈默：「新增关键词不用改 Python 逻辑，只改 `DEFAULT_KEYWORD_RULES`——运营也能 PR。」

---

## 午后：route_and_apply（14:10）

陈默打开 `cli_assistant.py`：

```python
if self.intent_router and self.auto_route:
    match = self.intent_router.route_and_apply(self, user_text)
```

强调顺序：**先路由再 add_user**。否则 system 还是上一轮模板。

林晓：「`/route` 为什么不 apply？」

陈默：「给运营验收规则，避免误切换污染会话。生产用 `auto_route`。」

---

## Demo 翻车与修复（15:00）

演示时 `context_provider` 未传，`rag_qa` 显示「暂无检索上下文」。

陈默：「这是显式失败，比 silent 空 context 好。`routed_assistant_demo` 里我们接了 `doc_reader`。」

周航切换分支，重跑 `NEXUS_LLM_MOCK=1 routed_assistant_demo.py`，通过。

---

## 测试讲评（16:20）

陈默逐条讲 `test_intent.py`：

- 前 5 条：分类器契约  
- 中间 3 条：Router 与 apply  
- 最后 4 条：助手集成 + 优先级  

「12 条不多，但覆盖**单元、集成、产品策略**三层。」

---

## 收尾（16:55）

陈默：「记住三个入口：`intent_demos` 看分类，`router_demos` 看变量，`routed_assistant_demo` 看端到端。」

预告 Day 19：「`context_provider` 从读文件变成检索。意图分类不变，变的只是槽位填充。」

---

## 学员问答摘录

**Q**：每轮都路由会不会抖？  
**A**：会。作业讨论「仅首句路由」扩展。

**Q**：能否导出命中日志？  
**A**：对 `IntentMatch` 打 log；企业案例见 23。

**Q**：英文用户？  
**A**：本期中文关键词；扩展需双语 rules 或 LLM。

---

## 课后作业布置（17:00）

见 `08_作业.md`。陈默强调作业 C 阈值设计要引用了合规案例。
