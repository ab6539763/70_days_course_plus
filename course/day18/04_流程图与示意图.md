# Day 18 流程图与示意图

**主题**：意图分类与模板路由全链路

---

## 1. 用户对话主流程（auto_route）

```mermaid
flowchart LR
    A[用户输入] --> B{斜杠命令?}
    B -->|是| C[handle_command]
    B -->|否| D{auto_route?}
    D -->|否| E[直接 chat_turn]
    D -->|是| F[IntentRouter.route_and_apply]
    F --> G[apply_template]
    G --> H[add_user]
    H --> I[LLMClient.complete]
    I --> J[add_assistant + 路由前缀]
    E --> H
```

## 2. 意图分类决策树（简化）

```mermaid
flowchart TD
    Q[用户话术] --> K1{含合规/审阅/宣传语?}
    K1 -->|是| CR[compliance_review]
    K1 -->|否| K2{含总结/摘要/要点?}
    K2 -->|是| DS[doc_summary]
    K2 -->|否| K3{含理财/产品/收益?}
    K3 -->|是| PF[product_faq]
    K3 -->|否| K4{含文档/资料/根据?}
    K4 -->|是| RQ[rag_qa]
    K4 -->|否| GN[general]
```

> 实际实现为**并行打分 + 同分优先级**，非严格 if-else 树；上图便于产品沟通。

## 3. IntentRouter 内部结构

```mermaid
graph TB
    subgraph IntentRouter
        CL[classify → classifier]
        BV[build_variables]
        RA[route_and_apply]
    end
    CL --> BV
    BV --> RA
    RA --> AT[assistant.apply_template]
```

## 4. 关键词命中示意

```
输入: "请根据产品说明书回答收益率是多少？"

rag_qa 规则命中: [文档, 说明书]  → score=2
product_faq 命中: [产品, 收益]    → score=2
同分 → INTENT_PRIORITY: compliance > doc_summary > product_faq > rag_qa
      → 选 product_faq（compliance/doc 未命中）
```

## 5. /route 命令流程

```mermaid
sequenceDiagram
    participant U as 用户
    participant CA as ChatAssistant
    participant IR as IntentRouter

    U->>CA: /route 总结要点
    CA->>IR: classify("总结要点")
    IR-->>CA: IntentMatch
    CA-->>U: match.summary() 文本
    Note over CA: 不调用 apply_template
    Note over CA: 不调用 LLM
```

## 6. routed_assistant_demo 脚本流

```mermaid
sequenceDiagram
    participant Script
    participant CA as ChatAssistant
    participant IR as IntentRouter

    Script->>CA: /route 请根据文档说明收益率
    CA-->>Script: 分类摘要 rag_qa

    Script->>CA: 根据资料，收益率是多少？
    CA->>IR: route_and_apply
    CA-->>Script: [路由: rag_qa] LLM 回复

    Script->>CA: /route 帮我审阅宣传语是否合规
    CA-->>Script: compliance_review

    Script->>CA: /exit
```

## 7. 模块依赖 ASCII 图

```
┌─────────────────────────────────────────┐
│           ChatAssistant                  │
│  intent_router, auto_route, /route       │
└─────────────────┬───────────────────────┘
                  │ route_and_apply
┌─────────────────▼───────────────────────┐
│            IntentRouter                  │
│  build_variables + default_registry      │
└───────┬─────────────────────┬───────────┘
        │ classify            │ get(name)
┌───────▼──────────┐   ┌──────▼──────────┐
│ RuleBasedIntent  │   │ PromptRegistry  │
│ Classifier       │   │ + library       │
└──────────────────┘   └─────────────────┘
```

## 8. Sprint 3 数据流总览

```mermaid
graph LR
    subgraph input[输入]
        UT[用户文本]
    end
    subgraph route[Day18]
        IC[IntentClassifier]
        TM[template_name]
    end
    subgraph prompt[Day17]
        PT[PromptTemplate.render]
        SM[system message]
    end
    subgraph llm[Day12-16]
        COMP[complete / stream]
    end
    UT --> IC --> TM --> PT --> SM --> COMP
```

## 9. 测试覆盖地图

| 区域 | 测试 |
|------|------|
| 分类器四类+general | test_classify_* |
| 变量构建 | test_router_build_variables_* |
| 助手集成 | test_route_and_apply_*、test_assistant_* |
| 优先级 | test_priority_compliance_over_rag |
| 可观测 | test_intent_match_summary |
