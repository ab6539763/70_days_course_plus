# Day 18 复习卡片

**用途**：打印裁剪，晨会速记

---

## 卡片 1：核心类

| 类 | 一句话 |
|----|--------|
| IntentMatch | 分类结果：intent + template + confidence |
| RuleBasedIntentClassifier | 关键词打分分类 |
| IntentRouter | 分类 + 变量 + apply_template |

---

## 卡片 2：关键常量

- `DEFAULT_KEYWORD_RULES` — 意图→关键词  
- `INTENT_TEMPLATE_MAP` — 意图→模板名  
- `INTENT_PRIORITY` — 同分决胜顺序  

---

## 卡片 3：置信度公式

```
confidence = min(0.95, 0.55 + 0.1 * 命中词数)
空输入 → 0.5；零命中 general → 0.6
```

---

## 卡片 4：优先级口诀

**合摘产文通**  
合规 → 摘要 → 产品 → 文档问答 → 通用

---

## 卡片 5：build_variables

| 模板 | 关键变量 |
|------|----------|
| rag_qa | context |
| doc_summary | document, max_points |
| compliance_review | text |
| product_faq | product_name |

---

## 卡片 6：ChatAssistant

- `intent_router` — 路由器实例  
- `auto_route` — 每轮自动 route_and_apply  
- `/route` — 仅预览 summary  

---

## 卡片 7：命令对比

| | /route | auto_route | /template |
|--|--------|------------|-----------|
| 切换模板 | ✗ | ✓ | ✓ |
| 调 LLM | ✗ | ✓ | ✗ |

---

## 卡片 8：演示命令

```bash
export PYTHONPATH=src
python3 src/day18/intent_demos.py
python3 src/day18/router_demos.py
NEXUS_LLM_MOCK=1 python3 src/day18/routed_assistant_demo.py
pytest tests/day18/test_intent.py -v
```

---

## 卡片 9：测试 12 条

分类 5 + 变量 2 + apply 1 + 助手 2 + summary 1 + 优先级 1

---

## 卡片 10：Day 17 → 18

模板库 → **自动选择模板**  
`/template` → **`/route` + auto_route**

---

## 卡片 11：常见坑

1. 未传 intent_router  
2. 以为 /route 会切换  
3. build_variables 缺键 ConfigError  

---

## 卡片 12：Day 19 预习

LLM 分类器替换 classifier；context 走向量检索。

---

## 自测问题（背面）

1. `classify("")` 返回什么 intent？  
2. 同分合规与 rag 谁赢？  
3. `route_and_apply` 调了 Day 17 什么方法？  

<details><summary>答案</summary>

1. general, 0.5  
2. compliance_review  
3. apply_template  

</details>
