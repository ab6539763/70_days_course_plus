# Day 18 完整代码走查

**仓库**：`nexus-agent-platform`  
**走查顺序**：intent.py → cli_assistant → demos → tests

---

## 1. prompts/intent.py 模块头

```python
"""
意图分类器 — 规则路由 + Prompt 模板选择

Day 18 MVP 使用关键词规则引擎；Day 19+ 可替换为 LLM 分类。
输出模板名供 ChatAssistant.apply_template 使用。

需求：ZL-NA-REQ-018
"""
```

明确边界：MVP 规则、输出对接 Day 17。

---

## 2. 常量块

```python
INTENT_TEMPLATE_MAP: dict[str, str] = { ... }
DEFAULT_KEYWORD_RULES: dict[str, list[str]] = { ... }
INTENT_PRIORITY: tuple[str, ...] = ( ... )
```

- `ContextProvider = Callable[[], str]` 类型别名供 Router 使用  

---

## 3. IntentMatch

```python
@dataclass
class IntentMatch:
    intent: str
    template_name: str
    confidence: float
    matched_keywords: list[str] = field(default_factory=list)
    user_text: str = ""

    def summary(self) -> str:
        ...
```

走查要点：`summary()` 格式与 `/route` 输出、测试 `test_intent_match_summary` 一致。

---

## 4. RuleBasedIntentClassifier.__init__

```python
def __init__(
    self,
    rules: dict[str, list[str]] | None = None,
    template_map: dict[str, str] | None = None,
    priority: tuple[str, ...] = INTENT_PRIORITY,
) -> None:
```

**依赖注入**：单测可传 mini rules，无需改全局常量。

---

## 5. classify 主逻辑

```python
def classify(self, text: str) -> IntentMatch:
    text = (text or "").strip()
    if not text:
        return self._default_match(text, confidence=0.5)
    scores: dict[str, tuple[int, list[str]]] = {}
    for intent, keywords in self.rules.items():
        matched = [kw for kw in keywords if kw in text]
        if matched:
            scores[intent] = (len(matched), matched)
    if not scores:
        return self._default_match(text, confidence=0.6)
    max_score = max(v[0] for v in scores.values())
    candidates = [k for k, v in scores.items() if v[0] == max_score]
    intent = self._pick_by_priority(candidates)
    ...
```

逐步对照 `04_流程图` classify 流程图。

---

## 6. IntentRouter.__init__

```python
def __init__(
    self,
    *,
    classifier: RuleBasedIntentClassifier | None = None,
    registry: PromptRegistry | None = None,
    company: str = "智链科技",
    context_provider: ContextProvider | None = None,
    default_product: str = "稳健增值系列产品",
) -> None:
```

默认组装：`classifier or RuleBasedIntentClassifier()`，`registry or default_registry`。

---

## 7. build_variables

逐分支阅读：

```python
if name == "rag_qa":
    context = self.context_provider() if self.context_provider else "（暂无检索上下文）"
    return {**base, "context": context}
```

与 `library.py` 各模板 `required_vars` 交叉验证。

---

## 8. route_and_apply

```python
def route_and_apply(self, assistant, user_text: str) -> IntentMatch:
    match = self.classify(user_text)
    variables = self.build_variables(match)
    assistant.apply_template(match.template_name, variables=variables)
    return match
```

**唯一**调用 Day 17 `apply_template` 的入口（自动路由路径）。

---

## 9. cli_assistant.py 片段

### 构造器

```python
self.intent_router = intent_router
self.auto_route = auto_route
self._last_intent = None
```

### /route

```python
if cmd == "/route":
    if not self.intent_router:
        return True, "意图路由未启用。请传入 intent_router。", False
    query = arg or "请帮我总结这份文档要点"
    match = self.intent_router.classify(query)
    return True, match.summary(), False
```

### chat_turn

```python
if self.intent_router and self.auto_route:
    match = self.intent_router.route_and_apply(self, user_text)
    self._last_intent = match
    route_info = f"[路由: {match.template_name}] "
```

---

## 10. day18/intent_demos.py

`SAMPLES` 五句覆盖四业务 + general，main 循环 `clf.classify` + `summary()`。

---

## 11. day18/router_demos.py

展示 `build_variables` 键列表，第二句触发 compliance。

---

## 12. day18/routed_assistant_demo.py

- `_load_sample_context`：`read_document` + 截断 400  
- `run_scripted` 跑 `/route` 与普通输入  

---

## 13. tests/day18/test_intent.py 结构

```
test_classify_*     ×5
test_router_*       ×2
test_route_and_apply ×1
test_assistant_*    ×2
test_intent_match_summary ×1
test_priority_*     ×1
= 12
```

Mock 模式：`LLMEnvConfig(mock=True)` + 固定 `SAMPLE` 响应。

---

## 14. 走查自检问题

1. `classify` 时间复杂度？O(意图数 × 关键词数 × 文本长)  
2. 线程安全？无共享可变状态，Classifier 可复用  
3. 如何热更新 rules？重建 Classifier 或 IntentRouter  

---

## 15. 建议调试断点

- `intent.py:classify` return 前  
- `intent.py:build_variables` 各分支  
- `cli_assistant.py:chat_turn` route_and_apply 后  
