# Day 15 晚自习

**建议时长**：90 分钟  
**目标**：巩固 Token 概念，完成演示脚本走读与自测

---

## 任务清单

### 任务 A：原理演示（20 min）

```bash
cd nexus-agent-platform
export PYTHONPATH=src
python3 src/day15/token_principle_demos.py
```

在笔记本抄写四组输出，并手算验证 `estimate_tokens` 是否与程序一致。

### 任务 B：API 对比演示（20 min）

```bash
python3 src/day15/token_counter_demo.py
```

回答：

1. 本地估算 prompt 是多少？  
2. Mock API 的 `prompt_tokens` 是多少？  
3. `compare_estimate` 偏差方向是正还是负？为什么？

### 任务 C：CLI 集成体验（20 min）

```bash
NEXUS_LLM_MOCK=1 python3 src/day15/assistant_token_demo.py
NEXUS_LLM_MOCK=1 python3 src/chat/cli_assistant.py
```

交互模式下至少对话 3 轮，输入 `/tokens`，截图或抄写报表。

### 任务 D：阅读测试（20 min）

打开 `tests/day15/test_token_counter.py`，为每个 `test_*` 函数写一句话说明测什么。

### 任务 E：思考题（10 min）

1. 若 system prompt 很长，对每轮 prompt_tokens 有何影响？  
2. 为何 `messages_snapshot` 不能放在 `add_assistant` 之后？  
3. 生产环境为何建议换 tiktoken？

---

## 拓展阅读（可选）

- [11_Token计数详解.md](11_Token计数详解.md)  
- [13_深度扩展_tiktoken与BPE.md](13_深度扩展_tiktoken与BPE.md)  
- [18_与Day5_usage对照表.md](18_与Day5_usage对照表.md)

---

## 明日预习

浏览 Day 16 课件目录（发布前占位）：关键词 **streaming**、**SSE**、**async**。

---

## 自习检查点

- [ ] 能默写 `usage` 三字段英文名  
- [ ] 能说出 `DEFAULT_INPUT_PRICE_PER_M` 默认值  
- [ ] 能解释 `/tokens` 与 `track_tokens` 的关系  
- [ ] `pytest tests/day15/ -v` 本地全绿
