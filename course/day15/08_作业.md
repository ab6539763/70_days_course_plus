# Day 15 作业

**截止**：Day 16 上课前  
**提交**：Git 分支 `day15-homework-姓名` 或课堂指定仓库

---

## 作业一：估算函数巩固（20 分）

在 `nexus-agent-platform` 中新建 `src/day15/homework_estimate.py`（个人练习，可不提交仓库），实现并测试：

1. 函数 `describe_estimate(text: str) -> str`，返回形如：  
   `CJK=4, other=8, tokens=6`
2. 覆盖空串、纯中文、纯英文、混合四类用例，打印结果。

**评分**：四类用例输出正确各 5 分。

---

## 作业二：费用计算器（25 分）

编写函数 `cost_for_turn(prompt: int, completion: int, *, input_ppm: float = 1.0, output_ppm: float = 2.0) -> float`：

- 返回人民币元，保留逻辑与 `TokenCounter.estimate_cost` 一致  
- 用例：`cost_for_turn(1_000_000, 500_000)` 应约等于 `2.0` 元

**提示**：可复用 `TokenUsage` + `TokenCounter`，但作业要求独立函数便于理解公式。

---

## 作业三：三轮对话报表（30 分）

使用 `ChatAssistant` + Mock transport，编写脚本 `homework_session_report.py`：

```python
# 伪代码要求
assistant = ChatAssistant(client=mock_client, track_tokens=True)
assistant.run_scripted(["问题1", "问题2", "问题3", "/tokens"])
```

Mock 的 `usage` 每轮递增（自行定义合理数字）。提交：

1. 脚本源码  
2. `/tokens` 完整输出截图或文本  
3. 200 字说明：为何第三轮 `prompt_tokens` 高于第一轮

---

## 作业四：概念简答（25 分）

书面回答（每项不少于 80 字）：

1. **Token 与字符的区别**是什么？举例说明。  
2. **prompt_tokens 与 completion_tokens** 分别计量什么？  
3. 多轮对话中，为何 **`/clear`（保留 system）** 能降低费用？  
4. MVP 使用启发式而非 `tiktoken` 的 **利弊** 各举两点。

---

## 加分项（+10 分）

阅读 [13_深度扩展_tiktoken与BPE.md](13_深度扩展_tiktoken与BPE.md)，在本地可选环境安装 `tiktoken`，写 `compare_tiktoken.py` 对比同一句中英文的 tiktoken 计数与 `estimate_tokens` 偏差，附表格。

---

## 提交清单

| 项 | 必须 |
|----|------|
| homework_session_report.py | ✓ |
| 简答 Markdown 或 PDF | ✓ |
| pytest 自测截图（若写了测试） | 可选 |
| tiktoken 加分脚本 | 可选 |

---

## 学术诚信

允许讨论思路，**代码与文字须独立完成**。抄袭 Mock 样本数据无妨，抄袭他人分析说明将扣分。
