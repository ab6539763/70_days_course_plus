# Day 35 预习：自适应路由与查询分类

**预告**：多 query 扩展提升了召回，但延迟随 query 数线性增长。Day 35 将引入 **Query Router** — 按意图决定是否 expand、rewrite 或直答 FAQ。

陈默：「简单查电话不必 expand 四路；复杂合规问句才走宽召回。」

## 预习问

1. expand 与 rewrite 能否按 query 类型动态开关？  
2. 路由误判如何降级回 Day35 默认管线？  

## Day36 路线图（预期）

| 模块 | 说明 |
|------|------|
| query_router.py | 意图分类 + 管线分支 |
| pipeline | route → expand? → rewrite → hybrid → … |
| 评估 | 延迟 P95 vs Recall@k |

## 与 Day35 关系

```
用户原问 → [route] → expand? → rewrite → hybrid → rerank → citations → LLM
```

## 预习阅读

浏览 `rag/expanding_retriever.py` 的 `enabled` 分支，思考路由层应放在 Retriever 外还是 API 层。

## 一句话

Day35 让检索**更广**；Day36 让管线**更省**。

---

## 预习作业

1. 列出 3 类应跳过 expand 的 query 示例。  
2. 说明路由配置应持久化在哪个 store 字段。  
3. 阅读 intent routing 基础概念（可选）。
