# Day 35 课堂笔记（上午）

**09:00–09:40** 第一节：质检发现「理财安全吗」这类模糊问句，单条 query 召回经...
**09:40–10:30** 第二节：rag/query_expander.py
**10:30–11:20** 第三节：ExpandingRetriever 核心逻辑
**11:20–12:00** 第四节：与 Day 34 对比走读

---

## 第一节：问题背景（40 min）

质检发现「理财安全吗」这类模糊问句，单条 query 召回经常漏掉真正相关的 chunk，Recall 不够宽。

引入 QueryExpander：把一条问句用模板/HyDE mock 拓展成多条候选 query，ExpandingRetriever 多路并发召回后按 chunk_id 去重合并，再进入既有的 rerank → citations 管线。

---

## 第二节：QueryExpander — Template / HyDE mock 扩展（50 min）

`rag/query_expander.py` 是本日的核心模块，负责实现「多查询扩展（Query Expansion / HyDE）」的主逻辑。

**10:15** 课堂练习：写出三组边界输入，预测 `ExpandingRetriever` 的行为。

---

## 第三节：ExpandingRetriever（50 min）

`ExpandingRetriever` 承接 Day 34 的检索结果，加上本日新增能力，再交给下游继续处理。

**10:55** 板书：Day 34 让回答有据可查；Day 35 让检索更广更全 — 一条问句拓展成多条 query 再合并召回。

---

## 第四节：与 Day 34 对比（40 min）

**11:40** 强调：Day 34 引用溯源（Citation Traceability） 的能力仍然保留，今天是在其基础上新增一层。
