# Day 35 授课实录

**09:05** 讲师展示上一轮 Sprint 遗留问题：质检发现「理财安全吗」这类模糊问句，单条 query 召回经常漏掉真正相关的 chunk，Recall 不够宽。
**09:30** 白板画出核心方案：引入 QueryExpander：把一条问句用模板/HyDE mock 拓展成多条候选 query，ExpandingRetriever 多路并发召回后按 chunk_id 去重合并，再进入既有的 rerank → citations 管线。
**10:10** `ExpandingRetriever` live coding。
**10:55** 走读 `rag/query_expander.py` 配置类。
**11:20** 第一次跑通 demo，全班确认输出正常。

**14:05** demo 案例逐条讲解：理财安全吗。
**14:50** 讲 `ExpansionConfig.enabled=false` 时的回退路径。
**15:15** `PUT /api/knowledge/expansion-config` 现场演示配置变更。
**15:58** pytest 第 20 个全绿。
**16:42** 预告 Day 36：自适应路由（Query Router）。

---

## 问答实录

**15:30 学员**：关闭本日新增能力最快的方式？
**讲师**：`PUT /api/knowledge/expansion-config` 把 `enabled` 设为 `false`，无需改代码。

---

## 讲师自评

- `ExpandingRetriever` live coding 时间刚好
- Lab 环节学员普遍能独立完成
- Day 36 预告已铺垫

---

## 课后作业布置原话

「tests/day35/ 必须全绿再提交，作业 A 的案例脚本周日 23:59 前 push。」
