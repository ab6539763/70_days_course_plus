# Day 17 架构设计

**模块**：`prompts/` 包  
**需求**：ZL-NA-REQ-017

---

## 1. 分层架构

```mermaid
graph TB
    subgraph app[应用层]
        Demo17["day17/* demos"]
        CLI[ChatAssistant]
    end
    subgraph prompts_pkg[prompts 包]
        Base[base.py PromptTemplate]
        Lib[library.py 内置模板]
        Reg[registry.py PromptRegistry]
        Txt["templates/*.txt"]
    end
    subgraph infra[基础设施]
        Models[models.ChatMessage]
        Exc[core.exceptions]
        Paths[core.paths]
    end
    subgraph tools_layer[工具层]
        DocReader[tools.doc_reader]
    end
    Demo17 --> Base
    Demo17 --> Reg
    CLI --> Reg
    CLI --> Base
    Base --> Models
    Reg --> Lib
    Reg --> Txt
    Reg --> Base
    DocReader --> Demo17
    Base --> Exc
    Reg --> Paths
```

## 2. 核心类与函数

| 符号 | 模块 | 职责 |
|------|------|------|
| `extract_variables` | base.py | 正则提取 `{var}` 列表 |
| `PromptTemplate` | base.py | 模板数据 + render / preview / to_system_message |
| `DEFAULT_ASSISTANT` 等 | library.py | 企业场景预设实例 |
| `BUILTIN_TEMPLATES` | library.py | name → Template 字典 |
| `PromptRegistry` | registry.py | 注册、查找、文件加载 |
| `default_registry` | registry.py | 全局单例，已 load_directory |
| `apply_template` | cli_assistant.py | 运行时切换 system |
| `/template` | cli_assistant.py | 用户命令入口 |

## 3. render 控制流

```mermaid
sequenceDiagram
    participant App
    participant PT as PromptTemplate
    participant Fmt as str.format

    App->>PT: "render(company='智链', context='...')"
    PT->>PT: 检查 required_vars ⊆ variables
    alt 缺变量
        PT-->>App: ConfigError
    else 齐全
        PT->>Fmt: "template.format(**variables)"
        Fmt-->>PT: 渲染文本
        PT-->>App: str
    end
```

## 4. ChatAssistant 初始化与模板

```mermaid
sequenceDiagram
    participant User
    participant CA as ChatAssistant
    participant Reg as PromptRegistry
    participant Hist as MessageHistory

    Note over CA: __init__ 若提供 prompt_template
    CA->>CA: "to_system_message(**template_variables)"
    CA->>Hist: "add(system) 若无 system"

    User->>CA: "/template rag_qa"
    CA->>Reg: "get('rag_qa')"
    CA->>CA: merge variables + render
    CA->>Hist: "清空后 add_system + 保留 user/assistant"
    CA-->>User: 已切换模板 preview
```

## 5. 文件加载流程

```mermaid
flowchart TD
    Start[load_from_file] --> Exists{"path.exists?"}
    Exists -->|否| SE[StorageError]
    Exists -->|是| Read[read_text UTF-8]
    Read --> ParseDesc{"首行 # ?"}
    ParseDesc -->|是| Desc["description = 行内容"]
    ParseDesc -->|否| Body[全文为 body]
    Desc --> Body
    Body --> Extract[extract_variables]
    Extract --> Register[register PromptTemplate]
    Register --> Done[返回 tmpl]
```

## 6. 模块依赖图

```
prompts/base.py
    → core.exceptions.ConfigError
    → models.ChatMessage

prompts/library.py
    → prompts.base.PromptTemplate

prompts/registry.py
    → prompts.base, prompts.library
    → core.paths.get_path("prompts_dir")

chat/cli_assistant.py
    → prompts.default_registry
    → PromptTemplate.apply via apply_template
```

**原则**：`library` 不依赖 `registry`；`registry` 聚合 `library` + 文件。

## 7. 与 LLM 层的边界

| 层 | 输入 | 输出 |
|----|------|------|
| Prompt | 变量 dict | `ChatMessage("system", rendered)` |
| LLM | `list[ChatMessage]` | `CompletionResult` |
| Streaming（Day 16） | 同上 | 逐 delta 显示 |

Prompt 层**不调用** LLM；仅构造 messages。`rag_prompt_demo.py` 在脚本层组装后调用 `LLMClient`。

## 8. 扩展点（后续 Sprint）

1. **Day 18**：`IntentClassifier` → 模板名  
2. **版本字段**：`PromptTemplate.version: str`（本期未实现）  
3. **多语言**：`name` 后缀 `_en` / `_zh`（约定即可）  
4. **模板组合**：system = header + body 两段 render（本期单段）

## 9. 设计权衡

| 选型 | 优点 | 缺点 |
|------|------|------|
| `str.format` 非 Jinja2 | 零依赖、与 Day 2 一致 | 无 if/for |
| dataclass | 清晰、可 repr | 非严格不可变 |
| 全局 default_registry | 演示简单 | 测试宜新建 `PromptRegistry()` |
| system 替换非追加 | 避免多 system 冲突 | `/system` 与 `/template` 需文档说明优先级 |
