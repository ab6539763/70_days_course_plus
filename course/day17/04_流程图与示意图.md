# Day 17 流程图与示意图

**主题**：Prompt 模板从定义到 API 调用的全链路

---

## 1. 学员日课总流程

```mermaid
flowchart TD
    A[09:00 晨会 ZL-NA-REQ-017] --> B[09:30 base.py 手写]
    B --> C[10:45 library.py 五模板]
    C --> D[午休]
    D --> E[14:00 PromptRegistry]
    E --> F[15:30 ChatAssistant 集成]
    F --> G[17:00 rag_prompt_demo]
    G --> H[19:00 晚自习 Lab]
    H --> I[作业 08_作业.md]
```

---

## 2. PromptTemplate 生命周期

```mermaid
stateDiagram-v2
    [*] --> Defined: 定义 template 字符串
    Defined --> Inferred: __post_init__ 推断 required_vars
    Inferred --> Preview: preview 部分变量
    Inferred --> Rendered: render 全变量
    Rendered --> SystemMsg: to_system_message
    SystemMsg --> InAPI: 进入 complete messages
    Preview --> Inferred: 调试循环
    Rendered --> Error: ConfigError 缺变量
    Error --> Inferred: 补全变量重试
```

---

## 3. 内置模板选型决策树

```mermaid
flowchart TD
    Start["业务场景?"] --> Q1{"需要外部资料?"}
    Q1 -->|是| RAG[RAG_QA + context]
    Q1 -->|否| Q2{"任务类型?"}
    Q2 -->|长文压缩| SUM[DOC_SUMMARY]
    Q2 -->|理财咨询| FAQ[PRODUCT_FAQ]
    Q2 -->|文案审阅| COMP[COMPLIANCE_REVIEW]
    Q2 -->|通用对话| DEF[DEFAULT_ASSISTANT]
    Q3{"客服渠道?"} --> CS[customer_service.txt]
    Start --> Q3
```

---

## 4. registry_demos 数据流

```mermaid
sequenceDiagram
    participant Main as registry_demos.py
    participant DR as default_registry
    participant Lib as BUILTIN_TEMPLATES
    participant File as customer_service.txt

    Main->>DR: import default_registry
    Note over DR: 模块加载时已 load_directory
    DR->>Lib: __init__ 复制内置
    DR->>File: glob *.txt
    File-->>DR: load_from_file
    Main->> "DR: list_names()"
    DR-->>Main: sorted names
    Main->> "DR: get("customer_service")"
    DR-->>Main: PromptTemplate
    Main->> "Main: render(company, channel, max_chars)"
```

---

## 5. rag_prompt_demo 端到端

```mermaid
sequenceDiagram
    participant Script as rag_prompt_demo.py
    participant DR as doc_reader
    participant RAG as RAG_QA
    participant LLM as LLMClient

    Script->> "DR: read_document(raw_notice.txt)"
    DR-->> "Script: Document(cleaned)"
    Script->> "Script: context = cleaned[:300]"
    Script->> "RAG: to_system_message(company, context)"
    RAG-->>Script: ChatMessage system
    Script->> "Script: user = 核心信息?"
    Script->> "LLM: complete([system, user])"
    LLM-->>Script: CompletionResult
    Script->>Script: 打印 system 长度 + 回复摘要
```

---

## 6. /template 命令分支

```mermaid
flowchart TD
    Input["/template ..."] --> Parse[split 命令与参数]
    Parse --> List{"arg == list?"}
    List -->|是| ShowList[list_names 输出]
    Parse --> Empty{"arg 为空?"}
    Empty -->|是| ShowCurrent[显示当前模板名]
    Parse --> Switch[apply_template 名称]
    Switch --> OK{"成功?"}
    OK -->|是| Preview[返回前 80 字预览]
    OK -->|否| Err[ConfigError 消息]
```

---

## 7. 与 Day 2 / Day 16 关系图

```mermaid
graph LR
    subgraph input[输入构造]
        D2[Day2 format]
        D17[Day17 Template]
        D2 --> D17
    end
    subgraph call[API 调用]
        D12[Day12 complete]
        D16[Day16 stream]
    end
    subgraph output[输出]
        Block[一次性 JSON]
        Stream[SSE delta]
    end
    D17 --> D12
    D17 --> D16
    D12 --> Block
    D16 --> Stream
```

---

## 8. Sprint 3 第三日位置

```mermaid
timeline
    title NexusAgent Phase 2 Sprint 3
    Day 15 : Token 计量
    Day 16 : 流式 SSE
    Day 17 : Prompt 模板库
    Day 18 : 意图分类
    Day 19 : RAG 索引预习
```

---

## 9. 模板变量数据流（RAG 场景）

```mermaid
flowchart LR
    File[(raw_notice.txt)]
    Reader[doc_reader]
    Ctx[context 字符串]
    Tmpl[RAG_QA.template]
    Render[render]
    Sys[system message]
    API[LLM API]

    File --> Reader --> Ctx --> Render
    Tmpl --> Render
    Render --> Sys --> API
```

---

## 10. 测试金字塔（Day 17）

```mermaid
graph TB
    E2E[assistant_prompt_demo 脚本]
    Int[test_assistant_apply_template]
    Unit["test_render / test_registry"]
    E2E --> Int --> Unit
```

单元测试覆盖：`extract_variables`、`ConfigError`、`load_from_file` tmp_path、`customer_service` 注册。
