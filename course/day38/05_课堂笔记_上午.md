# Day 38 课堂笔记（上午）

**09:00–09:40** 第一节：Day37 校验拒答后用户体验很差——机器人直接说「无法确认...
**09:40–10:30** 第二节：rag/validation_retry.py
**10:30–11:20** 第三节：apply_validation_retry 核心逻辑
**11:20–12:00** 第四节：与 Day 37 对比走读

---

## 第一节：问题背景（40 min）

Day37 校验拒答后用户体验很差——机器人直接说「无法确认」，一次机会就放弃了，没有给系统补救的余地。

实现 apply_validation_retry：校验失败且 retry_on_fail 时，用 rag_wide 意图强制放大检索池重新召回，再校验一次；仍失败才真正拒答。

---

## 第二节：apply_validation_retry — 校验失败后 rag_wide 重检索再校验（50 min）

`rag/validation_retry.py` 是本日的核心模块，负责实现「多轮 Self-RAG 校验重试」的主逻辑。

**10:15** 课堂练习：写出三组边界输入，预测 `apply_validation_retry` 的行为。

---

## 第三节：apply_validation_retry（50 min）

`apply_validation_retry` 承接 Day 37 的检索结果，加上本日新增能力，再交给下游继续处理。

**10:55** 板书：Day 37 让回答更准；Day 38 让校验失败后还能自动补救 — 强制 rag_wide 重检索再校验一次。

---

## 第四节：与 Day 37 对比（40 min）

**11:40** 强调：Day 37 Self-RAG 答案校验 的能力仍然保留，今天是在其基础上新增一层。
