# Day 38 授课实录

**09:05** 讲师展示上一轮 Sprint 遗留问题：Day37 校验拒答后用户体验很差——机器人直接说「无法确认」，一次机会就放弃了，没有给系统补救的余地。
**09:30** 白板画出核心方案：实现 apply_validation_retry：校验失败且 retry_on_fail 时，用 rag_wide 意图强制放大检索池重新召回，再校验一次；仍失败才真正拒答。
**10:10** `apply_validation_retry` live coding。
**10:55** 走读 `rag/validation_retry.py` 配置类。
**11:20** 第一次跑通 demo，全班确认输出正常。

**14:05** demo 案例逐条讲解：年化收益怎么样（首次窄召回未命中）。
**14:50** 讲 `ValidationConfig.enabled=false` 时的回退路径。
**15:15** `PUT /api/knowledge/validation-config` 现场演示配置变更。
**15:58** pytest 第 15 个全绿。
**16:42** 预告 Day 39：手写 ReAct Agent。

---

## 问答实录

**15:30 学员**：关闭本日新增能力最快的方式？
**讲师**：`PUT /api/knowledge/validation-config` 把 `enabled` 设为 `false`，无需改代码。

---

## 讲师自评

- `apply_validation_retry` live coding 时间刚好
- Lab 环节学员普遍能独立完成
- Day 39 预告已铺垫

---

## 课后作业布置原话

「tests/day38/ 必须全绿再提交，作业 A 的案例脚本周日 23:59 前 push。」
