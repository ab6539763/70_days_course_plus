# 企业案例集：多轮 Self-RAG 校验重试

## 案例 1：首次校验失败后补救成功

**场景**：年化收益怎么样（首次窄召回未命中）
**处理**：rag_wide 重检索后命中含数字的 chunk，二次校验通过

## 案例 2：重试仍失败

**场景**：彻底答非所问的场景
**处理**：二次校验仍不通过，最终拒答并标记 retries

## 案例 3：关闭重试

**场景**：retry_on_fail=false
**处理**：行为回退到 Day37 单次校验

## 案例复盘模板

| 日期 | 变更 | 影响 | 备注 |
|------|------|------|------|
| — | 上线本日新增能力 | — | 配合灰度开关 |

## Incident 降级预案

怀疑新逻辑引入回归时，`PUT /api/knowledge/validation-config` 将 `enabled` 置为 `false`，立即回退到 Day 37 行为。
