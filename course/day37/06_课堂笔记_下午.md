# Day 37 课堂笔记（下午）

**14:00–14:30** 第五节：day37/validation_demo.py 现场
**14:30–15:20** 第六节：RuleBasedAnswerValidator 源码走读
**15:20–16:00** 第七节：/api/knowledge/validation-config API
**16:00–16:45** 第八节：pytest + chat 回归

---

## 第五节：demo 现场（30 min）

```bash
PYTHONPATH=src python3 src/day37/validation_demo.py
```

记录每个 case study 的结果是否符合预期。

---

## 第六节：核心逻辑（50 min）

课堂上逐行读 `RuleBasedAnswerValidator`，重点讲输入输出与 Day 36 管线的衔接点。

---

## 第七节：API（40 min）

```bash
curl -s http://127.0.0.1:8000/api/knowledge/validation-config | jq .
curl -s -X PUT http://127.0.0.1:8000/api/knowledge/validation-config \
  -H 'Content-Type: application/json' -d '{"enabled": true}' | jq .
curl -s -X POST http://127.0.0.1:8000/api/knowledge/validation-preview \
  -H 'Content-Type: application/json' -d '{"query": "测试问题"}' | jq .
```

---

## 第八节：测试与 chat（45 min）

```bash
pytest tests/day37/ -v
```

**16:30** 里程碑：21 passed。

**16:40** 现场跑一次 `POST /api/chat`，对照响应中的相应字段。
