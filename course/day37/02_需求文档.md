# ZL-NA-REQ-037 产品需求文档（PRD）

**需求名称**：Self-RAG 答案校验
**优先级**：P0
**平台版本**：v0.37.0

---

## 1. 背景

合规部反馈：虽然每条回答都带了 citations，但 LLM 仍偶发「引用是 A、回答是 B」的答非所问。 实现 RuleBasedAnswerValidator，在 chat API 返回前校验 reply 与 citations 的 token 重合度打分；分数低于 min_score 且 refuse_on_fail 时拒答。

## 2. 目标

- `rag/answer_validator.py` — RuleBasedAnswerValidator — 引用-回复一致性打分
- `rag/validation_config.py` — ValidationConfig — enabled / min_score / refuse_on_fail
- `GET/PUT /api/knowledge/validation-config` 配置读写
- `POST /api/knowledge/validation-preview` 无状态预览
- `POST /api/chat` 端到端集成

## 3. 功能需求

### FR-001 RuleBasedAnswerValidator — 引用-回复一致性打分

- 实现位置：`rag/answer_validator.py`

### FR-002 ValidationConfig — enabled / min_score / refuse_on_fail

- 实现位置：`rag/validation_config.py`

### FR-003 持久化与 status

- `store.json` 新增 `validation_config` 字段
- `GET /api/knowledge/status` 含 `validation_config`
- `platform_version` 为 `v0.37.0`

### FR-004 Chat 集成

- `api/chat.py` 在管线中调用本日模块
- `ChatResponse` 相应字段附带本日审计信息

### FR-005 演示与测试

- `day37/validation_demo.py` 打印对比结果
- `day37/validation_api_demo.py` 演示 REST API
- `tests/day37/` 21 项覆盖单元 + API + chat

## 4. 非功能需求

### NFR-001 可观测性

status / preview 接口可读当前 `validation_config`。

### NFR-002 兼容性

`ValidationConfig.enabled=false` 时行为回退到 Day 36 逻辑，不影响既有回归。

### NFR-003 可测试性

`RuleBasedAnswerValidator` 可脱离 FastAPI 直接单测。

## 5. 非目标

- 接入真实模型替换当前 mock 规则实现
- 跨会话的长期缓存

---

## 5.1 FR 追溯矩阵

| FR | 实现位置 | 测试 |
|----|----------|------|
| FR-001 | rag/answer_validator.py | tests/day37/ |
| FR-002 | rag/validation_config.py | tests/day37/ |

## 5.2 验收标准

| ID | 场景 | 预期 |
|----|------|------|
| AC-01 | 默认配置 GET /api/knowledge/validation-config | enabled=true |
| AC-02 | PUT enabled=false | 200 且行为回退 |
| AC-03 | /api/knowledge/validation-preview | 返回结构化预览 |
| AC-04 | tests/day37/ | 全绿 |

---

## 6. 详细验收步骤

```bash
PYTHONPATH=src NEXUS_LLM_MOCK=1 pytest tests/day37/ -v
PYTHONPATH=src python3 src/day37/validation_demo.py
PYTHONPATH=src NEXUS_LLM_MOCK=1 python3 src/day37/validation_api_demo.py
```

---

## 7. 风险登记

| 风险 | 缓解 |
|------|------|
| mock 规则与真实业务分布不符 | 文档标注局限，预留切换真实模型接口 |
| 新字段影响既有前端解析 | 默认值向后兼容 |

---

## 8. 发布说明 v0.37.0

**新增**：rag/answer_validator.py、rag/validation_config.py
**变更**：`store.json` 新增 `validation_config`
**注意**：`ValidationConfig.enabled` 默认开启

---

## 9. 需求变更记录

| 版本 | 变更 |
|------|------|
| v0.37.0-draft | 仅核心类 |
| v0.37.0 | API + chat 集成 + 21 tests |

---

## 10. PRD 签字页

产品：________  研发：________  测试：________
