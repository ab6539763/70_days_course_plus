# Day 13 晚自习

**时间**：19:00 – 21:00  
**形式**：自主练习 + 助教答疑

---

## 必做清单（约 90 分钟）

### 1. 四演示脚本各跑一遍（20 分钟）

```bash
cd nexus-agent-platform
export PYTHONPATH=src

python3 src/day13/decorator_demos.py
python3 src/day13/retry_demos.py
python3 src/day13/async_demos.py
NEXUS_LLM_MOCK=1 python3 src/day13/resilient_client_demo.py
```

在笔记本记录每次脚本的**调用次数**与**总耗时**。

### 2. 精读 `retry.py`（30 分钟）

对照 [25_retry精读.md](25_retry精读.md)，回答：

1. `max_attempts=3` 时，`on_retry` 最多被调用几次？  
2. `base_delay=1, backoff_factor=2` 两次重试间的 sleep 分别是多少？  
3. 自定义 `retryable=lambda e: False` 的效果？

### 3. 手写迷你重试（20 分钟）

不抄库，实现 10 行版：

```python
def naive_retry(func, times=3):
    ...
```

要求：仅对 `APIError` 且 `status_code==503` 重试，固定 sleep 0.1s。

### 4. pytest（20 分钟）

```bash
python3 -m pytest tests/day13/ -v --tb=short
```

若有红，先查 [10_常见问题与排错指南.md](10_常见问题与排错指南.md)。

---

## 选修挑战

### 挑战 A：jitter

在 `retry.py` 的 `sleep_for` 上加 ±10% 随机抖动（不提交主分支，本地实验）。

### 挑战 B：统计重试次数

扩展 `ResilientLLMClient`，增加 `retry_count` 属性，统计上次 `complete` 重试了几次。

### 挑战 C：401 实验

写 5 行脚本，transport 固定抛 `APIError(..., status_code=401)`，确认只调用 1 次。

---

## 答疑热点（助教整理）

| 问题 | 简答 |
|------|------|
| 装饰器为何难调试？ | 看 `wrapper` 栈帧；用 `wraps` 保留名字 |
| 重试会重复扣费吗？ | 若请求已到达服务端可能重复计费——幂等设计 Day 30+ |
| 超时后线程还在跑？ | 是，MVP 限制；Day 16 异步取消 |

---

## 明日预习（Day 14）

阅读需求草案 `ZL-NA-REQ-014`（cli_assistant）：

- `messages: list[ChatMessage]` 在循环中累积  
- 命令 `exit` / `quit` 退出  
- 使用 `ResilientLLMClient` 而非裸 `LLMClient`

---

*作业：[08_作业.md](08_作业.md)*
