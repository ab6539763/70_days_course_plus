# Day 13 作业

**截止**：次日上午课前  
**提交**：个人仓库 `homework/day13/` 目录 + 课堂打卡

---

## 作业一：概念题（20 分）

1. 解释 Python 装饰器的「语法糖」`@decorator` 等价于什么操作？（3 分）  
2. `functools.wraps` 解决了什么问题？若不使用，`greet.__name__` 会是什么？（4 分）  
3. 列出 `DEFAULT_RETRYABLE_STATUS` 中全部状态码，并各用一句话说明含义。（5 分）  
4. 为何 `ConfigError` 和 `401` 不应重试？（4 分）  
5. 画出「内 `with_timeout`、外 `retry`」的调用顺序（文字或 ASCII 均可）。（4 分）

---

## 作业二：代码阅读（25 分）

阅读 `nexus-agent-platform/src/llm/retry.py`，回答：

1. `retry` 装饰器中 `delay` 变量如何随 attempt 变化？给出公式。（5 分）  
2. `with_timeout` 超时后抛出的异常类型与消息格式是什么？（5 分）  
3. `api_retry` 与直接 `@retry(...)` 有何区别？（5 分）  
4. `apply_resilience` 组合了哪两个装饰器？顺序为何？（5 分）  
5. `is_retryable_error` 对 `status_code=None` 的 `APIError` 返回什么？为什么？（5 分）

---

## 作业三：动手实践（35 分）

在 `homework/day13/` 创建 `my_retry_demo.py`：

### 要求

1. 使用项目内 `from llm.retry import retry`（5 分）  
2. 定义 `unstable_fetch()`：前 2 次抛 `APIError("busy", status_code=503)`，第 3 次返回 `"ok"`（10 分）  
3. 装饰参数：`max_attempts=5, base_delay=0.01, backoff_factor=2`（5 分）  
4. 打印每次 attempt 序号（5 分）  
5. 额外：对 `APIError(..., status_code=401)` 的函数装饰后，验证只执行 1 次（10 分）

### 运行

```bash
cd nexus-agent-platform
export PYTHONPATH=src
python3 homework/day13/my_retry_demo.py
```

---

## 作业四：ResilientLLMClient 实验（20 分）

1. 运行 `resilient_client_demo.py`，截图或粘贴终端输出（5 分）  
2. 修改 `fail_times=5` 而 `max_attempts=4`，描述最终行为（5 分）  
3. 将 `on_retry_log=False`，输出有何变化？（5 分）  
4. 简述 `ResilientLLMClient` 与 `LLMClient` 的继承关系及 `complete` 差异（5 分）

---

## 加分项（+10）

实现 `homework/day13/timed_greet.py`：结合 `@log_call`（自写）与 `@retry(max_attempts=2)` 装饰同一函数，并保证 `__doc__` 可读。

---

## 评分 rubric

| 等级 | 标准 |
|------|------|
| A | 四题完整，demo 可运行，能讲清不重试 401 的原因 |
| B | 少量概念偏差，代码可运行 |
| C | 演示脚本未跑通或概念题过半错误 |

---

*参考答案：[09_作业答案.md](09_作业答案.md)（讲师课后发布）*
