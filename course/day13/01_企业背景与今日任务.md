# Day 13 企业背景与今日任务

**日期**：2026 年 7 月 18 日  
**Sprint**：Sprint 2 第 6 天 — 装饰器与容错日

## 晨会纪要

### 赵岩（产品）

> 「内测用户反馈：高峰时段偶发超时、限流。MVP 不能因**瞬时故障**丢单。今天给 LLM 调用加**重试与超时**，明天做**多轮对话 CLI**。」

### 陈默（架构）

> 「Day 12 的 `LLMClient` 保持纯净。容错逻辑放进 `llm/retry.py` 装饰器 + `ResilientLLMClient` 子类。符合开闭原则：**扩展行为，不修改传输**。」

### 周航（DevOps）

> 「重试必须有上限和退避，不能 DDoS 自己的 API。`max_attempts=3`、`base_delay=1s`、`backoff_factor=2` 写进 `RetryPolicy` 默认值。日志里要能看见第几次重试。」

## 任务分配

| ID | 任务 | 负责人 | 优先级 |
|----|------|--------|--------|
| ZL-NA-REQ-013 | 实现 `llm/retry.py` | 全员 | P0 |
| ZL-NA-061 | `is_retryable_error` 状态码矩阵 | 林晓 | P0 |
| ZL-NA-062 | `ResilientLLMClient` | 林晓 | P0 |
| ZL-NA-063 | `decorator_demos` / `retry_demos` | 助教 | P1 |
| ZL-NA-064 | `async_demos` 异步预习 | 陈默 | P1 |
| ZL-NA-065 | `tests/day13/` 全绿 | 周航 | P0 |

## Definition of Done

- [ ] `python3 src/day13/decorator_demos.py` 打印 `greet` 与 `repeat(3)` 预期输出
- [ ] `python3 src/day13/retry_demos.py` 第三次调用成功
- [ ] `NEXUS_LLM_MOCK=1 python3 src/day13/resilient_client_demo.py` 503 重试后成功
- [ ] `pytest tests/day13/ -v` 全绿
- [ ] 能口述：为何 401 不重试、429 要重试
- [ ] 能解释装饰器组合顺序：内超时、外重试

## 今日节奏

| 时段 | 内容 |
|------|------|
| 09:00-10:30 | Python 装饰器、`functools.wraps`、装饰器工厂 |
| 10:45-12:00 | `retry` 指数退避、`is_retryable_error` |
| 14:00-15:30 | `with_timeout`、`ResilientLLMClient` |
| 15:45-17:00 | `async_demos` 预习、测试、排错 |

## 环境准备

```bash
cd nexus-agent-platform
export PYTHONPATH=src
# Mock 模式无需 Key
NEXUS_LLM_MOCK=1 python3 src/day13/resilient_client_demo.py
```

---

*下一篇：[02_需求文档.md](02_需求文档.md)*
