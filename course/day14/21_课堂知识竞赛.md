# Day 14 课堂知识竞赛

**形式**：6 组抢答，每组基础 10 题 + 2 道 Sprint 1 综合题。  
**用时**：约 25 分钟。

---

## 第一轮：快问快答

1. Day 14 需求编号是？ → **ZL-NA-REQ-014**  
2. 核心类名？ → **ChatAssistant**  
3. 默认客户端类？ → **ResilientLLMClient**  
4. 历史服务类？ → **MessageHistory**  
5. 哪条命令与 `/exit` 等价？ → **/quit**  
6. `/clear` 保留哪种 role？ → **system**  
7. CI 用哪个方法？ → **run_scripted**  
8. 交互入口函数？ → **run_cli**  
9. Mock 环境变量？ → **NEXUS_LLM_MOCK=1**  
10. 验收脚本文件名？ → **cli_assistant_demo.py**  

---

## 第二轮：代码理解

11. `handle_command` 返回几个值？ → **3**  
12. `chat_turn` 空输入返回什么关键词？ → **有效**  
13. `_process_line` 对话成功输出前缀？ → **助手:**  
14. 未知 `/foo` 是否调用 LLM？ → **否**  
15. 退出交互时尝试调用？ → **save_history**  

---

## 第三轮：Sprint 1 串联

16. Day 几实现 `LLMClient`？ → **12**  
17. Day 几实现 retry？ → **13**  
18. `ChatMessage` 三种 role？ → **system/user/assistant**  
19. 多轮对话 API 无什么？ → **状态/记忆（无状态）**  
20. Sprint 1 最后一天是 Day 几？ → **14**  

---

## 第四轮：场景题

21. 用户连问三轮，第三轮 `complete` 发送几条 user？（含历史） → **3 条 user（及对应 assistant）**  
22. 503 最终失败，用户看到？ → **API 调用失败**（主循环不退出）  
23. 想改人设不改历史对话，用？ → **/system**  
24. 想换话题保留人设？ → **/clear**  
25. Day 15 主题？ → **Token 计数**  

---

## 第五轮：纠错题

26. 「应在 `chat_turn` 里捕获 APIError」对还是错？ → **错，在 _process_line**  
27. 「`run_scripted` 会自动 save_history」对还是错？ → **错（除非脚本含 /save）**  
28. 「`is_command(' hello')` 为 True」对还是错？ → **错（strip 后不以 / 开头）**  

---

## 加分题

29. 画出 `chat_turn` 三步（口述即可）  
30. 用 30 秒向「产品经理」解释为何需要 `run_scripted`  

---

## 评分

| 轮次 | 每题分值 |
|------|----------|
| 一、二 | 1 分 |
| 三、四 | 2 分 |
| 五 | 2 分 |
| 加分 | 3 分 |

**冠军奖励**：Sprint 1 收官荣誉证书 + 优先选 Day 15 演示位。

---

## 答案卡（讲师用）

见上文箭头右侧。争议题以 `cli_assistant.py` 源码为准。

---

*深度讲义 [22_多轮对话深度讲义.md](22_多轮对话深度讲义.md)*
