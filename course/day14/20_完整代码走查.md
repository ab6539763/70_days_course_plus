# Day 14 完整代码走查

逐段解读 `nexus-agent-platform/src/chat/cli_assistant.py`。

---

## 文件头与导入

```python
"""
命令行多轮对话 AI 助手 — Sprint 1 阶段项目
需求：ZL-NA-REQ-014
"""
```

导入分四层：`core` 异常与路径、`llm` 客户端、`models` 配置、`services` 历史。

---

## COMMANDS 与 DEFAULT_SYSTEM_PROMPT

- `COMMANDS`：七项命令文案，供 `/help` 枚举  
- `DEFAULT_SYSTEM_PROMPT`：企业人设，可测试时传 `system_prompt=""` 关闭  

---

## ChatAssistant.__init__

```python
self.history = history or MessageHistory()
self.history_path = history_path or get_path("chat_session")
self.client = client or ResilientLLMClient(ModelConfig(), on_retry_log=on_retry_log)
```

**走查点**：

1. `history` 先于 `client`，因 system 注入要操作 history  
2. `on_retry_log` 仅传给 Resilient 层，Assistant 自身不实现重试  

```python
if system_prompt and not self._has_system_message():
    self.history.add_system(system_prompt)
```

---

## _has_system_message

```python
return any(m.role == "system" for m in self.history.messages)
```

避免重复默认 system；允许测试传入预置 history。

---

## is_command

```python
return text.strip().startswith("/")
```

**注意**：`" /help"` strip 后识别；`"你好/help"` 不是命令。

---

## handle_command（核心分支）

### /exit 与 /quit

```python
if cmd in ("/exit", "/quit"):
    return True, "再见！期待下次对话。", True
```

第三个 `True` 驱动主循环退出。

### /clear

```python
system_msgs = [m for m in self.history.messages if m.role == "system"]
self.history = MessageHistory(system_msgs)
```

新建 `MessageHistory` 实例，非原地删列表 —— 避免悬挂引用。

### /history

```python
lines = [f"  {i}. {m.format_line()}" for i, m in enumerate(self.history.messages, 1)]
```

使用 `ChatMessage.format_line()` 统一格式。

### /save

```python
self.save_history()
return True, f"已保存到 {self.history_path}", False
```

### /system

无 `arg` 返回用法提示；有则 `add_system(arg)`。

### 未知命令

```python
return True, f"未知命令 {cmd}，输入 /help 查看帮助。", False
```

`handled=True` 防止落入 LLM。

---

## chat_turn

```python
user_text = user_text.strip()
if not user_text:
    return "请输入有效内容。"

err = self.history.add_user(user_text)
if err:
    return f"消息无效：{err}"

result = self.client.complete(self.history.messages)
reply = result.message.content
self.history.add_assistant(reply)
return reply
```

**不走 _process_line 的前缀**：由 `_process_line` 加 `助手: `。

**异常**：不在此捕获，向上抛给 `_process_line`。

---

## save_history / load_history

```python
def save_history(self) -> None:
    self.history_path.parent.mkdir(parents=True, exist_ok=True)
    self.history.save(self.history_path)

def load_history(self) -> None:
    if self.history_path.exists():
        self.history = MessageHistory.load(self.history_path)
```

`load` 整体替换 `self.history` 对象。

---

## run_scripted

```python
for line in lines:
    out, done = self._process_line(line)
    if out:
        outputs.append(out)
    if done:
        break
return outputs
```

遇 `/exit` 立即停止处理后续行。

---

## _process_line

命令分支 → `handle_command`  
对话分支 → `try chat_turn` + 三类 `except`  

每条路径返回 `(str, bool)`。

---

## run_interactive

```python
while self._running:
    try:
        line = input_fn(prompt)
    except (EOFError, KeyboardInterrupt):
        output_fn("\n再见！")
        break
    ...
try:
    self.save_history()
except NexusError:
    pass
```

**走查点**：Ctrl+C 友好退出；保存失败不二次报错。

---

## run_cli

```python
def run_cli() -> None:
    ChatAssistant().run_interactive()
```

生产/教学默认入口。

---

## 与测试映射

| 代码段 | 测试 |
|--------|------|
| 默认 system | `test_assistant_has_default_system` |
| `/clear` | `test_clear_keeps_system` |
| 多轮 | `test_run_scripted_multi_turn` |
| API 失败 | `test_api_error_handled_in_scripted` |

---

## 走查结论

227 行实现 Sprint 1 全部能力整合：**编排简洁、边界清晰、双模式统一**。这是阶段项目应有的代码密度。

---

*竞赛题见 [21_课堂知识竞赛.md](21_课堂知识竞赛.md)*
