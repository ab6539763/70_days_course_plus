# Day 14 作业

**截止**：Day 15 课前  
**提交**：Git 分支或课堂指定仓库路径

---

## 基础题（必做）

### 作业 1：命令行为验证报告

在 Mock 模式下完成交互，截图或文字记录以下命令的**输入与完整输出**：

- `/help`
- `/clear`（清空前至少 2 轮对话）
- `/history`
- `/save`
- `/system 你是简洁助手`（无参数时再试一次）
- `/exit`

### 作业 2：run_scripted 单元测试

在 `tests/day14/` 新增 `test_homework_scripted.py`，包含：

1. 测试连续三行对话后 `/history` 输出非空  
2. 测试未知命令 `/foo` 返回含「未知命令」  
3. 使用 `tmp_path` 测试 `/save` 后文件存在  

要求：`pytest` 全绿。

### 作业 3：代码阅读问答（书面）

1. 画出 `ChatAssistant.__init__` 中初始化顺序（history → client → system）  
2. 说明 `handle_command` 返回三元组各字段含义  
3. 解释为何 `chat_turn` 不捕获 `APIError`  

---

## 提高题（选做）

### 作业 4：扩展命令 `/count`

在不破坏现有测试前提下，为 `ChatAssistant` 增加 `/count` 命令，输出当前 `messages` 条数（含 system）。

提示：修改 `COMMANDS` 与 `handle_command`。

### 作业 5：Sprint 1 学习地图

绘制一张 A4 知识地图：Day 1–14 每天**一个关键词** + **一个代表文件**，手写或 mermaid 均可。

### 作业 6：Day 15 预习笔记

阅读 `LLMClient.complete` 返回结构中的 `usage` 字段，写 200 字说明：为何多轮对话需要关注 token。

---

## 评分权重

| 题目 | 分值 |
|------|------|
| 作业 1 | 20% |
| 作业 2 | 40% |
| 作业 3 | 40% |
| 作业 4–6 | 附加分 |

---

## 提交清单

```
homework/day14/
├── command_report.md      # 作业 1
├── test_homework_scripted.py  # 作业 2
├── reading_answers.md     # 作业 3
└── optional/              # 选做
```

---

*参考答案见 [09_作业答案.md](09_作业答案.md)*
