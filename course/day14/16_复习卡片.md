# Day 14 复习卡片

> 建议打印裁剪， Sprint 1 收官复习用。

---

## 卡片 1：今日一句话

**Day 14** = 用 `ChatAssistant` 把 `MessageHistory` + `ResilientLLMClient` 编成可交互、可脚本的多轮 CLI 助手。

---

## 卡片 2：核心类

| 类/函数 | 作用 |
|---------|------|
| `ChatAssistant` | 多轮编排 |
| `run_cli()` | 交互入口 |
| `run_scripted` | CI 脚本入口 |
| `COMMANDS` | 斜杠命令表 |

---

## 卡片 3：七命令

`/help` `/exit` `/quit` `/clear` `/history` `/save` `/system`

---

## 卡片 4：chat_turn 三步

1. `add_user`  
2. `client.complete(messages)`  
3. `add_assistant(reply)`  

---

## 卡片 5：三元组

`handle_command` → `(handled, message, should_exit)`

---

## 卡片 6：/clear 语义

删除除 **system** 外全部消息，重建 `MessageHistory`。

---

## 卡片 7：双模式

| 模式 | 方法 |
|------|------|
| 人 | `run_interactive` |
| 机 | `run_scripted` |

---

## 卡片 8：异常在哪捕

`_process_line` 捕 `ConfigError` / `APIError` / `NexusError`，不崩循环。

---

## 卡片 9：Mock 命令

```bash
export NEXUS_LLM_MOCK=1
export PYTHONPATH=src
```

---

## 卡片 10：测试文件

`tests/day14/test_cli_assistant.py`

---

## 卡片 11：演示脚本

- `assistant_demos.py` — 组件  
- `cli_assistant_demo.py` — 验收  
- `sprint1_review.py` — 里程碑  

---

## 卡片 12：需求编号

**ZL-NA-REQ-014**

---

## 卡片 13：Sprint 1 起点

Day 1 CLI 与环境 → … → Day 14 cli_assistant

---

## 卡片 14：多轮本质

API 无状态；**客户端重复发送完整 messages**。

---

## 卡片 15：Day 15 预告

**Token 计数器** — `usage.prompt_tokens` / `completion_tokens`

---

## 卡片 16：默认 system

智链 NexusAgent 助手，专业简洁答企业文档与产品。

---

## 卡片 17：退出保存

`run_interactive` 结束 `try: save_history()`。

---

## 卡片 18：依赖注入

测试注入 `client=`、`history_path=tmp_path`。

---

## 卡片 19：未知命令

返回「未知命令」，建议 `/help`。

---

## 卡片 20：庆功 🎉

**Sprint 1 完成！** 你已是能交付终端 AI 助手的工程师。

---

*速查见 [17_cli_assistant速查手册.md](17_cli_assistant速查手册.md)*
