# 知识库重建详解

**Day 28 深度专题** | ZL-NA-REQ-028

## 1. 问题定义

设源文件集合 \(S\)，当前配置 \(	heta\)。生产索引 \(I = 	ext{Index}(	ext{Parse}(S), 	heta_{	ext{old}})\)。配置更新为 \(	heta_{	ext{new}}\) 后，需计算 \(I' = 	ext{Index}(	ext{Parse}(S), 	heta_{	ext{new}})\)。

**无法**通过修改 \(	heta\) 元数据把 \(I\) 就地变为 \(I'\)，因 chunk 文本边界已固定。

## 2. rebuild_store 不变式

- 重建后 \(orall d \in 	ext{documents}\)，\(d\) 来自 \(S\) 中某个源文件  
- \(	ext{chunk\_count}\) 仅取决于 \(|S|\) 与 \(	heta_{	ext{new}}\)  
- `last_rebuilt_at` 单调更新（同进程连续 rebuild）

## 3. 与 evaluate 协作

`rebuild_with_best_config`：

```python
results = run_ab_experiment(doc, PRESET_CONFIGS, queries)
best = pick_best_config(results)
store.set_chunk_config(best.config)
return rebuild_store(store, ...)
```

**注意**：evaluate 样例是 `product_notice.md`，全库 rebuild 包含所有源文件——最优配置未必对所有文档全局最优。

## 4. 清空顺序为何重要

必须先 `documents.clear()` 再逐文件 `_append_chunks`，最后单次 `_rebuild_index()`。若中途 `_rebuild_index()` 会导致空索引窗口内 chat 失败——教学单进程可接受。

## 5. 运维 Runbook（摘要）

1. 备份 `data/knowledge/store.json`  
2. 确认 uploads 文档版本  
3. POST evaluate 存档 JSON  
4. POST rebuild（或 apply_best_config）  
5. 检查 last_rebuilt_at、chunk_count  
6. chat 抽测 + 监控错误率  



## 附录：knowledge_store 持久化走读

### `nexus-agent-platform/src/rag/knowledge_store.py` 逐行走读

共 **819** 行。

**L1** `"""`

**L2** `知识库存储 — 文档 ingestion、分块索引与 JSON 持久化`

**L3** ``

**L4** `将 Day 19–20 的 RAG 管线升级为可写入、可落盘的企业知识库 MVP。`

**L5** ``

**L6** `需求：ZL-NA-REQ-025`

**L7** `"""`

**L8** ``

**L9** `from __future__ import annotations`

**L10** ``

**L11** `import threading`

**L12** `from dataclasses import dataclass, field`

**L13** `from datetime import datetime, timezone`

**L14** `from pathlib import Path`

**L15** `from typing import Any`

**L16** ``

**L17** `from core.paths import get_path`

**L18** `from agent.approval_config import ApprovalConfig`

**L19** `from agent.mcp_config import McpConfig`

**L20** `from agent.supervisor_config import SupervisorConfig`

**L21** `from agent.executor_config import ExecutorConfig`

**L22** `from agent.graph_config import GraphConfig`

**L23** `from agent.react_config import ReactConfig`

**L24** `from rag.chunker import TextChunk, chunk_documents, chunk_text`

**L25** `from rag.chunk_config import DEFAULT_CHUNK_CONFIG, ChunkConfig`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L26** `from rag.chunk_strategies import chunk_from_parsed`

**L27** `from rag.context import DocumentIndex, RAGContextService`

**L28** `from rag.chroma_retriever import ChromaEmbeddingRetriever`

**L29** `from rag.chroma_store import VECTOR_BACKEND, ChromaVectorIndex`

**L30** `from rag.citation_config import CitationConfig`

**L31** `from rag.expanding_retriever import ExpandingRetriever`

**L32** `from rag.expansion_config import ExpansionConfig`

**L33** `from rag.answer_validator import RuleBasedAnswerValidator, ValidationResult`

**L34** `from rag.route_config import RouteConfig`

**L35** `from rag.routing_retriever import RoutingRetriever`

**L36** `from rag.validation_config import ValidationConfig`

**L37** `from rag.hybrid_retriever import HybridRetriever`

**L38** `from rag.rerank_config import RerankConfig`

**L39** `from rag.reranker import MockCrossEncoderReranker`

**L40** `from rag.reranking_retriever import RerankingRetriever`

**L41** `from rag.retrieval_config import RetrievalConfig`

**L42** `from rag.rewrite_config import RewriteConfig`

**L43** `from rag.rewriting_retriever import RewritingRetriever`

**L44** `from tools.doc_reader import DocumentRecord, read_text_file`

**L45** `from tools.parsers.base import ParsedDocument`

**L46** `from utils.json_utils import load_json, save_json`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L47** `from utils.text_utils import clean_text`

**L48** ``

**L49** `STORE_VERSION = "1.1"`

**L50** `PLATFORM_VERSION = "0.44.0"`

**L51** `INDEX_MODE_INCREMENTAL = "incremental"`

**L52** `INDEX_MODE_FULL = "full"`

**L53** ``

**L54** ``

**L55** `@dataclass`

**L56** `class KnowledgeDocument:`

**L57** `    """已入库文档元数据"""`

**L58** ``

**L59** `    name: str`

**L60** `    ingested_at: str`

**L61** `    size_bytes: int = 0`

**L62** `    chunk_count: int = 0`

**L63** `    format: str = "txt"`

**L64** ``

**L65** `    def to_dict(self) -> dict[str, Any]:`

**L66** `        return {`

**L67** `            "name": self.name,`

**L68** `            "ingested_at": self.ingested_at,`

**L69** `            "size_bytes": self.size_bytes,`

**L70** `            "chunk_count": self.chunk_count,`

**L71** `            "format": self.format,`

**L72** `        }`

**L73** ``

**L74** `    @classmethod`

**L75** `    def from_dict(cls, data: dict[str, Any]) -> KnowledgeDocument:`

**L76** `        return cls(`

**L77** `            name=str(data.get("name", "")),`

**L78** `            ingested_at=str(data.get("ingested_at", "")),`

**L79** `            size_bytes=int(data.get("size_bytes", 0)),`

**L80** `            chunk_count=int(data.get("chunk_count", 0)),`

**L81** `            format=str(data.get("format", "txt")),`

**L82** `        )`

**L83** ``

**L84** ``

**L85** `@dataclass`

**L86** `class KnowledgeStore:`

**L87** `    """`

**L88** `    企业知识库 — 分块 + Chroma 向量索引 + JSON 元数据持久化`

**L89** ``

**L90** `    典型用法：`

**L91** `        store = KnowledgeStore.load_or_bootstrap()`

**L92** `        store.ingest_text("新产品说明…", filename="notice.txt")`

**L93** `        rag = store.as_rag_service()`

**L94** `    """`

**L95** ``

**L96** `    documents: list[KnowledgeDocument] = field(default_factory=list)`

**L97** `    chunks: list[TextChunk] = field(default_factory=list)`

**L98** `    embedding_state: dict[str, Any] = field(default_factory=dict)`

**L99** `    vector_backend: str = VECTOR_BACKEND`

**L100** `    chunk_config: ChunkConfig = field(default_factory=ChunkConfig)`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L101** `    last_rebuilt_at: str | None = None`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L102** `    last_incremental_at: str | None = None`

**L103** `    index_mode: str = INDEX_MODE_FULL`

**L104** `    retrieval_config: RetrievalConfig = field(default_factory=RetrievalConfig)`

**L105** `    rerank_config: RerankConfig = field(default_factory=RerankConfig)`

**L106** `    rewrite_config: RewriteConfig = field(default_factory=RewriteConfig)`

**L107** `    citation_config: CitationConfig = field(default_factory=CitationConfig)`

**L108** `    expansion_config: ExpansionConfig = field(default_factory=ExpansionConfig)`

**L109** `    route_config: RouteConfig = field(default_factory=RouteConfig)`

**L110** `    validation_config: ValidationConfig = field(default_factory=ValidationConfig)`

**L111** `    react_config: ReactConfig = field(default_factory=ReactConfig)`

**L112** `    executor_config: ExecutorConfig = field(default_factory=ExecutorConfig)`

**L113** `    graph_config: GraphConfig = field(default_factory=GraphConfig)`

**L114** `    approval_config: ApprovalConfig = field(default_factory=ApprovalConfig)`

**L115** `    supervisor_config: SupervisorConfig = field(default_factory=SupervisorConfig)`

**L116** `    mcp_config: McpConfig = field(default_factory=McpConfig)`

**L117** `    store_path: Path | None = None`

**L118** `    chroma_path: Path | None = None`

**L119** `    _rag_service: RAGContextService | None = field(default=None, repr=False)`

**L120** ``

**L121** `    @property`

**L122** `    def chunk_count(self) -> int:`

**L123** `        return len(self.chunks)`

**L124** ``

**L125** `    @property`

**L126** `    def document_count(self) -> int:`

**L127** `        return len(self.documents)`

**L128** ``

**L129** `    def as_rag_service(self) -> RAGContextService:`

**L130** `        """构建或返回缓存的 RAGContextService"""`

**L131** `        if self._rag_service is None:`

**L132** `            self._rag_service = self._build_rag_service()`

**L133** `        return self._rag_service`

**L134** ``

**L135** `    def invalidate_cache(self) -> None:`

**L136** `        self._rag_service = None`

**L137** ``

**L138** `    def get_chunk_config(self) -> ChunkConfig:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L139** `        return ChunkConfig.from_dict(self.chunk_config.to_dict())`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L140** ``

**L141** `    def set_chunk_config(self, config: ChunkConfig) -> ChunkConfig:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L142** `        config.validate()`

**L143** `        self.chunk_config = ChunkConfig.from_dict(config.to_dict())`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L144** `        return self.chunk_config`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L145** ``

**L146** `    def get_retrieval_config(self) -> RetrievalConfig:`

**L147** `        return RetrievalConfig.from_dict(self.retrieval_config.to_dict())`

**L148** ``

**L149** `    def set_retrieval_config(self, config: RetrievalConfig) -> RetrievalConfig:`

**L150** `        config.validate()`

**L151** `        self.retrieval_config = RetrievalConfig.from_dict(config.to_dict())`

**L152** `        self.invalidate_cache()`

**L153** `        return self.retrieval_config`

**L154** ``

**L155** `    def get_rerank_config(self) -> RerankConfig:`

**L156** `        return RerankConfig.from_dict(self.rerank_config.to_dict())`

**L157** ``

**L158** `    def set_rerank_config(self, config: RerankConfig) -> RerankConfig:`

**L159** `        config.validate()`

**L160** `        self.rerank_config = RerankConfig.from_dict(config.to_dict())`

**L161** `        self.invalidate_cache()`

**L162** `        return self.rerank_config`

**L163** ``

**L164** `    def get_rewrite_config(self) -> RewriteConfig:`

**L165** `        return RewriteConfig.from_dict(self.rewrite_config.to_dict())`

**L166** ``

**L167** `    def set_rewrite_config(self, config: RewriteConfig) -> RewriteConfig:`

**L168** `        config.validate()`

**L169** `        self.rewrite_config = RewriteConfig.from_dict(config.to_dict())`

**L170** `        self.invalidate_cache()`

**L171** `        return self.rewrite_config`

**L172** ``

**L173** `    def get_citation_config(self) -> CitationConfig:`

**L174** `        return CitationConfig.from_dict(self.citation_config.to_dict())`

**L175** ``

**L176** `    def set_citation_config(self, config: CitationConfig) -> CitationConfig:`

**L177** `        config.validate()`

**L178** `        self.citation_config = CitationConfig.from_dict(config.to_dict())`

**L179** `        return self.citation_config`

**L180** ``

**L181** `    def get_expansion_config(self) -> ExpansionConfig:`

**L182** `        return ExpansionConfig.from_dict(self.expansion_config.to_dict())`

**L183** ``

**L184** `    def set_expansion_config(self, config: ExpansionConfig) -> ExpansionConfig:`

**L185** `        config.validate()`

**L186** `        self.expansion_config = ExpansionConfig.from_dict(config.to_dict())`

**L187** `        self.invalidate_cache()`

**L188** `        return self.expansion_config`

**L189** ``

**L190** `    def get_route_config(self) -> RouteConfig:`

**L191** `        return RouteConfig.from_dict(self.route_config.to_dict())`

**L192** ``

**L193** `    def set_route_config(self, config: RouteConfig) -> RouteConfig:`

**L194** `        config.validate()`

**L195** `        self.route_config = RouteConfig.from_dict(config.to_dict())`

**L196** `        self.invalidate_cache()`

**L197** `        return self.route_config`

**L198** ``

**L199** `    def get_validation_config(self) -> ValidationConfig:`

**L200** `        return ValidationConfig.from_dict(self.validation_config.to_dict())`

**L201** ``

**L202** `    def set_validation_config(self, config: ValidationConfig) -> ValidationConfig:`

**L203** `        config.validate()`

**L204** `        self.validation_config = ValidationConfig.from_dict(config.to_dict())`

**L205** `        return self.validation_config`

**L206** ``

**L207** `    def get_react_config(self) -> ReactConfig:`

**L208** `        return ReactConfig.from_dict(self.react_config.to_dict())`

**L209** ``

**L210** `    def set_react_config(self, config: ReactConfig) -> ReactConfig:`

**L211** `        config.validate()`

**L212** `        self.react_config = ReactConfig.from_dict(config.to_dict())`

**L213** `        return self.react_config`

**L214** ``

**L215** `    def get_executor_config(self) -> ExecutorConfig:`

**L216** `        return ExecutorConfig.from_dict(self.executor_config.to_dict())`

**L217** ``

**L218** `    def set_executor_config(self, config: ExecutorConfig) -> ExecutorConfig:`

**L219** `        config.validate()`

**L220** `        self.executor_config = ExecutorConfig.from_dict(config.to_dict())`

**L221** `        return self.executor_config`

**L222** ``

**L223** `    def get_graph_config(self) -> GraphConfig:`

**L224** `        return GraphConfig.from_dict(self.graph_config.to_dict())`

**L225** ``

**L226** `    def set_graph_config(self, config: GraphConfig) -> GraphConfig:`

**L227** `        config.validate()`

**L228** `        self.graph_config = GraphConfig.from_dict(config.to_dict())`

**L229** `        return self.graph_config`

**L230** ``

**L231** `    def get_approval_config(self) -> ApprovalConfig:`

**L232** `        return ApprovalConfig.from_dict(self.approval_config.to_dict())`

**L233** ``

**L234** `    def set_approval_config(self, config: ApprovalConfig) -> ApprovalConfig:`

**L235** `        config.validate()`

**L236** `        self.approval_config = ApprovalConfig.from_dict(config.to_dict())`

**L237** `        return self.approval_config`

**L238** ``

**L239** `    def get_supervisor_config(self) -> SupervisorConfig:`

**L240** `        return SupervisorConfig.from_dict(self.supervisor_config.to_dict())`

**L241** ``

**L242** `    def set_supervisor_config(self, config: SupervisorConfig) -> SupervisorConfig:`

**L243** `        config.validate()`

**L244** `        self.supervisor_config = SupervisorConfig.from_dict(config.to_dict())`

**L245** `        return self.supervisor_config`

**L246** ``

**L247** `    def get_mcp_config(self) -> McpConfig:`

**L248** `        return McpConfig.from_dict(self.mcp_config.to_dict())`

**L249** ``

**L250** `    def set_mcp_config(self, config: McpConfig) -> McpConfig:`

**L251** `        config.validate()`

**L252** `        self.mcp_config = McpConfig.from_dict(config.to_dict())`

**L253** `        return self.mcp_config`

**L254** ``

**L255** `    def validate_answer(`

**L256** `        self,`

**L257** `        query: str,`

**L258** `        reply: str,`

**L259** `        citations: list[dict[str, Any]],`

**L260** `    ) -> ValidationResult | None:`

**L261** `        """按当前 validation_config 校验 reply 与 citations 一致性"""`

**L262** `        cfg = self.get_validation_config()`

**L263** `        if not cfg.enabled:`

**L264** `            return None`

**L265** `        validator = RuleBasedAnswerValidator(config=cfg)`

**L266** `        return validator.validate(query, reply, citations)`

**L267** ``

**L268** `    def fetch_citations(self, query: str) -> dict[str, Any]:`

**L269** `        """按当前 citation_config 检索并返回引用包 dict"""`

**L270** `        cfg = self.get_citation_config()`

**L271** `        if not cfg.enabled:`

**L272** `            return {`

**L273** `                "query": query.strip(),`

**L274** `                "citations": [],`

**L275** `                "rewrite": None,`

**L276** `                "expansion": None,`

**L277** `                "route": None,`

**L278** `            }`

**L279** `        rag = self.as_rag_service()`

**L280** `        bundle = rag.retrieve_citation_bundle(query, config=cfg)`

**L281** `        return bundle.to_dict()`

**L282** ``

**L283** `    def fetch_citations_retry(self, query: str, *, attempt: int = 1) -> dict[str, Any]:`

**L284** `        """Self-RAG 重试 — 强制 rag_wide 并放大 citation pool"""`

**L285** `        from rag.citation_config import CitationConfig`

**L286** `        from rag.route_config import INTENT_RAG_WIDE`

**L287** ``

**L288** `        cfg = self.get_citation_config()`

**L289** `        if not cfg.enabled:`

**L290** `            return self.fetch_citations(query)`

**L291** ``

**L292** `        boosted = CitationConfig.from_dict(`

**L293** `            {`

**L294** `                **cfg.to_dict(),`

**L295** `                "max_citations": min(100, max(cfg.max_citations, 20) + attempt * 10),`

**L296** `            }`

**L297** `        )`

**L298** `        rag = self.as_rag_service()`

**L299** `        bundle = rag.retrieve_citation_bundle(`

**L300** `            query,`

**L301** `            config=boosted,`

**L302** `            intent_override=INTENT_RAG_WIDE,`

**L303** `        )`

**L304** `        data = bundle.to_dict()`

**L305** `        data["retry_attempt"] = attempt`

**L306** `        return data`

**L307** ``

**L308** `    def ingest_text(`

**L309** `        self,`

**L310** `        content: str,`

**L311** `        *,`

**L312** `        filename: str,`

**L313** `        clean: bool = True,`

**L314** `        chunk_size: int | None = None,`

**L315** `        overlap: int | None = None,`

**L316** `    ) -> KnowledgeDocument:`

**L317** `        """将文本写入知识库并重建索引"""`

**L318** `        cfg = self.get_chunk_config()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L319** `        cs = chunk_size if chunk_size is not None else cfg.chunk_size`

**L320** `        ov = overlap if overlap is not None else cfg.overlap`

**L321** `        text = (content or "").strip()`

**L322** `        if not text:`

**L323** `            raise ValueError("文档内容不能为空")`

**L324** `        if not filename.strip():`

**L325** `            raise ValueError("filename 不能为空")`

**L326** ``

**L327** `        cleaned = text`

**L328** `        if clean:`

**L329** `            cleaned, _ = clean_text(text)`

**L330** `        doc = DocumentRecord(`

**L331** `            path=Path(filename),`

**L332** `            content=text,`

**L333** `            encoding="utf-8",`

**L334** `            size_bytes=len(text.encode("utf-8")),`

**L335** `            cleaned=cleaned,`

**L336** `        )`

**L337** `        new_chunks = chunk_documents(`

**L338** `            [doc],`

**L339** `            chunk_size=cs,`

**L340** `            overlap=ov,`

**L341** `            use_cleaned=clean,`

**L342** `        )`

**L343** `        self._append_chunks(filename, new_chunks, size_bytes=doc.size_bytes)`

**L344** `        self._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L345** `        return self.documents[-1]`

**L346** ``

**L347** `    def ingest_file(`

**L348** `        self,`

**L349** `        path: Path,`

**L350** `        *,`

**L351** `        clean: bool = True,`

**L352** `        chunk_size: int = 200,`

**L353** `        overlap: int = 40,`

**L354** `    ) -> KnowledgeDocument:`

**L355** `        """从磁盘文件 ingestion"""`

**L356** `        content, encoding = read_text_file(path)`

**L357** `        cleaned = content`

**L358** `        if clean:`

**L359** `            cleaned, _ = clean_text(content)`

**L360** `        doc = DocumentRecord(`

**L361** `            path=path,`

**L362** `            content=content,`

**L363** `            encoding=encoding,`

**L364** `            size_bytes=path.stat().st_size,`

**L365** `            cleaned=cleaned,`

**L366** `        )`

**L367** `        new_chunks = chunk_documents(`

**L368** `            [doc],`

**L369** `            chunk_size=chunk_size,`

**L370** `            overlap=overlap,`

**L371** `            use_cleaned=clean,`

**L372** `        )`

**L373** `        self._append_chunks(path.name, new_chunks, size_bytes=doc.size_bytes)`

**L374** `        self._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L375** `        return self.documents[-1]`

**L376** ``

**L377** `    def ingest_bytes(`

**L378** `        self,`

**L379** `        data: bytes,`

**L380** `        *,`

**L381** `        filename: str,`

**L382** `        clean: bool = True,`

**L383** `        chunk_strategy: str = "auto",`

**L384** `        incremental: bool = True,`

**L385** `    ) -> KnowledgeDocument:`

**L386** `        """处理上传二进制 — Day 26 起委托 doc_parser"""`

**L387** `        from tools.doc_parser import parse_bytes`

**L388** ``

**L389** `        parsed = parse_bytes(data, filename)`

**L390** `        cfg = self.get_chunk_config()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L391** `        return self.ingest_parsed(`

**L392** `            parsed,`

**L393** `            clean=clean,`

**L394** `            chunk_strategy=chunk_strategy if chunk_strategy != "auto" else cfg.strategy,`

**L395** `            incremental=incremental,`

**L396** `        )`

**L397** ``

**L398** `    def ingest_parsed(`

**L399** `        self,`

**L400** `        parsed: ParsedDocument,`

**L401** `        *,`

**L402** `        clean: bool = True,`

**L403** `        chunk_strategy: str | None = None,`

**L404** `        chunk_size: int | None = None,`

**L405** `        overlap: int | None = None,`

**L406** `        incremental: bool = False,`

**L407** `    ) -> KnowledgeDocument:`

**L408** `        """将 ParsedDocument 写入知识库"""`

**L409** `        cfg = self.get_chunk_config()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L410** `        strategy = chunk_strategy if chunk_strategy is not None else cfg.strategy`

**L411** `        cs = chunk_size if chunk_size is not None else cfg.chunk_size`

**L412** `        ov = overlap if overlap is not None else cfg.overlap`

**L413** `        text = (parsed.plain_text or "").strip()`

**L414** `        if not text:`

**L415** `            raise ValueError("解析结果为空")`

**L416** ``

**L417** `        if clean:`

**L418** `            text, _ = clean_text(text)`

**L419** `            parsed.plain_text = text`

**L420** `            for section in parsed.sections:`

**L421** `                section.body, _ = clean_text(section.body)`

**L422** ``

**L423** `        new_chunks = chunk_from_parsed(`

**L424** `            parsed,`

**L425** `            strategy=strategy,`

**L426** `            chunk_size=cs,`

**L427** `            overlap=ov,`

**L428** `        )`

**L429** `        size_bytes = len(parsed.plain_text.encode("utf-8"))`

**L430** ``

**L431** `        if incremental:`

**L432** `            removed = self._remove_document_by_source(parsed.filename)`

**L433** `            self._append_chunks(`

**L434** `                parsed.filename,`

**L435** `                new_chunks,`

**L436** `                size_bytes=size_bytes,`

**L437** `                doc_format=parsed.format,`

**L438** `            )`

**L439** `            self._incremental_index(new_chunks, replaced_count=len(removed))`

**L440** `        else:`

**L441** `            self._append_chunks(`

**L442** `                parsed.filename,`

**L443** `                new_chunks,`

**L444** `                size_bytes=size_bytes,`

**L445** `                doc_format=parsed.format,`

**L446** `            )`

**L447** `            self._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L448** `        return self.documents[-1]`

**L449** ``

**L450** `    def save(self, path: Path | None = None) -> Path:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L451** `        """持久化到 JSON"""`

**L452** `        target = path or self.store_path or _default_store_path()`

**L453** `        self.store_path = target`

**L454** `        payload = {`

**L455** `            "version": STORE_VERSION,`

**L456** `            "platform_version": PLATFORM_VERSION,`

**L457** `            "documents": [d.to_dict() for d in self.documents],`

**L458** `            "chunks": [_chunk_to_dict(c) for c in self.chunks],`

**L459** `            "embedding": self.embedding_state,`

**L460** `            "vector_backend": self.vector_backend,`

**L461** `            "chunk_config": self.chunk_config.to_dict(),`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L462** `            "last_rebuilt_at": self.last_rebuilt_at,`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L463** `            "last_incremental_at": self.last_incremental_at,`

**L464** `            "index_mode": self.index_mode,`

**L465** `            "retrieval_config": self.retrieval_config.to_dict(),`

**L466** `            "rerank_config": self.rerank_config.to_dict(),`

**L467** `            "rewrite_config": self.rewrite_config.to_dict(),`

**L468** `            "citation_config": self.citation_config.to_dict(),`

**L469** `            "expansion_config": self.expansion_config.to_dict(),`

**L470** `            "route_config": self.route_config.to_dict(),`

**L471** `            "validation_config": self.validation_config.to_dict(),`

**L472** `            "react_config": self.react_config.to_dict(),`

**L473** `            "executor_config": self.executor_config.to_dict(),`

**L474** `            "graph_config": self.graph_config.to_dict(),`

**L475** `            "approval_config": self.approval_config.to_dict(),`

**L476** `            "supervisor_config": self.supervisor_config.to_dict(),`

**L477** `            "mcp_config": self.mcp_config.to_dict(),`

**L478** `        }`

**L479** `        save_json(target, payload)`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L480** `        return target`

**L481** ``

**L482** `    @classmethod`

**L483** `    def load(cls, path: Path) -> KnowledgeStore:`

**L484** `        """从 JSON 加载知识库"""`

**L485** `        raw = load_json(path, default=None)`

**L486** `        if not raw:`

**L487** `            return cls.bootstrap_from_sample_docs(store_path=path)`

**L488** ``

**L489** `        store = cls(store_path=path)`

**L490** `        store.documents = [`

**L491** `            KnowledgeDocument.from_dict(d) for d in raw.get("documents", [])`

**L492** `        ]`

**L493** `        store.chunks = [_chunk_from_dict(c) for c in raw.get("chunks", [])]`

**L494** `        store.embedding_state = dict(raw.get("embedding") or {})`

**L495** `        store.vector_backend = str(raw.get("vector_backend") or VECTOR_BACKEND)`

**L496** `        if raw.get("chunk_config"):`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L497** `            store.chunk_config = ChunkConfig.from_dict(raw["chunk_config"])`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L498** `        store.last_rebuilt_at = raw.get("last_rebuilt_at")`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L499** `        store.last_incremental_at = raw.get("last_incremental_at")`

**L500** `        store.index_mode = str(raw.get("index_mode") or INDEX_MODE_FULL)`

**L501** `        if raw.get("retrieval_config"):`

**L502** `            store.retrieval_config = RetrievalConfig.from_dict(raw["retrieval_config"])`

**L503** `        if raw.get("rerank_config"):`

**L504** `            store.rerank_config = RerankConfig.from_dict(raw["rerank_config"])`

**L505** `        if raw.get("rewrite_config"):`

**L506** `            store.rewrite_config = RewriteConfig.from_dict(raw["rewrite_config"])`

**L507** `        if raw.get("citation_config"):`

**L508** `            store.citation_config = CitationConfig.from_dict(raw["citation_config"])`

**L509** `        if raw.get("expansion_config"):`

**L510** `            store.expansion_config = ExpansionConfig.from_dict(raw["expansion_config"])`

**L511** `        if raw.get("route_config"):`

**L512** `            store.route_config = RouteConfig.from_dict(raw["route_config"])`

**L513** `        if raw.get("validation_config"):`

**L514** `            store.validation_config = ValidationConfig.from_dict(raw["validation_config"])`

**L515** `        if raw.get("react_config"):`

**L516** `            store.react_config = ReactConfig.from_dict(raw["react_config"])`

**L517** `        if raw.get("executor_config"):`

**L518** `            store.executor_config = ExecutorConfig.from_dict(raw["executor_config"])`

**L519** `        if raw.get("graph_config"):`

**L520** `            store.graph_config = GraphConfig.from_dict(raw["graph_config"])`

**L521** `        if raw.get("approval_config"):`

**L522** `            store.approval_config = ApprovalConfig.from_dict(raw["approval_config"])`

**L523** `        if raw.get("supervisor_config"):`

**L524** `            store.supervisor_config = SupervisorConfig.from_dict(raw["supervisor_config"])`

**L525** `        if raw.get("mcp_config"):`

**L526** `            store.mcp_config = McpConfig.from_dict(raw["mcp_config"])`

**L527** `        store._sync_chroma_from_json()`

**L528** `        store._rag_service = store._build_rag_service()`

**L529** `        return store`

**L530** ``

**L531** `    @classmethod`

**L532** `    def load_or_bootstrap(cls, path: Path | None = None) -> KnowledgeStore:`

**L533** `        """加载已有库，不存在则从 sample_docs 引导"""`

**L534** `        target = path or _default_store_path()`

**L535** `        if target.is_file():`

**L536** `            return cls.load(target)`

**L537** `        store = cls.bootstrap_from_sample_docs(store_path=target)`

**L538** `        store.save(target)`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L539** `        return store`

**L540** ``

**L541** `    @classmethod`

**L542** `    def bootstrap_from_sample_docs(cls, *, store_path: Path | None = None) -> KnowledgeStore:`

**L543** `        """用内置 sample_docs 初始化知识库"""`

**L544** `        rag = RAGContextService.from_sample_docs(use_embedding=True)`

**L545** `        store = cls(store_path=store_path)`

**L546** `        now = _utc_now()`

**L547** ``

**L548** `        by_source: dict[str, list[TextChunk]] = {}`

**L549** `        for chunk in rag.index.chunks:`

**L550** `            by_source.setdefault(chunk.source, []).append(chunk)`

**L551** ``

**L552** `        for source, chunks in sorted(by_source.items()):`

**L553** `            store.documents.append(`

**L554** `                KnowledgeDocument(`

**L555** `                    name=source,`

**L556** `                    ingested_at=now,`

**L557** `                    size_bytes=sum(len(c.text) for c in chunks),`

**L558** `                    chunk_count=len(chunks),`

**L559** `                )`

**L560** `            )`

**L561** `        store.chunks = list(rag.index.chunks)`

**L562** `        retriever = rag.index.retriever`

**L563** `        from rag.embedding_retriever import EmbeddingRetriever`

**L564** ``

**L565** `        if isinstance(retriever, EmbeddingRetriever):`

**L566** `            store.embedding_state = retriever._client.model.export_state()`

**L567** `        store._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L568** `        return store`

**L569** ``

**L570** `    def status_dict(self) -> dict[str, Any]:`

**L571** `        from tools.doc_parser import supported_formats`

**L572** ``

**L573** `        return {`

**L574** `            "document_count": self.document_count,`

**L575** `            "chunk_count": self.chunk_count,`

**L576** `            "documents": [d.to_dict() for d in self.documents],`

**L577** `            "store_path": str(self.store_path) if self.store_path else None,`

**L578** `            "platform_version": PLATFORM_VERSION,`

**L579** `            "supported_formats": supported_formats(),`

**L580** `            "chunk_config": self.chunk_config.to_dict(),`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L581** `            "last_rebuilt_at": self.last_rebuilt_at,`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L582** `            "last_incremental_at": self.last_incremental_at,`

**L583** `            "index_mode": self.index_mode,`

**L584** `            "retrieval_config": self.retrieval_config.to_dict(),`

**L585** `            "rerank_config": self.rerank_config.to_dict(),`

**L586** `            "rewrite_config": self.rewrite_config.to_dict(),`

**L587** `            "citation_config": self.citation_config.to_dict(),`

**L588** `            "expansion_config": self.expansion_config.to_dict(),`

**L589** `            "route_config": self.route_config.to_dict(),`

**L590** `            "validation_config": self.validation_config.to_dict(),`

**L591** `            "react_config": self.react_config.to_dict(),`

**L592** `            "executor_config": self.executor_config.to_dict(),`

**L593** `            "graph_config": self.graph_config.to_dict(),`

**L594** `            "approval_config": self.approval_config.to_dict(),`

**L595** `            "supervisor_config": self.supervisor_config.to_dict(),`

**L596** `            "mcp_config": self.mcp_config.to_dict(),`

**L597** `            "vector_backend": self.vector_backend,`

**L598** `            "chroma_path": str(self._resolve_chroma_path()),`

**L599** `            "chroma_count": self._chroma_index().count() if self.chunks else 0,`

**L600** `        }`

**L601** ``

**L602** `    def _append_chunks(`

**L603** `        self,`

**L604** `        filename: str,`

**L605** `        new_chunks: list[TextChunk],`

**L606** `        *,`

**L607** `        size_bytes: int,`

**L608** `        doc_format: str = "txt",`

**L609** `    ) -> None:`

**L610** `        base_index = len(self.chunks)`

**L611** `        reindexed: list[TextChunk] = []`

**L612** `        for i, chunk in enumerate(new_chunks):`

**L613** `            reindexed.append(`

**L614** `                TextChunk(`

**L615** `                    chunk_id=chunk.chunk_id,`

**L616** `                    text=chunk.text,`

**L617** `                    source=filename,`

**L618** `                    index=base_index + i,`

**L619** `                    start_char=chunk.start_char,`

**L620** `                    end_char=chunk.end_char,`

**L621** `                )`

**L622** `            )`

**L623** `        self.chunks.extend(reindexed)`

**L624** `        self.documents.append(`

**L625** `            KnowledgeDocument(`

**L626** `                name=filename,`

**L627** `                ingested_at=_utc_now(),`

**L628** `                size_bytes=size_bytes,`

**L629** `                chunk_count=len(reindexed),`

**L630** `                format=doc_format,`

**L631** `            )`

**L632** `        )`

**L633** ``

**L634** `    def _remove_document_by_source(self, filename: str) -> list[str]:`

**L635** `        """移除同名文档及其 chunks，并从 Chroma 删除旧向量"""`

**L636** `        removed_ids = [c.chunk_id for c in self.chunks if c.source == filename]`

**L637** `        if not removed_ids:`

**L638** `            return []`

**L639** ``

**L640** `        self._chroma_index().delete_by_ids(removed_ids)`

**L641** `        self.documents = [d for d in self.documents if d.name != filename]`

**L642** `        self.chunks = [c for c in self.chunks if c.source != filename]`

**L643** `        self.chunks = [`

**L644** `            TextChunk(`

**L645** `                chunk_id=c.chunk_id,`

**L646** `                text=c.text,`

**L647** `                source=c.source,`

**L648** `                index=i,`

**L649** `                start_char=c.start_char,`

**L650** `                end_char=c.end_char,`

**L651** `            )`

**L652** `            for i, c in enumerate(self.chunks)`

**L653** `        ]`

**L654** `        return removed_ids`

**L655** ``

**L656** `    def _resolve_chroma_path(self) -> Path:`

**L657** `        if self.chroma_path is not None:`

**L658** `            return self.chroma_path`

**L659** `        if self.store_path is not None:`

**L660** `            return self.store_path.parent / "chroma"`

**L661** `        return get_path("knowledge_chroma")`

**L662** ``

**L663** `    def _chroma_index(self) -> ChromaVectorIndex:`

**L664** `        return ChromaVectorIndex(self._resolve_chroma_path())`

**L665** ``

**L666** `    def _sync_chroma_from_json(self) -> None:`

**L667** `        """从 JSON 元数据恢复 Chroma（迁移或冷启动）"""`

**L668** `        if not self.chunks or not self.embedding_state:`

**L669** `            return`

**L670** `        chroma = self._chroma_index()`

**L671** `        if chroma.count() > 0:`

**L672** `            return`

**L673** `        from rag.embedding import EmbeddingClient`

**L674** ``

**L675** `        client = EmbeddingClient()`

**L676** `        client.model.load_state(self.embedding_state)`

**L677** `        vectors = client.embed_batch([c.text for c in self.chunks])`

**L678** `        chroma.upsert_chunks(self.chunks, vectors)`

**L679** ``

**L680** `    def _rebuild_index(self) -> None:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L681** `        from rag.embedding_retriever import EmbeddingRetriever`

**L682** ``

**L683** `        if not self.chunks:`

**L684** `            self.embedding_state = {}`

**L685** `            self._chroma_index().reset()`

**L686** `            self.invalidate_cache()`

**L687** `            return`

**L688** ``

**L689** `        retriever = EmbeddingRetriever(self.chunks)`

**L690** `        self.embedding_state = retriever._client.model.export_state()`

**L691** `        vectors = retriever._client.embed_batch([c.text for c in self.chunks])`

**L692** `        chroma = self._chroma_index()`

**L693** `        chroma.reset()`

**L694** `        chroma.upsert_chunks(self.chunks, vectors)`

**L695** `        self.index_mode = INDEX_MODE_FULL`

**L696** `        self.invalidate_cache()`

**L697** ``

**L698** `    def _incremental_index(`

**L699** `        self,`

**L700** `        affected_chunks: list[TextChunk],`

**L701** `        *,`

**L702** `        replaced_count: int = 0,`

**L703** `    ) -> bool:`

**L704** `        """`

**L705** `        增量更新 Chroma：不 reset collection，仅 upsert 受影响 chunk。`

**L706** ``

**L707** `        若 TF-IDF 词表扩张，则回退为全量 upsert（仍不 reset）。`

**L708** `        返回是否发生词表扩张。`

**L709** `        """`

**L710** `        from rag.embedding_retriever import EmbeddingRetriever`

**L711** ``

**L712** `        if not self.chunks:`

**L713** `            self.embedding_state = {}`

**L714** `            self._chroma_index().reset()`

**L715** `            self.last_incremental_at = _utc_now()`

**L716** `            self.index_mode = INDEX_MODE_INCREMENTAL`

**L717** `            self.invalidate_cache()`

**L718** `            return False`

**L719** ``

**L720** `        old_vocab = dict(self.embedding_state.get("vocab") or {})`

**L721** `        retriever = EmbeddingRetriever(self.chunks)`

**L722** `        self.embedding_state = retriever._client.model.export_state()`

**L723** `        new_vocab = dict(self.embedding_state.get("vocab") or {})`

**L724** `        vocab_expanded = len(new_vocab) > len(old_vocab) and bool(old_vocab)`

**L725** ``

**L726** `        if vocab_expanded:`

**L727** `            affected_chunks = list(self.chunks)`

**L728** ``

**L729** `        client = retriever._client`

**L730** `        vectors = client.embed_batch([c.text for c in affected_chunks])`

**L731** `        chroma = self._chroma_index()`

**L732** `        if vocab_expanded:`

**L733** `            # TF-IDF 维度变化时 Chroma collection 须重建`

**L734** `            chroma.reset()`

**L735** `        chroma.upsert_chunks(affected_chunks, vectors)`

**L736** `        self.last_incremental_at = _utc_now()`

**L737** `        self.index_mode = INDEX_MODE_INCREMENTAL`

**L738** `        self.invalidate_cache()`

**L739** `        return vocab_expanded`

**L740** ``

**L741** `    def _build_rag_service(self) -> RAGContextService:`

**L742** `        if not self.chunks:`

**L743** `            return RAGContextService()`

**L744** ``

**L745** `        from rag.embedding import EmbeddingClient`

**L746** `        from rag.retriever import KeywordRetriever`

**L747** ``

**L748** `        client = EmbeddingClient()`

**L749** `        client.model.load_state(self.embedding_state)`

**L750** `        self._sync_chroma_from_json()`

**L751** `        chroma = self._chroma_index()`

**L752** `        vector = ChromaEmbeddingRetriever(self.chunks, chroma, client=client)`

**L753** `        keyword = KeywordRetriever(self.chunks)`

**L754** `        cfg = self.get_retrieval_config()`

**L755** `        hybrid = HybridRetriever(self.chunks, keyword, vector, config=cfg)`

**L756** `        rerank_cfg = self.get_rerank_config()`

**L757** `        reranking = RerankingRetriever(`

**L758** `            hybrid,`

**L759** `            reranker=MockCrossEncoderReranker(),`

**L760** `            config=rerank_cfg,`

**L761** `        )`

**L762** `        rewrite_cfg = self.get_rewrite_config()`

**L763** `        rewriting = RewritingRetriever(reranking, config=rewrite_cfg)`

**L764** `        expansion_cfg = self.get_expansion_config()`

**L765** `        expanding = ExpandingRetriever(rewriting, config=expansion_cfg)`

**L766** `        route_cfg = self.get_route_config()`

**L767** `        retriever = RoutingRetriever(expanding, config=route_cfg)`

**L768** `        index = DocumentIndex(chunks=self.chunks, retriever=retriever)`

**L769** `        return RAGContextService(index)`

**L770** ``

**L771** ``

**L772** `_store: KnowledgeStore | None = None`

**L773** `_store_lock = threading.Lock()`

**L774** ``

**L775** ``

**L776** `def get_knowledge_store(*, reload: bool = False) -> KnowledgeStore:`

**L777** `    """全局知识库单例（API 与 factory 共享）"""`

**L778** `    global _store`

**L779** `    with _store_lock:`

**L780** `        if _store is None or reload:`

**L781** `            _store = KnowledgeStore.load_or_bootstrap()`

**L782** `        return _store`

**L783** ``

**L784** ``

**L785** `def set_knowledge_store(store: KnowledgeStore) -> None:`

**L786** `    """测试注入用"""`

**L787** `    global _store`

**L788** `    with _store_lock:`

**L789** `        _store = store`

**L790** ``

**L791** ``

**L792** `def _default_store_path() -> Path:`

**L793** `    return get_path("knowledge_store")`

**L794** ``

**L795** ``

**L796** `def _utc_now() -> str:`

**L797** `    return datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")`

**L798** ``

**L799** ``

**L800** `def _chunk_to_dict(chunk: TextChunk) -> dict[str, Any]:`

**L801** `    return {`

**L802** `        "chunk_id": chunk.chunk_id,`

**L803** `        "text": chunk.text,`

**L804** `        "source": chunk.source,`

**L805** `        "index": chunk.index,`

**L806** `        "start_char": chunk.start_char,`

**L807** `        "end_char": chunk.end_char,`

**L808** `    }`

**L809** ``

**L810** ``

**L811** `def _chunk_from_dict(data: dict[str, Any]) -> TextChunk:`

**L812** `    return TextChunk(`

**L813** `        chunk_id=str(data.get("chunk_id", "")),`

**L814** `        text=str(data.get("text", "")),`

**L815** `        source=str(data.get("source", "")),`

**L816** `        index=int(data.get("index", 0)),`

**L817** `        start_char=int(data.get("start_char", 0)),`

**L818** `        end_char=int(data.get("end_char", 0)),`

**L819** `    )`


## 附录：api/knowledge.py 重建端点走读

### `nexus-agent-platform/src/api/knowledge.py` 逐行走读

共 **450** 行。

**L1** `"""`

**L2** `知识库 REST API — 文档上传、分块调参与检索评估`

**L3** ``

**L4** `需求：ZL-NA-REQ-025 / ZL-NA-REQ-026 / ZL-NA-REQ-027 / ZL-NA-REQ-028 / ZL-NA-REQ-029 / ZL-NA-REQ-030 / ZL-NA-REQ-031 / ZL-NA-REQ-032 / ZL-NA-REQ-033 / ZL-NA-REQ-034 / ZL-NA-REQ-035 / ZL-NA-REQ-036 / ZL-NA-REQ-037 / ZL-NA-REQ-038`

**L5** `"""`

**L6** ``

**L7** `from __future__ import annotations`

**L8** ``

**L9** `from pathlib import Path`

**L10** ``

**L11** `from fastapi import APIRouter, File, HTTPException, UploadFile`

**L12** ``

**L13** `from api.schemas import (`

**L14** `    ChunkConfigRequest,`

**L15** `    ChunkConfigResponse,`

**L16** `    CitationConfigRequest,`

**L17** `    CitationConfigResponse,`

**L18** `    CitationPreviewRequest,`

**L19** `    CitationPreviewResponse,`

**L20** `    ExpansionConfigRequest,`

**L21** `    ExpansionConfigResponse,`

**L22** `    ExpansionPreviewRequest,`

**L23** `    ExpansionPreviewResponse,`

**L24** `    EvaluateRequest,`

**L25** `    EvaluateResponse,`

**L26** `    KnowledgeStatusResponse,`

**L27** `    KnowledgeUploadResponse,`

**L28** `    RebuildRequest,`

**L29** `    RebuildResponse,`

**L30** `    RerankConfigRequest,`

**L31** `    RerankConfigResponse,`

**L32** `    RewriteConfigRequest,`

**L33** `    RewriteConfigResponse,`

**L34** `    RewritePreviewRequest,`

**L35** `    RewritePreviewResponse,`

**L36** `    RouteConfigRequest,`

**L37** `    RouteConfigResponse,`

**L38** `    RoutePreviewRequest,`

**L39** `    RoutePreviewResponse,`

**L40** `    ValidationConfigRequest,`

**L41** `    ValidationConfigResponse,`

**L42** `    ValidationPreviewRequest,`

**L43** `    ValidationPreviewResponse,`

**L44** `    ValidationRetryPreviewRequest,`

**L45** `    ValidationRetryPreviewResponse,`

**L46** `    RetrievalConfigRequest,`

**L47** `    RetrievalConfigResponse,`

**L48** `)`

**L49** `from api.sessions import session_manager`
  → rebuild API 相关行。

**L50** `from core.exceptions import NexusError, StorageError`

**L51** `from rag.chunk_config import PRESET_CONFIGS, ChunkConfig`

**L52** `from rag.ingestion import ingest_upload`

**L53** `from rag.knowledge_rebuild import rebuild_store, rebuild_with_best_config`
  → rebuild API 相关行。

**L54** `from rag.knowledge_store import get_knowledge_store`

**L55** `from rag.citation_config import CitationConfig`

**L56** `from rag.expansion_config import ExpansionConfig`

**L57** `from rag.query_expander import build_expander`

**L58** `from rag.query_rewriter import RuleBasedQueryRewriter`

**L59** `from rag.query_router import RuleBasedQueryRouter`

**L60** `from rag.route_config import RouteConfig`

**L61** `from rag.validation_config import ValidationConfig`

**L62** `from rag.rerank_config import RerankConfig`

**L63** `from rag.rewrite_config import RewriteConfig`

**L64** `from rag.retrieval_config import RetrievalConfig`

**L65** `from rag.retrieval_eval import EvalQuery, pick_best_config, run_ab_experiment`

**L66** `from tools.doc_parser import parse_bytes`

**L67** ``

**L68** `router = APIRouter(prefix="/api/knowledge", tags=["knowledge"])`

**L69** ``

**L70** `MAX_UPLOAD_BYTES = 512_000  # 500 KB 教学上限`

**L71** `_EVAL_SAMPLE = (`

**L72** `    Path(__file__).resolve().parent.parent / "day26" / "sample_docs" / "product_notice.md"`

**L73** `)`

**L74** ``

**L75** ``

**L76** `@router.get("/retrieval-config", response_model=RetrievalConfigResponse)`

**L77** `def get_retrieval_config() -> RetrievalConfigResponse:`

**L78** `    """返回当前检索模式（vector / keyword / hybrid）与融合参数"""`

**L79** `    cfg = get_knowledge_store().get_retrieval_config()`

**L80** `    return RetrievalConfigResponse(**cfg.to_dict())`

**L81** ``

**L82** ``

**L83** `@router.put("/retrieval-config", response_model=RetrievalConfigResponse)`

**L84** `def update_retrieval_config(body: RetrievalConfigRequest) -> RetrievalConfigResponse:`

**L85** `    """更新检索策略；变更后清除 RAG 缓存"""`

**L86** `    store = get_knowledge_store()`

**L87** `    try:`

**L88** `        cfg = RetrievalConfig.from_dict(body.model_dump())`

**L89** `        cfg.validate()`

**L90** `        store.set_retrieval_config(cfg)`

**L91** `        store.save()`

**L92** `    except ValueError as exc:`

**L93** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L94** `    return RetrievalConfigResponse(**cfg.to_dict())`

**L95** ``

**L96** ``

**L97** `@router.get("/rerank-config", response_model=RerankConfigResponse)`

**L98** `def get_rerank_config() -> RerankConfigResponse:`

**L99** `    """返回 rerank 开关、候选池大小与模型标识"""`

**L100** `    cfg = get_knowledge_store().get_rerank_config()`

**L101** `    return RerankConfigResponse(**cfg.to_dict())`

**L102** ``

**L103** ``

**L104** `@router.put("/rerank-config", response_model=RerankConfigResponse)`

**L105** `def update_rerank_config(body: RerankConfigRequest) -> RerankConfigResponse:`

**L106** `    """更新 rerank 策略；变更后清除 RAG 缓存"""`

**L107** `    store = get_knowledge_store()`

**L108** `    try:`

**L109** `        cfg = RerankConfig.from_dict(body.model_dump())`

**L110** `        cfg.validate()`

**L111** `        store.set_rerank_config(cfg)`

**L112** `        store.save()`

**L113** `    except ValueError as exc:`

**L114** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L115** `    return RerankConfigResponse(**cfg.to_dict())`

**L116** ``

**L117** ``

**L118** `@router.get("/rewrite-config", response_model=RewriteConfigResponse)`

**L119** `def get_rewrite_config() -> RewriteConfigResponse:`

**L120** `    """返回查询改写开关与规则模式"""`

**L121** `    cfg = get_knowledge_store().get_rewrite_config()`

**L122** `    return RewriteConfigResponse(**cfg.to_dict())`

**L123** ``

**L124** ``

**L125** `@router.put("/rewrite-config", response_model=RewriteConfigResponse)`

**L126** `def update_rewrite_config(body: RewriteConfigRequest) -> RewriteConfigResponse:`

**L127** `    """更新查询改写策略；变更后清除 RAG 缓存"""`

**L128** `    store = get_knowledge_store()`

**L129** `    try:`

**L130** `        cfg = RewriteConfig.from_dict(body.model_dump())`

**L131** `        cfg.validate()`

**L132** `        store.set_rewrite_config(cfg)`

**L133** `        store.save()`

**L134** `    except ValueError as exc:`

**L135** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L136** `    return RewriteConfigResponse(**cfg.to_dict())`

**L137** ``

**L138** ``

**L139** `@router.post("/rewrite-preview", response_model=RewritePreviewResponse)`

**L140** `def rewrite_preview(body: RewritePreviewRequest) -> RewritePreviewResponse:`

**L141** `    """预览单条 query 的规则改写结果（不触发检索）"""`

**L142** `    store = get_knowledge_store()`

**L143** `    cfg = store.get_rewrite_config()`

**L144** `    rewriter = RuleBasedQueryRewriter(config=cfg)`

**L145** `    result = rewriter.rewrite(body.query)`

**L146** `    return RewritePreviewResponse(**result.to_dict())`

**L147** ``

**L148** ``

**L149** `@router.get("/citation-config", response_model=CitationConfigResponse)`

**L150** `def get_citation_config() -> CitationConfigResponse:`

**L151** `    """返回引用溯源开关与展示参数"""`

**L152** `    cfg = get_knowledge_store().get_citation_config()`

**L153** `    return CitationConfigResponse(**cfg.to_dict())`

**L154** ``

**L155** ``

**L156** `@router.put("/citation-config", response_model=CitationConfigResponse)`

**L157** `def update_citation_config(body: CitationConfigRequest) -> CitationConfigResponse:`

**L158** `    """更新引用溯源策略并持久化"""`

**L159** `    store = get_knowledge_store()`

**L160** `    try:`

**L161** `        cfg = CitationConfig.from_dict(body.model_dump())`

**L162** `        cfg.validate()`

**L163** `        store.set_citation_config(cfg)`

**L164** `        store.save()`

**L165** `    except ValueError as exc:`

**L166** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L167** `    return CitationConfigResponse(**cfg.to_dict())`

**L168** ``

**L169** ``

**L170** `@router.post("/citation-preview", response_model=CitationPreviewResponse)`

**L171** `def citation_preview(body: CitationPreviewRequest) -> CitationPreviewResponse:`

**L172** `    """预览单条 query 的检索引用（含 rewrite / expansion 审计）"""`

**L173** `    store = get_knowledge_store()`

**L174** `    data = store.fetch_citations(body.query)`

**L175** `    return CitationPreviewResponse(**data)`

**L176** ``

**L177** ``

**L178** `@router.get("/expansion-config", response_model=ExpansionConfigResponse)`

**L179** `def get_expansion_config() -> ExpansionConfigResponse:`

**L180** `    """返回多 query 扩展开关与参数"""`

**L181** `    cfg = get_knowledge_store().get_expansion_config()`

**L182** `    return ExpansionConfigResponse(**cfg.to_dict())`

**L183** ``

**L184** ``

**L185** `@router.put("/expansion-config", response_model=ExpansionConfigResponse)`

**L186** `def update_expansion_config(body: ExpansionConfigRequest) -> ExpansionConfigResponse:`

**L187** `    """更新多 query 扩展策略并持久化"""`

**L188** `    store = get_knowledge_store()`

**L189** `    try:`

**L190** `        cfg = ExpansionConfig.from_dict(body.model_dump())`

**L191** `        cfg.validate()`

**L192** `        store.set_expansion_config(cfg)`

**L193** `        store.save()`

**L194** `    except ValueError as exc:`

**L195** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L196** `    return ExpansionConfigResponse(**cfg.to_dict())`

**L197** ``

**L198** ``

**L199** `@router.post("/expansion-preview", response_model=ExpansionPreviewResponse)`

**L200** `def expansion_preview(body: ExpansionPreviewRequest) -> ExpansionPreviewResponse:`

**L201** `    """预览单条 query 的多路扩展结果"""`

**L202** `    store = get_knowledge_store()`

**L203** `    cfg = store.get_expansion_config()`

**L204** `    expander = build_expander(cfg)`

**L205** `    result = expander.expand(body.query)`

**L206** `    return ExpansionPreviewResponse(**result.to_dict())`

**L207** ``

**L208** ``

**L209** `@router.get("/route-config", response_model=RouteConfigResponse)`

**L210** `def get_route_config() -> RouteConfigResponse:`

**L211** `    """返回检索管线路由开关与默认意图"""`

**L212** `    cfg = get_knowledge_store().get_route_config()`

**L213** `    return RouteConfigResponse(**cfg.to_dict())`

**L214** ``

**L215** ``

**L216** `@router.put("/route-config", response_model=RouteConfigResponse)`

**L217** `def update_route_config(body: RouteConfigRequest) -> RouteConfigResponse:`

**L218** `    """更新检索管线路由策略并持久化"""`

**L219** `    store = get_knowledge_store()`

**L220** `    try:`

**L221** `        cfg = RouteConfig.from_dict(body.model_dump())`

**L222** `        cfg.validate()`

**L223** `        store.set_route_config(cfg)`

**L224** `        store.save()`

**L225** `    except ValueError as exc:`

**L226** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L227** `    return RouteConfigResponse(**cfg.to_dict())`

**L228** ``

**L229** ``

**L230** `@router.post("/route-preview", response_model=RoutePreviewResponse)`

**L231** `def route_preview(body: RoutePreviewRequest) -> RoutePreviewResponse:`

**L232** `    """预览单条 query 的路由决策（expand/rewrite 开关）"""`

**L233** `    store = get_knowledge_store()`

**L234** `    cfg = store.get_route_config()`

**L235** `    router = RuleBasedQueryRouter(config=cfg)`

**L236** `    result = router.route(body.query)`

**L237** `    return RoutePreviewResponse(**result.to_dict())`

**L238** ``

**L239** ``

**L240** `@router.get("/validation-config", response_model=ValidationConfigResponse)`

**L241** `def get_validation_config() -> ValidationConfigResponse:`

**L242** `    """返回答案校验开关与阈值"""`

**L243** `    cfg = get_knowledge_store().get_validation_config()`

**L244** `    return ValidationConfigResponse(**cfg.to_dict())`

**L245** ``

**L246** ``

**L247** `@router.put("/validation-config", response_model=ValidationConfigResponse)`

**L248** `def update_validation_config(body: ValidationConfigRequest) -> ValidationConfigResponse:`

**L249** `    """更新答案校验策略并持久化"""`

**L250** `    store = get_knowledge_store()`

**L251** `    try:`

**L252** `        cfg = ValidationConfig.from_dict(body.model_dump())`

**L253** `        cfg.validate()`

**L254** `        store.set_validation_config(cfg)`

**L255** `        store.save()`

**L256** `    except ValueError as exc:`

**L257** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L258** `    return ValidationConfigResponse(**cfg.to_dict())`

**L259** ``

**L260** ``

**L261** `@router.post("/validation-preview", response_model=ValidationPreviewResponse)`

**L262** `def validation_preview(body: ValidationPreviewRequest) -> ValidationPreviewResponse:`

**L263** `    """预览单条 query + reply 的引用一致性校验"""`

**L264** `    from rag.answer_validator import RuleBasedAnswerValidator`

**L265** ``

**L266** `    store = get_knowledge_store()`

**L267** `    citations = list(body.citations)`

**L268** `    if not citations:`

**L269** `        cite_data = store.fetch_citations(body.query)`

**L270** `        citations = cite_data.get("citations") or []`

**L271** `    cfg = store.get_validation_config()`

**L272** `    preview_cfg = ValidationConfig.from_dict({**cfg.to_dict(), "enabled": True})`

**L273** `    result = RuleBasedAnswerValidator(config=preview_cfg).validate(`

**L274** `        body.query, body.reply, citations`

**L275** `    )`

**L276** `    return ValidationPreviewResponse(**result.to_dict())`

**L277** ``

**L278** ``

**L279** `@router.post("/validation-retry-preview", response_model=ValidationRetryPreviewResponse)`

**L280** `def validation_retry_preview(`

**L281** `    body: ValidationRetryPreviewRequest,`

**L282** `) -> ValidationRetryPreviewResponse:`

**L283** `    """模拟校验失败后的 rag_wide 重检索与再校验"""`

**L284** `    from rag.validation_retry import apply_validation_retry`

**L285** ``

**L286** `    store = get_knowledge_store()`

**L287** `    cite_data = store.fetch_citations(body.query)`

**L288** `    citations = list(body.citations) or cite_data.get("citations") or []`

**L289** `    if body.citations:`

**L290** `        cite_data = {**cite_data, "citations": citations}`

**L291** ``

**L292** `    saved = store.get_validation_config()`

**L293** `    retry_cfg = ValidationConfig.from_dict(`

**L294** `        {**saved.to_dict(), "enabled": True, "retry_on_fail": True, "max_retries": 1}`

**L295** `    )`

**L296** `    store.set_validation_config(retry_cfg)`

**L297** `    try:`

**L298** `        outcome = apply_validation_retry(`

**L299** `            store,`

**L300** `            body.query,`

**L301** `            body.reply,`

**L302** `            citations,`

**L303** `            cite_data,`

**L304** `        )`

**L305** `    finally:`

**L306** `        store.set_validation_config(saved)`

**L307** ``

**L308** `    if outcome is None:`

**L309** `        raise HTTPException(status_code=400, detail="校验已关闭")`

**L310** `    payload = outcome.validation.to_dict()`

**L311** `    payload["retry_route"] = outcome.cite_data.get("route")`

**L312** `    return ValidationRetryPreviewResponse(**payload)`

**L313** ``

**L314** ``

**L315** `@router.get("/chunk-config", response_model=ChunkConfigResponse)`

**L316** `def get_chunk_config() -> ChunkConfigResponse:`

**L317** `    """返回当前知识库默认分块参数"""`

**L318** `    cfg = get_knowledge_store().get_chunk_config()`

**L319** `    return ChunkConfigResponse(**cfg.to_dict())`

**L320** ``

**L321** ``

**L322** `@router.put("/chunk-config", response_model=ChunkConfigResponse)`

**L323** `def update_chunk_config(body: ChunkConfigRequest) -> ChunkConfigResponse:`

**L324** `    """更新默认分块参数（影响后续上传）"""`

**L325** `    store = get_knowledge_store()`

**L326** `    try:`

**L327** `        cfg = ChunkConfig.from_dict(body.model_dump())`

**L328** `        cfg.validate()`

**L329** `        store.set_chunk_config(cfg)`

**L330** `        store.save()`

**L331** `    except ValueError as exc:`

**L332** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L333** `    return ChunkConfigResponse(**cfg.to_dict())`

**L334** ``

**L335** ``

**L336** `@router.post("/evaluate", response_model=EvaluateResponse)`

**L337** `def evaluate_chunk_configs(body: EvaluateRequest | None = None) -> EvaluateResponse:`

**L338** `    """`

**L339** `    对内置样例文档运行 A/B 分块评估，返回 hit@1 与推荐配置。`

**L340** ``

**L341** `    默认使用 PRESET_CONFIGS 四套预设与 day27 评估问句集。`

**L342** `    """`

**L343** `    body = body or EvaluateRequest()`

**L344** `    if not _EVAL_SAMPLE.is_file():`

**L345** `        raise HTTPException(status_code=500, detail="评估样例文档缺失")`

**L346** ``

**L347** `    doc = parse_bytes(_EVAL_SAMPLE.read_bytes(), _EVAL_SAMPLE.name)`

**L348** `    from day27.constants import EVAL_QUERIES`

**L349** ``

**L350** `    queries = [EvalQuery.from_dict(q) for q in EVAL_QUERIES]`

**L351** ``

**L352** `    if body.use_presets and not body.configs:`

**L353** `        configs = list(PRESET_CONFIGS)`

**L354** `    else:`

**L355** `        configs = [ChunkConfig.from_dict(c.model_dump()) for c in body.configs]`

**L356** `        for c in configs:`

**L357** `            c.validate()`

**L358** ``

**L359** `    results = run_ab_experiment(doc, configs, queries)`

**L360** `    best = pick_best_config(results)`

**L361** `    if not best:`

**L362** `        raise HTTPException(status_code=500, detail="评估未产生结果")`

**L363** ``

**L364** `    return EvaluateResponse(`

**L365** `        best_config=best.to_dict()["config"],`

**L366** `        results=[r.to_dict() for r in results],`

**L367** `        eval_query_count=len(queries),`

**L368** `    )`

**L369** ``

**L370** ``

**L371** `@router.post("/rebuild", response_model=RebuildResponse)`
  → rebuild API 相关行。

**L372** `def rebuild_knowledge_base(body: RebuildRequest | None = None) -> RebuildResponse:`
  → rebuild API 相关行。

**L373** `    """`

**L374** `    按当前 chunk_config（或 evaluate 最优配置）全量重建知识库。`

**L375** ``

**L376** `    重扫 sample_docs + knowledge_uploads，清空后重新分块与索引。`

**L377** `    """`

**L378** `    body = body or RebuildRequest()`

**L379** `    store = get_knowledge_store()`

**L380** ``

**L381** `    if body.apply_best_config:`
  → rebuild API 相关行。

**L382** `        if not _EVAL_SAMPLE.is_file():`

**L383** `            raise HTTPException(status_code=500, detail="评估样例文档缺失")`

**L384** `        report = rebuild_with_best_config(`
  → rebuild API 相关行。

**L385** `            store,`

**L386** `            eval_sample_path=_EVAL_SAMPLE,`

**L387** `            include_sample_docs=body.include_sample_docs,`

**L388** `        )`

**L389** `    else:`

**L390** `        report = rebuild_store(store, include_sample_docs=body.include_sample_docs)`
  → rebuild API 相关行。

**L391** ``

**L392** `    cleared = session_manager.clear_all()`
  → rebuild API 相关行。

**L393** `    data = report.to_dict()`

**L394** `    data["sessions_cleared"] = cleared`

**L395** `    return RebuildResponse(**data)`

**L396** ``

**L397** ``

**L398** `@router.get("/status", response_model=KnowledgeStatusResponse)`

**L399** `def knowledge_status() -> KnowledgeStatusResponse:`

**L400** `    """返回知识库文档数、分块数、支持格式与文档列表"""`

**L401** `    store = get_knowledge_store()`

**L402** `    data = store.status_dict()`

**L403** `    return KnowledgeStatusResponse(**data)`

**L404** ``

**L405** ``

**L406** `@router.post("/upload", response_model=KnowledgeUploadResponse)`

**L407** `async def upload_document(`

**L408** `    file: UploadFile = File(..., description="企业文档 (.txt / .md / .pdf)"),`

**L409** `) -> KnowledgeUploadResponse:`

**L410** `    """`

**L411** `    上传企业文档到知识库：解析 → 落盘 → 分块 → Chroma 向量索引 → 持久化。`

**L412** ``

**L413** `    Day 26 起支持 Markdown 与 PDF。上传成功后清除服务端会话。`

**L414** `    """`

**L415** `    if not file.filename:`

**L416** `        raise HTTPException(status_code=422, detail="缺少文件名")`

**L417** ``

**L418** `    data = await file.read()`

**L419** `    if not data:`

**L420** `        raise HTTPException(status_code=422, detail="文件内容为空")`

**L421** `    if len(data) > MAX_UPLOAD_BYTES:`

**L422** `        raise HTTPException(status_code=413, detail="文件超过 500KB 上限")`

**L423** ``

**L424** `    try:`

**L425** `        meta = ingest_upload(data, file.filename)`

**L426** `    except ValueError as exc:`

**L427** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L428** `    except NexusError as exc:`

**L429** `        status = 400 if exc.code in (`

**L430** `            "PDF_PARSE_ERROR", "PDF_EMPTY", "UNSUPPORTED_FORMAT", "EMPTY_FILE"`

**L431** `        ) else 500`

**L432** `        raise HTTPException(status_code=status, detail=exc.message) from exc`

**L433** `    except StorageError as exc:`

**L434** `        raise HTTPException(status_code=500, detail=exc.message) from exc`

**L435** `    except UnicodeDecodeError as exc:`

**L436** `        raise HTTPException(status_code=400, detail="文本文件须为 UTF-8 编码") from exc`

**L437** ``

**L438** `    cleared = session_manager.clear_all()`
  → rebuild API 相关行。

**L439** `    store = get_knowledge_store()`

**L440** ``

**L441** `    return KnowledgeUploadResponse(`

**L442** `        filename=meta.name,`

**L443** `        format=meta.format,`

**L444** `        chunk_count=meta.chunk_count,`

**L445** `        document_count=store.document_count,`

**L446** `        total_chunks=store.chunk_count,`

**L447** `        sessions_cleared=cleared,`

**L448** `        index_mode=store.index_mode,`

**L449** `        message=f"文档已入库（{meta.format}），增量索引已更新",`

**L450** `    )`


## 6. 失败恢复

重建失败 mid-loop 可能导致空库——教学环境可重新 bootstrap；生产应事务化或先写临时 store。
