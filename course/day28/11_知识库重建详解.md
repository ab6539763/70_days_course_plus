# 知识库重建详解

**Day 28 深度专题** | ZL-NA-REQ-028

## 1. 问题定义

设源文件集合 \(S\)，当前配置 \(	heta\)。生产索引 \(I = 	ext{Index}(	ext{Parse}(S), 	heta_{	ext{old}})\)。配置更新为 \(	heta_{	ext{new}}\) 后，需计算 \(I' = 	ext{Index}(	ext{Parse}(S), 	heta_{	ext{new}})\)。

**无法**通过修改 \(	heta\) 元数据把 \(I\) 就地变为 \(I'\)，因 chunk 文本边界已固定。

## 2. rebuild_store 不变式

- 重建后 \(orall d \in 	ext{documents}\)，\(d\) 来自 \(S\) 中某个源文件  
- \(	ext{chunk\_count}\) 仅取决于 \(|S|\) 与 \(	heta_{	ext{new}}\)  
- `last_rebuilt_at` 单调更新（同进程连续 rebuild）

## 3. 与 evaluate 协作

`rebuild_with_best_config`：

```python
results = run_ab_experiment(doc, PRESET_CONFIGS, queries)
best = pick_best_config(results)
store.set_chunk_config(best.config)
return rebuild_store(store, ...)
```

**注意**：evaluate 样例是 `product_notice.md`，全库 rebuild 包含所有源文件——最优配置未必对所有文档全局最优。

## 4. 清空顺序为何重要

必须先 `documents.clear()` 再逐文件 `_append_chunks`，最后单次 `_rebuild_index()`。若中途 `_rebuild_index()` 会导致空索引窗口内 chat 失败——教学单进程可接受。

## 5. 运维 Runbook（摘要）

1. 备份 `data/knowledge/store.json`  
2. 确认 uploads 文档版本  
3. POST evaluate 存档 JSON  
4. POST rebuild（或 apply_best_config）  
5. 检查 last_rebuilt_at、chunk_count  
6. chat 抽测 + 监控错误率  



## 附录：knowledge_store 持久化走读

### `nexus-agent-platform/src/rag/knowledge_store.py` 逐行走读

共 **682** 行。

**L1** `"""`

**L2** `知识库存储 — 文档 ingestion、分块索引与 JSON 持久化`

**L3** ``

**L4** `将 Day 19–20 的 RAG 管线升级为可写入、可落盘的企业知识库 MVP。`

**L5** ``

**L6** `需求：ZL-NA-REQ-025`

**L7** `"""`

**L8** ``

**L9** `from __future__ import annotations`

**L10** ``

**L11** `import threading`

**L12** `from dataclasses import dataclass, field`

**L13** `from datetime import datetime, timezone`

**L14** `from pathlib import Path`

**L15** `from typing import Any`

**L16** ``

**L17** `from core.paths import get_path`

**L18** `from rag.chunker import TextChunk, chunk_documents, chunk_text`

**L19** `from rag.chunk_config import DEFAULT_CHUNK_CONFIG, ChunkConfig`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L20** `from rag.chunk_strategies import chunk_from_parsed`

**L21** `from rag.context import DocumentIndex, RAGContextService`

**L22** `from rag.chroma_retriever import ChromaEmbeddingRetriever`

**L23** `from rag.chroma_store import VECTOR_BACKEND, ChromaVectorIndex`

**L24** `from rag.citation_config import CitationConfig`

**L25** `from rag.expanding_retriever import ExpandingRetriever`

**L26** `from rag.expansion_config import ExpansionConfig`

**L27** `from rag.route_config import RouteConfig`

**L28** `from rag.routing_retriever import RoutingRetriever`

**L29** `from rag.hybrid_retriever import HybridRetriever`

**L30** `from rag.rerank_config import RerankConfig`

**L31** `from rag.reranker import MockCrossEncoderReranker`

**L32** `from rag.reranking_retriever import RerankingRetriever`

**L33** `from rag.retrieval_config import RetrievalConfig`

**L34** `from rag.rewrite_config import RewriteConfig`

**L35** `from rag.rewriting_retriever import RewritingRetriever`

**L36** `from tools.doc_reader import DocumentRecord, read_text_file`

**L37** `from tools.parsers.base import ParsedDocument`

**L38** `from utils.json_utils import load_json, save_json`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L39** `from utils.text_utils import clean_text`

**L40** ``

**L41** `STORE_VERSION = "1.1"`

**L42** `PLATFORM_VERSION = "0.36.0"`

**L43** `INDEX_MODE_INCREMENTAL = "incremental"`

**L44** `INDEX_MODE_FULL = "full"`

**L45** ``

**L46** ``

**L47** `@dataclass`

**L48** `class KnowledgeDocument:`

**L49** `    """已入库文档元数据"""`

**L50** ``

**L51** `    name: str`

**L52** `    ingested_at: str`

**L53** `    size_bytes: int = 0`

**L54** `    chunk_count: int = 0`

**L55** `    format: str = "txt"`

**L56** ``

**L57** `    def to_dict(self) -> dict[str, Any]:`

**L58** `        return {`

**L59** `            "name": self.name,`

**L60** `            "ingested_at": self.ingested_at,`

**L61** `            "size_bytes": self.size_bytes,`

**L62** `            "chunk_count": self.chunk_count,`

**L63** `            "format": self.format,`

**L64** `        }`

**L65** ``

**L66** `    @classmethod`

**L67** `    def from_dict(cls, data: dict[str, Any]) -> KnowledgeDocument:`

**L68** `        return cls(`

**L69** `            name=str(data.get("name", "")),`

**L70** `            ingested_at=str(data.get("ingested_at", "")),`

**L71** `            size_bytes=int(data.get("size_bytes", 0)),`

**L72** `            chunk_count=int(data.get("chunk_count", 0)),`

**L73** `            format=str(data.get("format", "txt")),`

**L74** `        )`

**L75** ``

**L76** ``

**L77** `@dataclass`

**L78** `class KnowledgeStore:`

**L79** `    """`

**L80** `    企业知识库 — 分块 + Chroma 向量索引 + JSON 元数据持久化`

**L81** ``

**L82** `    典型用法：`

**L83** `        store = KnowledgeStore.load_or_bootstrap()`

**L84** `        store.ingest_text("新产品说明…", filename="notice.txt")`

**L85** `        rag = store.as_rag_service()`

**L86** `    """`

**L87** ``

**L88** `    documents: list[KnowledgeDocument] = field(default_factory=list)`

**L89** `    chunks: list[TextChunk] = field(default_factory=list)`

**L90** `    embedding_state: dict[str, Any] = field(default_factory=dict)`

**L91** `    vector_backend: str = VECTOR_BACKEND`

**L92** `    chunk_config: ChunkConfig = field(default_factory=ChunkConfig)`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L93** `    last_rebuilt_at: str | None = None`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L94** `    last_incremental_at: str | None = None`

**L95** `    index_mode: str = INDEX_MODE_FULL`

**L96** `    retrieval_config: RetrievalConfig = field(default_factory=RetrievalConfig)`

**L97** `    rerank_config: RerankConfig = field(default_factory=RerankConfig)`

**L98** `    rewrite_config: RewriteConfig = field(default_factory=RewriteConfig)`

**L99** `    citation_config: CitationConfig = field(default_factory=CitationConfig)`

**L100** `    expansion_config: ExpansionConfig = field(default_factory=ExpansionConfig)`

**L101** `    route_config: RouteConfig = field(default_factory=RouteConfig)`

**L102** `    store_path: Path | None = None`

**L103** `    chroma_path: Path | None = None`

**L104** `    _rag_service: RAGContextService | None = field(default=None, repr=False)`

**L105** ``

**L106** `    @property`

**L107** `    def chunk_count(self) -> int:`

**L108** `        return len(self.chunks)`

**L109** ``

**L110** `    @property`

**L111** `    def document_count(self) -> int:`

**L112** `        return len(self.documents)`

**L113** ``

**L114** `    def as_rag_service(self) -> RAGContextService:`

**L115** `        """构建或返回缓存的 RAGContextService"""`

**L116** `        if self._rag_service is None:`

**L117** `            self._rag_service = self._build_rag_service()`

**L118** `        return self._rag_service`

**L119** ``

**L120** `    def invalidate_cache(self) -> None:`

**L121** `        self._rag_service = None`

**L122** ``

**L123** `    def get_chunk_config(self) -> ChunkConfig:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L124** `        return ChunkConfig.from_dict(self.chunk_config.to_dict())`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L125** ``

**L126** `    def set_chunk_config(self, config: ChunkConfig) -> ChunkConfig:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L127** `        config.validate()`

**L128** `        self.chunk_config = ChunkConfig.from_dict(config.to_dict())`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L129** `        return self.chunk_config`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L130** ``

**L131** `    def get_retrieval_config(self) -> RetrievalConfig:`

**L132** `        return RetrievalConfig.from_dict(self.retrieval_config.to_dict())`

**L133** ``

**L134** `    def set_retrieval_config(self, config: RetrievalConfig) -> RetrievalConfig:`

**L135** `        config.validate()`

**L136** `        self.retrieval_config = RetrievalConfig.from_dict(config.to_dict())`

**L137** `        self.invalidate_cache()`

**L138** `        return self.retrieval_config`

**L139** ``

**L140** `    def get_rerank_config(self) -> RerankConfig:`

**L141** `        return RerankConfig.from_dict(self.rerank_config.to_dict())`

**L142** ``

**L143** `    def set_rerank_config(self, config: RerankConfig) -> RerankConfig:`

**L144** `        config.validate()`

**L145** `        self.rerank_config = RerankConfig.from_dict(config.to_dict())`

**L146** `        self.invalidate_cache()`

**L147** `        return self.rerank_config`

**L148** ``

**L149** `    def get_rewrite_config(self) -> RewriteConfig:`

**L150** `        return RewriteConfig.from_dict(self.rewrite_config.to_dict())`

**L151** ``

**L152** `    def set_rewrite_config(self, config: RewriteConfig) -> RewriteConfig:`

**L153** `        config.validate()`

**L154** `        self.rewrite_config = RewriteConfig.from_dict(config.to_dict())`

**L155** `        self.invalidate_cache()`

**L156** `        return self.rewrite_config`

**L157** ``

**L158** `    def get_citation_config(self) -> CitationConfig:`

**L159** `        return CitationConfig.from_dict(self.citation_config.to_dict())`

**L160** ``

**L161** `    def set_citation_config(self, config: CitationConfig) -> CitationConfig:`

**L162** `        config.validate()`

**L163** `        self.citation_config = CitationConfig.from_dict(config.to_dict())`

**L164** `        return self.citation_config`

**L165** ``

**L166** `    def get_expansion_config(self) -> ExpansionConfig:`

**L167** `        return ExpansionConfig.from_dict(self.expansion_config.to_dict())`

**L168** ``

**L169** `    def set_expansion_config(self, config: ExpansionConfig) -> ExpansionConfig:`

**L170** `        config.validate()`

**L171** `        self.expansion_config = ExpansionConfig.from_dict(config.to_dict())`

**L172** `        self.invalidate_cache()`

**L173** `        return self.expansion_config`

**L174** ``

**L175** `    def get_route_config(self) -> RouteConfig:`

**L176** `        return RouteConfig.from_dict(self.route_config.to_dict())`

**L177** ``

**L178** `    def set_route_config(self, config: RouteConfig) -> RouteConfig:`

**L179** `        config.validate()`

**L180** `        self.route_config = RouteConfig.from_dict(config.to_dict())`

**L181** `        self.invalidate_cache()`

**L182** `        return self.route_config`

**L183** ``

**L184** `    def fetch_citations(self, query: str) -> dict[str, Any]:`

**L185** `        """按当前 citation_config 检索并返回引用包 dict"""`

**L186** `        cfg = self.get_citation_config()`

**L187** `        if not cfg.enabled:`

**L188** `            return {`

**L189** `                "query": query.strip(),`

**L190** `                "citations": [],`

**L191** `                "rewrite": None,`

**L192** `                "expansion": None,`

**L193** `                "route": None,`

**L194** `            }`

**L195** `        rag = self.as_rag_service()`

**L196** `        bundle = rag.retrieve_citation_bundle(query, config=cfg)`

**L197** `        return bundle.to_dict()`

**L198** ``

**L199** `    def ingest_text(`

**L200** `        self,`

**L201** `        content: str,`

**L202** `        *,`

**L203** `        filename: str,`

**L204** `        clean: bool = True,`

**L205** `        chunk_size: int | None = None,`

**L206** `        overlap: int | None = None,`

**L207** `    ) -> KnowledgeDocument:`

**L208** `        """将文本写入知识库并重建索引"""`

**L209** `        cfg = self.get_chunk_config()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L210** `        cs = chunk_size if chunk_size is not None else cfg.chunk_size`

**L211** `        ov = overlap if overlap is not None else cfg.overlap`

**L212** `        text = (content or "").strip()`

**L213** `        if not text:`

**L214** `            raise ValueError("文档内容不能为空")`

**L215** `        if not filename.strip():`

**L216** `            raise ValueError("filename 不能为空")`

**L217** ``

**L218** `        cleaned = text`

**L219** `        if clean:`

**L220** `            cleaned, _ = clean_text(text)`

**L221** `        doc = DocumentRecord(`

**L222** `            path=Path(filename),`

**L223** `            content=text,`

**L224** `            encoding="utf-8",`

**L225** `            size_bytes=len(text.encode("utf-8")),`

**L226** `            cleaned=cleaned,`

**L227** `        )`

**L228** `        new_chunks = chunk_documents(`

**L229** `            [doc],`

**L230** `            chunk_size=cs,`

**L231** `            overlap=ov,`

**L232** `            use_cleaned=clean,`

**L233** `        )`

**L234** `        self._append_chunks(filename, new_chunks, size_bytes=doc.size_bytes)`

**L235** `        self._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L236** `        return self.documents[-1]`

**L237** ``

**L238** `    def ingest_file(`

**L239** `        self,`

**L240** `        path: Path,`

**L241** `        *,`

**L242** `        clean: bool = True,`

**L243** `        chunk_size: int = 200,`

**L244** `        overlap: int = 40,`

**L245** `    ) -> KnowledgeDocument:`

**L246** `        """从磁盘文件 ingestion"""`

**L247** `        content, encoding = read_text_file(path)`

**L248** `        cleaned = content`

**L249** `        if clean:`

**L250** `            cleaned, _ = clean_text(content)`

**L251** `        doc = DocumentRecord(`

**L252** `            path=path,`

**L253** `            content=content,`

**L254** `            encoding=encoding,`

**L255** `            size_bytes=path.stat().st_size,`

**L256** `            cleaned=cleaned,`

**L257** `        )`

**L258** `        new_chunks = chunk_documents(`

**L259** `            [doc],`

**L260** `            chunk_size=chunk_size,`

**L261** `            overlap=overlap,`

**L262** `            use_cleaned=clean,`

**L263** `        )`

**L264** `        self._append_chunks(path.name, new_chunks, size_bytes=doc.size_bytes)`

**L265** `        self._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L266** `        return self.documents[-1]`

**L267** ``

**L268** `    def ingest_bytes(`

**L269** `        self,`

**L270** `        data: bytes,`

**L271** `        *,`

**L272** `        filename: str,`

**L273** `        clean: bool = True,`

**L274** `        chunk_strategy: str = "auto",`

**L275** `        incremental: bool = True,`

**L276** `    ) -> KnowledgeDocument:`

**L277** `        """处理上传二进制 — Day 26 起委托 doc_parser"""`

**L278** `        from tools.doc_parser import parse_bytes`

**L279** ``

**L280** `        parsed = parse_bytes(data, filename)`

**L281** `        cfg = self.get_chunk_config()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L282** `        return self.ingest_parsed(`

**L283** `            parsed,`

**L284** `            clean=clean,`

**L285** `            chunk_strategy=chunk_strategy if chunk_strategy != "auto" else cfg.strategy,`

**L286** `            incremental=incremental,`

**L287** `        )`

**L288** ``

**L289** `    def ingest_parsed(`

**L290** `        self,`

**L291** `        parsed: ParsedDocument,`

**L292** `        *,`

**L293** `        clean: bool = True,`

**L294** `        chunk_strategy: str | None = None,`

**L295** `        chunk_size: int | None = None,`

**L296** `        overlap: int | None = None,`

**L297** `        incremental: bool = False,`

**L298** `    ) -> KnowledgeDocument:`

**L299** `        """将 ParsedDocument 写入知识库"""`

**L300** `        cfg = self.get_chunk_config()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L301** `        strategy = chunk_strategy if chunk_strategy is not None else cfg.strategy`

**L302** `        cs = chunk_size if chunk_size is not None else cfg.chunk_size`

**L303** `        ov = overlap if overlap is not None else cfg.overlap`

**L304** `        text = (parsed.plain_text or "").strip()`

**L305** `        if not text:`

**L306** `            raise ValueError("解析结果为空")`

**L307** ``

**L308** `        if clean:`

**L309** `            text, _ = clean_text(text)`

**L310** `            parsed.plain_text = text`

**L311** `            for section in parsed.sections:`

**L312** `                section.body, _ = clean_text(section.body)`

**L313** ``

**L314** `        new_chunks = chunk_from_parsed(`

**L315** `            parsed,`

**L316** `            strategy=strategy,`

**L317** `            chunk_size=cs,`

**L318** `            overlap=ov,`

**L319** `        )`

**L320** `        size_bytes = len(parsed.plain_text.encode("utf-8"))`

**L321** ``

**L322** `        if incremental:`

**L323** `            removed = self._remove_document_by_source(parsed.filename)`

**L324** `            self._append_chunks(`

**L325** `                parsed.filename,`

**L326** `                new_chunks,`

**L327** `                size_bytes=size_bytes,`

**L328** `                doc_format=parsed.format,`

**L329** `            )`

**L330** `            self._incremental_index(new_chunks, replaced_count=len(removed))`

**L331** `        else:`

**L332** `            self._append_chunks(`

**L333** `                parsed.filename,`

**L334** `                new_chunks,`

**L335** `                size_bytes=size_bytes,`

**L336** `                doc_format=parsed.format,`

**L337** `            )`

**L338** `            self._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L339** `        return self.documents[-1]`

**L340** ``

**L341** `    def save(self, path: Path | None = None) -> Path:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L342** `        """持久化到 JSON"""`

**L343** `        target = path or self.store_path or _default_store_path()`

**L344** `        self.store_path = target`

**L345** `        payload = {`

**L346** `            "version": STORE_VERSION,`

**L347** `            "platform_version": PLATFORM_VERSION,`

**L348** `            "documents": [d.to_dict() for d in self.documents],`

**L349** `            "chunks": [_chunk_to_dict(c) for c in self.chunks],`

**L350** `            "embedding": self.embedding_state,`

**L351** `            "vector_backend": self.vector_backend,`

**L352** `            "chunk_config": self.chunk_config.to_dict(),`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L353** `            "last_rebuilt_at": self.last_rebuilt_at,`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L354** `            "last_incremental_at": self.last_incremental_at,`

**L355** `            "index_mode": self.index_mode,`

**L356** `            "retrieval_config": self.retrieval_config.to_dict(),`

**L357** `            "rerank_config": self.rerank_config.to_dict(),`

**L358** `            "rewrite_config": self.rewrite_config.to_dict(),`

**L359** `            "citation_config": self.citation_config.to_dict(),`

**L360** `            "expansion_config": self.expansion_config.to_dict(),`

**L361** `            "route_config": self.route_config.to_dict(),`

**L362** `        }`

**L363** `        save_json(target, payload)`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L364** `        return target`

**L365** ``

**L366** `    @classmethod`

**L367** `    def load(cls, path: Path) -> KnowledgeStore:`

**L368** `        """从 JSON 加载知识库"""`

**L369** `        raw = load_json(path, default=None)`

**L370** `        if not raw:`

**L371** `            return cls.bootstrap_from_sample_docs(store_path=path)`

**L372** ``

**L373** `        store = cls(store_path=path)`

**L374** `        store.documents = [`

**L375** `            KnowledgeDocument.from_dict(d) for d in raw.get("documents", [])`

**L376** `        ]`

**L377** `        store.chunks = [_chunk_from_dict(c) for c in raw.get("chunks", [])]`

**L378** `        store.embedding_state = dict(raw.get("embedding") or {})`

**L379** `        store.vector_backend = str(raw.get("vector_backend") or VECTOR_BACKEND)`

**L380** `        if raw.get("chunk_config"):`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L381** `            store.chunk_config = ChunkConfig.from_dict(raw["chunk_config"])`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L382** `        store.last_rebuilt_at = raw.get("last_rebuilt_at")`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L383** `        store.last_incremental_at = raw.get("last_incremental_at")`

**L384** `        store.index_mode = str(raw.get("index_mode") or INDEX_MODE_FULL)`

**L385** `        if raw.get("retrieval_config"):`

**L386** `            store.retrieval_config = RetrievalConfig.from_dict(raw["retrieval_config"])`

**L387** `        if raw.get("rerank_config"):`

**L388** `            store.rerank_config = RerankConfig.from_dict(raw["rerank_config"])`

**L389** `        if raw.get("rewrite_config"):`

**L390** `            store.rewrite_config = RewriteConfig.from_dict(raw["rewrite_config"])`

**L391** `        if raw.get("citation_config"):`

**L392** `            store.citation_config = CitationConfig.from_dict(raw["citation_config"])`

**L393** `        if raw.get("expansion_config"):`

**L394** `            store.expansion_config = ExpansionConfig.from_dict(raw["expansion_config"])`

**L395** `        if raw.get("route_config"):`

**L396** `            store.route_config = RouteConfig.from_dict(raw["route_config"])`

**L397** `        store._sync_chroma_from_json()`

**L398** `        store._rag_service = store._build_rag_service()`

**L399** `        return store`

**L400** ``

**L401** `    @classmethod`

**L402** `    def load_or_bootstrap(cls, path: Path | None = None) -> KnowledgeStore:`

**L403** `        """加载已有库，不存在则从 sample_docs 引导"""`

**L404** `        target = path or _default_store_path()`

**L405** `        if target.is_file():`

**L406** `            return cls.load(target)`

**L407** `        store = cls.bootstrap_from_sample_docs(store_path=target)`

**L408** `        store.save(target)`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L409** `        return store`

**L410** ``

**L411** `    @classmethod`

**L412** `    def bootstrap_from_sample_docs(cls, *, store_path: Path | None = None) -> KnowledgeStore:`

**L413** `        """用内置 sample_docs 初始化知识库"""`

**L414** `        rag = RAGContextService.from_sample_docs(use_embedding=True)`

**L415** `        store = cls(store_path=store_path)`

**L416** `        now = _utc_now()`

**L417** ``

**L418** `        by_source: dict[str, list[TextChunk]] = {}`

**L419** `        for chunk in rag.index.chunks:`

**L420** `            by_source.setdefault(chunk.source, []).append(chunk)`

**L421** ``

**L422** `        for source, chunks in sorted(by_source.items()):`

**L423** `            store.documents.append(`

**L424** `                KnowledgeDocument(`

**L425** `                    name=source,`

**L426** `                    ingested_at=now,`

**L427** `                    size_bytes=sum(len(c.text) for c in chunks),`

**L428** `                    chunk_count=len(chunks),`

**L429** `                )`

**L430** `            )`

**L431** `        store.chunks = list(rag.index.chunks)`

**L432** `        retriever = rag.index.retriever`

**L433** `        from rag.embedding_retriever import EmbeddingRetriever`

**L434** ``

**L435** `        if isinstance(retriever, EmbeddingRetriever):`

**L436** `            store.embedding_state = retriever._client.model.export_state()`

**L437** `        store._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L438** `        return store`

**L439** ``

**L440** `    def status_dict(self) -> dict[str, Any]:`

**L441** `        from tools.doc_parser import supported_formats`

**L442** ``

**L443** `        return {`

**L444** `            "document_count": self.document_count,`

**L445** `            "chunk_count": self.chunk_count,`

**L446** `            "documents": [d.to_dict() for d in self.documents],`

**L447** `            "store_path": str(self.store_path) if self.store_path else None,`

**L448** `            "platform_version": PLATFORM_VERSION,`

**L449** `            "supported_formats": supported_formats(),`

**L450** `            "chunk_config": self.chunk_config.to_dict(),`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L451** `            "last_rebuilt_at": self.last_rebuilt_at,`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L452** `            "last_incremental_at": self.last_incremental_at,`

**L453** `            "index_mode": self.index_mode,`

**L454** `            "retrieval_config": self.retrieval_config.to_dict(),`

**L455** `            "rerank_config": self.rerank_config.to_dict(),`

**L456** `            "rewrite_config": self.rewrite_config.to_dict(),`

**L457** `            "citation_config": self.citation_config.to_dict(),`

**L458** `            "expansion_config": self.expansion_config.to_dict(),`

**L459** `            "route_config": self.route_config.to_dict(),`

**L460** `            "vector_backend": self.vector_backend,`

**L461** `            "chroma_path": str(self._resolve_chroma_path()),`

**L462** `            "chroma_count": self._chroma_index().count() if self.chunks else 0,`

**L463** `        }`

**L464** ``

**L465** `    def _append_chunks(`

**L466** `        self,`

**L467** `        filename: str,`

**L468** `        new_chunks: list[TextChunk],`

**L469** `        *,`

**L470** `        size_bytes: int,`

**L471** `        doc_format: str = "txt",`

**L472** `    ) -> None:`

**L473** `        base_index = len(self.chunks)`

**L474** `        reindexed: list[TextChunk] = []`

**L475** `        for i, chunk in enumerate(new_chunks):`

**L476** `            reindexed.append(`

**L477** `                TextChunk(`

**L478** `                    chunk_id=chunk.chunk_id,`

**L479** `                    text=chunk.text,`

**L480** `                    source=filename,`

**L481** `                    index=base_index + i,`

**L482** `                    start_char=chunk.start_char,`

**L483** `                    end_char=chunk.end_char,`

**L484** `                )`

**L485** `            )`

**L486** `        self.chunks.extend(reindexed)`

**L487** `        self.documents.append(`

**L488** `            KnowledgeDocument(`

**L489** `                name=filename,`

**L490** `                ingested_at=_utc_now(),`

**L491** `                size_bytes=size_bytes,`

**L492** `                chunk_count=len(reindexed),`

**L493** `                format=doc_format,`

**L494** `            )`

**L495** `        )`

**L496** ``

**L497** `    def _remove_document_by_source(self, filename: str) -> list[str]:`

**L498** `        """移除同名文档及其 chunks，并从 Chroma 删除旧向量"""`

**L499** `        removed_ids = [c.chunk_id for c in self.chunks if c.source == filename]`

**L500** `        if not removed_ids:`

**L501** `            return []`

**L502** ``

**L503** `        self._chroma_index().delete_by_ids(removed_ids)`

**L504** `        self.documents = [d for d in self.documents if d.name != filename]`

**L505** `        self.chunks = [c for c in self.chunks if c.source != filename]`

**L506** `        self.chunks = [`

**L507** `            TextChunk(`

**L508** `                chunk_id=c.chunk_id,`

**L509** `                text=c.text,`

**L510** `                source=c.source,`

**L511** `                index=i,`

**L512** `                start_char=c.start_char,`

**L513** `                end_char=c.end_char,`

**L514** `            )`

**L515** `            for i, c in enumerate(self.chunks)`

**L516** `        ]`

**L517** `        return removed_ids`

**L518** ``

**L519** `    def _resolve_chroma_path(self) -> Path:`

**L520** `        if self.chroma_path is not None:`

**L521** `            return self.chroma_path`

**L522** `        if self.store_path is not None:`

**L523** `            return self.store_path.parent / "chroma"`

**L524** `        return get_path("knowledge_chroma")`

**L525** ``

**L526** `    def _chroma_index(self) -> ChromaVectorIndex:`

**L527** `        return ChromaVectorIndex(self._resolve_chroma_path())`

**L528** ``

**L529** `    def _sync_chroma_from_json(self) -> None:`

**L530** `        """从 JSON 元数据恢复 Chroma（迁移或冷启动）"""`

**L531** `        if not self.chunks or not self.embedding_state:`

**L532** `            return`

**L533** `        chroma = self._chroma_index()`

**L534** `        if chroma.count() > 0:`

**L535** `            return`

**L536** `        from rag.embedding import EmbeddingClient`

**L537** ``

**L538** `        client = EmbeddingClient()`

**L539** `        client.model.load_state(self.embedding_state)`

**L540** `        vectors = client.embed_batch([c.text for c in self.chunks])`

**L541** `        chroma.upsert_chunks(self.chunks, vectors)`

**L542** ``

**L543** `    def _rebuild_index(self) -> None:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L544** `        from rag.embedding_retriever import EmbeddingRetriever`

**L545** ``

**L546** `        if not self.chunks:`

**L547** `            self.embedding_state = {}`

**L548** `            self._chroma_index().reset()`

**L549** `            self.invalidate_cache()`

**L550** `            return`

**L551** ``

**L552** `        retriever = EmbeddingRetriever(self.chunks)`

**L553** `        self.embedding_state = retriever._client.model.export_state()`

**L554** `        vectors = retriever._client.embed_batch([c.text for c in self.chunks])`

**L555** `        chroma = self._chroma_index()`

**L556** `        chroma.reset()`

**L557** `        chroma.upsert_chunks(self.chunks, vectors)`

**L558** `        self.index_mode = INDEX_MODE_FULL`

**L559** `        self.invalidate_cache()`

**L560** ``

**L561** `    def _incremental_index(`

**L562** `        self,`

**L563** `        affected_chunks: list[TextChunk],`

**L564** `        *,`

**L565** `        replaced_count: int = 0,`

**L566** `    ) -> bool:`

**L567** `        """`

**L568** `        增量更新 Chroma：不 reset collection，仅 upsert 受影响 chunk。`

**L569** ``

**L570** `        若 TF-IDF 词表扩张，则回退为全量 upsert（仍不 reset）。`

**L571** `        返回是否发生词表扩张。`

**L572** `        """`

**L573** `        from rag.embedding_retriever import EmbeddingRetriever`

**L574** ``

**L575** `        if not self.chunks:`

**L576** `            self.embedding_state = {}`

**L577** `            self._chroma_index().reset()`

**L578** `            self.last_incremental_at = _utc_now()`

**L579** `            self.index_mode = INDEX_MODE_INCREMENTAL`

**L580** `            self.invalidate_cache()`

**L581** `            return False`

**L582** ``

**L583** `        old_vocab = dict(self.embedding_state.get("vocab") or {})`

**L584** `        retriever = EmbeddingRetriever(self.chunks)`

**L585** `        self.embedding_state = retriever._client.model.export_state()`

**L586** `        new_vocab = dict(self.embedding_state.get("vocab") or {})`

**L587** `        vocab_expanded = len(new_vocab) > len(old_vocab) and bool(old_vocab)`

**L588** ``

**L589** `        if vocab_expanded:`

**L590** `            affected_chunks = list(self.chunks)`

**L591** ``

**L592** `        client = retriever._client`

**L593** `        vectors = client.embed_batch([c.text for c in affected_chunks])`

**L594** `        chroma = self._chroma_index()`

**L595** `        if vocab_expanded:`

**L596** `            # TF-IDF 维度变化时 Chroma collection 须重建`

**L597** `            chroma.reset()`

**L598** `        chroma.upsert_chunks(affected_chunks, vectors)`

**L599** `        self.last_incremental_at = _utc_now()`

**L600** `        self.index_mode = INDEX_MODE_INCREMENTAL`

**L601** `        self.invalidate_cache()`

**L602** `        return vocab_expanded`

**L603** ``

**L604** `    def _build_rag_service(self) -> RAGContextService:`

**L605** `        if not self.chunks:`

**L606** `            return RAGContextService()`

**L607** ``

**L608** `        from rag.embedding import EmbeddingClient`

**L609** `        from rag.retriever import KeywordRetriever`

**L610** ``

**L611** `        client = EmbeddingClient()`

**L612** `        client.model.load_state(self.embedding_state)`

**L613** `        self._sync_chroma_from_json()`

**L614** `        chroma = self._chroma_index()`

**L615** `        vector = ChromaEmbeddingRetriever(self.chunks, chroma, client=client)`

**L616** `        keyword = KeywordRetriever(self.chunks)`

**L617** `        cfg = self.get_retrieval_config()`

**L618** `        hybrid = HybridRetriever(self.chunks, keyword, vector, config=cfg)`

**L619** `        rerank_cfg = self.get_rerank_config()`

**L620** `        reranking = RerankingRetriever(`

**L621** `            hybrid,`

**L622** `            reranker=MockCrossEncoderReranker(),`

**L623** `            config=rerank_cfg,`

**L624** `        )`

**L625** `        rewrite_cfg = self.get_rewrite_config()`

**L626** `        rewriting = RewritingRetriever(reranking, config=rewrite_cfg)`

**L627** `        expansion_cfg = self.get_expansion_config()`

**L628** `        expanding = ExpandingRetriever(rewriting, config=expansion_cfg)`

**L629** `        route_cfg = self.get_route_config()`

**L630** `        retriever = RoutingRetriever(expanding, config=route_cfg)`

**L631** `        index = DocumentIndex(chunks=self.chunks, retriever=retriever)`

**L632** `        return RAGContextService(index)`

**L633** ``

**L634** ``

**L635** `_store: KnowledgeStore | None = None`

**L636** `_store_lock = threading.Lock()`

**L637** ``

**L638** ``

**L639** `def get_knowledge_store(*, reload: bool = False) -> KnowledgeStore:`

**L640** `    """全局知识库单例（API 与 factory 共享）"""`

**L641** `    global _store`

**L642** `    with _store_lock:`

**L643** `        if _store is None or reload:`

**L644** `            _store = KnowledgeStore.load_or_bootstrap()`

**L645** `        return _store`

**L646** ``

**L647** ``

**L648** `def set_knowledge_store(store: KnowledgeStore) -> None:`

**L649** `    """测试注入用"""`

**L650** `    global _store`

**L651** `    with _store_lock:`

**L652** `        _store = store`

**L653** ``

**L654** ``

**L655** `def _default_store_path() -> Path:`

**L656** `    return get_path("knowledge_store")`

**L657** ``

**L658** ``

**L659** `def _utc_now() -> str:`

**L660** `    return datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")`

**L661** ``

**L662** ``

**L663** `def _chunk_to_dict(chunk: TextChunk) -> dict[str, Any]:`

**L664** `    return {`

**L665** `        "chunk_id": chunk.chunk_id,`

**L666** `        "text": chunk.text,`

**L667** `        "source": chunk.source,`

**L668** `        "index": chunk.index,`

**L669** `        "start_char": chunk.start_char,`

**L670** `        "end_char": chunk.end_char,`

**L671** `    }`

**L672** ``

**L673** ``

**L674** `def _chunk_from_dict(data: dict[str, Any]) -> TextChunk:`

**L675** `    return TextChunk(`

**L676** `        chunk_id=str(data.get("chunk_id", "")),`

**L677** `        text=str(data.get("text", "")),`

**L678** `        source=str(data.get("source", "")),`

**L679** `        index=int(data.get("index", 0)),`

**L680** `        start_char=int(data.get("start_char", 0)),`

**L681** `        end_char=int(data.get("end_char", 0)),`

**L682** `    )`


## 附录：api/knowledge.py 重建端点走读

### `nexus-agent-platform/src/api/knowledge.py` 逐行走读

共 **368** 行。

**L1** `"""`

**L2** `知识库 REST API — 文档上传、分块调参与检索评估`

**L3** ``

**L4** `需求：ZL-NA-REQ-025 / ZL-NA-REQ-026 / ZL-NA-REQ-027 / ZL-NA-REQ-028 / ZL-NA-REQ-029 / ZL-NA-REQ-030 / ZL-NA-REQ-031 / ZL-NA-REQ-032 / ZL-NA-REQ-033 / ZL-NA-REQ-034 / ZL-NA-REQ-035 / ZL-NA-REQ-036`

**L5** `"""`

**L6** ``

**L7** `from __future__ import annotations`

**L8** ``

**L9** `from pathlib import Path`

**L10** ``

**L11** `from fastapi import APIRouter, File, HTTPException, UploadFile`

**L12** ``

**L13** `from api.schemas import (`

**L14** `    ChunkConfigRequest,`

**L15** `    ChunkConfigResponse,`

**L16** `    CitationConfigRequest,`

**L17** `    CitationConfigResponse,`

**L18** `    CitationPreviewRequest,`

**L19** `    CitationPreviewResponse,`

**L20** `    ExpansionConfigRequest,`

**L21** `    ExpansionConfigResponse,`

**L22** `    ExpansionPreviewRequest,`

**L23** `    ExpansionPreviewResponse,`

**L24** `    EvaluateRequest,`

**L25** `    EvaluateResponse,`

**L26** `    KnowledgeStatusResponse,`

**L27** `    KnowledgeUploadResponse,`

**L28** `    RebuildRequest,`

**L29** `    RebuildResponse,`

**L30** `    RerankConfigRequest,`

**L31** `    RerankConfigResponse,`

**L32** `    RewriteConfigRequest,`

**L33** `    RewriteConfigResponse,`

**L34** `    RewritePreviewRequest,`

**L35** `    RewritePreviewResponse,`

**L36** `    RouteConfigRequest,`

**L37** `    RouteConfigResponse,`

**L38** `    RoutePreviewRequest,`

**L39** `    RoutePreviewResponse,`

**L40** `    RetrievalConfigRequest,`

**L41** `    RetrievalConfigResponse,`

**L42** `)`

**L43** `from api.sessions import session_manager`
  → rebuild API 相关行。

**L44** `from core.exceptions import NexusError, StorageError`

**L45** `from rag.chunk_config import PRESET_CONFIGS, ChunkConfig`

**L46** `from rag.ingestion import ingest_upload`

**L47** `from rag.knowledge_rebuild import rebuild_store, rebuild_with_best_config`
  → rebuild API 相关行。

**L48** `from rag.knowledge_store import get_knowledge_store`

**L49** `from rag.citation_config import CitationConfig`

**L50** `from rag.expansion_config import ExpansionConfig`

**L51** `from rag.query_expander import build_expander`

**L52** `from rag.query_rewriter import RuleBasedQueryRewriter`

**L53** `from rag.query_router import RuleBasedQueryRouter`

**L54** `from rag.route_config import RouteConfig`

**L55** `from rag.rerank_config import RerankConfig`

**L56** `from rag.rewrite_config import RewriteConfig`

**L57** `from rag.retrieval_config import RetrievalConfig`

**L58** `from rag.retrieval_eval import EvalQuery, pick_best_config, run_ab_experiment`

**L59** `from tools.doc_parser import parse_bytes`

**L60** ``

**L61** `router = APIRouter(prefix="/api/knowledge", tags=["knowledge"])`

**L62** ``

**L63** `MAX_UPLOAD_BYTES = 512_000  # 500 KB 教学上限`

**L64** `_EVAL_SAMPLE = (`

**L65** `    Path(__file__).resolve().parent.parent / "day26" / "sample_docs" / "product_notice.md"`

**L66** `)`

**L67** ``

**L68** ``

**L69** `@router.get("/retrieval-config", response_model=RetrievalConfigResponse)`

**L70** `def get_retrieval_config() -> RetrievalConfigResponse:`

**L71** `    """返回当前检索模式（vector / keyword / hybrid）与融合参数"""`

**L72** `    cfg = get_knowledge_store().get_retrieval_config()`

**L73** `    return RetrievalConfigResponse(**cfg.to_dict())`

**L74** ``

**L75** ``

**L76** `@router.put("/retrieval-config", response_model=RetrievalConfigResponse)`

**L77** `def update_retrieval_config(body: RetrievalConfigRequest) -> RetrievalConfigResponse:`

**L78** `    """更新检索策略；变更后清除 RAG 缓存"""`

**L79** `    store = get_knowledge_store()`

**L80** `    try:`

**L81** `        cfg = RetrievalConfig.from_dict(body.model_dump())`

**L82** `        cfg.validate()`

**L83** `        store.set_retrieval_config(cfg)`

**L84** `        store.save()`

**L85** `    except ValueError as exc:`

**L86** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L87** `    return RetrievalConfigResponse(**cfg.to_dict())`

**L88** ``

**L89** ``

**L90** `@router.get("/rerank-config", response_model=RerankConfigResponse)`

**L91** `def get_rerank_config() -> RerankConfigResponse:`

**L92** `    """返回 rerank 开关、候选池大小与模型标识"""`

**L93** `    cfg = get_knowledge_store().get_rerank_config()`

**L94** `    return RerankConfigResponse(**cfg.to_dict())`

**L95** ``

**L96** ``

**L97** `@router.put("/rerank-config", response_model=RerankConfigResponse)`

**L98** `def update_rerank_config(body: RerankConfigRequest) -> RerankConfigResponse:`

**L99** `    """更新 rerank 策略；变更后清除 RAG 缓存"""`

**L100** `    store = get_knowledge_store()`

**L101** `    try:`

**L102** `        cfg = RerankConfig.from_dict(body.model_dump())`

**L103** `        cfg.validate()`

**L104** `        store.set_rerank_config(cfg)`

**L105** `        store.save()`

**L106** `    except ValueError as exc:`

**L107** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L108** `    return RerankConfigResponse(**cfg.to_dict())`

**L109** ``

**L110** ``

**L111** `@router.get("/rewrite-config", response_model=RewriteConfigResponse)`

**L112** `def get_rewrite_config() -> RewriteConfigResponse:`

**L113** `    """返回查询改写开关与规则模式"""`

**L114** `    cfg = get_knowledge_store().get_rewrite_config()`

**L115** `    return RewriteConfigResponse(**cfg.to_dict())`

**L116** ``

**L117** ``

**L118** `@router.put("/rewrite-config", response_model=RewriteConfigResponse)`

**L119** `def update_rewrite_config(body: RewriteConfigRequest) -> RewriteConfigResponse:`

**L120** `    """更新查询改写策略；变更后清除 RAG 缓存"""`

**L121** `    store = get_knowledge_store()`

**L122** `    try:`

**L123** `        cfg = RewriteConfig.from_dict(body.model_dump())`

**L124** `        cfg.validate()`

**L125** `        store.set_rewrite_config(cfg)`

**L126** `        store.save()`

**L127** `    except ValueError as exc:`

**L128** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L129** `    return RewriteConfigResponse(**cfg.to_dict())`

**L130** ``

**L131** ``

**L132** `@router.post("/rewrite-preview", response_model=RewritePreviewResponse)`

**L133** `def rewrite_preview(body: RewritePreviewRequest) -> RewritePreviewResponse:`

**L134** `    """预览单条 query 的规则改写结果（不触发检索）"""`

**L135** `    store = get_knowledge_store()`

**L136** `    cfg = store.get_rewrite_config()`

**L137** `    rewriter = RuleBasedQueryRewriter(config=cfg)`

**L138** `    result = rewriter.rewrite(body.query)`

**L139** `    return RewritePreviewResponse(**result.to_dict())`

**L140** ``

**L141** ``

**L142** `@router.get("/citation-config", response_model=CitationConfigResponse)`

**L143** `def get_citation_config() -> CitationConfigResponse:`

**L144** `    """返回引用溯源开关与展示参数"""`

**L145** `    cfg = get_knowledge_store().get_citation_config()`

**L146** `    return CitationConfigResponse(**cfg.to_dict())`

**L147** ``

**L148** ``

**L149** `@router.put("/citation-config", response_model=CitationConfigResponse)`

**L150** `def update_citation_config(body: CitationConfigRequest) -> CitationConfigResponse:`

**L151** `    """更新引用溯源策略并持久化"""`

**L152** `    store = get_knowledge_store()`

**L153** `    try:`

**L154** `        cfg = CitationConfig.from_dict(body.model_dump())`

**L155** `        cfg.validate()`

**L156** `        store.set_citation_config(cfg)`

**L157** `        store.save()`

**L158** `    except ValueError as exc:`

**L159** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L160** `    return CitationConfigResponse(**cfg.to_dict())`

**L161** ``

**L162** ``

**L163** `@router.post("/citation-preview", response_model=CitationPreviewResponse)`

**L164** `def citation_preview(body: CitationPreviewRequest) -> CitationPreviewResponse:`

**L165** `    """预览单条 query 的检索引用（含 rewrite / expansion 审计）"""`

**L166** `    store = get_knowledge_store()`

**L167** `    data = store.fetch_citations(body.query)`

**L168** `    return CitationPreviewResponse(**data)`

**L169** ``

**L170** ``

**L171** `@router.get("/expansion-config", response_model=ExpansionConfigResponse)`

**L172** `def get_expansion_config() -> ExpansionConfigResponse:`

**L173** `    """返回多 query 扩展开关与参数"""`

**L174** `    cfg = get_knowledge_store().get_expansion_config()`

**L175** `    return ExpansionConfigResponse(**cfg.to_dict())`

**L176** ``

**L177** ``

**L178** `@router.put("/expansion-config", response_model=ExpansionConfigResponse)`

**L179** `def update_expansion_config(body: ExpansionConfigRequest) -> ExpansionConfigResponse:`

**L180** `    """更新多 query 扩展策略并持久化"""`

**L181** `    store = get_knowledge_store()`

**L182** `    try:`

**L183** `        cfg = ExpansionConfig.from_dict(body.model_dump())`

**L184** `        cfg.validate()`

**L185** `        store.set_expansion_config(cfg)`

**L186** `        store.save()`

**L187** `    except ValueError as exc:`

**L188** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L189** `    return ExpansionConfigResponse(**cfg.to_dict())`

**L190** ``

**L191** ``

**L192** `@router.post("/expansion-preview", response_model=ExpansionPreviewResponse)`

**L193** `def expansion_preview(body: ExpansionPreviewRequest) -> ExpansionPreviewResponse:`

**L194** `    """预览单条 query 的多路扩展结果"""`

**L195** `    store = get_knowledge_store()`

**L196** `    cfg = store.get_expansion_config()`

**L197** `    expander = build_expander(cfg)`

**L198** `    result = expander.expand(body.query)`

**L199** `    return ExpansionPreviewResponse(**result.to_dict())`

**L200** ``

**L201** ``

**L202** `@router.get("/route-config", response_model=RouteConfigResponse)`

**L203** `def get_route_config() -> RouteConfigResponse:`

**L204** `    """返回检索管线路由开关与默认意图"""`

**L205** `    cfg = get_knowledge_store().get_route_config()`

**L206** `    return RouteConfigResponse(**cfg.to_dict())`

**L207** ``

**L208** ``

**L209** `@router.put("/route-config", response_model=RouteConfigResponse)`

**L210** `def update_route_config(body: RouteConfigRequest) -> RouteConfigResponse:`

**L211** `    """更新检索管线路由策略并持久化"""`

**L212** `    store = get_knowledge_store()`

**L213** `    try:`

**L214** `        cfg = RouteConfig.from_dict(body.model_dump())`

**L215** `        cfg.validate()`

**L216** `        store.set_route_config(cfg)`

**L217** `        store.save()`

**L218** `    except ValueError as exc:`

**L219** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L220** `    return RouteConfigResponse(**cfg.to_dict())`

**L221** ``

**L222** ``

**L223** `@router.post("/route-preview", response_model=RoutePreviewResponse)`

**L224** `def route_preview(body: RoutePreviewRequest) -> RoutePreviewResponse:`

**L225** `    """预览单条 query 的路由决策（expand/rewrite 开关）"""`

**L226** `    store = get_knowledge_store()`

**L227** `    cfg = store.get_route_config()`

**L228** `    router = RuleBasedQueryRouter(config=cfg)`

**L229** `    result = router.route(body.query)`

**L230** `    return RoutePreviewResponse(**result.to_dict())`

**L231** ``

**L232** ``

**L233** `@router.get("/chunk-config", response_model=ChunkConfigResponse)`

**L234** `def get_chunk_config() -> ChunkConfigResponse:`

**L235** `    """返回当前知识库默认分块参数"""`

**L236** `    cfg = get_knowledge_store().get_chunk_config()`

**L237** `    return ChunkConfigResponse(**cfg.to_dict())`

**L238** ``

**L239** ``

**L240** `@router.put("/chunk-config", response_model=ChunkConfigResponse)`

**L241** `def update_chunk_config(body: ChunkConfigRequest) -> ChunkConfigResponse:`

**L242** `    """更新默认分块参数（影响后续上传）"""`

**L243** `    store = get_knowledge_store()`

**L244** `    try:`

**L245** `        cfg = ChunkConfig.from_dict(body.model_dump())`

**L246** `        cfg.validate()`

**L247** `        store.set_chunk_config(cfg)`

**L248** `        store.save()`

**L249** `    except ValueError as exc:`

**L250** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L251** `    return ChunkConfigResponse(**cfg.to_dict())`

**L252** ``

**L253** ``

**L254** `@router.post("/evaluate", response_model=EvaluateResponse)`

**L255** `def evaluate_chunk_configs(body: EvaluateRequest | None = None) -> EvaluateResponse:`

**L256** `    """`

**L257** `    对内置样例文档运行 A/B 分块评估，返回 hit@1 与推荐配置。`

**L258** ``

**L259** `    默认使用 PRESET_CONFIGS 四套预设与 day27 评估问句集。`

**L260** `    """`

**L261** `    body = body or EvaluateRequest()`

**L262** `    if not _EVAL_SAMPLE.is_file():`

**L263** `        raise HTTPException(status_code=500, detail="评估样例文档缺失")`

**L264** ``

**L265** `    doc = parse_bytes(_EVAL_SAMPLE.read_bytes(), _EVAL_SAMPLE.name)`

**L266** `    from day27.constants import EVAL_QUERIES`

**L267** ``

**L268** `    queries = [EvalQuery.from_dict(q) for q in EVAL_QUERIES]`

**L269** ``

**L270** `    if body.use_presets and not body.configs:`

**L271** `        configs = list(PRESET_CONFIGS)`

**L272** `    else:`

**L273** `        configs = [ChunkConfig.from_dict(c.model_dump()) for c in body.configs]`

**L274** `        for c in configs:`

**L275** `            c.validate()`

**L276** ``

**L277** `    results = run_ab_experiment(doc, configs, queries)`

**L278** `    best = pick_best_config(results)`

**L279** `    if not best:`

**L280** `        raise HTTPException(status_code=500, detail="评估未产生结果")`

**L281** ``

**L282** `    return EvaluateResponse(`

**L283** `        best_config=best.to_dict()["config"],`

**L284** `        results=[r.to_dict() for r in results],`

**L285** `        eval_query_count=len(queries),`

**L286** `    )`

**L287** ``

**L288** ``

**L289** `@router.post("/rebuild", response_model=RebuildResponse)`
  → rebuild API 相关行。

**L290** `def rebuild_knowledge_base(body: RebuildRequest | None = None) -> RebuildResponse:`
  → rebuild API 相关行。

**L291** `    """`

**L292** `    按当前 chunk_config（或 evaluate 最优配置）全量重建知识库。`

**L293** ``

**L294** `    重扫 sample_docs + knowledge_uploads，清空后重新分块与索引。`

**L295** `    """`

**L296** `    body = body or RebuildRequest()`

**L297** `    store = get_knowledge_store()`

**L298** ``

**L299** `    if body.apply_best_config:`
  → rebuild API 相关行。

**L300** `        if not _EVAL_SAMPLE.is_file():`

**L301** `            raise HTTPException(status_code=500, detail="评估样例文档缺失")`

**L302** `        report = rebuild_with_best_config(`
  → rebuild API 相关行。

**L303** `            store,`

**L304** `            eval_sample_path=_EVAL_SAMPLE,`

**L305** `            include_sample_docs=body.include_sample_docs,`

**L306** `        )`

**L307** `    else:`

**L308** `        report = rebuild_store(store, include_sample_docs=body.include_sample_docs)`
  → rebuild API 相关行。

**L309** ``

**L310** `    cleared = session_manager.clear_all()`
  → rebuild API 相关行。

**L311** `    data = report.to_dict()`

**L312** `    data["sessions_cleared"] = cleared`

**L313** `    return RebuildResponse(**data)`

**L314** ``

**L315** ``

**L316** `@router.get("/status", response_model=KnowledgeStatusResponse)`

**L317** `def knowledge_status() -> KnowledgeStatusResponse:`

**L318** `    """返回知识库文档数、分块数、支持格式与文档列表"""`

**L319** `    store = get_knowledge_store()`

**L320** `    data = store.status_dict()`

**L321** `    return KnowledgeStatusResponse(**data)`

**L322** ``

**L323** ``

**L324** `@router.post("/upload", response_model=KnowledgeUploadResponse)`

**L325** `async def upload_document(`

**L326** `    file: UploadFile = File(..., description="企业文档 (.txt / .md / .pdf)"),`

**L327** `) -> KnowledgeUploadResponse:`

**L328** `    """`

**L329** `    上传企业文档到知识库：解析 → 落盘 → 分块 → Chroma 向量索引 → 持久化。`

**L330** ``

**L331** `    Day 26 起支持 Markdown 与 PDF。上传成功后清除服务端会话。`

**L332** `    """`

**L333** `    if not file.filename:`

**L334** `        raise HTTPException(status_code=422, detail="缺少文件名")`

**L335** ``

**L336** `    data = await file.read()`

**L337** `    if not data:`

**L338** `        raise HTTPException(status_code=422, detail="文件内容为空")`

**L339** `    if len(data) > MAX_UPLOAD_BYTES:`

**L340** `        raise HTTPException(status_code=413, detail="文件超过 500KB 上限")`

**L341** ``

**L342** `    try:`

**L343** `        meta = ingest_upload(data, file.filename)`

**L344** `    except ValueError as exc:`

**L345** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L346** `    except NexusError as exc:`

**L347** `        status = 400 if exc.code in (`

**L348** `            "PDF_PARSE_ERROR", "PDF_EMPTY", "UNSUPPORTED_FORMAT", "EMPTY_FILE"`

**L349** `        ) else 500`

**L350** `        raise HTTPException(status_code=status, detail=exc.message) from exc`

**L351** `    except StorageError as exc:`

**L352** `        raise HTTPException(status_code=500, detail=exc.message) from exc`

**L353** `    except UnicodeDecodeError as exc:`

**L354** `        raise HTTPException(status_code=400, detail="文本文件须为 UTF-8 编码") from exc`

**L355** ``

**L356** `    cleared = session_manager.clear_all()`
  → rebuild API 相关行。

**L357** `    store = get_knowledge_store()`

**L358** ``

**L359** `    return KnowledgeUploadResponse(`

**L360** `        filename=meta.name,`

**L361** `        format=meta.format,`

**L362** `        chunk_count=meta.chunk_count,`

**L363** `        document_count=store.document_count,`

**L364** `        total_chunks=store.chunk_count,`

**L365** `        sessions_cleared=cleared,`

**L366** `        index_mode=store.index_mode,`

**L367** `        message=f"文档已入库（{meta.format}），增量索引已更新",`

**L368** `    )`


## 6. 失败恢复

重建失败 mid-loop 可能导致空库——教学环境可重新 bootstrap；生产应事务化或先写临时 store。
