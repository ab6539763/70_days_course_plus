# 知识库重建详解

**Day 28 深度专题** | ZL-NA-REQ-028

## 1. 问题定义

设源文件集合 \(S\)，当前配置 \(	heta\)。生产索引 \(I = 	ext{Index}(	ext{Parse}(S), 	heta_{	ext{old}})\)。配置更新为 \(	heta_{	ext{new}}\) 后，需计算 \(I' = 	ext{Index}(	ext{Parse}(S), 	heta_{	ext{new}})\)。

**无法**通过修改 \(	heta\) 元数据把 \(I\) 就地变为 \(I'\)，因 chunk 文本边界已固定。

## 2. rebuild_store 不变式

- 重建后 \(orall d \in 	ext{documents}\)，\(d\) 来自 \(S\) 中某个源文件  
- \(	ext{chunk\_count}\) 仅取决于 \(|S|\) 与 \(	heta_{	ext{new}}\)  
- `last_rebuilt_at` 单调更新（同进程连续 rebuild）

## 3. 与 evaluate 协作

`rebuild_with_best_config`：

```python
results = run_ab_experiment(doc, PRESET_CONFIGS, queries)
best = pick_best_config(results)
store.set_chunk_config(best.config)
return rebuild_store(store, ...)
```

**注意**：evaluate 样例是 `product_notice.md`，全库 rebuild 包含所有源文件——最优配置未必对所有文档全局最优。

## 4. 清空顺序为何重要

必须先 `documents.clear()` 再逐文件 `_append_chunks`，最后单次 `_rebuild_index()`。若中途 `_rebuild_index()` 会导致空索引窗口内 chat 失败——教学单进程可接受。

## 5. 运维 Runbook（摘要）

1. 备份 `data/knowledge/store.json`  
2. 确认 uploads 文档版本  
3. POST evaluate 存档 JSON  
4. POST rebuild（或 apply_best_config）  
5. 检查 last_rebuilt_at、chunk_count  
6. chat 抽测 + 监控错误率  



## 附录：knowledge_store 持久化走读

### `nexus-agent-platform/src/rag/knowledge_store.py` 逐行走读

共 **640** 行。

**L1** `"""`

**L2** `知识库存储 — 文档 ingestion、分块索引与 JSON 持久化`

**L3** ``

**L4** `将 Day 19–20 的 RAG 管线升级为可写入、可落盘的企业知识库 MVP。`

**L5** ``

**L6** `需求：ZL-NA-REQ-025`

**L7** `"""`

**L8** ``

**L9** `from __future__ import annotations`

**L10** ``

**L11** `import threading`

**L12** `from dataclasses import dataclass, field`

**L13** `from datetime import datetime, timezone`

**L14** `from pathlib import Path`

**L15** `from typing import Any`

**L16** ``

**L17** `from core.paths import get_path`

**L18** `from rag.chunker import TextChunk, chunk_documents, chunk_text`

**L19** `from rag.chunk_config import DEFAULT_CHUNK_CONFIG, ChunkConfig`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L20** `from rag.chunk_strategies import chunk_from_parsed`

**L21** `from rag.context import DocumentIndex, RAGContextService`

**L22** `from rag.chroma_retriever import ChromaEmbeddingRetriever`

**L23** `from rag.chroma_store import VECTOR_BACKEND, ChromaVectorIndex`

**L24** `from rag.citation_config import CitationConfig`

**L25** `from rag.hybrid_retriever import HybridRetriever`

**L26** `from rag.rerank_config import RerankConfig`

**L27** `from rag.reranker import MockCrossEncoderReranker`

**L28** `from rag.reranking_retriever import RerankingRetriever`

**L29** `from rag.retrieval_config import RetrievalConfig`

**L30** `from rag.rewrite_config import RewriteConfig`

**L31** `from rag.rewriting_retriever import RewritingRetriever`

**L32** `from tools.doc_reader import DocumentRecord, read_text_file`

**L33** `from tools.parsers.base import ParsedDocument`

**L34** `from utils.json_utils import load_json, save_json`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L35** `from utils.text_utils import clean_text`

**L36** ``

**L37** `STORE_VERSION = "1.1"`

**L38** `PLATFORM_VERSION = "0.34.0"`

**L39** `INDEX_MODE_INCREMENTAL = "incremental"`

**L40** `INDEX_MODE_FULL = "full"`

**L41** ``

**L42** ``

**L43** `@dataclass`

**L44** `class KnowledgeDocument:`

**L45** `    """已入库文档元数据"""`

**L46** ``

**L47** `    name: str`

**L48** `    ingested_at: str`

**L49** `    size_bytes: int = 0`

**L50** `    chunk_count: int = 0`

**L51** `    format: str = "txt"`

**L52** ``

**L53** `    def to_dict(self) -> dict[str, Any]:`

**L54** `        return {`

**L55** `            "name": self.name,`

**L56** `            "ingested_at": self.ingested_at,`

**L57** `            "size_bytes": self.size_bytes,`

**L58** `            "chunk_count": self.chunk_count,`

**L59** `            "format": self.format,`

**L60** `        }`

**L61** ``

**L62** `    @classmethod`

**L63** `    def from_dict(cls, data: dict[str, Any]) -> KnowledgeDocument:`

**L64** `        return cls(`

**L65** `            name=str(data.get("name", "")),`

**L66** `            ingested_at=str(data.get("ingested_at", "")),`

**L67** `            size_bytes=int(data.get("size_bytes", 0)),`

**L68** `            chunk_count=int(data.get("chunk_count", 0)),`

**L69** `            format=str(data.get("format", "txt")),`

**L70** `        )`

**L71** ``

**L72** ``

**L73** `@dataclass`

**L74** `class KnowledgeStore:`

**L75** `    """`

**L76** `    企业知识库 — 分块 + Chroma 向量索引 + JSON 元数据持久化`

**L77** ``

**L78** `    典型用法：`

**L79** `        store = KnowledgeStore.load_or_bootstrap()`

**L80** `        store.ingest_text("新产品说明…", filename="notice.txt")`

**L81** `        rag = store.as_rag_service()`

**L82** `    """`

**L83** ``

**L84** `    documents: list[KnowledgeDocument] = field(default_factory=list)`

**L85** `    chunks: list[TextChunk] = field(default_factory=list)`

**L86** `    embedding_state: dict[str, Any] = field(default_factory=dict)`

**L87** `    vector_backend: str = VECTOR_BACKEND`

**L88** `    chunk_config: ChunkConfig = field(default_factory=ChunkConfig)`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L89** `    last_rebuilt_at: str | None = None`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L90** `    last_incremental_at: str | None = None`

**L91** `    index_mode: str = INDEX_MODE_FULL`

**L92** `    retrieval_config: RetrievalConfig = field(default_factory=RetrievalConfig)`

**L93** `    rerank_config: RerankConfig = field(default_factory=RerankConfig)`

**L94** `    rewrite_config: RewriteConfig = field(default_factory=RewriteConfig)`

**L95** `    citation_config: CitationConfig = field(default_factory=CitationConfig)`

**L96** `    store_path: Path | None = None`

**L97** `    chroma_path: Path | None = None`

**L98** `    _rag_service: RAGContextService | None = field(default=None, repr=False)`

**L99** ``

**L100** `    @property`

**L101** `    def chunk_count(self) -> int:`

**L102** `        return len(self.chunks)`

**L103** ``

**L104** `    @property`

**L105** `    def document_count(self) -> int:`

**L106** `        return len(self.documents)`

**L107** ``

**L108** `    def as_rag_service(self) -> RAGContextService:`

**L109** `        """构建或返回缓存的 RAGContextService"""`

**L110** `        if self._rag_service is None:`

**L111** `            self._rag_service = self._build_rag_service()`

**L112** `        return self._rag_service`

**L113** ``

**L114** `    def invalidate_cache(self) -> None:`

**L115** `        self._rag_service = None`

**L116** ``

**L117** `    def get_chunk_config(self) -> ChunkConfig:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L118** `        return ChunkConfig.from_dict(self.chunk_config.to_dict())`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L119** ``

**L120** `    def set_chunk_config(self, config: ChunkConfig) -> ChunkConfig:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L121** `        config.validate()`

**L122** `        self.chunk_config = ChunkConfig.from_dict(config.to_dict())`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L123** `        return self.chunk_config`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L124** ``

**L125** `    def get_retrieval_config(self) -> RetrievalConfig:`

**L126** `        return RetrievalConfig.from_dict(self.retrieval_config.to_dict())`

**L127** ``

**L128** `    def set_retrieval_config(self, config: RetrievalConfig) -> RetrievalConfig:`

**L129** `        config.validate()`

**L130** `        self.retrieval_config = RetrievalConfig.from_dict(config.to_dict())`

**L131** `        self.invalidate_cache()`

**L132** `        return self.retrieval_config`

**L133** ``

**L134** `    def get_rerank_config(self) -> RerankConfig:`

**L135** `        return RerankConfig.from_dict(self.rerank_config.to_dict())`

**L136** ``

**L137** `    def set_rerank_config(self, config: RerankConfig) -> RerankConfig:`

**L138** `        config.validate()`

**L139** `        self.rerank_config = RerankConfig.from_dict(config.to_dict())`

**L140** `        self.invalidate_cache()`

**L141** `        return self.rerank_config`

**L142** ``

**L143** `    def get_rewrite_config(self) -> RewriteConfig:`

**L144** `        return RewriteConfig.from_dict(self.rewrite_config.to_dict())`

**L145** ``

**L146** `    def set_rewrite_config(self, config: RewriteConfig) -> RewriteConfig:`

**L147** `        config.validate()`

**L148** `        self.rewrite_config = RewriteConfig.from_dict(config.to_dict())`

**L149** `        self.invalidate_cache()`

**L150** `        return self.rewrite_config`

**L151** ``

**L152** `    def get_citation_config(self) -> CitationConfig:`

**L153** `        return CitationConfig.from_dict(self.citation_config.to_dict())`

**L154** ``

**L155** `    def set_citation_config(self, config: CitationConfig) -> CitationConfig:`

**L156** `        config.validate()`

**L157** `        self.citation_config = CitationConfig.from_dict(config.to_dict())`

**L158** `        return self.citation_config`

**L159** ``

**L160** `    def fetch_citations(self, query: str) -> dict[str, Any]:`

**L161** `        """按当前 citation_config 检索并返回引用包 dict"""`

**L162** `        cfg = self.get_citation_config()`

**L163** `        if not cfg.enabled:`

**L164** `            return {"query": query.strip(), "citations": [], "rewrite": None}`

**L165** `        rag = self.as_rag_service()`

**L166** `        bundle = rag.retrieve_citation_bundle(query, config=cfg)`

**L167** `        return bundle.to_dict()`

**L168** ``

**L169** `    def ingest_text(`

**L170** `        self,`

**L171** `        content: str,`

**L172** `        *,`

**L173** `        filename: str,`

**L174** `        clean: bool = True,`

**L175** `        chunk_size: int | None = None,`

**L176** `        overlap: int | None = None,`

**L177** `    ) -> KnowledgeDocument:`

**L178** `        """将文本写入知识库并重建索引"""`

**L179** `        cfg = self.get_chunk_config()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L180** `        cs = chunk_size if chunk_size is not None else cfg.chunk_size`

**L181** `        ov = overlap if overlap is not None else cfg.overlap`

**L182** `        text = (content or "").strip()`

**L183** `        if not text:`

**L184** `            raise ValueError("文档内容不能为空")`

**L185** `        if not filename.strip():`

**L186** `            raise ValueError("filename 不能为空")`

**L187** ``

**L188** `        cleaned = text`

**L189** `        if clean:`

**L190** `            cleaned, _ = clean_text(text)`

**L191** `        doc = DocumentRecord(`

**L192** `            path=Path(filename),`

**L193** `            content=text,`

**L194** `            encoding="utf-8",`

**L195** `            size_bytes=len(text.encode("utf-8")),`

**L196** `            cleaned=cleaned,`

**L197** `        )`

**L198** `        new_chunks = chunk_documents(`

**L199** `            [doc],`

**L200** `            chunk_size=cs,`

**L201** `            overlap=ov,`

**L202** `            use_cleaned=clean,`

**L203** `        )`

**L204** `        self._append_chunks(filename, new_chunks, size_bytes=doc.size_bytes)`

**L205** `        self._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L206** `        return self.documents[-1]`

**L207** ``

**L208** `    def ingest_file(`

**L209** `        self,`

**L210** `        path: Path,`

**L211** `        *,`

**L212** `        clean: bool = True,`

**L213** `        chunk_size: int = 200,`

**L214** `        overlap: int = 40,`

**L215** `    ) -> KnowledgeDocument:`

**L216** `        """从磁盘文件 ingestion"""`

**L217** `        content, encoding = read_text_file(path)`

**L218** `        cleaned = content`

**L219** `        if clean:`

**L220** `            cleaned, _ = clean_text(content)`

**L221** `        doc = DocumentRecord(`

**L222** `            path=path,`

**L223** `            content=content,`

**L224** `            encoding=encoding,`

**L225** `            size_bytes=path.stat().st_size,`

**L226** `            cleaned=cleaned,`

**L227** `        )`

**L228** `        new_chunks = chunk_documents(`

**L229** `            [doc],`

**L230** `            chunk_size=chunk_size,`

**L231** `            overlap=overlap,`

**L232** `            use_cleaned=clean,`

**L233** `        )`

**L234** `        self._append_chunks(path.name, new_chunks, size_bytes=doc.size_bytes)`

**L235** `        self._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L236** `        return self.documents[-1]`

**L237** ``

**L238** `    def ingest_bytes(`

**L239** `        self,`

**L240** `        data: bytes,`

**L241** `        *,`

**L242** `        filename: str,`

**L243** `        clean: bool = True,`

**L244** `        chunk_strategy: str = "auto",`

**L245** `        incremental: bool = True,`

**L246** `    ) -> KnowledgeDocument:`

**L247** `        """处理上传二进制 — Day 26 起委托 doc_parser"""`

**L248** `        from tools.doc_parser import parse_bytes`

**L249** ``

**L250** `        parsed = parse_bytes(data, filename)`

**L251** `        cfg = self.get_chunk_config()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L252** `        return self.ingest_parsed(`

**L253** `            parsed,`

**L254** `            clean=clean,`

**L255** `            chunk_strategy=chunk_strategy if chunk_strategy != "auto" else cfg.strategy,`

**L256** `            incremental=incremental,`

**L257** `        )`

**L258** ``

**L259** `    def ingest_parsed(`

**L260** `        self,`

**L261** `        parsed: ParsedDocument,`

**L262** `        *,`

**L263** `        clean: bool = True,`

**L264** `        chunk_strategy: str | None = None,`

**L265** `        chunk_size: int | None = None,`

**L266** `        overlap: int | None = None,`

**L267** `        incremental: bool = False,`

**L268** `    ) -> KnowledgeDocument:`

**L269** `        """将 ParsedDocument 写入知识库"""`

**L270** `        cfg = self.get_chunk_config()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L271** `        strategy = chunk_strategy if chunk_strategy is not None else cfg.strategy`

**L272** `        cs = chunk_size if chunk_size is not None else cfg.chunk_size`

**L273** `        ov = overlap if overlap is not None else cfg.overlap`

**L274** `        text = (parsed.plain_text or "").strip()`

**L275** `        if not text:`

**L276** `            raise ValueError("解析结果为空")`

**L277** ``

**L278** `        if clean:`

**L279** `            text, _ = clean_text(text)`

**L280** `            parsed.plain_text = text`

**L281** `            for section in parsed.sections:`

**L282** `                section.body, _ = clean_text(section.body)`

**L283** ``

**L284** `        new_chunks = chunk_from_parsed(`

**L285** `            parsed,`

**L286** `            strategy=strategy,`

**L287** `            chunk_size=cs,`

**L288** `            overlap=ov,`

**L289** `        )`

**L290** `        size_bytes = len(parsed.plain_text.encode("utf-8"))`

**L291** ``

**L292** `        if incremental:`

**L293** `            removed = self._remove_document_by_source(parsed.filename)`

**L294** `            self._append_chunks(`

**L295** `                parsed.filename,`

**L296** `                new_chunks,`

**L297** `                size_bytes=size_bytes,`

**L298** `                doc_format=parsed.format,`

**L299** `            )`

**L300** `            self._incremental_index(new_chunks, replaced_count=len(removed))`

**L301** `        else:`

**L302** `            self._append_chunks(`

**L303** `                parsed.filename,`

**L304** `                new_chunks,`

**L305** `                size_bytes=size_bytes,`

**L306** `                doc_format=parsed.format,`

**L307** `            )`

**L308** `            self._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L309** `        return self.documents[-1]`

**L310** ``

**L311** `    def save(self, path: Path | None = None) -> Path:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L312** `        """持久化到 JSON"""`

**L313** `        target = path or self.store_path or _default_store_path()`

**L314** `        self.store_path = target`

**L315** `        payload = {`

**L316** `            "version": STORE_VERSION,`

**L317** `            "platform_version": PLATFORM_VERSION,`

**L318** `            "documents": [d.to_dict() for d in self.documents],`

**L319** `            "chunks": [_chunk_to_dict(c) for c in self.chunks],`

**L320** `            "embedding": self.embedding_state,`

**L321** `            "vector_backend": self.vector_backend,`

**L322** `            "chunk_config": self.chunk_config.to_dict(),`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L323** `            "last_rebuilt_at": self.last_rebuilt_at,`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L324** `            "last_incremental_at": self.last_incremental_at,`

**L325** `            "index_mode": self.index_mode,`

**L326** `            "retrieval_config": self.retrieval_config.to_dict(),`

**L327** `            "rerank_config": self.rerank_config.to_dict(),`

**L328** `            "rewrite_config": self.rewrite_config.to_dict(),`

**L329** `            "citation_config": self.citation_config.to_dict(),`

**L330** `        }`

**L331** `        save_json(target, payload)`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L332** `        return target`

**L333** ``

**L334** `    @classmethod`

**L335** `    def load(cls, path: Path) -> KnowledgeStore:`

**L336** `        """从 JSON 加载知识库"""`

**L337** `        raw = load_json(path, default=None)`

**L338** `        if not raw:`

**L339** `            return cls.bootstrap_from_sample_docs(store_path=path)`

**L340** ``

**L341** `        store = cls(store_path=path)`

**L342** `        store.documents = [`

**L343** `            KnowledgeDocument.from_dict(d) for d in raw.get("documents", [])`

**L344** `        ]`

**L345** `        store.chunks = [_chunk_from_dict(c) for c in raw.get("chunks", [])]`

**L346** `        store.embedding_state = dict(raw.get("embedding") or {})`

**L347** `        store.vector_backend = str(raw.get("vector_backend") or VECTOR_BACKEND)`

**L348** `        if raw.get("chunk_config"):`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L349** `            store.chunk_config = ChunkConfig.from_dict(raw["chunk_config"])`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L350** `        store.last_rebuilt_at = raw.get("last_rebuilt_at")`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L351** `        store.last_incremental_at = raw.get("last_incremental_at")`

**L352** `        store.index_mode = str(raw.get("index_mode") or INDEX_MODE_FULL)`

**L353** `        if raw.get("retrieval_config"):`

**L354** `            store.retrieval_config = RetrievalConfig.from_dict(raw["retrieval_config"])`

**L355** `        if raw.get("rerank_config"):`

**L356** `            store.rerank_config = RerankConfig.from_dict(raw["rerank_config"])`

**L357** `        if raw.get("rewrite_config"):`

**L358** `            store.rewrite_config = RewriteConfig.from_dict(raw["rewrite_config"])`

**L359** `        if raw.get("citation_config"):`

**L360** `            store.citation_config = CitationConfig.from_dict(raw["citation_config"])`

**L361** `        store._sync_chroma_from_json()`

**L362** `        store._rag_service = store._build_rag_service()`

**L363** `        return store`

**L364** ``

**L365** `    @classmethod`

**L366** `    def load_or_bootstrap(cls, path: Path | None = None) -> KnowledgeStore:`

**L367** `        """加载已有库，不存在则从 sample_docs 引导"""`

**L368** `        target = path or _default_store_path()`

**L369** `        if target.is_file():`

**L370** `            return cls.load(target)`

**L371** `        store = cls.bootstrap_from_sample_docs(store_path=target)`

**L372** `        store.save(target)`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L373** `        return store`

**L374** ``

**L375** `    @classmethod`

**L376** `    def bootstrap_from_sample_docs(cls, *, store_path: Path | None = None) -> KnowledgeStore:`

**L377** `        """用内置 sample_docs 初始化知识库"""`

**L378** `        rag = RAGContextService.from_sample_docs(use_embedding=True)`

**L379** `        store = cls(store_path=store_path)`

**L380** `        now = _utc_now()`

**L381** ``

**L382** `        by_source: dict[str, list[TextChunk]] = {}`

**L383** `        for chunk in rag.index.chunks:`

**L384** `            by_source.setdefault(chunk.source, []).append(chunk)`

**L385** ``

**L386** `        for source, chunks in sorted(by_source.items()):`

**L387** `            store.documents.append(`

**L388** `                KnowledgeDocument(`

**L389** `                    name=source,`

**L390** `                    ingested_at=now,`

**L391** `                    size_bytes=sum(len(c.text) for c in chunks),`

**L392** `                    chunk_count=len(chunks),`

**L393** `                )`

**L394** `            )`

**L395** `        store.chunks = list(rag.index.chunks)`

**L396** `        retriever = rag.index.retriever`

**L397** `        from rag.embedding_retriever import EmbeddingRetriever`

**L398** ``

**L399** `        if isinstance(retriever, EmbeddingRetriever):`

**L400** `            store.embedding_state = retriever._client.model.export_state()`

**L401** `        store._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L402** `        return store`

**L403** ``

**L404** `    def status_dict(self) -> dict[str, Any]:`

**L405** `        from tools.doc_parser import supported_formats`

**L406** ``

**L407** `        return {`

**L408** `            "document_count": self.document_count,`

**L409** `            "chunk_count": self.chunk_count,`

**L410** `            "documents": [d.to_dict() for d in self.documents],`

**L411** `            "store_path": str(self.store_path) if self.store_path else None,`

**L412** `            "platform_version": PLATFORM_VERSION,`

**L413** `            "supported_formats": supported_formats(),`

**L414** `            "chunk_config": self.chunk_config.to_dict(),`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L415** `            "last_rebuilt_at": self.last_rebuilt_at,`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L416** `            "last_incremental_at": self.last_incremental_at,`

**L417** `            "index_mode": self.index_mode,`

**L418** `            "retrieval_config": self.retrieval_config.to_dict(),`

**L419** `            "rerank_config": self.rerank_config.to_dict(),`

**L420** `            "rewrite_config": self.rewrite_config.to_dict(),`

**L421** `            "citation_config": self.citation_config.to_dict(),`

**L422** `            "vector_backend": self.vector_backend,`

**L423** `            "chroma_path": str(self._resolve_chroma_path()),`

**L424** `            "chroma_count": self._chroma_index().count() if self.chunks else 0,`

**L425** `        }`

**L426** ``

**L427** `    def _append_chunks(`

**L428** `        self,`

**L429** `        filename: str,`

**L430** `        new_chunks: list[TextChunk],`

**L431** `        *,`

**L432** `        size_bytes: int,`

**L433** `        doc_format: str = "txt",`

**L434** `    ) -> None:`

**L435** `        base_index = len(self.chunks)`

**L436** `        reindexed: list[TextChunk] = []`

**L437** `        for i, chunk in enumerate(new_chunks):`

**L438** `            reindexed.append(`

**L439** `                TextChunk(`

**L440** `                    chunk_id=chunk.chunk_id,`

**L441** `                    text=chunk.text,`

**L442** `                    source=filename,`

**L443** `                    index=base_index + i,`

**L444** `                    start_char=chunk.start_char,`

**L445** `                    end_char=chunk.end_char,`

**L446** `                )`

**L447** `            )`

**L448** `        self.chunks.extend(reindexed)`

**L449** `        self.documents.append(`

**L450** `            KnowledgeDocument(`

**L451** `                name=filename,`

**L452** `                ingested_at=_utc_now(),`

**L453** `                size_bytes=size_bytes,`

**L454** `                chunk_count=len(reindexed),`

**L455** `                format=doc_format,`

**L456** `            )`

**L457** `        )`

**L458** ``

**L459** `    def _remove_document_by_source(self, filename: str) -> list[str]:`

**L460** `        """移除同名文档及其 chunks，并从 Chroma 删除旧向量"""`

**L461** `        removed_ids = [c.chunk_id for c in self.chunks if c.source == filename]`

**L462** `        if not removed_ids:`

**L463** `            return []`

**L464** ``

**L465** `        self._chroma_index().delete_by_ids(removed_ids)`

**L466** `        self.documents = [d for d in self.documents if d.name != filename]`

**L467** `        self.chunks = [c for c in self.chunks if c.source != filename]`

**L468** `        self.chunks = [`

**L469** `            TextChunk(`

**L470** `                chunk_id=c.chunk_id,`

**L471** `                text=c.text,`

**L472** `                source=c.source,`

**L473** `                index=i,`

**L474** `                start_char=c.start_char,`

**L475** `                end_char=c.end_char,`

**L476** `            )`

**L477** `            for i, c in enumerate(self.chunks)`

**L478** `        ]`

**L479** `        return removed_ids`

**L480** ``

**L481** `    def _resolve_chroma_path(self) -> Path:`

**L482** `        if self.chroma_path is not None:`

**L483** `            return self.chroma_path`

**L484** `        if self.store_path is not None:`

**L485** `            return self.store_path.parent / "chroma"`

**L486** `        return get_path("knowledge_chroma")`

**L487** ``

**L488** `    def _chroma_index(self) -> ChromaVectorIndex:`

**L489** `        return ChromaVectorIndex(self._resolve_chroma_path())`

**L490** ``

**L491** `    def _sync_chroma_from_json(self) -> None:`

**L492** `        """从 JSON 元数据恢复 Chroma（迁移或冷启动）"""`

**L493** `        if not self.chunks or not self.embedding_state:`

**L494** `            return`

**L495** `        chroma = self._chroma_index()`

**L496** `        if chroma.count() > 0:`

**L497** `            return`

**L498** `        from rag.embedding import EmbeddingClient`

**L499** ``

**L500** `        client = EmbeddingClient()`

**L501** `        client.model.load_state(self.embedding_state)`

**L502** `        vectors = client.embed_batch([c.text for c in self.chunks])`

**L503** `        chroma.upsert_chunks(self.chunks, vectors)`

**L504** ``

**L505** `    def _rebuild_index(self) -> None:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L506** `        from rag.embedding_retriever import EmbeddingRetriever`

**L507** ``

**L508** `        if not self.chunks:`

**L509** `            self.embedding_state = {}`

**L510** `            self._chroma_index().reset()`

**L511** `            self.invalidate_cache()`

**L512** `            return`

**L513** ``

**L514** `        retriever = EmbeddingRetriever(self.chunks)`

**L515** `        self.embedding_state = retriever._client.model.export_state()`

**L516** `        vectors = retriever._client.embed_batch([c.text for c in self.chunks])`

**L517** `        chroma = self._chroma_index()`

**L518** `        chroma.reset()`

**L519** `        chroma.upsert_chunks(self.chunks, vectors)`

**L520** `        self.index_mode = INDEX_MODE_FULL`

**L521** `        self.invalidate_cache()`

**L522** ``

**L523** `    def _incremental_index(`

**L524** `        self,`

**L525** `        affected_chunks: list[TextChunk],`

**L526** `        *,`

**L527** `        replaced_count: int = 0,`

**L528** `    ) -> bool:`

**L529** `        """`

**L530** `        增量更新 Chroma：不 reset collection，仅 upsert 受影响 chunk。`

**L531** ``

**L532** `        若 TF-IDF 词表扩张，则回退为全量 upsert（仍不 reset）。`

**L533** `        返回是否发生词表扩张。`

**L534** `        """`

**L535** `        from rag.embedding_retriever import EmbeddingRetriever`

**L536** ``

**L537** `        if not self.chunks:`

**L538** `            self.embedding_state = {}`

**L539** `            self._chroma_index().reset()`

**L540** `            self.last_incremental_at = _utc_now()`

**L541** `            self.index_mode = INDEX_MODE_INCREMENTAL`

**L542** `            self.invalidate_cache()`

**L543** `            return False`

**L544** ``

**L545** `        old_vocab = dict(self.embedding_state.get("vocab") or {})`

**L546** `        retriever = EmbeddingRetriever(self.chunks)`

**L547** `        self.embedding_state = retriever._client.model.export_state()`

**L548** `        new_vocab = dict(self.embedding_state.get("vocab") or {})`

**L549** `        vocab_expanded = len(new_vocab) > len(old_vocab) and bool(old_vocab)`

**L550** ``

**L551** `        if vocab_expanded:`

**L552** `            affected_chunks = list(self.chunks)`

**L553** ``

**L554** `        client = retriever._client`

**L555** `        vectors = client.embed_batch([c.text for c in affected_chunks])`

**L556** `        chroma = self._chroma_index()`

**L557** `        if vocab_expanded:`

**L558** `            # TF-IDF 维度变化时 Chroma collection 须重建`

**L559** `            chroma.reset()`

**L560** `        chroma.upsert_chunks(affected_chunks, vectors)`

**L561** `        self.last_incremental_at = _utc_now()`

**L562** `        self.index_mode = INDEX_MODE_INCREMENTAL`

**L563** `        self.invalidate_cache()`

**L564** `        return vocab_expanded`

**L565** ``

**L566** `    def _build_rag_service(self) -> RAGContextService:`

**L567** `        if not self.chunks:`

**L568** `            return RAGContextService()`

**L569** ``

**L570** `        from rag.embedding import EmbeddingClient`

**L571** `        from rag.retriever import KeywordRetriever`

**L572** ``

**L573** `        client = EmbeddingClient()`

**L574** `        client.model.load_state(self.embedding_state)`

**L575** `        self._sync_chroma_from_json()`

**L576** `        chroma = self._chroma_index()`

**L577** `        vector = ChromaEmbeddingRetriever(self.chunks, chroma, client=client)`

**L578** `        keyword = KeywordRetriever(self.chunks)`

**L579** `        cfg = self.get_retrieval_config()`

**L580** `        hybrid = HybridRetriever(self.chunks, keyword, vector, config=cfg)`

**L581** `        rerank_cfg = self.get_rerank_config()`

**L582** `        reranking = RerankingRetriever(`

**L583** `            hybrid,`

**L584** `            reranker=MockCrossEncoderReranker(),`

**L585** `            config=rerank_cfg,`

**L586** `        )`

**L587** `        rewrite_cfg = self.get_rewrite_config()`

**L588** `        retriever = RewritingRetriever(reranking, config=rewrite_cfg)`

**L589** `        index = DocumentIndex(chunks=self.chunks, retriever=retriever)`

**L590** `        return RAGContextService(index)`

**L591** ``

**L592** ``

**L593** `_store: KnowledgeStore | None = None`

**L594** `_store_lock = threading.Lock()`

**L595** ``

**L596** ``

**L597** `def get_knowledge_store(*, reload: bool = False) -> KnowledgeStore:`

**L598** `    """全局知识库单例（API 与 factory 共享）"""`

**L599** `    global _store`

**L600** `    with _store_lock:`

**L601** `        if _store is None or reload:`

**L602** `            _store = KnowledgeStore.load_or_bootstrap()`

**L603** `        return _store`

**L604** ``

**L605** ``

**L606** `def set_knowledge_store(store: KnowledgeStore) -> None:`

**L607** `    """测试注入用"""`

**L608** `    global _store`

**L609** `    with _store_lock:`

**L610** `        _store = store`

**L611** ``

**L612** ``

**L613** `def _default_store_path() -> Path:`

**L614** `    return get_path("knowledge_store")`

**L615** ``

**L616** ``

**L617** `def _utc_now() -> str:`

**L618** `    return datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")`

**L619** ``

**L620** ``

**L621** `def _chunk_to_dict(chunk: TextChunk) -> dict[str, Any]:`

**L622** `    return {`

**L623** `        "chunk_id": chunk.chunk_id,`

**L624** `        "text": chunk.text,`

**L625** `        "source": chunk.source,`

**L626** `        "index": chunk.index,`

**L627** `        "start_char": chunk.start_char,`

**L628** `        "end_char": chunk.end_char,`

**L629** `    }`

**L630** ``

**L631** ``

**L632** `def _chunk_from_dict(data: dict[str, Any]) -> TextChunk:`

**L633** `    return TextChunk(`

**L634** `        chunk_id=str(data.get("chunk_id", "")),`

**L635** `        text=str(data.get("text", "")),`

**L636** `        source=str(data.get("source", "")),`

**L637** `        index=int(data.get("index", 0)),`

**L638** `        start_char=int(data.get("start_char", 0)),`

**L639** `        end_char=int(data.get("end_char", 0)),`

**L640** `    )`


## 附录：api/knowledge.py 重建端点走读

### `nexus-agent-platform/src/api/knowledge.py` 逐行走读

共 **294** 行。

**L1** `"""`

**L2** `知识库 REST API — 文档上传、分块调参与检索评估`

**L3** ``

**L4** `需求：ZL-NA-REQ-025 / ZL-NA-REQ-026 / ZL-NA-REQ-027 / ZL-NA-REQ-028 / ZL-NA-REQ-029 / ZL-NA-REQ-030 / ZL-NA-REQ-031 / ZL-NA-REQ-032 / ZL-NA-REQ-033 / ZL-NA-REQ-034`

**L5** `"""`

**L6** ``

**L7** `from __future__ import annotations`

**L8** ``

**L9** `from pathlib import Path`

**L10** ``

**L11** `from fastapi import APIRouter, File, HTTPException, UploadFile`

**L12** ``

**L13** `from api.schemas import (`

**L14** `    ChunkConfigRequest,`

**L15** `    ChunkConfigResponse,`

**L16** `    CitationConfigRequest,`

**L17** `    CitationConfigResponse,`

**L18** `    CitationPreviewRequest,`

**L19** `    CitationPreviewResponse,`

**L20** `    EvaluateRequest,`

**L21** `    EvaluateResponse,`

**L22** `    KnowledgeStatusResponse,`

**L23** `    KnowledgeUploadResponse,`

**L24** `    RebuildRequest,`

**L25** `    RebuildResponse,`

**L26** `    RerankConfigRequest,`

**L27** `    RerankConfigResponse,`

**L28** `    RewriteConfigRequest,`

**L29** `    RewriteConfigResponse,`

**L30** `    RewritePreviewRequest,`

**L31** `    RewritePreviewResponse,`

**L32** `    RetrievalConfigRequest,`

**L33** `    RetrievalConfigResponse,`

**L34** `)`

**L35** `from api.sessions import session_manager`
  → rebuild API 相关行。

**L36** `from core.exceptions import NexusError, StorageError`

**L37** `from rag.chunk_config import PRESET_CONFIGS, ChunkConfig`

**L38** `from rag.ingestion import ingest_upload`

**L39** `from rag.knowledge_rebuild import rebuild_store, rebuild_with_best_config`
  → rebuild API 相关行。

**L40** `from rag.knowledge_store import get_knowledge_store`

**L41** `from rag.citation_config import CitationConfig`

**L42** `from rag.query_rewriter import RuleBasedQueryRewriter`

**L43** `from rag.rerank_config import RerankConfig`

**L44** `from rag.rewrite_config import RewriteConfig`

**L45** `from rag.retrieval_config import RetrievalConfig`

**L46** `from rag.retrieval_eval import EvalQuery, pick_best_config, run_ab_experiment`

**L47** `from tools.doc_parser import parse_bytes`

**L48** ``

**L49** `router = APIRouter(prefix="/api/knowledge", tags=["knowledge"])`

**L50** ``

**L51** `MAX_UPLOAD_BYTES = 512_000  # 500 KB 教学上限`

**L52** `_EVAL_SAMPLE = (`

**L53** `    Path(__file__).resolve().parent.parent / "day26" / "sample_docs" / "product_notice.md"`

**L54** `)`

**L55** ``

**L56** ``

**L57** `@router.get("/retrieval-config", response_model=RetrievalConfigResponse)`

**L58** `def get_retrieval_config() -> RetrievalConfigResponse:`

**L59** `    """返回当前检索模式（vector / keyword / hybrid）与融合参数"""`

**L60** `    cfg = get_knowledge_store().get_retrieval_config()`

**L61** `    return RetrievalConfigResponse(**cfg.to_dict())`

**L62** ``

**L63** ``

**L64** `@router.put("/retrieval-config", response_model=RetrievalConfigResponse)`

**L65** `def update_retrieval_config(body: RetrievalConfigRequest) -> RetrievalConfigResponse:`

**L66** `    """更新检索策略；变更后清除 RAG 缓存"""`

**L67** `    store = get_knowledge_store()`

**L68** `    try:`

**L69** `        cfg = RetrievalConfig.from_dict(body.model_dump())`

**L70** `        cfg.validate()`

**L71** `        store.set_retrieval_config(cfg)`

**L72** `        store.save()`

**L73** `    except ValueError as exc:`

**L74** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L75** `    return RetrievalConfigResponse(**cfg.to_dict())`

**L76** ``

**L77** ``

**L78** `@router.get("/rerank-config", response_model=RerankConfigResponse)`

**L79** `def get_rerank_config() -> RerankConfigResponse:`

**L80** `    """返回 rerank 开关、候选池大小与模型标识"""`

**L81** `    cfg = get_knowledge_store().get_rerank_config()`

**L82** `    return RerankConfigResponse(**cfg.to_dict())`

**L83** ``

**L84** ``

**L85** `@router.put("/rerank-config", response_model=RerankConfigResponse)`

**L86** `def update_rerank_config(body: RerankConfigRequest) -> RerankConfigResponse:`

**L87** `    """更新 rerank 策略；变更后清除 RAG 缓存"""`

**L88** `    store = get_knowledge_store()`

**L89** `    try:`

**L90** `        cfg = RerankConfig.from_dict(body.model_dump())`

**L91** `        cfg.validate()`

**L92** `        store.set_rerank_config(cfg)`

**L93** `        store.save()`

**L94** `    except ValueError as exc:`

**L95** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L96** `    return RerankConfigResponse(**cfg.to_dict())`

**L97** ``

**L98** ``

**L99** `@router.get("/rewrite-config", response_model=RewriteConfigResponse)`

**L100** `def get_rewrite_config() -> RewriteConfigResponse:`

**L101** `    """返回查询改写开关与规则模式"""`

**L102** `    cfg = get_knowledge_store().get_rewrite_config()`

**L103** `    return RewriteConfigResponse(**cfg.to_dict())`

**L104** ``

**L105** ``

**L106** `@router.put("/rewrite-config", response_model=RewriteConfigResponse)`

**L107** `def update_rewrite_config(body: RewriteConfigRequest) -> RewriteConfigResponse:`

**L108** `    """更新查询改写策略；变更后清除 RAG 缓存"""`

**L109** `    store = get_knowledge_store()`

**L110** `    try:`

**L111** `        cfg = RewriteConfig.from_dict(body.model_dump())`

**L112** `        cfg.validate()`

**L113** `        store.set_rewrite_config(cfg)`

**L114** `        store.save()`

**L115** `    except ValueError as exc:`

**L116** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L117** `    return RewriteConfigResponse(**cfg.to_dict())`

**L118** ``

**L119** ``

**L120** `@router.post("/rewrite-preview", response_model=RewritePreviewResponse)`

**L121** `def rewrite_preview(body: RewritePreviewRequest) -> RewritePreviewResponse:`

**L122** `    """预览单条 query 的规则改写结果（不触发检索）"""`

**L123** `    store = get_knowledge_store()`

**L124** `    cfg = store.get_rewrite_config()`

**L125** `    rewriter = RuleBasedQueryRewriter(config=cfg)`

**L126** `    result = rewriter.rewrite(body.query)`

**L127** `    return RewritePreviewResponse(**result.to_dict())`

**L128** ``

**L129** ``

**L130** `@router.get("/citation-config", response_model=CitationConfigResponse)`

**L131** `def get_citation_config() -> CitationConfigResponse:`

**L132** `    """返回引用溯源开关与展示参数"""`

**L133** `    cfg = get_knowledge_store().get_citation_config()`

**L134** `    return CitationConfigResponse(**cfg.to_dict())`

**L135** ``

**L136** ``

**L137** `@router.put("/citation-config", response_model=CitationConfigResponse)`

**L138** `def update_citation_config(body: CitationConfigRequest) -> CitationConfigResponse:`

**L139** `    """更新引用溯源策略并持久化"""`

**L140** `    store = get_knowledge_store()`

**L141** `    try:`

**L142** `        cfg = CitationConfig.from_dict(body.model_dump())`

**L143** `        cfg.validate()`

**L144** `        store.set_citation_config(cfg)`

**L145** `        store.save()`

**L146** `    except ValueError as exc:`

**L147** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L148** `    return CitationConfigResponse(**cfg.to_dict())`

**L149** ``

**L150** ``

**L151** `@router.post("/citation-preview", response_model=CitationPreviewResponse)`

**L152** `def citation_preview(body: CitationPreviewRequest) -> CitationPreviewResponse:`

**L153** `    """预览单条 query 的检索引用（含 rewrite 审计）"""`

**L154** `    store = get_knowledge_store()`

**L155** `    data = store.fetch_citations(body.query)`

**L156** `    return CitationPreviewResponse(**data)`

**L157** ``

**L158** ``

**L159** `@router.get("/chunk-config", response_model=ChunkConfigResponse)`

**L160** `def get_chunk_config() -> ChunkConfigResponse:`

**L161** `    """返回当前知识库默认分块参数"""`

**L162** `    cfg = get_knowledge_store().get_chunk_config()`

**L163** `    return ChunkConfigResponse(**cfg.to_dict())`

**L164** ``

**L165** ``

**L166** `@router.put("/chunk-config", response_model=ChunkConfigResponse)`

**L167** `def update_chunk_config(body: ChunkConfigRequest) -> ChunkConfigResponse:`

**L168** `    """更新默认分块参数（影响后续上传）"""`

**L169** `    store = get_knowledge_store()`

**L170** `    try:`

**L171** `        cfg = ChunkConfig.from_dict(body.model_dump())`

**L172** `        cfg.validate()`

**L173** `        store.set_chunk_config(cfg)`

**L174** `        store.save()`

**L175** `    except ValueError as exc:`

**L176** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L177** `    return ChunkConfigResponse(**cfg.to_dict())`

**L178** ``

**L179** ``

**L180** `@router.post("/evaluate", response_model=EvaluateResponse)`

**L181** `def evaluate_chunk_configs(body: EvaluateRequest | None = None) -> EvaluateResponse:`

**L182** `    """`

**L183** `    对内置样例文档运行 A/B 分块评估，返回 hit@1 与推荐配置。`

**L184** ``

**L185** `    默认使用 PRESET_CONFIGS 四套预设与 day27 评估问句集。`

**L186** `    """`

**L187** `    body = body or EvaluateRequest()`

**L188** `    if not _EVAL_SAMPLE.is_file():`

**L189** `        raise HTTPException(status_code=500, detail="评估样例文档缺失")`

**L190** ``

**L191** `    doc = parse_bytes(_EVAL_SAMPLE.read_bytes(), _EVAL_SAMPLE.name)`

**L192** `    from day27.constants import EVAL_QUERIES`

**L193** ``

**L194** `    queries = [EvalQuery.from_dict(q) for q in EVAL_QUERIES]`

**L195** ``

**L196** `    if body.use_presets and not body.configs:`

**L197** `        configs = list(PRESET_CONFIGS)`

**L198** `    else:`

**L199** `        configs = [ChunkConfig.from_dict(c.model_dump()) for c in body.configs]`

**L200** `        for c in configs:`

**L201** `            c.validate()`

**L202** ``

**L203** `    results = run_ab_experiment(doc, configs, queries)`

**L204** `    best = pick_best_config(results)`

**L205** `    if not best:`

**L206** `        raise HTTPException(status_code=500, detail="评估未产生结果")`

**L207** ``

**L208** `    return EvaluateResponse(`

**L209** `        best_config=best.to_dict()["config"],`

**L210** `        results=[r.to_dict() for r in results],`

**L211** `        eval_query_count=len(queries),`

**L212** `    )`

**L213** ``

**L214** ``

**L215** `@router.post("/rebuild", response_model=RebuildResponse)`
  → rebuild API 相关行。

**L216** `def rebuild_knowledge_base(body: RebuildRequest | None = None) -> RebuildResponse:`
  → rebuild API 相关行。

**L217** `    """`

**L218** `    按当前 chunk_config（或 evaluate 最优配置）全量重建知识库。`

**L219** ``

**L220** `    重扫 sample_docs + knowledge_uploads，清空后重新分块与索引。`

**L221** `    """`

**L222** `    body = body or RebuildRequest()`

**L223** `    store = get_knowledge_store()`

**L224** ``

**L225** `    if body.apply_best_config:`
  → rebuild API 相关行。

**L226** `        if not _EVAL_SAMPLE.is_file():`

**L227** `            raise HTTPException(status_code=500, detail="评估样例文档缺失")`

**L228** `        report = rebuild_with_best_config(`
  → rebuild API 相关行。

**L229** `            store,`

**L230** `            eval_sample_path=_EVAL_SAMPLE,`

**L231** `            include_sample_docs=body.include_sample_docs,`

**L232** `        )`

**L233** `    else:`

**L234** `        report = rebuild_store(store, include_sample_docs=body.include_sample_docs)`
  → rebuild API 相关行。

**L235** ``

**L236** `    cleared = session_manager.clear_all()`
  → rebuild API 相关行。

**L237** `    data = report.to_dict()`

**L238** `    data["sessions_cleared"] = cleared`

**L239** `    return RebuildResponse(**data)`

**L240** ``

**L241** ``

**L242** `@router.get("/status", response_model=KnowledgeStatusResponse)`

**L243** `def knowledge_status() -> KnowledgeStatusResponse:`

**L244** `    """返回知识库文档数、分块数、支持格式与文档列表"""`

**L245** `    store = get_knowledge_store()`

**L246** `    data = store.status_dict()`

**L247** `    return KnowledgeStatusResponse(**data)`

**L248** ``

**L249** ``

**L250** `@router.post("/upload", response_model=KnowledgeUploadResponse)`

**L251** `async def upload_document(`

**L252** `    file: UploadFile = File(..., description="企业文档 (.txt / .md / .pdf)"),`

**L253** `) -> KnowledgeUploadResponse:`

**L254** `    """`

**L255** `    上传企业文档到知识库：解析 → 落盘 → 分块 → Chroma 向量索引 → 持久化。`

**L256** ``

**L257** `    Day 26 起支持 Markdown 与 PDF。上传成功后清除服务端会话。`

**L258** `    """`

**L259** `    if not file.filename:`

**L260** `        raise HTTPException(status_code=422, detail="缺少文件名")`

**L261** ``

**L262** `    data = await file.read()`

**L263** `    if not data:`

**L264** `        raise HTTPException(status_code=422, detail="文件内容为空")`

**L265** `    if len(data) > MAX_UPLOAD_BYTES:`

**L266** `        raise HTTPException(status_code=413, detail="文件超过 500KB 上限")`

**L267** ``

**L268** `    try:`

**L269** `        meta = ingest_upload(data, file.filename)`

**L270** `    except ValueError as exc:`

**L271** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L272** `    except NexusError as exc:`

**L273** `        status = 400 if exc.code in (`

**L274** `            "PDF_PARSE_ERROR", "PDF_EMPTY", "UNSUPPORTED_FORMAT", "EMPTY_FILE"`

**L275** `        ) else 500`

**L276** `        raise HTTPException(status_code=status, detail=exc.message) from exc`

**L277** `    except StorageError as exc:`

**L278** `        raise HTTPException(status_code=500, detail=exc.message) from exc`

**L279** `    except UnicodeDecodeError as exc:`

**L280** `        raise HTTPException(status_code=400, detail="文本文件须为 UTF-8 编码") from exc`

**L281** ``

**L282** `    cleared = session_manager.clear_all()`
  → rebuild API 相关行。

**L283** `    store = get_knowledge_store()`

**L284** ``

**L285** `    return KnowledgeUploadResponse(`

**L286** `        filename=meta.name,`

**L287** `        format=meta.format,`

**L288** `        chunk_count=meta.chunk_count,`

**L289** `        document_count=store.document_count,`

**L290** `        total_chunks=store.chunk_count,`

**L291** `        sessions_cleared=cleared,`

**L292** `        index_mode=store.index_mode,`

**L293** `        message=f"文档已入库（{meta.format}），增量索引已更新",`

**L294** `    )`


## 6. 失败恢复

重建失败 mid-loop 可能导致空库——教学环境可重新 bootstrap；生产应事务化或先写临时 store。
