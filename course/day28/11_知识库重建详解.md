# 知识库重建详解

**Day 28 深度专题** | ZL-NA-REQ-028

## 1. 问题定义

设源文件集合 \(S\)，当前配置 \(	heta\)。生产索引 \(I = 	ext{Index}(	ext{Parse}(S), 	heta_{	ext{old}})\)。配置更新为 \(	heta_{	ext{new}}\) 后，需计算 \(I' = 	ext{Index}(	ext{Parse}(S), 	heta_{	ext{new}})\)。

**无法**通过修改 \(	heta\) 元数据把 \(I\) 就地变为 \(I'\)，因 chunk 文本边界已固定。

## 2. rebuild_store 不变式

- 重建后 \(orall d \in 	ext{documents}\)，\(d\) 来自 \(S\) 中某个源文件  
- \(	ext{chunk\_count}\) 仅取决于 \(|S|\) 与 \(	heta_{	ext{new}}\)  
- `last_rebuilt_at` 单调更新（同进程连续 rebuild）

## 3. 与 evaluate 协作

`rebuild_with_best_config`：

```python
results = run_ab_experiment(doc, PRESET_CONFIGS, queries)
best = pick_best_config(results)
store.set_chunk_config(best.config)
return rebuild_store(store, ...)
```

**注意**：evaluate 样例是 `product_notice.md`，全库 rebuild 包含所有源文件——最优配置未必对所有文档全局最优。

## 4. 清空顺序为何重要

必须先 `documents.clear()` 再逐文件 `_append_chunks`，最后单次 `_rebuild_index()`。若中途 `_rebuild_index()` 会导致空索引窗口内 chat 失败——教学单进程可接受。

## 5. 运维 Runbook（摘要）

1. 备份 `data/knowledge/store.json`  
2. 确认 uploads 文档版本  
3. POST evaluate 存档 JSON  
4. POST rebuild（或 apply_best_config）  
5. 检查 last_rebuilt_at、chunk_count  
6. chat 抽测 + 监控错误率  



## 附录：knowledge_store 持久化走读

### `nexus-agent-platform/src/rag/knowledge_store.py` 逐行走读

共 **749** 行。

**L1** `"""`

**L2** `知识库存储 — 文档 ingestion、分块索引与 JSON 持久化`

**L3** ``

**L4** `将 Day 19–20 的 RAG 管线升级为可写入、可落盘的企业知识库 MVP。`

**L5** ``

**L6** `需求：ZL-NA-REQ-025`

**L7** `"""`

**L8** ``

**L9** `from __future__ import annotations`

**L10** ``

**L11** `import threading`

**L12** `from dataclasses import dataclass, field`

**L13** `from datetime import datetime, timezone`

**L14** `from pathlib import Path`

**L15** `from typing import Any`

**L16** ``

**L17** `from core.paths import get_path`

**L18** `from agent.react_config import ReactConfig`

**L19** `from rag.chunker import TextChunk, chunk_documents, chunk_text`

**L20** `from rag.chunk_config import DEFAULT_CHUNK_CONFIG, ChunkConfig`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L21** `from rag.chunk_strategies import chunk_from_parsed`

**L22** `from rag.context import DocumentIndex, RAGContextService`

**L23** `from rag.chroma_retriever import ChromaEmbeddingRetriever`

**L24** `from rag.chroma_store import VECTOR_BACKEND, ChromaVectorIndex`

**L25** `from rag.citation_config import CitationConfig`

**L26** `from rag.expanding_retriever import ExpandingRetriever`

**L27** `from rag.expansion_config import ExpansionConfig`

**L28** `from rag.answer_validator import RuleBasedAnswerValidator, ValidationResult`

**L29** `from rag.route_config import RouteConfig`

**L30** `from rag.routing_retriever import RoutingRetriever`

**L31** `from rag.validation_config import ValidationConfig`

**L32** `from rag.hybrid_retriever import HybridRetriever`

**L33** `from rag.rerank_config import RerankConfig`

**L34** `from rag.reranker import MockCrossEncoderReranker`

**L35** `from rag.reranking_retriever import RerankingRetriever`

**L36** `from rag.retrieval_config import RetrievalConfig`

**L37** `from rag.rewrite_config import RewriteConfig`

**L38** `from rag.rewriting_retriever import RewritingRetriever`

**L39** `from tools.doc_reader import DocumentRecord, read_text_file`

**L40** `from tools.parsers.base import ParsedDocument`

**L41** `from utils.json_utils import load_json, save_json`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L42** `from utils.text_utils import clean_text`

**L43** ``

**L44** `STORE_VERSION = "1.1"`

**L45** `PLATFORM_VERSION = "0.39.0"`

**L46** `INDEX_MODE_INCREMENTAL = "incremental"`

**L47** `INDEX_MODE_FULL = "full"`

**L48** ``

**L49** ``

**L50** `@dataclass`

**L51** `class KnowledgeDocument:`

**L52** `    """已入库文档元数据"""`

**L53** ``

**L54** `    name: str`

**L55** `    ingested_at: str`

**L56** `    size_bytes: int = 0`

**L57** `    chunk_count: int = 0`

**L58** `    format: str = "txt"`

**L59** ``

**L60** `    def to_dict(self) -> dict[str, Any]:`

**L61** `        return {`

**L62** `            "name": self.name,`

**L63** `            "ingested_at": self.ingested_at,`

**L64** `            "size_bytes": self.size_bytes,`

**L65** `            "chunk_count": self.chunk_count,`

**L66** `            "format": self.format,`

**L67** `        }`

**L68** ``

**L69** `    @classmethod`

**L70** `    def from_dict(cls, data: dict[str, Any]) -> KnowledgeDocument:`

**L71** `        return cls(`

**L72** `            name=str(data.get("name", "")),`

**L73** `            ingested_at=str(data.get("ingested_at", "")),`

**L74** `            size_bytes=int(data.get("size_bytes", 0)),`

**L75** `            chunk_count=int(data.get("chunk_count", 0)),`

**L76** `            format=str(data.get("format", "txt")),`

**L77** `        )`

**L78** ``

**L79** ``

**L80** `@dataclass`

**L81** `class KnowledgeStore:`

**L82** `    """`

**L83** `    企业知识库 — 分块 + Chroma 向量索引 + JSON 元数据持久化`

**L84** ``

**L85** `    典型用法：`

**L86** `        store = KnowledgeStore.load_or_bootstrap()`

**L87** `        store.ingest_text("新产品说明…", filename="notice.txt")`

**L88** `        rag = store.as_rag_service()`

**L89** `    """`

**L90** ``

**L91** `    documents: list[KnowledgeDocument] = field(default_factory=list)`

**L92** `    chunks: list[TextChunk] = field(default_factory=list)`

**L93** `    embedding_state: dict[str, Any] = field(default_factory=dict)`

**L94** `    vector_backend: str = VECTOR_BACKEND`

**L95** `    chunk_config: ChunkConfig = field(default_factory=ChunkConfig)`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L96** `    last_rebuilt_at: str | None = None`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L97** `    last_incremental_at: str | None = None`

**L98** `    index_mode: str = INDEX_MODE_FULL`

**L99** `    retrieval_config: RetrievalConfig = field(default_factory=RetrievalConfig)`

**L100** `    rerank_config: RerankConfig = field(default_factory=RerankConfig)`

**L101** `    rewrite_config: RewriteConfig = field(default_factory=RewriteConfig)`

**L102** `    citation_config: CitationConfig = field(default_factory=CitationConfig)`

**L103** `    expansion_config: ExpansionConfig = field(default_factory=ExpansionConfig)`

**L104** `    route_config: RouteConfig = field(default_factory=RouteConfig)`

**L105** `    validation_config: ValidationConfig = field(default_factory=ValidationConfig)`

**L106** `    react_config: ReactConfig = field(default_factory=ReactConfig)`

**L107** `    store_path: Path | None = None`

**L108** `    chroma_path: Path | None = None`

**L109** `    _rag_service: RAGContextService | None = field(default=None, repr=False)`

**L110** ``

**L111** `    @property`

**L112** `    def chunk_count(self) -> int:`

**L113** `        return len(self.chunks)`

**L114** ``

**L115** `    @property`

**L116** `    def document_count(self) -> int:`

**L117** `        return len(self.documents)`

**L118** ``

**L119** `    def as_rag_service(self) -> RAGContextService:`

**L120** `        """构建或返回缓存的 RAGContextService"""`

**L121** `        if self._rag_service is None:`

**L122** `            self._rag_service = self._build_rag_service()`

**L123** `        return self._rag_service`

**L124** ``

**L125** `    def invalidate_cache(self) -> None:`

**L126** `        self._rag_service = None`

**L127** ``

**L128** `    def get_chunk_config(self) -> ChunkConfig:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L129** `        return ChunkConfig.from_dict(self.chunk_config.to_dict())`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L130** ``

**L131** `    def set_chunk_config(self, config: ChunkConfig) -> ChunkConfig:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L132** `        config.validate()`

**L133** `        self.chunk_config = ChunkConfig.from_dict(config.to_dict())`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L134** `        return self.chunk_config`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L135** ``

**L136** `    def get_retrieval_config(self) -> RetrievalConfig:`

**L137** `        return RetrievalConfig.from_dict(self.retrieval_config.to_dict())`

**L138** ``

**L139** `    def set_retrieval_config(self, config: RetrievalConfig) -> RetrievalConfig:`

**L140** `        config.validate()`

**L141** `        self.retrieval_config = RetrievalConfig.from_dict(config.to_dict())`

**L142** `        self.invalidate_cache()`

**L143** `        return self.retrieval_config`

**L144** ``

**L145** `    def get_rerank_config(self) -> RerankConfig:`

**L146** `        return RerankConfig.from_dict(self.rerank_config.to_dict())`

**L147** ``

**L148** `    def set_rerank_config(self, config: RerankConfig) -> RerankConfig:`

**L149** `        config.validate()`

**L150** `        self.rerank_config = RerankConfig.from_dict(config.to_dict())`

**L151** `        self.invalidate_cache()`

**L152** `        return self.rerank_config`

**L153** ``

**L154** `    def get_rewrite_config(self) -> RewriteConfig:`

**L155** `        return RewriteConfig.from_dict(self.rewrite_config.to_dict())`

**L156** ``

**L157** `    def set_rewrite_config(self, config: RewriteConfig) -> RewriteConfig:`

**L158** `        config.validate()`

**L159** `        self.rewrite_config = RewriteConfig.from_dict(config.to_dict())`

**L160** `        self.invalidate_cache()`

**L161** `        return self.rewrite_config`

**L162** ``

**L163** `    def get_citation_config(self) -> CitationConfig:`

**L164** `        return CitationConfig.from_dict(self.citation_config.to_dict())`

**L165** ``

**L166** `    def set_citation_config(self, config: CitationConfig) -> CitationConfig:`

**L167** `        config.validate()`

**L168** `        self.citation_config = CitationConfig.from_dict(config.to_dict())`

**L169** `        return self.citation_config`

**L170** ``

**L171** `    def get_expansion_config(self) -> ExpansionConfig:`

**L172** `        return ExpansionConfig.from_dict(self.expansion_config.to_dict())`

**L173** ``

**L174** `    def set_expansion_config(self, config: ExpansionConfig) -> ExpansionConfig:`

**L175** `        config.validate()`

**L176** `        self.expansion_config = ExpansionConfig.from_dict(config.to_dict())`

**L177** `        self.invalidate_cache()`

**L178** `        return self.expansion_config`

**L179** ``

**L180** `    def get_route_config(self) -> RouteConfig:`

**L181** `        return RouteConfig.from_dict(self.route_config.to_dict())`

**L182** ``

**L183** `    def set_route_config(self, config: RouteConfig) -> RouteConfig:`

**L184** `        config.validate()`

**L185** `        self.route_config = RouteConfig.from_dict(config.to_dict())`

**L186** `        self.invalidate_cache()`

**L187** `        return self.route_config`

**L188** ``

**L189** `    def get_validation_config(self) -> ValidationConfig:`

**L190** `        return ValidationConfig.from_dict(self.validation_config.to_dict())`

**L191** ``

**L192** `    def set_validation_config(self, config: ValidationConfig) -> ValidationConfig:`

**L193** `        config.validate()`

**L194** `        self.validation_config = ValidationConfig.from_dict(config.to_dict())`

**L195** `        return self.validation_config`

**L196** ``

**L197** `    def get_react_config(self) -> ReactConfig:`

**L198** `        return ReactConfig.from_dict(self.react_config.to_dict())`

**L199** ``

**L200** `    def set_react_config(self, config: ReactConfig) -> ReactConfig:`

**L201** `        config.validate()`

**L202** `        self.react_config = ReactConfig.from_dict(config.to_dict())`

**L203** `        return self.react_config`

**L204** ``

**L205** `    def validate_answer(`

**L206** `        self,`

**L207** `        query: str,`

**L208** `        reply: str,`

**L209** `        citations: list[dict[str, Any]],`

**L210** `    ) -> ValidationResult | None:`

**L211** `        """按当前 validation_config 校验 reply 与 citations 一致性"""`

**L212** `        cfg = self.get_validation_config()`

**L213** `        if not cfg.enabled:`

**L214** `            return None`

**L215** `        validator = RuleBasedAnswerValidator(config=cfg)`

**L216** `        return validator.validate(query, reply, citations)`

**L217** ``

**L218** `    def fetch_citations(self, query: str) -> dict[str, Any]:`

**L219** `        """按当前 citation_config 检索并返回引用包 dict"""`

**L220** `        cfg = self.get_citation_config()`

**L221** `        if not cfg.enabled:`

**L222** `            return {`

**L223** `                "query": query.strip(),`

**L224** `                "citations": [],`

**L225** `                "rewrite": None,`

**L226** `                "expansion": None,`

**L227** `                "route": None,`

**L228** `            }`

**L229** `        rag = self.as_rag_service()`

**L230** `        bundle = rag.retrieve_citation_bundle(query, config=cfg)`

**L231** `        return bundle.to_dict()`

**L232** ``

**L233** `    def fetch_citations_retry(self, query: str, *, attempt: int = 1) -> dict[str, Any]:`

**L234** `        """Self-RAG 重试 — 强制 rag_wide 并放大 citation pool"""`

**L235** `        from rag.citation_config import CitationConfig`

**L236** `        from rag.route_config import INTENT_RAG_WIDE`

**L237** ``

**L238** `        cfg = self.get_citation_config()`

**L239** `        if not cfg.enabled:`

**L240** `            return self.fetch_citations(query)`

**L241** ``

**L242** `        boosted = CitationConfig.from_dict(`

**L243** `            {`

**L244** `                **cfg.to_dict(),`

**L245** `                "max_citations": min(100, max(cfg.max_citations, 20) + attempt * 10),`

**L246** `            }`

**L247** `        )`

**L248** `        rag = self.as_rag_service()`

**L249** `        bundle = rag.retrieve_citation_bundle(`

**L250** `            query,`

**L251** `            config=boosted,`

**L252** `            intent_override=INTENT_RAG_WIDE,`

**L253** `        )`

**L254** `        data = bundle.to_dict()`

**L255** `        data["retry_attempt"] = attempt`

**L256** `        return data`

**L257** ``

**L258** `    def ingest_text(`

**L259** `        self,`

**L260** `        content: str,`

**L261** `        *,`

**L262** `        filename: str,`

**L263** `        clean: bool = True,`

**L264** `        chunk_size: int | None = None,`

**L265** `        overlap: int | None = None,`

**L266** `    ) -> KnowledgeDocument:`

**L267** `        """将文本写入知识库并重建索引"""`

**L268** `        cfg = self.get_chunk_config()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L269** `        cs = chunk_size if chunk_size is not None else cfg.chunk_size`

**L270** `        ov = overlap if overlap is not None else cfg.overlap`

**L271** `        text = (content or "").strip()`

**L272** `        if not text:`

**L273** `            raise ValueError("文档内容不能为空")`

**L274** `        if not filename.strip():`

**L275** `            raise ValueError("filename 不能为空")`

**L276** ``

**L277** `        cleaned = text`

**L278** `        if clean:`

**L279** `            cleaned, _ = clean_text(text)`

**L280** `        doc = DocumentRecord(`

**L281** `            path=Path(filename),`

**L282** `            content=text,`

**L283** `            encoding="utf-8",`

**L284** `            size_bytes=len(text.encode("utf-8")),`

**L285** `            cleaned=cleaned,`

**L286** `        )`

**L287** `        new_chunks = chunk_documents(`

**L288** `            [doc],`

**L289** `            chunk_size=cs,`

**L290** `            overlap=ov,`

**L291** `            use_cleaned=clean,`

**L292** `        )`

**L293** `        self._append_chunks(filename, new_chunks, size_bytes=doc.size_bytes)`

**L294** `        self._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L295** `        return self.documents[-1]`

**L296** ``

**L297** `    def ingest_file(`

**L298** `        self,`

**L299** `        path: Path,`

**L300** `        *,`

**L301** `        clean: bool = True,`

**L302** `        chunk_size: int = 200,`

**L303** `        overlap: int = 40,`

**L304** `    ) -> KnowledgeDocument:`

**L305** `        """从磁盘文件 ingestion"""`

**L306** `        content, encoding = read_text_file(path)`

**L307** `        cleaned = content`

**L308** `        if clean:`

**L309** `            cleaned, _ = clean_text(content)`

**L310** `        doc = DocumentRecord(`

**L311** `            path=path,`

**L312** `            content=content,`

**L313** `            encoding=encoding,`

**L314** `            size_bytes=path.stat().st_size,`

**L315** `            cleaned=cleaned,`

**L316** `        )`

**L317** `        new_chunks = chunk_documents(`

**L318** `            [doc],`

**L319** `            chunk_size=chunk_size,`

**L320** `            overlap=overlap,`

**L321** `            use_cleaned=clean,`

**L322** `        )`

**L323** `        self._append_chunks(path.name, new_chunks, size_bytes=doc.size_bytes)`

**L324** `        self._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L325** `        return self.documents[-1]`

**L326** ``

**L327** `    def ingest_bytes(`

**L328** `        self,`

**L329** `        data: bytes,`

**L330** `        *,`

**L331** `        filename: str,`

**L332** `        clean: bool = True,`

**L333** `        chunk_strategy: str = "auto",`

**L334** `        incremental: bool = True,`

**L335** `    ) -> KnowledgeDocument:`

**L336** `        """处理上传二进制 — Day 26 起委托 doc_parser"""`

**L337** `        from tools.doc_parser import parse_bytes`

**L338** ``

**L339** `        parsed = parse_bytes(data, filename)`

**L340** `        cfg = self.get_chunk_config()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L341** `        return self.ingest_parsed(`

**L342** `            parsed,`

**L343** `            clean=clean,`

**L344** `            chunk_strategy=chunk_strategy if chunk_strategy != "auto" else cfg.strategy,`

**L345** `            incremental=incremental,`

**L346** `        )`

**L347** ``

**L348** `    def ingest_parsed(`

**L349** `        self,`

**L350** `        parsed: ParsedDocument,`

**L351** `        *,`

**L352** `        clean: bool = True,`

**L353** `        chunk_strategy: str | None = None,`

**L354** `        chunk_size: int | None = None,`

**L355** `        overlap: int | None = None,`

**L356** `        incremental: bool = False,`

**L357** `    ) -> KnowledgeDocument:`

**L358** `        """将 ParsedDocument 写入知识库"""`

**L359** `        cfg = self.get_chunk_config()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L360** `        strategy = chunk_strategy if chunk_strategy is not None else cfg.strategy`

**L361** `        cs = chunk_size if chunk_size is not None else cfg.chunk_size`

**L362** `        ov = overlap if overlap is not None else cfg.overlap`

**L363** `        text = (parsed.plain_text or "").strip()`

**L364** `        if not text:`

**L365** `            raise ValueError("解析结果为空")`

**L366** ``

**L367** `        if clean:`

**L368** `            text, _ = clean_text(text)`

**L369** `            parsed.plain_text = text`

**L370** `            for section in parsed.sections:`

**L371** `                section.body, _ = clean_text(section.body)`

**L372** ``

**L373** `        new_chunks = chunk_from_parsed(`

**L374** `            parsed,`

**L375** `            strategy=strategy,`

**L376** `            chunk_size=cs,`

**L377** `            overlap=ov,`

**L378** `        )`

**L379** `        size_bytes = len(parsed.plain_text.encode("utf-8"))`

**L380** ``

**L381** `        if incremental:`

**L382** `            removed = self._remove_document_by_source(parsed.filename)`

**L383** `            self._append_chunks(`

**L384** `                parsed.filename,`

**L385** `                new_chunks,`

**L386** `                size_bytes=size_bytes,`

**L387** `                doc_format=parsed.format,`

**L388** `            )`

**L389** `            self._incremental_index(new_chunks, replaced_count=len(removed))`

**L390** `        else:`

**L391** `            self._append_chunks(`

**L392** `                parsed.filename,`

**L393** `                new_chunks,`

**L394** `                size_bytes=size_bytes,`

**L395** `                doc_format=parsed.format,`

**L396** `            )`

**L397** `            self._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L398** `        return self.documents[-1]`

**L399** ``

**L400** `    def save(self, path: Path | None = None) -> Path:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L401** `        """持久化到 JSON"""`

**L402** `        target = path or self.store_path or _default_store_path()`

**L403** `        self.store_path = target`

**L404** `        payload = {`

**L405** `            "version": STORE_VERSION,`

**L406** `            "platform_version": PLATFORM_VERSION,`

**L407** `            "documents": [d.to_dict() for d in self.documents],`

**L408** `            "chunks": [_chunk_to_dict(c) for c in self.chunks],`

**L409** `            "embedding": self.embedding_state,`

**L410** `            "vector_backend": self.vector_backend,`

**L411** `            "chunk_config": self.chunk_config.to_dict(),`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L412** `            "last_rebuilt_at": self.last_rebuilt_at,`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L413** `            "last_incremental_at": self.last_incremental_at,`

**L414** `            "index_mode": self.index_mode,`

**L415** `            "retrieval_config": self.retrieval_config.to_dict(),`

**L416** `            "rerank_config": self.rerank_config.to_dict(),`

**L417** `            "rewrite_config": self.rewrite_config.to_dict(),`

**L418** `            "citation_config": self.citation_config.to_dict(),`

**L419** `            "expansion_config": self.expansion_config.to_dict(),`

**L420** `            "route_config": self.route_config.to_dict(),`

**L421** `            "validation_config": self.validation_config.to_dict(),`

**L422** `            "react_config": self.react_config.to_dict(),`

**L423** `        }`

**L424** `        save_json(target, payload)`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L425** `        return target`

**L426** ``

**L427** `    @classmethod`

**L428** `    def load(cls, path: Path) -> KnowledgeStore:`

**L429** `        """从 JSON 加载知识库"""`

**L430** `        raw = load_json(path, default=None)`

**L431** `        if not raw:`

**L432** `            return cls.bootstrap_from_sample_docs(store_path=path)`

**L433** ``

**L434** `        store = cls(store_path=path)`

**L435** `        store.documents = [`

**L436** `            KnowledgeDocument.from_dict(d) for d in raw.get("documents", [])`

**L437** `        ]`

**L438** `        store.chunks = [_chunk_from_dict(c) for c in raw.get("chunks", [])]`

**L439** `        store.embedding_state = dict(raw.get("embedding") or {})`

**L440** `        store.vector_backend = str(raw.get("vector_backend") or VECTOR_BACKEND)`

**L441** `        if raw.get("chunk_config"):`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L442** `            store.chunk_config = ChunkConfig.from_dict(raw["chunk_config"])`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L443** `        store.last_rebuilt_at = raw.get("last_rebuilt_at")`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L444** `        store.last_incremental_at = raw.get("last_incremental_at")`

**L445** `        store.index_mode = str(raw.get("index_mode") or INDEX_MODE_FULL)`

**L446** `        if raw.get("retrieval_config"):`

**L447** `            store.retrieval_config = RetrievalConfig.from_dict(raw["retrieval_config"])`

**L448** `        if raw.get("rerank_config"):`

**L449** `            store.rerank_config = RerankConfig.from_dict(raw["rerank_config"])`

**L450** `        if raw.get("rewrite_config"):`

**L451** `            store.rewrite_config = RewriteConfig.from_dict(raw["rewrite_config"])`

**L452** `        if raw.get("citation_config"):`

**L453** `            store.citation_config = CitationConfig.from_dict(raw["citation_config"])`

**L454** `        if raw.get("expansion_config"):`

**L455** `            store.expansion_config = ExpansionConfig.from_dict(raw["expansion_config"])`

**L456** `        if raw.get("route_config"):`

**L457** `            store.route_config = RouteConfig.from_dict(raw["route_config"])`

**L458** `        if raw.get("validation_config"):`

**L459** `            store.validation_config = ValidationConfig.from_dict(raw["validation_config"])`

**L460** `        if raw.get("react_config"):`

**L461** `            store.react_config = ReactConfig.from_dict(raw["react_config"])`

**L462** `        store._sync_chroma_from_json()`

**L463** `        store._rag_service = store._build_rag_service()`

**L464** `        return store`

**L465** ``

**L466** `    @classmethod`

**L467** `    def load_or_bootstrap(cls, path: Path | None = None) -> KnowledgeStore:`

**L468** `        """加载已有库，不存在则从 sample_docs 引导"""`

**L469** `        target = path or _default_store_path()`

**L470** `        if target.is_file():`

**L471** `            return cls.load(target)`

**L472** `        store = cls.bootstrap_from_sample_docs(store_path=target)`

**L473** `        store.save(target)`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L474** `        return store`

**L475** ``

**L476** `    @classmethod`

**L477** `    def bootstrap_from_sample_docs(cls, *, store_path: Path | None = None) -> KnowledgeStore:`

**L478** `        """用内置 sample_docs 初始化知识库"""`

**L479** `        rag = RAGContextService.from_sample_docs(use_embedding=True)`

**L480** `        store = cls(store_path=store_path)`

**L481** `        now = _utc_now()`

**L482** ``

**L483** `        by_source: dict[str, list[TextChunk]] = {}`

**L484** `        for chunk in rag.index.chunks:`

**L485** `            by_source.setdefault(chunk.source, []).append(chunk)`

**L486** ``

**L487** `        for source, chunks in sorted(by_source.items()):`

**L488** `            store.documents.append(`

**L489** `                KnowledgeDocument(`

**L490** `                    name=source,`

**L491** `                    ingested_at=now,`

**L492** `                    size_bytes=sum(len(c.text) for c in chunks),`

**L493** `                    chunk_count=len(chunks),`

**L494** `                )`

**L495** `            )`

**L496** `        store.chunks = list(rag.index.chunks)`

**L497** `        retriever = rag.index.retriever`

**L498** `        from rag.embedding_retriever import EmbeddingRetriever`

**L499** ``

**L500** `        if isinstance(retriever, EmbeddingRetriever):`

**L501** `            store.embedding_state = retriever._client.model.export_state()`

**L502** `        store._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L503** `        return store`

**L504** ``

**L505** `    def status_dict(self) -> dict[str, Any]:`

**L506** `        from tools.doc_parser import supported_formats`

**L507** ``

**L508** `        return {`

**L509** `            "document_count": self.document_count,`

**L510** `            "chunk_count": self.chunk_count,`

**L511** `            "documents": [d.to_dict() for d in self.documents],`

**L512** `            "store_path": str(self.store_path) if self.store_path else None,`

**L513** `            "platform_version": PLATFORM_VERSION,`

**L514** `            "supported_formats": supported_formats(),`

**L515** `            "chunk_config": self.chunk_config.to_dict(),`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L516** `            "last_rebuilt_at": self.last_rebuilt_at,`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L517** `            "last_incremental_at": self.last_incremental_at,`

**L518** `            "index_mode": self.index_mode,`

**L519** `            "retrieval_config": self.retrieval_config.to_dict(),`

**L520** `            "rerank_config": self.rerank_config.to_dict(),`

**L521** `            "rewrite_config": self.rewrite_config.to_dict(),`

**L522** `            "citation_config": self.citation_config.to_dict(),`

**L523** `            "expansion_config": self.expansion_config.to_dict(),`

**L524** `            "route_config": self.route_config.to_dict(),`

**L525** `            "validation_config": self.validation_config.to_dict(),`

**L526** `            "react_config": self.react_config.to_dict(),`

**L527** `            "vector_backend": self.vector_backend,`

**L528** `            "chroma_path": str(self._resolve_chroma_path()),`

**L529** `            "chroma_count": self._chroma_index().count() if self.chunks else 0,`

**L530** `        }`

**L531** ``

**L532** `    def _append_chunks(`

**L533** `        self,`

**L534** `        filename: str,`

**L535** `        new_chunks: list[TextChunk],`

**L536** `        *,`

**L537** `        size_bytes: int,`

**L538** `        doc_format: str = "txt",`

**L539** `    ) -> None:`

**L540** `        base_index = len(self.chunks)`

**L541** `        reindexed: list[TextChunk] = []`

**L542** `        for i, chunk in enumerate(new_chunks):`

**L543** `            reindexed.append(`

**L544** `                TextChunk(`

**L545** `                    chunk_id=chunk.chunk_id,`

**L546** `                    text=chunk.text,`

**L547** `                    source=filename,`

**L548** `                    index=base_index + i,`

**L549** `                    start_char=chunk.start_char,`

**L550** `                    end_char=chunk.end_char,`

**L551** `                )`

**L552** `            )`

**L553** `        self.chunks.extend(reindexed)`

**L554** `        self.documents.append(`

**L555** `            KnowledgeDocument(`

**L556** `                name=filename,`

**L557** `                ingested_at=_utc_now(),`

**L558** `                size_bytes=size_bytes,`

**L559** `                chunk_count=len(reindexed),`

**L560** `                format=doc_format,`

**L561** `            )`

**L562** `        )`

**L563** ``

**L564** `    def _remove_document_by_source(self, filename: str) -> list[str]:`

**L565** `        """移除同名文档及其 chunks，并从 Chroma 删除旧向量"""`

**L566** `        removed_ids = [c.chunk_id for c in self.chunks if c.source == filename]`

**L567** `        if not removed_ids:`

**L568** `            return []`

**L569** ``

**L570** `        self._chroma_index().delete_by_ids(removed_ids)`

**L571** `        self.documents = [d for d in self.documents if d.name != filename]`

**L572** `        self.chunks = [c for c in self.chunks if c.source != filename]`

**L573** `        self.chunks = [`

**L574** `            TextChunk(`

**L575** `                chunk_id=c.chunk_id,`

**L576** `                text=c.text,`

**L577** `                source=c.source,`

**L578** `                index=i,`

**L579** `                start_char=c.start_char,`

**L580** `                end_char=c.end_char,`

**L581** `            )`

**L582** `            for i, c in enumerate(self.chunks)`

**L583** `        ]`

**L584** `        return removed_ids`

**L585** ``

**L586** `    def _resolve_chroma_path(self) -> Path:`

**L587** `        if self.chroma_path is not None:`

**L588** `            return self.chroma_path`

**L589** `        if self.store_path is not None:`

**L590** `            return self.store_path.parent / "chroma"`

**L591** `        return get_path("knowledge_chroma")`

**L592** ``

**L593** `    def _chroma_index(self) -> ChromaVectorIndex:`

**L594** `        return ChromaVectorIndex(self._resolve_chroma_path())`

**L595** ``

**L596** `    def _sync_chroma_from_json(self) -> None:`

**L597** `        """从 JSON 元数据恢复 Chroma（迁移或冷启动）"""`

**L598** `        if not self.chunks or not self.embedding_state:`

**L599** `            return`

**L600** `        chroma = self._chroma_index()`

**L601** `        if chroma.count() > 0:`

**L602** `            return`

**L603** `        from rag.embedding import EmbeddingClient`

**L604** ``

**L605** `        client = EmbeddingClient()`

**L606** `        client.model.load_state(self.embedding_state)`

**L607** `        vectors = client.embed_batch([c.text for c in self.chunks])`

**L608** `        chroma.upsert_chunks(self.chunks, vectors)`

**L609** ``

**L610** `    def _rebuild_index(self) -> None:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L611** `        from rag.embedding_retriever import EmbeddingRetriever`

**L612** ``

**L613** `        if not self.chunks:`

**L614** `            self.embedding_state = {}`

**L615** `            self._chroma_index().reset()`

**L616** `            self.invalidate_cache()`

**L617** `            return`

**L618** ``

**L619** `        retriever = EmbeddingRetriever(self.chunks)`

**L620** `        self.embedding_state = retriever._client.model.export_state()`

**L621** `        vectors = retriever._client.embed_batch([c.text for c in self.chunks])`

**L622** `        chroma = self._chroma_index()`

**L623** `        chroma.reset()`

**L624** `        chroma.upsert_chunks(self.chunks, vectors)`

**L625** `        self.index_mode = INDEX_MODE_FULL`

**L626** `        self.invalidate_cache()`

**L627** ``

**L628** `    def _incremental_index(`

**L629** `        self,`

**L630** `        affected_chunks: list[TextChunk],`

**L631** `        *,`

**L632** `        replaced_count: int = 0,`

**L633** `    ) -> bool:`

**L634** `        """`

**L635** `        增量更新 Chroma：不 reset collection，仅 upsert 受影响 chunk。`

**L636** ``

**L637** `        若 TF-IDF 词表扩张，则回退为全量 upsert（仍不 reset）。`

**L638** `        返回是否发生词表扩张。`

**L639** `        """`

**L640** `        from rag.embedding_retriever import EmbeddingRetriever`

**L641** ``

**L642** `        if not self.chunks:`

**L643** `            self.embedding_state = {}`

**L644** `            self._chroma_index().reset()`

**L645** `            self.last_incremental_at = _utc_now()`

**L646** `            self.index_mode = INDEX_MODE_INCREMENTAL`

**L647** `            self.invalidate_cache()`

**L648** `            return False`

**L649** ``

**L650** `        old_vocab = dict(self.embedding_state.get("vocab") or {})`

**L651** `        retriever = EmbeddingRetriever(self.chunks)`

**L652** `        self.embedding_state = retriever._client.model.export_state()`

**L653** `        new_vocab = dict(self.embedding_state.get("vocab") or {})`

**L654** `        vocab_expanded = len(new_vocab) > len(old_vocab) and bool(old_vocab)`

**L655** ``

**L656** `        if vocab_expanded:`

**L657** `            affected_chunks = list(self.chunks)`

**L658** ``

**L659** `        client = retriever._client`

**L660** `        vectors = client.embed_batch([c.text for c in affected_chunks])`

**L661** `        chroma = self._chroma_index()`

**L662** `        if vocab_expanded:`

**L663** `            # TF-IDF 维度变化时 Chroma collection 须重建`

**L664** `            chroma.reset()`

**L665** `        chroma.upsert_chunks(affected_chunks, vectors)`

**L666** `        self.last_incremental_at = _utc_now()`

**L667** `        self.index_mode = INDEX_MODE_INCREMENTAL`

**L668** `        self.invalidate_cache()`

**L669** `        return vocab_expanded`

**L670** ``

**L671** `    def _build_rag_service(self) -> RAGContextService:`

**L672** `        if not self.chunks:`

**L673** `            return RAGContextService()`

**L674** ``

**L675** `        from rag.embedding import EmbeddingClient`

**L676** `        from rag.retriever import KeywordRetriever`

**L677** ``

**L678** `        client = EmbeddingClient()`

**L679** `        client.model.load_state(self.embedding_state)`

**L680** `        self._sync_chroma_from_json()`

**L681** `        chroma = self._chroma_index()`

**L682** `        vector = ChromaEmbeddingRetriever(self.chunks, chroma, client=client)`

**L683** `        keyword = KeywordRetriever(self.chunks)`

**L684** `        cfg = self.get_retrieval_config()`

**L685** `        hybrid = HybridRetriever(self.chunks, keyword, vector, config=cfg)`

**L686** `        rerank_cfg = self.get_rerank_config()`

**L687** `        reranking = RerankingRetriever(`

**L688** `            hybrid,`

**L689** `            reranker=MockCrossEncoderReranker(),`

**L690** `            config=rerank_cfg,`

**L691** `        )`

**L692** `        rewrite_cfg = self.get_rewrite_config()`

**L693** `        rewriting = RewritingRetriever(reranking, config=rewrite_cfg)`

**L694** `        expansion_cfg = self.get_expansion_config()`

**L695** `        expanding = ExpandingRetriever(rewriting, config=expansion_cfg)`

**L696** `        route_cfg = self.get_route_config()`

**L697** `        retriever = RoutingRetriever(expanding, config=route_cfg)`

**L698** `        index = DocumentIndex(chunks=self.chunks, retriever=retriever)`

**L699** `        return RAGContextService(index)`

**L700** ``

**L701** ``

**L702** `_store: KnowledgeStore | None = None`

**L703** `_store_lock = threading.Lock()`

**L704** ``

**L705** ``

**L706** `def get_knowledge_store(*, reload: bool = False) -> KnowledgeStore:`

**L707** `    """全局知识库单例（API 与 factory 共享）"""`

**L708** `    global _store`

**L709** `    with _store_lock:`

**L710** `        if _store is None or reload:`

**L711** `            _store = KnowledgeStore.load_or_bootstrap()`

**L712** `        return _store`

**L713** ``

**L714** ``

**L715** `def set_knowledge_store(store: KnowledgeStore) -> None:`

**L716** `    """测试注入用"""`

**L717** `    global _store`

**L718** `    with _store_lock:`

**L719** `        _store = store`

**L720** ``

**L721** ``

**L722** `def _default_store_path() -> Path:`

**L723** `    return get_path("knowledge_store")`

**L724** ``

**L725** ``

**L726** `def _utc_now() -> str:`

**L727** `    return datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")`

**L728** ``

**L729** ``

**L730** `def _chunk_to_dict(chunk: TextChunk) -> dict[str, Any]:`

**L731** `    return {`

**L732** `        "chunk_id": chunk.chunk_id,`

**L733** `        "text": chunk.text,`

**L734** `        "source": chunk.source,`

**L735** `        "index": chunk.index,`

**L736** `        "start_char": chunk.start_char,`

**L737** `        "end_char": chunk.end_char,`

**L738** `    }`

**L739** ``

**L740** ``

**L741** `def _chunk_from_dict(data: dict[str, Any]) -> TextChunk:`

**L742** `    return TextChunk(`

**L743** `        chunk_id=str(data.get("chunk_id", "")),`

**L744** `        text=str(data.get("text", "")),`

**L745** `        source=str(data.get("source", "")),`

**L746** `        index=int(data.get("index", 0)),`

**L747** `        start_char=int(data.get("start_char", 0)),`

**L748** `        end_char=int(data.get("end_char", 0)),`

**L749** `    )`


## 附录：api/knowledge.py 重建端点走读

### `nexus-agent-platform/src/api/knowledge.py` 逐行走读

共 **450** 行。

**L1** `"""`

**L2** `知识库 REST API — 文档上传、分块调参与检索评估`

**L3** ``

**L4** `需求：ZL-NA-REQ-025 / ZL-NA-REQ-026 / ZL-NA-REQ-027 / ZL-NA-REQ-028 / ZL-NA-REQ-029 / ZL-NA-REQ-030 / ZL-NA-REQ-031 / ZL-NA-REQ-032 / ZL-NA-REQ-033 / ZL-NA-REQ-034 / ZL-NA-REQ-035 / ZL-NA-REQ-036 / ZL-NA-REQ-037 / ZL-NA-REQ-038`

**L5** `"""`

**L6** ``

**L7** `from __future__ import annotations`

**L8** ``

**L9** `from pathlib import Path`

**L10** ``

**L11** `from fastapi import APIRouter, File, HTTPException, UploadFile`

**L12** ``

**L13** `from api.schemas import (`

**L14** `    ChunkConfigRequest,`

**L15** `    ChunkConfigResponse,`

**L16** `    CitationConfigRequest,`

**L17** `    CitationConfigResponse,`

**L18** `    CitationPreviewRequest,`

**L19** `    CitationPreviewResponse,`

**L20** `    ExpansionConfigRequest,`

**L21** `    ExpansionConfigResponse,`

**L22** `    ExpansionPreviewRequest,`

**L23** `    ExpansionPreviewResponse,`

**L24** `    EvaluateRequest,`

**L25** `    EvaluateResponse,`

**L26** `    KnowledgeStatusResponse,`

**L27** `    KnowledgeUploadResponse,`

**L28** `    RebuildRequest,`

**L29** `    RebuildResponse,`

**L30** `    RerankConfigRequest,`

**L31** `    RerankConfigResponse,`

**L32** `    RewriteConfigRequest,`

**L33** `    RewriteConfigResponse,`

**L34** `    RewritePreviewRequest,`

**L35** `    RewritePreviewResponse,`

**L36** `    RouteConfigRequest,`

**L37** `    RouteConfigResponse,`

**L38** `    RoutePreviewRequest,`

**L39** `    RoutePreviewResponse,`

**L40** `    ValidationConfigRequest,`

**L41** `    ValidationConfigResponse,`

**L42** `    ValidationPreviewRequest,`

**L43** `    ValidationPreviewResponse,`

**L44** `    ValidationRetryPreviewRequest,`

**L45** `    ValidationRetryPreviewResponse,`

**L46** `    RetrievalConfigRequest,`

**L47** `    RetrievalConfigResponse,`

**L48** `)`

**L49** `from api.sessions import session_manager`
  → rebuild API 相关行。

**L50** `from core.exceptions import NexusError, StorageError`

**L51** `from rag.chunk_config import PRESET_CONFIGS, ChunkConfig`

**L52** `from rag.ingestion import ingest_upload`

**L53** `from rag.knowledge_rebuild import rebuild_store, rebuild_with_best_config`
  → rebuild API 相关行。

**L54** `from rag.knowledge_store import get_knowledge_store`

**L55** `from rag.citation_config import CitationConfig`

**L56** `from rag.expansion_config import ExpansionConfig`

**L57** `from rag.query_expander import build_expander`

**L58** `from rag.query_rewriter import RuleBasedQueryRewriter`

**L59** `from rag.query_router import RuleBasedQueryRouter`

**L60** `from rag.route_config import RouteConfig`

**L61** `from rag.validation_config import ValidationConfig`

**L62** `from rag.rerank_config import RerankConfig`

**L63** `from rag.rewrite_config import RewriteConfig`

**L64** `from rag.retrieval_config import RetrievalConfig`

**L65** `from rag.retrieval_eval import EvalQuery, pick_best_config, run_ab_experiment`

**L66** `from tools.doc_parser import parse_bytes`

**L67** ``

**L68** `router = APIRouter(prefix="/api/knowledge", tags=["knowledge"])`

**L69** ``

**L70** `MAX_UPLOAD_BYTES = 512_000  # 500 KB 教学上限`

**L71** `_EVAL_SAMPLE = (`

**L72** `    Path(__file__).resolve().parent.parent / "day26" / "sample_docs" / "product_notice.md"`

**L73** `)`

**L74** ``

**L75** ``

**L76** `@router.get("/retrieval-config", response_model=RetrievalConfigResponse)`

**L77** `def get_retrieval_config() -> RetrievalConfigResponse:`

**L78** `    """返回当前检索模式（vector / keyword / hybrid）与融合参数"""`

**L79** `    cfg = get_knowledge_store().get_retrieval_config()`

**L80** `    return RetrievalConfigResponse(**cfg.to_dict())`

**L81** ``

**L82** ``

**L83** `@router.put("/retrieval-config", response_model=RetrievalConfigResponse)`

**L84** `def update_retrieval_config(body: RetrievalConfigRequest) -> RetrievalConfigResponse:`

**L85** `    """更新检索策略；变更后清除 RAG 缓存"""`

**L86** `    store = get_knowledge_store()`

**L87** `    try:`

**L88** `        cfg = RetrievalConfig.from_dict(body.model_dump())`

**L89** `        cfg.validate()`

**L90** `        store.set_retrieval_config(cfg)`

**L91** `        store.save()`

**L92** `    except ValueError as exc:`

**L93** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L94** `    return RetrievalConfigResponse(**cfg.to_dict())`

**L95** ``

**L96** ``

**L97** `@router.get("/rerank-config", response_model=RerankConfigResponse)`

**L98** `def get_rerank_config() -> RerankConfigResponse:`

**L99** `    """返回 rerank 开关、候选池大小与模型标识"""`

**L100** `    cfg = get_knowledge_store().get_rerank_config()`

**L101** `    return RerankConfigResponse(**cfg.to_dict())`

**L102** ``

**L103** ``

**L104** `@router.put("/rerank-config", response_model=RerankConfigResponse)`

**L105** `def update_rerank_config(body: RerankConfigRequest) -> RerankConfigResponse:`

**L106** `    """更新 rerank 策略；变更后清除 RAG 缓存"""`

**L107** `    store = get_knowledge_store()`

**L108** `    try:`

**L109** `        cfg = RerankConfig.from_dict(body.model_dump())`

**L110** `        cfg.validate()`

**L111** `        store.set_rerank_config(cfg)`

**L112** `        store.save()`

**L113** `    except ValueError as exc:`

**L114** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L115** `    return RerankConfigResponse(**cfg.to_dict())`

**L116** ``

**L117** ``

**L118** `@router.get("/rewrite-config", response_model=RewriteConfigResponse)`

**L119** `def get_rewrite_config() -> RewriteConfigResponse:`

**L120** `    """返回查询改写开关与规则模式"""`

**L121** `    cfg = get_knowledge_store().get_rewrite_config()`

**L122** `    return RewriteConfigResponse(**cfg.to_dict())`

**L123** ``

**L124** ``

**L125** `@router.put("/rewrite-config", response_model=RewriteConfigResponse)`

**L126** `def update_rewrite_config(body: RewriteConfigRequest) -> RewriteConfigResponse:`

**L127** `    """更新查询改写策略；变更后清除 RAG 缓存"""`

**L128** `    store = get_knowledge_store()`

**L129** `    try:`

**L130** `        cfg = RewriteConfig.from_dict(body.model_dump())`

**L131** `        cfg.validate()`

**L132** `        store.set_rewrite_config(cfg)`

**L133** `        store.save()`

**L134** `    except ValueError as exc:`

**L135** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L136** `    return RewriteConfigResponse(**cfg.to_dict())`

**L137** ``

**L138** ``

**L139** `@router.post("/rewrite-preview", response_model=RewritePreviewResponse)`

**L140** `def rewrite_preview(body: RewritePreviewRequest) -> RewritePreviewResponse:`

**L141** `    """预览单条 query 的规则改写结果（不触发检索）"""`

**L142** `    store = get_knowledge_store()`

**L143** `    cfg = store.get_rewrite_config()`

**L144** `    rewriter = RuleBasedQueryRewriter(config=cfg)`

**L145** `    result = rewriter.rewrite(body.query)`

**L146** `    return RewritePreviewResponse(**result.to_dict())`

**L147** ``

**L148** ``

**L149** `@router.get("/citation-config", response_model=CitationConfigResponse)`

**L150** `def get_citation_config() -> CitationConfigResponse:`

**L151** `    """返回引用溯源开关与展示参数"""`

**L152** `    cfg = get_knowledge_store().get_citation_config()`

**L153** `    return CitationConfigResponse(**cfg.to_dict())`

**L154** ``

**L155** ``

**L156** `@router.put("/citation-config", response_model=CitationConfigResponse)`

**L157** `def update_citation_config(body: CitationConfigRequest) -> CitationConfigResponse:`

**L158** `    """更新引用溯源策略并持久化"""`

**L159** `    store = get_knowledge_store()`

**L160** `    try:`

**L161** `        cfg = CitationConfig.from_dict(body.model_dump())`

**L162** `        cfg.validate()`

**L163** `        store.set_citation_config(cfg)`

**L164** `        store.save()`

**L165** `    except ValueError as exc:`

**L166** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L167** `    return CitationConfigResponse(**cfg.to_dict())`

**L168** ``

**L169** ``

**L170** `@router.post("/citation-preview", response_model=CitationPreviewResponse)`

**L171** `def citation_preview(body: CitationPreviewRequest) -> CitationPreviewResponse:`

**L172** `    """预览单条 query 的检索引用（含 rewrite / expansion 审计）"""`

**L173** `    store = get_knowledge_store()`

**L174** `    data = store.fetch_citations(body.query)`

**L175** `    return CitationPreviewResponse(**data)`

**L176** ``

**L177** ``

**L178** `@router.get("/expansion-config", response_model=ExpansionConfigResponse)`

**L179** `def get_expansion_config() -> ExpansionConfigResponse:`

**L180** `    """返回多 query 扩展开关与参数"""`

**L181** `    cfg = get_knowledge_store().get_expansion_config()`

**L182** `    return ExpansionConfigResponse(**cfg.to_dict())`

**L183** ``

**L184** ``

**L185** `@router.put("/expansion-config", response_model=ExpansionConfigResponse)`

**L186** `def update_expansion_config(body: ExpansionConfigRequest) -> ExpansionConfigResponse:`

**L187** `    """更新多 query 扩展策略并持久化"""`

**L188** `    store = get_knowledge_store()`

**L189** `    try:`

**L190** `        cfg = ExpansionConfig.from_dict(body.model_dump())`

**L191** `        cfg.validate()`

**L192** `        store.set_expansion_config(cfg)`

**L193** `        store.save()`

**L194** `    except ValueError as exc:`

**L195** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L196** `    return ExpansionConfigResponse(**cfg.to_dict())`

**L197** ``

**L198** ``

**L199** `@router.post("/expansion-preview", response_model=ExpansionPreviewResponse)`

**L200** `def expansion_preview(body: ExpansionPreviewRequest) -> ExpansionPreviewResponse:`

**L201** `    """预览单条 query 的多路扩展结果"""`

**L202** `    store = get_knowledge_store()`

**L203** `    cfg = store.get_expansion_config()`

**L204** `    expander = build_expander(cfg)`

**L205** `    result = expander.expand(body.query)`

**L206** `    return ExpansionPreviewResponse(**result.to_dict())`

**L207** ``

**L208** ``

**L209** `@router.get("/route-config", response_model=RouteConfigResponse)`

**L210** `def get_route_config() -> RouteConfigResponse:`

**L211** `    """返回检索管线路由开关与默认意图"""`

**L212** `    cfg = get_knowledge_store().get_route_config()`

**L213** `    return RouteConfigResponse(**cfg.to_dict())`

**L214** ``

**L215** ``

**L216** `@router.put("/route-config", response_model=RouteConfigResponse)`

**L217** `def update_route_config(body: RouteConfigRequest) -> RouteConfigResponse:`

**L218** `    """更新检索管线路由策略并持久化"""`

**L219** `    store = get_knowledge_store()`

**L220** `    try:`

**L221** `        cfg = RouteConfig.from_dict(body.model_dump())`

**L222** `        cfg.validate()`

**L223** `        store.set_route_config(cfg)`

**L224** `        store.save()`

**L225** `    except ValueError as exc:`

**L226** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L227** `    return RouteConfigResponse(**cfg.to_dict())`

**L228** ``

**L229** ``

**L230** `@router.post("/route-preview", response_model=RoutePreviewResponse)`

**L231** `def route_preview(body: RoutePreviewRequest) -> RoutePreviewResponse:`

**L232** `    """预览单条 query 的路由决策（expand/rewrite 开关）"""`

**L233** `    store = get_knowledge_store()`

**L234** `    cfg = store.get_route_config()`

**L235** `    router = RuleBasedQueryRouter(config=cfg)`

**L236** `    result = router.route(body.query)`

**L237** `    return RoutePreviewResponse(**result.to_dict())`

**L238** ``

**L239** ``

**L240** `@router.get("/validation-config", response_model=ValidationConfigResponse)`

**L241** `def get_validation_config() -> ValidationConfigResponse:`

**L242** `    """返回答案校验开关与阈值"""`

**L243** `    cfg = get_knowledge_store().get_validation_config()`

**L244** `    return ValidationConfigResponse(**cfg.to_dict())`

**L245** ``

**L246** ``

**L247** `@router.put("/validation-config", response_model=ValidationConfigResponse)`

**L248** `def update_validation_config(body: ValidationConfigRequest) -> ValidationConfigResponse:`

**L249** `    """更新答案校验策略并持久化"""`

**L250** `    store = get_knowledge_store()`

**L251** `    try:`

**L252** `        cfg = ValidationConfig.from_dict(body.model_dump())`

**L253** `        cfg.validate()`

**L254** `        store.set_validation_config(cfg)`

**L255** `        store.save()`

**L256** `    except ValueError as exc:`

**L257** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L258** `    return ValidationConfigResponse(**cfg.to_dict())`

**L259** ``

**L260** ``

**L261** `@router.post("/validation-preview", response_model=ValidationPreviewResponse)`

**L262** `def validation_preview(body: ValidationPreviewRequest) -> ValidationPreviewResponse:`

**L263** `    """预览单条 query + reply 的引用一致性校验"""`

**L264** `    from rag.answer_validator import RuleBasedAnswerValidator`

**L265** ``

**L266** `    store = get_knowledge_store()`

**L267** `    citations = list(body.citations)`

**L268** `    if not citations:`

**L269** `        cite_data = store.fetch_citations(body.query)`

**L270** `        citations = cite_data.get("citations") or []`

**L271** `    cfg = store.get_validation_config()`

**L272** `    preview_cfg = ValidationConfig.from_dict({**cfg.to_dict(), "enabled": True})`

**L273** `    result = RuleBasedAnswerValidator(config=preview_cfg).validate(`

**L274** `        body.query, body.reply, citations`

**L275** `    )`

**L276** `    return ValidationPreviewResponse(**result.to_dict())`

**L277** ``

**L278** ``

**L279** `@router.post("/validation-retry-preview", response_model=ValidationRetryPreviewResponse)`

**L280** `def validation_retry_preview(`

**L281** `    body: ValidationRetryPreviewRequest,`

**L282** `) -> ValidationRetryPreviewResponse:`

**L283** `    """模拟校验失败后的 rag_wide 重检索与再校验"""`

**L284** `    from rag.validation_retry import apply_validation_retry`

**L285** ``

**L286** `    store = get_knowledge_store()`

**L287** `    cite_data = store.fetch_citations(body.query)`

**L288** `    citations = list(body.citations) or cite_data.get("citations") or []`

**L289** `    if body.citations:`

**L290** `        cite_data = {**cite_data, "citations": citations}`

**L291** ``

**L292** `    saved = store.get_validation_config()`

**L293** `    retry_cfg = ValidationConfig.from_dict(`

**L294** `        {**saved.to_dict(), "enabled": True, "retry_on_fail": True, "max_retries": 1}`

**L295** `    )`

**L296** `    store.set_validation_config(retry_cfg)`

**L297** `    try:`

**L298** `        outcome = apply_validation_retry(`

**L299** `            store,`

**L300** `            body.query,`

**L301** `            body.reply,`

**L302** `            citations,`

**L303** `            cite_data,`

**L304** `        )`

**L305** `    finally:`

**L306** `        store.set_validation_config(saved)`

**L307** ``

**L308** `    if outcome is None:`

**L309** `        raise HTTPException(status_code=400, detail="校验已关闭")`

**L310** `    payload = outcome.validation.to_dict()`

**L311** `    payload["retry_route"] = outcome.cite_data.get("route")`

**L312** `    return ValidationRetryPreviewResponse(**payload)`

**L313** ``

**L314** ``

**L315** `@router.get("/chunk-config", response_model=ChunkConfigResponse)`

**L316** `def get_chunk_config() -> ChunkConfigResponse:`

**L317** `    """返回当前知识库默认分块参数"""`

**L318** `    cfg = get_knowledge_store().get_chunk_config()`

**L319** `    return ChunkConfigResponse(**cfg.to_dict())`

**L320** ``

**L321** ``

**L322** `@router.put("/chunk-config", response_model=ChunkConfigResponse)`

**L323** `def update_chunk_config(body: ChunkConfigRequest) -> ChunkConfigResponse:`

**L324** `    """更新默认分块参数（影响后续上传）"""`

**L325** `    store = get_knowledge_store()`

**L326** `    try:`

**L327** `        cfg = ChunkConfig.from_dict(body.model_dump())`

**L328** `        cfg.validate()`

**L329** `        store.set_chunk_config(cfg)`

**L330** `        store.save()`

**L331** `    except ValueError as exc:`

**L332** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L333** `    return ChunkConfigResponse(**cfg.to_dict())`

**L334** ``

**L335** ``

**L336** `@router.post("/evaluate", response_model=EvaluateResponse)`

**L337** `def evaluate_chunk_configs(body: EvaluateRequest | None = None) -> EvaluateResponse:`

**L338** `    """`

**L339** `    对内置样例文档运行 A/B 分块评估，返回 hit@1 与推荐配置。`

**L340** ``

**L341** `    默认使用 PRESET_CONFIGS 四套预设与 day27 评估问句集。`

**L342** `    """`

**L343** `    body = body or EvaluateRequest()`

**L344** `    if not _EVAL_SAMPLE.is_file():`

**L345** `        raise HTTPException(status_code=500, detail="评估样例文档缺失")`

**L346** ``

**L347** `    doc = parse_bytes(_EVAL_SAMPLE.read_bytes(), _EVAL_SAMPLE.name)`

**L348** `    from day27.constants import EVAL_QUERIES`

**L349** ``

**L350** `    queries = [EvalQuery.from_dict(q) for q in EVAL_QUERIES]`

**L351** ``

**L352** `    if body.use_presets and not body.configs:`

**L353** `        configs = list(PRESET_CONFIGS)`

**L354** `    else:`

**L355** `        configs = [ChunkConfig.from_dict(c.model_dump()) for c in body.configs]`

**L356** `        for c in configs:`

**L357** `            c.validate()`

**L358** ``

**L359** `    results = run_ab_experiment(doc, configs, queries)`

**L360** `    best = pick_best_config(results)`

**L361** `    if not best:`

**L362** `        raise HTTPException(status_code=500, detail="评估未产生结果")`

**L363** ``

**L364** `    return EvaluateResponse(`

**L365** `        best_config=best.to_dict()["config"],`

**L366** `        results=[r.to_dict() for r in results],`

**L367** `        eval_query_count=len(queries),`

**L368** `    )`

**L369** ``

**L370** ``

**L371** `@router.post("/rebuild", response_model=RebuildResponse)`
  → rebuild API 相关行。

**L372** `def rebuild_knowledge_base(body: RebuildRequest | None = None) -> RebuildResponse:`
  → rebuild API 相关行。

**L373** `    """`

**L374** `    按当前 chunk_config（或 evaluate 最优配置）全量重建知识库。`

**L375** ``

**L376** `    重扫 sample_docs + knowledge_uploads，清空后重新分块与索引。`

**L377** `    """`

**L378** `    body = body or RebuildRequest()`

**L379** `    store = get_knowledge_store()`

**L380** ``

**L381** `    if body.apply_best_config:`
  → rebuild API 相关行。

**L382** `        if not _EVAL_SAMPLE.is_file():`

**L383** `            raise HTTPException(status_code=500, detail="评估样例文档缺失")`

**L384** `        report = rebuild_with_best_config(`
  → rebuild API 相关行。

**L385** `            store,`

**L386** `            eval_sample_path=_EVAL_SAMPLE,`

**L387** `            include_sample_docs=body.include_sample_docs,`

**L388** `        )`

**L389** `    else:`

**L390** `        report = rebuild_store(store, include_sample_docs=body.include_sample_docs)`
  → rebuild API 相关行。

**L391** ``

**L392** `    cleared = session_manager.clear_all()`
  → rebuild API 相关行。

**L393** `    data = report.to_dict()`

**L394** `    data["sessions_cleared"] = cleared`

**L395** `    return RebuildResponse(**data)`

**L396** ``

**L397** ``

**L398** `@router.get("/status", response_model=KnowledgeStatusResponse)`

**L399** `def knowledge_status() -> KnowledgeStatusResponse:`

**L400** `    """返回知识库文档数、分块数、支持格式与文档列表"""`

**L401** `    store = get_knowledge_store()`

**L402** `    data = store.status_dict()`

**L403** `    return KnowledgeStatusResponse(**data)`

**L404** ``

**L405** ``

**L406** `@router.post("/upload", response_model=KnowledgeUploadResponse)`

**L407** `async def upload_document(`

**L408** `    file: UploadFile = File(..., description="企业文档 (.txt / .md / .pdf)"),`

**L409** `) -> KnowledgeUploadResponse:`

**L410** `    """`

**L411** `    上传企业文档到知识库：解析 → 落盘 → 分块 → Chroma 向量索引 → 持久化。`

**L412** ``

**L413** `    Day 26 起支持 Markdown 与 PDF。上传成功后清除服务端会话。`

**L414** `    """`

**L415** `    if not file.filename:`

**L416** `        raise HTTPException(status_code=422, detail="缺少文件名")`

**L417** ``

**L418** `    data = await file.read()`

**L419** `    if not data:`

**L420** `        raise HTTPException(status_code=422, detail="文件内容为空")`

**L421** `    if len(data) > MAX_UPLOAD_BYTES:`

**L422** `        raise HTTPException(status_code=413, detail="文件超过 500KB 上限")`

**L423** ``

**L424** `    try:`

**L425** `        meta = ingest_upload(data, file.filename)`

**L426** `    except ValueError as exc:`

**L427** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L428** `    except NexusError as exc:`

**L429** `        status = 400 if exc.code in (`

**L430** `            "PDF_PARSE_ERROR", "PDF_EMPTY", "UNSUPPORTED_FORMAT", "EMPTY_FILE"`

**L431** `        ) else 500`

**L432** `        raise HTTPException(status_code=status, detail=exc.message) from exc`

**L433** `    except StorageError as exc:`

**L434** `        raise HTTPException(status_code=500, detail=exc.message) from exc`

**L435** `    except UnicodeDecodeError as exc:`

**L436** `        raise HTTPException(status_code=400, detail="文本文件须为 UTF-8 编码") from exc`

**L437** ``

**L438** `    cleared = session_manager.clear_all()`
  → rebuild API 相关行。

**L439** `    store = get_knowledge_store()`

**L440** ``

**L441** `    return KnowledgeUploadResponse(`

**L442** `        filename=meta.name,`

**L443** `        format=meta.format,`

**L444** `        chunk_count=meta.chunk_count,`

**L445** `        document_count=store.document_count,`

**L446** `        total_chunks=store.chunk_count,`

**L447** `        sessions_cleared=cleared,`

**L448** `        index_mode=store.index_mode,`

**L449** `        message=f"文档已入库（{meta.format}），增量索引已更新",`

**L450** `    )`


## 6. 失败恢复

重建失败 mid-loop 可能导致空库——教学环境可重新 bootstrap；生产应事务化或先写临时 store。
