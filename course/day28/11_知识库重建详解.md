# 知识库重建详解

**Day 28 深度专题** | ZL-NA-REQ-028

## 1. 问题定义

设源文件集合 \(S\)，当前配置 \(	heta\)。生产索引 \(I = 	ext{Index}(	ext{Parse}(S), 	heta_{	ext{old}})\)。配置更新为 \(	heta_{	ext{new}}\) 后，需计算 \(I' = 	ext{Index}(	ext{Parse}(S), 	heta_{	ext{new}})\)。

**无法**通过修改 \(	heta\) 元数据把 \(I\) 就地变为 \(I'\)，因 chunk 文本边界已固定。

## 2. rebuild_store 不变式

- 重建后 \(orall d \in 	ext{documents}\)，\(d\) 来自 \(S\) 中某个源文件  
- \(	ext{chunk\_count}\) 仅取决于 \(|S|\) 与 \(	heta_{	ext{new}}\)  
- `last_rebuilt_at` 单调更新（同进程连续 rebuild）

## 3. 与 evaluate 协作

`rebuild_with_best_config`：

```python
results = run_ab_experiment(doc, PRESET_CONFIGS, queries)
best = pick_best_config(results)
store.set_chunk_config(best.config)
return rebuild_store(store, ...)
```

**注意**：evaluate 样例是 `product_notice.md`，全库 rebuild 包含所有源文件——最优配置未必对所有文档全局最优。

## 4. 清空顺序为何重要

必须先 `documents.clear()` 再逐文件 `_append_chunks`，最后单次 `_rebuild_index()`。若中途 `_rebuild_index()` 会导致空索引窗口内 chat 失败——教学单进程可接受。

## 5. 运维 Runbook（摘要）

1. 备份 `data/knowledge/store.json`  
2. 确认 uploads 文档版本  
3. POST evaluate 存档 JSON  
4. POST rebuild（或 apply_best_config）  
5. 检查 last_rebuilt_at、chunk_count  
6. chat 抽测 + 监控错误率  



## 附录：knowledge_store 持久化走读

### `nexus-agent-platform/src/rag/knowledge_store.py` 逐行走读

共 **735** 行。

**L1** `"""`

**L2** `知识库存储 — 文档 ingestion、分块索引与 JSON 持久化`

**L3** ``

**L4** `将 Day 19–20 的 RAG 管线升级为可写入、可落盘的企业知识库 MVP。`

**L5** ``

**L6** `需求：ZL-NA-REQ-025`

**L7** `"""`

**L8** ``

**L9** `from __future__ import annotations`

**L10** ``

**L11** `import threading`

**L12** `from dataclasses import dataclass, field`

**L13** `from datetime import datetime, timezone`

**L14** `from pathlib import Path`

**L15** `from typing import Any`

**L16** ``

**L17** `from core.paths import get_path`

**L18** `from rag.chunker import TextChunk, chunk_documents, chunk_text`

**L19** `from rag.chunk_config import DEFAULT_CHUNK_CONFIG, ChunkConfig`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L20** `from rag.chunk_strategies import chunk_from_parsed`

**L21** `from rag.context import DocumentIndex, RAGContextService`

**L22** `from rag.chroma_retriever import ChromaEmbeddingRetriever`

**L23** `from rag.chroma_store import VECTOR_BACKEND, ChromaVectorIndex`

**L24** `from rag.citation_config import CitationConfig`

**L25** `from rag.expanding_retriever import ExpandingRetriever`

**L26** `from rag.expansion_config import ExpansionConfig`

**L27** `from rag.answer_validator import RuleBasedAnswerValidator, ValidationResult`

**L28** `from rag.route_config import RouteConfig`

**L29** `from rag.routing_retriever import RoutingRetriever`

**L30** `from rag.validation_config import ValidationConfig`

**L31** `from rag.hybrid_retriever import HybridRetriever`

**L32** `from rag.rerank_config import RerankConfig`

**L33** `from rag.reranker import MockCrossEncoderReranker`

**L34** `from rag.reranking_retriever import RerankingRetriever`

**L35** `from rag.retrieval_config import RetrievalConfig`

**L36** `from rag.rewrite_config import RewriteConfig`

**L37** `from rag.rewriting_retriever import RewritingRetriever`

**L38** `from tools.doc_reader import DocumentRecord, read_text_file`

**L39** `from tools.parsers.base import ParsedDocument`

**L40** `from utils.json_utils import load_json, save_json`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L41** `from utils.text_utils import clean_text`

**L42** ``

**L43** `STORE_VERSION = "1.1"`

**L44** `PLATFORM_VERSION = "0.38.0"`

**L45** `INDEX_MODE_INCREMENTAL = "incremental"`

**L46** `INDEX_MODE_FULL = "full"`

**L47** ``

**L48** ``

**L49** `@dataclass`

**L50** `class KnowledgeDocument:`

**L51** `    """已入库文档元数据"""`

**L52** ``

**L53** `    name: str`

**L54** `    ingested_at: str`

**L55** `    size_bytes: int = 0`

**L56** `    chunk_count: int = 0`

**L57** `    format: str = "txt"`

**L58** ``

**L59** `    def to_dict(self) -> dict[str, Any]:`

**L60** `        return {`

**L61** `            "name": self.name,`

**L62** `            "ingested_at": self.ingested_at,`

**L63** `            "size_bytes": self.size_bytes,`

**L64** `            "chunk_count": self.chunk_count,`

**L65** `            "format": self.format,`

**L66** `        }`

**L67** ``

**L68** `    @classmethod`

**L69** `    def from_dict(cls, data: dict[str, Any]) -> KnowledgeDocument:`

**L70** `        return cls(`

**L71** `            name=str(data.get("name", "")),`

**L72** `            ingested_at=str(data.get("ingested_at", "")),`

**L73** `            size_bytes=int(data.get("size_bytes", 0)),`

**L74** `            chunk_count=int(data.get("chunk_count", 0)),`

**L75** `            format=str(data.get("format", "txt")),`

**L76** `        )`

**L77** ``

**L78** ``

**L79** `@dataclass`

**L80** `class KnowledgeStore:`

**L81** `    """`

**L82** `    企业知识库 — 分块 + Chroma 向量索引 + JSON 元数据持久化`

**L83** ``

**L84** `    典型用法：`

**L85** `        store = KnowledgeStore.load_or_bootstrap()`

**L86** `        store.ingest_text("新产品说明…", filename="notice.txt")`

**L87** `        rag = store.as_rag_service()`

**L88** `    """`

**L89** ``

**L90** `    documents: list[KnowledgeDocument] = field(default_factory=list)`

**L91** `    chunks: list[TextChunk] = field(default_factory=list)`

**L92** `    embedding_state: dict[str, Any] = field(default_factory=dict)`

**L93** `    vector_backend: str = VECTOR_BACKEND`

**L94** `    chunk_config: ChunkConfig = field(default_factory=ChunkConfig)`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L95** `    last_rebuilt_at: str | None = None`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L96** `    last_incremental_at: str | None = None`

**L97** `    index_mode: str = INDEX_MODE_FULL`

**L98** `    retrieval_config: RetrievalConfig = field(default_factory=RetrievalConfig)`

**L99** `    rerank_config: RerankConfig = field(default_factory=RerankConfig)`

**L100** `    rewrite_config: RewriteConfig = field(default_factory=RewriteConfig)`

**L101** `    citation_config: CitationConfig = field(default_factory=CitationConfig)`

**L102** `    expansion_config: ExpansionConfig = field(default_factory=ExpansionConfig)`

**L103** `    route_config: RouteConfig = field(default_factory=RouteConfig)`

**L104** `    validation_config: ValidationConfig = field(default_factory=ValidationConfig)`

**L105** `    store_path: Path | None = None`

**L106** `    chroma_path: Path | None = None`

**L107** `    _rag_service: RAGContextService | None = field(default=None, repr=False)`

**L108** ``

**L109** `    @property`

**L110** `    def chunk_count(self) -> int:`

**L111** `        return len(self.chunks)`

**L112** ``

**L113** `    @property`

**L114** `    def document_count(self) -> int:`

**L115** `        return len(self.documents)`

**L116** ``

**L117** `    def as_rag_service(self) -> RAGContextService:`

**L118** `        """构建或返回缓存的 RAGContextService"""`

**L119** `        if self._rag_service is None:`

**L120** `            self._rag_service = self._build_rag_service()`

**L121** `        return self._rag_service`

**L122** ``

**L123** `    def invalidate_cache(self) -> None:`

**L124** `        self._rag_service = None`

**L125** ``

**L126** `    def get_chunk_config(self) -> ChunkConfig:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L127** `        return ChunkConfig.from_dict(self.chunk_config.to_dict())`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L128** ``

**L129** `    def set_chunk_config(self, config: ChunkConfig) -> ChunkConfig:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L130** `        config.validate()`

**L131** `        self.chunk_config = ChunkConfig.from_dict(config.to_dict())`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L132** `        return self.chunk_config`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L133** ``

**L134** `    def get_retrieval_config(self) -> RetrievalConfig:`

**L135** `        return RetrievalConfig.from_dict(self.retrieval_config.to_dict())`

**L136** ``

**L137** `    def set_retrieval_config(self, config: RetrievalConfig) -> RetrievalConfig:`

**L138** `        config.validate()`

**L139** `        self.retrieval_config = RetrievalConfig.from_dict(config.to_dict())`

**L140** `        self.invalidate_cache()`

**L141** `        return self.retrieval_config`

**L142** ``

**L143** `    def get_rerank_config(self) -> RerankConfig:`

**L144** `        return RerankConfig.from_dict(self.rerank_config.to_dict())`

**L145** ``

**L146** `    def set_rerank_config(self, config: RerankConfig) -> RerankConfig:`

**L147** `        config.validate()`

**L148** `        self.rerank_config = RerankConfig.from_dict(config.to_dict())`

**L149** `        self.invalidate_cache()`

**L150** `        return self.rerank_config`

**L151** ``

**L152** `    def get_rewrite_config(self) -> RewriteConfig:`

**L153** `        return RewriteConfig.from_dict(self.rewrite_config.to_dict())`

**L154** ``

**L155** `    def set_rewrite_config(self, config: RewriteConfig) -> RewriteConfig:`

**L156** `        config.validate()`

**L157** `        self.rewrite_config = RewriteConfig.from_dict(config.to_dict())`

**L158** `        self.invalidate_cache()`

**L159** `        return self.rewrite_config`

**L160** ``

**L161** `    def get_citation_config(self) -> CitationConfig:`

**L162** `        return CitationConfig.from_dict(self.citation_config.to_dict())`

**L163** ``

**L164** `    def set_citation_config(self, config: CitationConfig) -> CitationConfig:`

**L165** `        config.validate()`

**L166** `        self.citation_config = CitationConfig.from_dict(config.to_dict())`

**L167** `        return self.citation_config`

**L168** ``

**L169** `    def get_expansion_config(self) -> ExpansionConfig:`

**L170** `        return ExpansionConfig.from_dict(self.expansion_config.to_dict())`

**L171** ``

**L172** `    def set_expansion_config(self, config: ExpansionConfig) -> ExpansionConfig:`

**L173** `        config.validate()`

**L174** `        self.expansion_config = ExpansionConfig.from_dict(config.to_dict())`

**L175** `        self.invalidate_cache()`

**L176** `        return self.expansion_config`

**L177** ``

**L178** `    def get_route_config(self) -> RouteConfig:`

**L179** `        return RouteConfig.from_dict(self.route_config.to_dict())`

**L180** ``

**L181** `    def set_route_config(self, config: RouteConfig) -> RouteConfig:`

**L182** `        config.validate()`

**L183** `        self.route_config = RouteConfig.from_dict(config.to_dict())`

**L184** `        self.invalidate_cache()`

**L185** `        return self.route_config`

**L186** ``

**L187** `    def get_validation_config(self) -> ValidationConfig:`

**L188** `        return ValidationConfig.from_dict(self.validation_config.to_dict())`

**L189** ``

**L190** `    def set_validation_config(self, config: ValidationConfig) -> ValidationConfig:`

**L191** `        config.validate()`

**L192** `        self.validation_config = ValidationConfig.from_dict(config.to_dict())`

**L193** `        return self.validation_config`

**L194** ``

**L195** `    def validate_answer(`

**L196** `        self,`

**L197** `        query: str,`

**L198** `        reply: str,`

**L199** `        citations: list[dict[str, Any]],`

**L200** `    ) -> ValidationResult | None:`

**L201** `        """按当前 validation_config 校验 reply 与 citations 一致性"""`

**L202** `        cfg = self.get_validation_config()`

**L203** `        if not cfg.enabled:`

**L204** `            return None`

**L205** `        validator = RuleBasedAnswerValidator(config=cfg)`

**L206** `        return validator.validate(query, reply, citations)`

**L207** ``

**L208** `    def fetch_citations(self, query: str) -> dict[str, Any]:`

**L209** `        """按当前 citation_config 检索并返回引用包 dict"""`

**L210** `        cfg = self.get_citation_config()`

**L211** `        if not cfg.enabled:`

**L212** `            return {`

**L213** `                "query": query.strip(),`

**L214** `                "citations": [],`

**L215** `                "rewrite": None,`

**L216** `                "expansion": None,`

**L217** `                "route": None,`

**L218** `            }`

**L219** `        rag = self.as_rag_service()`

**L220** `        bundle = rag.retrieve_citation_bundle(query, config=cfg)`

**L221** `        return bundle.to_dict()`

**L222** ``

**L223** `    def fetch_citations_retry(self, query: str, *, attempt: int = 1) -> dict[str, Any]:`

**L224** `        """Self-RAG 重试 — 强制 rag_wide 并放大 citation pool"""`

**L225** `        from rag.citation_config import CitationConfig`

**L226** `        from rag.route_config import INTENT_RAG_WIDE`

**L227** ``

**L228** `        cfg = self.get_citation_config()`

**L229** `        if not cfg.enabled:`

**L230** `            return self.fetch_citations(query)`

**L231** ``

**L232** `        boosted = CitationConfig.from_dict(`

**L233** `            {`

**L234** `                **cfg.to_dict(),`

**L235** `                "max_citations": min(100, max(cfg.max_citations, 20) + attempt * 10),`

**L236** `            }`

**L237** `        )`

**L238** `        rag = self.as_rag_service()`

**L239** `        bundle = rag.retrieve_citation_bundle(`

**L240** `            query,`

**L241** `            config=boosted,`

**L242** `            intent_override=INTENT_RAG_WIDE,`

**L243** `        )`

**L244** `        data = bundle.to_dict()`

**L245** `        data["retry_attempt"] = attempt`

**L246** `        return data`

**L247** ``

**L248** `    def ingest_text(`

**L249** `        self,`

**L250** `        content: str,`

**L251** `        *,`

**L252** `        filename: str,`

**L253** `        clean: bool = True,`

**L254** `        chunk_size: int | None = None,`

**L255** `        overlap: int | None = None,`

**L256** `    ) -> KnowledgeDocument:`

**L257** `        """将文本写入知识库并重建索引"""`

**L258** `        cfg = self.get_chunk_config()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L259** `        cs = chunk_size if chunk_size is not None else cfg.chunk_size`

**L260** `        ov = overlap if overlap is not None else cfg.overlap`

**L261** `        text = (content or "").strip()`

**L262** `        if not text:`

**L263** `            raise ValueError("文档内容不能为空")`

**L264** `        if not filename.strip():`

**L265** `            raise ValueError("filename 不能为空")`

**L266** ``

**L267** `        cleaned = text`

**L268** `        if clean:`

**L269** `            cleaned, _ = clean_text(text)`

**L270** `        doc = DocumentRecord(`

**L271** `            path=Path(filename),`

**L272** `            content=text,`

**L273** `            encoding="utf-8",`

**L274** `            size_bytes=len(text.encode("utf-8")),`

**L275** `            cleaned=cleaned,`

**L276** `        )`

**L277** `        new_chunks = chunk_documents(`

**L278** `            [doc],`

**L279** `            chunk_size=cs,`

**L280** `            overlap=ov,`

**L281** `            use_cleaned=clean,`

**L282** `        )`

**L283** `        self._append_chunks(filename, new_chunks, size_bytes=doc.size_bytes)`

**L284** `        self._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L285** `        return self.documents[-1]`

**L286** ``

**L287** `    def ingest_file(`

**L288** `        self,`

**L289** `        path: Path,`

**L290** `        *,`

**L291** `        clean: bool = True,`

**L292** `        chunk_size: int = 200,`

**L293** `        overlap: int = 40,`

**L294** `    ) -> KnowledgeDocument:`

**L295** `        """从磁盘文件 ingestion"""`

**L296** `        content, encoding = read_text_file(path)`

**L297** `        cleaned = content`

**L298** `        if clean:`

**L299** `            cleaned, _ = clean_text(content)`

**L300** `        doc = DocumentRecord(`

**L301** `            path=path,`

**L302** `            content=content,`

**L303** `            encoding=encoding,`

**L304** `            size_bytes=path.stat().st_size,`

**L305** `            cleaned=cleaned,`

**L306** `        )`

**L307** `        new_chunks = chunk_documents(`

**L308** `            [doc],`

**L309** `            chunk_size=chunk_size,`

**L310** `            overlap=overlap,`

**L311** `            use_cleaned=clean,`

**L312** `        )`

**L313** `        self._append_chunks(path.name, new_chunks, size_bytes=doc.size_bytes)`

**L314** `        self._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L315** `        return self.documents[-1]`

**L316** ``

**L317** `    def ingest_bytes(`

**L318** `        self,`

**L319** `        data: bytes,`

**L320** `        *,`

**L321** `        filename: str,`

**L322** `        clean: bool = True,`

**L323** `        chunk_strategy: str = "auto",`

**L324** `        incremental: bool = True,`

**L325** `    ) -> KnowledgeDocument:`

**L326** `        """处理上传二进制 — Day 26 起委托 doc_parser"""`

**L327** `        from tools.doc_parser import parse_bytes`

**L328** ``

**L329** `        parsed = parse_bytes(data, filename)`

**L330** `        cfg = self.get_chunk_config()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L331** `        return self.ingest_parsed(`

**L332** `            parsed,`

**L333** `            clean=clean,`

**L334** `            chunk_strategy=chunk_strategy if chunk_strategy != "auto" else cfg.strategy,`

**L335** `            incremental=incremental,`

**L336** `        )`

**L337** ``

**L338** `    def ingest_parsed(`

**L339** `        self,`

**L340** `        parsed: ParsedDocument,`

**L341** `        *,`

**L342** `        clean: bool = True,`

**L343** `        chunk_strategy: str | None = None,`

**L344** `        chunk_size: int | None = None,`

**L345** `        overlap: int | None = None,`

**L346** `        incremental: bool = False,`

**L347** `    ) -> KnowledgeDocument:`

**L348** `        """将 ParsedDocument 写入知识库"""`

**L349** `        cfg = self.get_chunk_config()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L350** `        strategy = chunk_strategy if chunk_strategy is not None else cfg.strategy`

**L351** `        cs = chunk_size if chunk_size is not None else cfg.chunk_size`

**L352** `        ov = overlap if overlap is not None else cfg.overlap`

**L353** `        text = (parsed.plain_text or "").strip()`

**L354** `        if not text:`

**L355** `            raise ValueError("解析结果为空")`

**L356** ``

**L357** `        if clean:`

**L358** `            text, _ = clean_text(text)`

**L359** `            parsed.plain_text = text`

**L360** `            for section in parsed.sections:`

**L361** `                section.body, _ = clean_text(section.body)`

**L362** ``

**L363** `        new_chunks = chunk_from_parsed(`

**L364** `            parsed,`

**L365** `            strategy=strategy,`

**L366** `            chunk_size=cs,`

**L367** `            overlap=ov,`

**L368** `        )`

**L369** `        size_bytes = len(parsed.plain_text.encode("utf-8"))`

**L370** ``

**L371** `        if incremental:`

**L372** `            removed = self._remove_document_by_source(parsed.filename)`

**L373** `            self._append_chunks(`

**L374** `                parsed.filename,`

**L375** `                new_chunks,`

**L376** `                size_bytes=size_bytes,`

**L377** `                doc_format=parsed.format,`

**L378** `            )`

**L379** `            self._incremental_index(new_chunks, replaced_count=len(removed))`

**L380** `        else:`

**L381** `            self._append_chunks(`

**L382** `                parsed.filename,`

**L383** `                new_chunks,`

**L384** `                size_bytes=size_bytes,`

**L385** `                doc_format=parsed.format,`

**L386** `            )`

**L387** `            self._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L388** `        return self.documents[-1]`

**L389** ``

**L390** `    def save(self, path: Path | None = None) -> Path:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L391** `        """持久化到 JSON"""`

**L392** `        target = path or self.store_path or _default_store_path()`

**L393** `        self.store_path = target`

**L394** `        payload = {`

**L395** `            "version": STORE_VERSION,`

**L396** `            "platform_version": PLATFORM_VERSION,`

**L397** `            "documents": [d.to_dict() for d in self.documents],`

**L398** `            "chunks": [_chunk_to_dict(c) for c in self.chunks],`

**L399** `            "embedding": self.embedding_state,`

**L400** `            "vector_backend": self.vector_backend,`

**L401** `            "chunk_config": self.chunk_config.to_dict(),`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L402** `            "last_rebuilt_at": self.last_rebuilt_at,`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L403** `            "last_incremental_at": self.last_incremental_at,`

**L404** `            "index_mode": self.index_mode,`

**L405** `            "retrieval_config": self.retrieval_config.to_dict(),`

**L406** `            "rerank_config": self.rerank_config.to_dict(),`

**L407** `            "rewrite_config": self.rewrite_config.to_dict(),`

**L408** `            "citation_config": self.citation_config.to_dict(),`

**L409** `            "expansion_config": self.expansion_config.to_dict(),`

**L410** `            "route_config": self.route_config.to_dict(),`

**L411** `            "validation_config": self.validation_config.to_dict(),`

**L412** `        }`

**L413** `        save_json(target, payload)`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L414** `        return target`

**L415** ``

**L416** `    @classmethod`

**L417** `    def load(cls, path: Path) -> KnowledgeStore:`

**L418** `        """从 JSON 加载知识库"""`

**L419** `        raw = load_json(path, default=None)`

**L420** `        if not raw:`

**L421** `            return cls.bootstrap_from_sample_docs(store_path=path)`

**L422** ``

**L423** `        store = cls(store_path=path)`

**L424** `        store.documents = [`

**L425** `            KnowledgeDocument.from_dict(d) for d in raw.get("documents", [])`

**L426** `        ]`

**L427** `        store.chunks = [_chunk_from_dict(c) for c in raw.get("chunks", [])]`

**L428** `        store.embedding_state = dict(raw.get("embedding") or {})`

**L429** `        store.vector_backend = str(raw.get("vector_backend") or VECTOR_BACKEND)`

**L430** `        if raw.get("chunk_config"):`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L431** `            store.chunk_config = ChunkConfig.from_dict(raw["chunk_config"])`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L432** `        store.last_rebuilt_at = raw.get("last_rebuilt_at")`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L433** `        store.last_incremental_at = raw.get("last_incremental_at")`

**L434** `        store.index_mode = str(raw.get("index_mode") or INDEX_MODE_FULL)`

**L435** `        if raw.get("retrieval_config"):`

**L436** `            store.retrieval_config = RetrievalConfig.from_dict(raw["retrieval_config"])`

**L437** `        if raw.get("rerank_config"):`

**L438** `            store.rerank_config = RerankConfig.from_dict(raw["rerank_config"])`

**L439** `        if raw.get("rewrite_config"):`

**L440** `            store.rewrite_config = RewriteConfig.from_dict(raw["rewrite_config"])`

**L441** `        if raw.get("citation_config"):`

**L442** `            store.citation_config = CitationConfig.from_dict(raw["citation_config"])`

**L443** `        if raw.get("expansion_config"):`

**L444** `            store.expansion_config = ExpansionConfig.from_dict(raw["expansion_config"])`

**L445** `        if raw.get("route_config"):`

**L446** `            store.route_config = RouteConfig.from_dict(raw["route_config"])`

**L447** `        if raw.get("validation_config"):`

**L448** `            store.validation_config = ValidationConfig.from_dict(raw["validation_config"])`

**L449** `        store._sync_chroma_from_json()`

**L450** `        store._rag_service = store._build_rag_service()`

**L451** `        return store`

**L452** ``

**L453** `    @classmethod`

**L454** `    def load_or_bootstrap(cls, path: Path | None = None) -> KnowledgeStore:`

**L455** `        """加载已有库，不存在则从 sample_docs 引导"""`

**L456** `        target = path or _default_store_path()`

**L457** `        if target.is_file():`

**L458** `            return cls.load(target)`

**L459** `        store = cls.bootstrap_from_sample_docs(store_path=target)`

**L460** `        store.save(target)`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L461** `        return store`

**L462** ``

**L463** `    @classmethod`

**L464** `    def bootstrap_from_sample_docs(cls, *, store_path: Path | None = None) -> KnowledgeStore:`

**L465** `        """用内置 sample_docs 初始化知识库"""`

**L466** `        rag = RAGContextService.from_sample_docs(use_embedding=True)`

**L467** `        store = cls(store_path=store_path)`

**L468** `        now = _utc_now()`

**L469** ``

**L470** `        by_source: dict[str, list[TextChunk]] = {}`

**L471** `        for chunk in rag.index.chunks:`

**L472** `            by_source.setdefault(chunk.source, []).append(chunk)`

**L473** ``

**L474** `        for source, chunks in sorted(by_source.items()):`

**L475** `            store.documents.append(`

**L476** `                KnowledgeDocument(`

**L477** `                    name=source,`

**L478** `                    ingested_at=now,`

**L479** `                    size_bytes=sum(len(c.text) for c in chunks),`

**L480** `                    chunk_count=len(chunks),`

**L481** `                )`

**L482** `            )`

**L483** `        store.chunks = list(rag.index.chunks)`

**L484** `        retriever = rag.index.retriever`

**L485** `        from rag.embedding_retriever import EmbeddingRetriever`

**L486** ``

**L487** `        if isinstance(retriever, EmbeddingRetriever):`

**L488** `            store.embedding_state = retriever._client.model.export_state()`

**L489** `        store._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L490** `        return store`

**L491** ``

**L492** `    def status_dict(self) -> dict[str, Any]:`

**L493** `        from tools.doc_parser import supported_formats`

**L494** ``

**L495** `        return {`

**L496** `            "document_count": self.document_count,`

**L497** `            "chunk_count": self.chunk_count,`

**L498** `            "documents": [d.to_dict() for d in self.documents],`

**L499** `            "store_path": str(self.store_path) if self.store_path else None,`

**L500** `            "platform_version": PLATFORM_VERSION,`

**L501** `            "supported_formats": supported_formats(),`

**L502** `            "chunk_config": self.chunk_config.to_dict(),`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L503** `            "last_rebuilt_at": self.last_rebuilt_at,`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L504** `            "last_incremental_at": self.last_incremental_at,`

**L505** `            "index_mode": self.index_mode,`

**L506** `            "retrieval_config": self.retrieval_config.to_dict(),`

**L507** `            "rerank_config": self.rerank_config.to_dict(),`

**L508** `            "rewrite_config": self.rewrite_config.to_dict(),`

**L509** `            "citation_config": self.citation_config.to_dict(),`

**L510** `            "expansion_config": self.expansion_config.to_dict(),`

**L511** `            "route_config": self.route_config.to_dict(),`

**L512** `            "validation_config": self.validation_config.to_dict(),`

**L513** `            "vector_backend": self.vector_backend,`

**L514** `            "chroma_path": str(self._resolve_chroma_path()),`

**L515** `            "chroma_count": self._chroma_index().count() if self.chunks else 0,`

**L516** `        }`

**L517** ``

**L518** `    def _append_chunks(`

**L519** `        self,`

**L520** `        filename: str,`

**L521** `        new_chunks: list[TextChunk],`

**L522** `        *,`

**L523** `        size_bytes: int,`

**L524** `        doc_format: str = "txt",`

**L525** `    ) -> None:`

**L526** `        base_index = len(self.chunks)`

**L527** `        reindexed: list[TextChunk] = []`

**L528** `        for i, chunk in enumerate(new_chunks):`

**L529** `            reindexed.append(`

**L530** `                TextChunk(`

**L531** `                    chunk_id=chunk.chunk_id,`

**L532** `                    text=chunk.text,`

**L533** `                    source=filename,`

**L534** `                    index=base_index + i,`

**L535** `                    start_char=chunk.start_char,`

**L536** `                    end_char=chunk.end_char,`

**L537** `                )`

**L538** `            )`

**L539** `        self.chunks.extend(reindexed)`

**L540** `        self.documents.append(`

**L541** `            KnowledgeDocument(`

**L542** `                name=filename,`

**L543** `                ingested_at=_utc_now(),`

**L544** `                size_bytes=size_bytes,`

**L545** `                chunk_count=len(reindexed),`

**L546** `                format=doc_format,`

**L547** `            )`

**L548** `        )`

**L549** ``

**L550** `    def _remove_document_by_source(self, filename: str) -> list[str]:`

**L551** `        """移除同名文档及其 chunks，并从 Chroma 删除旧向量"""`

**L552** `        removed_ids = [c.chunk_id for c in self.chunks if c.source == filename]`

**L553** `        if not removed_ids:`

**L554** `            return []`

**L555** ``

**L556** `        self._chroma_index().delete_by_ids(removed_ids)`

**L557** `        self.documents = [d for d in self.documents if d.name != filename]`

**L558** `        self.chunks = [c for c in self.chunks if c.source != filename]`

**L559** `        self.chunks = [`

**L560** `            TextChunk(`

**L561** `                chunk_id=c.chunk_id,`

**L562** `                text=c.text,`

**L563** `                source=c.source,`

**L564** `                index=i,`

**L565** `                start_char=c.start_char,`

**L566** `                end_char=c.end_char,`

**L567** `            )`

**L568** `            for i, c in enumerate(self.chunks)`

**L569** `        ]`

**L570** `        return removed_ids`

**L571** ``

**L572** `    def _resolve_chroma_path(self) -> Path:`

**L573** `        if self.chroma_path is not None:`

**L574** `            return self.chroma_path`

**L575** `        if self.store_path is not None:`

**L576** `            return self.store_path.parent / "chroma"`

**L577** `        return get_path("knowledge_chroma")`

**L578** ``

**L579** `    def _chroma_index(self) -> ChromaVectorIndex:`

**L580** `        return ChromaVectorIndex(self._resolve_chroma_path())`

**L581** ``

**L582** `    def _sync_chroma_from_json(self) -> None:`

**L583** `        """从 JSON 元数据恢复 Chroma（迁移或冷启动）"""`

**L584** `        if not self.chunks or not self.embedding_state:`

**L585** `            return`

**L586** `        chroma = self._chroma_index()`

**L587** `        if chroma.count() > 0:`

**L588** `            return`

**L589** `        from rag.embedding import EmbeddingClient`

**L590** ``

**L591** `        client = EmbeddingClient()`

**L592** `        client.model.load_state(self.embedding_state)`

**L593** `        vectors = client.embed_batch([c.text for c in self.chunks])`

**L594** `        chroma.upsert_chunks(self.chunks, vectors)`

**L595** ``

**L596** `    def _rebuild_index(self) -> None:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L597** `        from rag.embedding_retriever import EmbeddingRetriever`

**L598** ``

**L599** `        if not self.chunks:`

**L600** `            self.embedding_state = {}`

**L601** `            self._chroma_index().reset()`

**L602** `            self.invalidate_cache()`

**L603** `            return`

**L604** ``

**L605** `        retriever = EmbeddingRetriever(self.chunks)`

**L606** `        self.embedding_state = retriever._client.model.export_state()`

**L607** `        vectors = retriever._client.embed_batch([c.text for c in self.chunks])`

**L608** `        chroma = self._chroma_index()`

**L609** `        chroma.reset()`

**L610** `        chroma.upsert_chunks(self.chunks, vectors)`

**L611** `        self.index_mode = INDEX_MODE_FULL`

**L612** `        self.invalidate_cache()`

**L613** ``

**L614** `    def _incremental_index(`

**L615** `        self,`

**L616** `        affected_chunks: list[TextChunk],`

**L617** `        *,`

**L618** `        replaced_count: int = 0,`

**L619** `    ) -> bool:`

**L620** `        """`

**L621** `        增量更新 Chroma：不 reset collection，仅 upsert 受影响 chunk。`

**L622** ``

**L623** `        若 TF-IDF 词表扩张，则回退为全量 upsert（仍不 reset）。`

**L624** `        返回是否发生词表扩张。`

**L625** `        """`

**L626** `        from rag.embedding_retriever import EmbeddingRetriever`

**L627** ``

**L628** `        if not self.chunks:`

**L629** `            self.embedding_state = {}`

**L630** `            self._chroma_index().reset()`

**L631** `            self.last_incremental_at = _utc_now()`

**L632** `            self.index_mode = INDEX_MODE_INCREMENTAL`

**L633** `            self.invalidate_cache()`

**L634** `            return False`

**L635** ``

**L636** `        old_vocab = dict(self.embedding_state.get("vocab") or {})`

**L637** `        retriever = EmbeddingRetriever(self.chunks)`

**L638** `        self.embedding_state = retriever._client.model.export_state()`

**L639** `        new_vocab = dict(self.embedding_state.get("vocab") or {})`

**L640** `        vocab_expanded = len(new_vocab) > len(old_vocab) and bool(old_vocab)`

**L641** ``

**L642** `        if vocab_expanded:`

**L643** `            affected_chunks = list(self.chunks)`

**L644** ``

**L645** `        client = retriever._client`

**L646** `        vectors = client.embed_batch([c.text for c in affected_chunks])`

**L647** `        chroma = self._chroma_index()`

**L648** `        if vocab_expanded:`

**L649** `            # TF-IDF 维度变化时 Chroma collection 须重建`

**L650** `            chroma.reset()`

**L651** `        chroma.upsert_chunks(affected_chunks, vectors)`

**L652** `        self.last_incremental_at = _utc_now()`

**L653** `        self.index_mode = INDEX_MODE_INCREMENTAL`

**L654** `        self.invalidate_cache()`

**L655** `        return vocab_expanded`

**L656** ``

**L657** `    def _build_rag_service(self) -> RAGContextService:`

**L658** `        if not self.chunks:`

**L659** `            return RAGContextService()`

**L660** ``

**L661** `        from rag.embedding import EmbeddingClient`

**L662** `        from rag.retriever import KeywordRetriever`

**L663** ``

**L664** `        client = EmbeddingClient()`

**L665** `        client.model.load_state(self.embedding_state)`

**L666** `        self._sync_chroma_from_json()`

**L667** `        chroma = self._chroma_index()`

**L668** `        vector = ChromaEmbeddingRetriever(self.chunks, chroma, client=client)`

**L669** `        keyword = KeywordRetriever(self.chunks)`

**L670** `        cfg = self.get_retrieval_config()`

**L671** `        hybrid = HybridRetriever(self.chunks, keyword, vector, config=cfg)`

**L672** `        rerank_cfg = self.get_rerank_config()`

**L673** `        reranking = RerankingRetriever(`

**L674** `            hybrid,`

**L675** `            reranker=MockCrossEncoderReranker(),`

**L676** `            config=rerank_cfg,`

**L677** `        )`

**L678** `        rewrite_cfg = self.get_rewrite_config()`

**L679** `        rewriting = RewritingRetriever(reranking, config=rewrite_cfg)`

**L680** `        expansion_cfg = self.get_expansion_config()`

**L681** `        expanding = ExpandingRetriever(rewriting, config=expansion_cfg)`

**L682** `        route_cfg = self.get_route_config()`

**L683** `        retriever = RoutingRetriever(expanding, config=route_cfg)`

**L684** `        index = DocumentIndex(chunks=self.chunks, retriever=retriever)`

**L685** `        return RAGContextService(index)`

**L686** ``

**L687** ``

**L688** `_store: KnowledgeStore | None = None`

**L689** `_store_lock = threading.Lock()`

**L690** ``

**L691** ``

**L692** `def get_knowledge_store(*, reload: bool = False) -> KnowledgeStore:`

**L693** `    """全局知识库单例（API 与 factory 共享）"""`

**L694** `    global _store`

**L695** `    with _store_lock:`

**L696** `        if _store is None or reload:`

**L697** `            _store = KnowledgeStore.load_or_bootstrap()`

**L698** `        return _store`

**L699** ``

**L700** ``

**L701** `def set_knowledge_store(store: KnowledgeStore) -> None:`

**L702** `    """测试注入用"""`

**L703** `    global _store`

**L704** `    with _store_lock:`

**L705** `        _store = store`

**L706** ``

**L707** ``

**L708** `def _default_store_path() -> Path:`

**L709** `    return get_path("knowledge_store")`

**L710** ``

**L711** ``

**L712** `def _utc_now() -> str:`

**L713** `    return datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")`

**L714** ``

**L715** ``

**L716** `def _chunk_to_dict(chunk: TextChunk) -> dict[str, Any]:`

**L717** `    return {`

**L718** `        "chunk_id": chunk.chunk_id,`

**L719** `        "text": chunk.text,`

**L720** `        "source": chunk.source,`

**L721** `        "index": chunk.index,`

**L722** `        "start_char": chunk.start_char,`

**L723** `        "end_char": chunk.end_char,`

**L724** `    }`

**L725** ``

**L726** ``

**L727** `def _chunk_from_dict(data: dict[str, Any]) -> TextChunk:`

**L728** `    return TextChunk(`

**L729** `        chunk_id=str(data.get("chunk_id", "")),`

**L730** `        text=str(data.get("text", "")),`

**L731** `        source=str(data.get("source", "")),`

**L732** `        index=int(data.get("index", 0)),`

**L733** `        start_char=int(data.get("start_char", 0)),`

**L734** `        end_char=int(data.get("end_char", 0)),`

**L735** `    )`


## 附录：api/knowledge.py 重建端点走读

### `nexus-agent-platform/src/api/knowledge.py` 逐行走读

共 **450** 行。

**L1** `"""`

**L2** `知识库 REST API — 文档上传、分块调参与检索评估`

**L3** ``

**L4** `需求：ZL-NA-REQ-025 / ZL-NA-REQ-026 / ZL-NA-REQ-027 / ZL-NA-REQ-028 / ZL-NA-REQ-029 / ZL-NA-REQ-030 / ZL-NA-REQ-031 / ZL-NA-REQ-032 / ZL-NA-REQ-033 / ZL-NA-REQ-034 / ZL-NA-REQ-035 / ZL-NA-REQ-036 / ZL-NA-REQ-037 / ZL-NA-REQ-038`

**L5** `"""`

**L6** ``

**L7** `from __future__ import annotations`

**L8** ``

**L9** `from pathlib import Path`

**L10** ``

**L11** `from fastapi import APIRouter, File, HTTPException, UploadFile`

**L12** ``

**L13** `from api.schemas import (`

**L14** `    ChunkConfigRequest,`

**L15** `    ChunkConfigResponse,`

**L16** `    CitationConfigRequest,`

**L17** `    CitationConfigResponse,`

**L18** `    CitationPreviewRequest,`

**L19** `    CitationPreviewResponse,`

**L20** `    ExpansionConfigRequest,`

**L21** `    ExpansionConfigResponse,`

**L22** `    ExpansionPreviewRequest,`

**L23** `    ExpansionPreviewResponse,`

**L24** `    EvaluateRequest,`

**L25** `    EvaluateResponse,`

**L26** `    KnowledgeStatusResponse,`

**L27** `    KnowledgeUploadResponse,`

**L28** `    RebuildRequest,`

**L29** `    RebuildResponse,`

**L30** `    RerankConfigRequest,`

**L31** `    RerankConfigResponse,`

**L32** `    RewriteConfigRequest,`

**L33** `    RewriteConfigResponse,`

**L34** `    RewritePreviewRequest,`

**L35** `    RewritePreviewResponse,`

**L36** `    RouteConfigRequest,`

**L37** `    RouteConfigResponse,`

**L38** `    RoutePreviewRequest,`

**L39** `    RoutePreviewResponse,`

**L40** `    ValidationConfigRequest,`

**L41** `    ValidationConfigResponse,`

**L42** `    ValidationPreviewRequest,`

**L43** `    ValidationPreviewResponse,`

**L44** `    ValidationRetryPreviewRequest,`

**L45** `    ValidationRetryPreviewResponse,`

**L46** `    RetrievalConfigRequest,`

**L47** `    RetrievalConfigResponse,`

**L48** `)`

**L49** `from api.sessions import session_manager`
  → rebuild API 相关行。

**L50** `from core.exceptions import NexusError, StorageError`

**L51** `from rag.chunk_config import PRESET_CONFIGS, ChunkConfig`

**L52** `from rag.ingestion import ingest_upload`

**L53** `from rag.knowledge_rebuild import rebuild_store, rebuild_with_best_config`
  → rebuild API 相关行。

**L54** `from rag.knowledge_store import get_knowledge_store`

**L55** `from rag.citation_config import CitationConfig`

**L56** `from rag.expansion_config import ExpansionConfig`

**L57** `from rag.query_expander import build_expander`

**L58** `from rag.query_rewriter import RuleBasedQueryRewriter`

**L59** `from rag.query_router import RuleBasedQueryRouter`

**L60** `from rag.route_config import RouteConfig`

**L61** `from rag.validation_config import ValidationConfig`

**L62** `from rag.rerank_config import RerankConfig`

**L63** `from rag.rewrite_config import RewriteConfig`

**L64** `from rag.retrieval_config import RetrievalConfig`

**L65** `from rag.retrieval_eval import EvalQuery, pick_best_config, run_ab_experiment`

**L66** `from tools.doc_parser import parse_bytes`

**L67** ``

**L68** `router = APIRouter(prefix="/api/knowledge", tags=["knowledge"])`

**L69** ``

**L70** `MAX_UPLOAD_BYTES = 512_000  # 500 KB 教学上限`

**L71** `_EVAL_SAMPLE = (`

**L72** `    Path(__file__).resolve().parent.parent / "day26" / "sample_docs" / "product_notice.md"`

**L73** `)`

**L74** ``

**L75** ``

**L76** `@router.get("/retrieval-config", response_model=RetrievalConfigResponse)`

**L77** `def get_retrieval_config() -> RetrievalConfigResponse:`

**L78** `    """返回当前检索模式（vector / keyword / hybrid）与融合参数"""`

**L79** `    cfg = get_knowledge_store().get_retrieval_config()`

**L80** `    return RetrievalConfigResponse(**cfg.to_dict())`

**L81** ``

**L82** ``

**L83** `@router.put("/retrieval-config", response_model=RetrievalConfigResponse)`

**L84** `def update_retrieval_config(body: RetrievalConfigRequest) -> RetrievalConfigResponse:`

**L85** `    """更新检索策略；变更后清除 RAG 缓存"""`

**L86** `    store = get_knowledge_store()`

**L87** `    try:`

**L88** `        cfg = RetrievalConfig.from_dict(body.model_dump())`

**L89** `        cfg.validate()`

**L90** `        store.set_retrieval_config(cfg)`

**L91** `        store.save()`

**L92** `    except ValueError as exc:`

**L93** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L94** `    return RetrievalConfigResponse(**cfg.to_dict())`

**L95** ``

**L96** ``

**L97** `@router.get("/rerank-config", response_model=RerankConfigResponse)`

**L98** `def get_rerank_config() -> RerankConfigResponse:`

**L99** `    """返回 rerank 开关、候选池大小与模型标识"""`

**L100** `    cfg = get_knowledge_store().get_rerank_config()`

**L101** `    return RerankConfigResponse(**cfg.to_dict())`

**L102** ``

**L103** ``

**L104** `@router.put("/rerank-config", response_model=RerankConfigResponse)`

**L105** `def update_rerank_config(body: RerankConfigRequest) -> RerankConfigResponse:`

**L106** `    """更新 rerank 策略；变更后清除 RAG 缓存"""`

**L107** `    store = get_knowledge_store()`

**L108** `    try:`

**L109** `        cfg = RerankConfig.from_dict(body.model_dump())`

**L110** `        cfg.validate()`

**L111** `        store.set_rerank_config(cfg)`

**L112** `        store.save()`

**L113** `    except ValueError as exc:`

**L114** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L115** `    return RerankConfigResponse(**cfg.to_dict())`

**L116** ``

**L117** ``

**L118** `@router.get("/rewrite-config", response_model=RewriteConfigResponse)`

**L119** `def get_rewrite_config() -> RewriteConfigResponse:`

**L120** `    """返回查询改写开关与规则模式"""`

**L121** `    cfg = get_knowledge_store().get_rewrite_config()`

**L122** `    return RewriteConfigResponse(**cfg.to_dict())`

**L123** ``

**L124** ``

**L125** `@router.put("/rewrite-config", response_model=RewriteConfigResponse)`

**L126** `def update_rewrite_config(body: RewriteConfigRequest) -> RewriteConfigResponse:`

**L127** `    """更新查询改写策略；变更后清除 RAG 缓存"""`

**L128** `    store = get_knowledge_store()`

**L129** `    try:`

**L130** `        cfg = RewriteConfig.from_dict(body.model_dump())`

**L131** `        cfg.validate()`

**L132** `        store.set_rewrite_config(cfg)`

**L133** `        store.save()`

**L134** `    except ValueError as exc:`

**L135** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L136** `    return RewriteConfigResponse(**cfg.to_dict())`

**L137** ``

**L138** ``

**L139** `@router.post("/rewrite-preview", response_model=RewritePreviewResponse)`

**L140** `def rewrite_preview(body: RewritePreviewRequest) -> RewritePreviewResponse:`

**L141** `    """预览单条 query 的规则改写结果（不触发检索）"""`

**L142** `    store = get_knowledge_store()`

**L143** `    cfg = store.get_rewrite_config()`

**L144** `    rewriter = RuleBasedQueryRewriter(config=cfg)`

**L145** `    result = rewriter.rewrite(body.query)`

**L146** `    return RewritePreviewResponse(**result.to_dict())`

**L147** ``

**L148** ``

**L149** `@router.get("/citation-config", response_model=CitationConfigResponse)`

**L150** `def get_citation_config() -> CitationConfigResponse:`

**L151** `    """返回引用溯源开关与展示参数"""`

**L152** `    cfg = get_knowledge_store().get_citation_config()`

**L153** `    return CitationConfigResponse(**cfg.to_dict())`

**L154** ``

**L155** ``

**L156** `@router.put("/citation-config", response_model=CitationConfigResponse)`

**L157** `def update_citation_config(body: CitationConfigRequest) -> CitationConfigResponse:`

**L158** `    """更新引用溯源策略并持久化"""`

**L159** `    store = get_knowledge_store()`

**L160** `    try:`

**L161** `        cfg = CitationConfig.from_dict(body.model_dump())`

**L162** `        cfg.validate()`

**L163** `        store.set_citation_config(cfg)`

**L164** `        store.save()`

**L165** `    except ValueError as exc:`

**L166** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L167** `    return CitationConfigResponse(**cfg.to_dict())`

**L168** ``

**L169** ``

**L170** `@router.post("/citation-preview", response_model=CitationPreviewResponse)`

**L171** `def citation_preview(body: CitationPreviewRequest) -> CitationPreviewResponse:`

**L172** `    """预览单条 query 的检索引用（含 rewrite / expansion 审计）"""`

**L173** `    store = get_knowledge_store()`

**L174** `    data = store.fetch_citations(body.query)`

**L175** `    return CitationPreviewResponse(**data)`

**L176** ``

**L177** ``

**L178** `@router.get("/expansion-config", response_model=ExpansionConfigResponse)`

**L179** `def get_expansion_config() -> ExpansionConfigResponse:`

**L180** `    """返回多 query 扩展开关与参数"""`

**L181** `    cfg = get_knowledge_store().get_expansion_config()`

**L182** `    return ExpansionConfigResponse(**cfg.to_dict())`

**L183** ``

**L184** ``

**L185** `@router.put("/expansion-config", response_model=ExpansionConfigResponse)`

**L186** `def update_expansion_config(body: ExpansionConfigRequest) -> ExpansionConfigResponse:`

**L187** `    """更新多 query 扩展策略并持久化"""`

**L188** `    store = get_knowledge_store()`

**L189** `    try:`

**L190** `        cfg = ExpansionConfig.from_dict(body.model_dump())`

**L191** `        cfg.validate()`

**L192** `        store.set_expansion_config(cfg)`

**L193** `        store.save()`

**L194** `    except ValueError as exc:`

**L195** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L196** `    return ExpansionConfigResponse(**cfg.to_dict())`

**L197** ``

**L198** ``

**L199** `@router.post("/expansion-preview", response_model=ExpansionPreviewResponse)`

**L200** `def expansion_preview(body: ExpansionPreviewRequest) -> ExpansionPreviewResponse:`

**L201** `    """预览单条 query 的多路扩展结果"""`

**L202** `    store = get_knowledge_store()`

**L203** `    cfg = store.get_expansion_config()`

**L204** `    expander = build_expander(cfg)`

**L205** `    result = expander.expand(body.query)`

**L206** `    return ExpansionPreviewResponse(**result.to_dict())`

**L207** ``

**L208** ``

**L209** `@router.get("/route-config", response_model=RouteConfigResponse)`

**L210** `def get_route_config() -> RouteConfigResponse:`

**L211** `    """返回检索管线路由开关与默认意图"""`

**L212** `    cfg = get_knowledge_store().get_route_config()`

**L213** `    return RouteConfigResponse(**cfg.to_dict())`

**L214** ``

**L215** ``

**L216** `@router.put("/route-config", response_model=RouteConfigResponse)`

**L217** `def update_route_config(body: RouteConfigRequest) -> RouteConfigResponse:`

**L218** `    """更新检索管线路由策略并持久化"""`

**L219** `    store = get_knowledge_store()`

**L220** `    try:`

**L221** `        cfg = RouteConfig.from_dict(body.model_dump())`

**L222** `        cfg.validate()`

**L223** `        store.set_route_config(cfg)`

**L224** `        store.save()`

**L225** `    except ValueError as exc:`

**L226** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L227** `    return RouteConfigResponse(**cfg.to_dict())`

**L228** ``

**L229** ``

**L230** `@router.post("/route-preview", response_model=RoutePreviewResponse)`

**L231** `def route_preview(body: RoutePreviewRequest) -> RoutePreviewResponse:`

**L232** `    """预览单条 query 的路由决策（expand/rewrite 开关）"""`

**L233** `    store = get_knowledge_store()`

**L234** `    cfg = store.get_route_config()`

**L235** `    router = RuleBasedQueryRouter(config=cfg)`

**L236** `    result = router.route(body.query)`

**L237** `    return RoutePreviewResponse(**result.to_dict())`

**L238** ``

**L239** ``

**L240** `@router.get("/validation-config", response_model=ValidationConfigResponse)`

**L241** `def get_validation_config() -> ValidationConfigResponse:`

**L242** `    """返回答案校验开关与阈值"""`

**L243** `    cfg = get_knowledge_store().get_validation_config()`

**L244** `    return ValidationConfigResponse(**cfg.to_dict())`

**L245** ``

**L246** ``

**L247** `@router.put("/validation-config", response_model=ValidationConfigResponse)`

**L248** `def update_validation_config(body: ValidationConfigRequest) -> ValidationConfigResponse:`

**L249** `    """更新答案校验策略并持久化"""`

**L250** `    store = get_knowledge_store()`

**L251** `    try:`

**L252** `        cfg = ValidationConfig.from_dict(body.model_dump())`

**L253** `        cfg.validate()`

**L254** `        store.set_validation_config(cfg)`

**L255** `        store.save()`

**L256** `    except ValueError as exc:`

**L257** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L258** `    return ValidationConfigResponse(**cfg.to_dict())`

**L259** ``

**L260** ``

**L261** `@router.post("/validation-preview", response_model=ValidationPreviewResponse)`

**L262** `def validation_preview(body: ValidationPreviewRequest) -> ValidationPreviewResponse:`

**L263** `    """预览单条 query + reply 的引用一致性校验"""`

**L264** `    from rag.answer_validator import RuleBasedAnswerValidator`

**L265** ``

**L266** `    store = get_knowledge_store()`

**L267** `    citations = list(body.citations)`

**L268** `    if not citations:`

**L269** `        cite_data = store.fetch_citations(body.query)`

**L270** `        citations = cite_data.get("citations") or []`

**L271** `    cfg = store.get_validation_config()`

**L272** `    preview_cfg = ValidationConfig.from_dict({**cfg.to_dict(), "enabled": True})`

**L273** `    result = RuleBasedAnswerValidator(config=preview_cfg).validate(`

**L274** `        body.query, body.reply, citations`

**L275** `    )`

**L276** `    return ValidationPreviewResponse(**result.to_dict())`

**L277** ``

**L278** ``

**L279** `@router.post("/validation-retry-preview", response_model=ValidationRetryPreviewResponse)`

**L280** `def validation_retry_preview(`

**L281** `    body: ValidationRetryPreviewRequest,`

**L282** `) -> ValidationRetryPreviewResponse:`

**L283** `    """模拟校验失败后的 rag_wide 重检索与再校验"""`

**L284** `    from rag.validation_retry import apply_validation_retry`

**L285** ``

**L286** `    store = get_knowledge_store()`

**L287** `    cite_data = store.fetch_citations(body.query)`

**L288** `    citations = list(body.citations) or cite_data.get("citations") or []`

**L289** `    if body.citations:`

**L290** `        cite_data = {**cite_data, "citations": citations}`

**L291** ``

**L292** `    saved = store.get_validation_config()`

**L293** `    retry_cfg = ValidationConfig.from_dict(`

**L294** `        {**saved.to_dict(), "enabled": True, "retry_on_fail": True, "max_retries": 1}`

**L295** `    )`

**L296** `    store.set_validation_config(retry_cfg)`

**L297** `    try:`

**L298** `        outcome = apply_validation_retry(`

**L299** `            store,`

**L300** `            body.query,`

**L301** `            body.reply,`

**L302** `            citations,`

**L303** `            cite_data,`

**L304** `        )`

**L305** `    finally:`

**L306** `        store.set_validation_config(saved)`

**L307** ``

**L308** `    if outcome is None:`

**L309** `        raise HTTPException(status_code=400, detail="校验已关闭")`

**L310** `    payload = outcome.validation.to_dict()`

**L311** `    payload["retry_route"] = outcome.cite_data.get("route")`

**L312** `    return ValidationRetryPreviewResponse(**payload)`

**L313** ``

**L314** ``

**L315** `@router.get("/chunk-config", response_model=ChunkConfigResponse)`

**L316** `def get_chunk_config() -> ChunkConfigResponse:`

**L317** `    """返回当前知识库默认分块参数"""`

**L318** `    cfg = get_knowledge_store().get_chunk_config()`

**L319** `    return ChunkConfigResponse(**cfg.to_dict())`

**L320** ``

**L321** ``

**L322** `@router.put("/chunk-config", response_model=ChunkConfigResponse)`

**L323** `def update_chunk_config(body: ChunkConfigRequest) -> ChunkConfigResponse:`

**L324** `    """更新默认分块参数（影响后续上传）"""`

**L325** `    store = get_knowledge_store()`

**L326** `    try:`

**L327** `        cfg = ChunkConfig.from_dict(body.model_dump())`

**L328** `        cfg.validate()`

**L329** `        store.set_chunk_config(cfg)`

**L330** `        store.save()`

**L331** `    except ValueError as exc:`

**L332** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L333** `    return ChunkConfigResponse(**cfg.to_dict())`

**L334** ``

**L335** ``

**L336** `@router.post("/evaluate", response_model=EvaluateResponse)`

**L337** `def evaluate_chunk_configs(body: EvaluateRequest | None = None) -> EvaluateResponse:`

**L338** `    """`

**L339** `    对内置样例文档运行 A/B 分块评估，返回 hit@1 与推荐配置。`

**L340** ``

**L341** `    默认使用 PRESET_CONFIGS 四套预设与 day27 评估问句集。`

**L342** `    """`

**L343** `    body = body or EvaluateRequest()`

**L344** `    if not _EVAL_SAMPLE.is_file():`

**L345** `        raise HTTPException(status_code=500, detail="评估样例文档缺失")`

**L346** ``

**L347** `    doc = parse_bytes(_EVAL_SAMPLE.read_bytes(), _EVAL_SAMPLE.name)`

**L348** `    from day27.constants import EVAL_QUERIES`

**L349** ``

**L350** `    queries = [EvalQuery.from_dict(q) for q in EVAL_QUERIES]`

**L351** ``

**L352** `    if body.use_presets and not body.configs:`

**L353** `        configs = list(PRESET_CONFIGS)`

**L354** `    else:`

**L355** `        configs = [ChunkConfig.from_dict(c.model_dump()) for c in body.configs]`

**L356** `        for c in configs:`

**L357** `            c.validate()`

**L358** ``

**L359** `    results = run_ab_experiment(doc, configs, queries)`

**L360** `    best = pick_best_config(results)`

**L361** `    if not best:`

**L362** `        raise HTTPException(status_code=500, detail="评估未产生结果")`

**L363** ``

**L364** `    return EvaluateResponse(`

**L365** `        best_config=best.to_dict()["config"],`

**L366** `        results=[r.to_dict() for r in results],`

**L367** `        eval_query_count=len(queries),`

**L368** `    )`

**L369** ``

**L370** ``

**L371** `@router.post("/rebuild", response_model=RebuildResponse)`
  → rebuild API 相关行。

**L372** `def rebuild_knowledge_base(body: RebuildRequest | None = None) -> RebuildResponse:`
  → rebuild API 相关行。

**L373** `    """`

**L374** `    按当前 chunk_config（或 evaluate 最优配置）全量重建知识库。`

**L375** ``

**L376** `    重扫 sample_docs + knowledge_uploads，清空后重新分块与索引。`

**L377** `    """`

**L378** `    body = body or RebuildRequest()`

**L379** `    store = get_knowledge_store()`

**L380** ``

**L381** `    if body.apply_best_config:`
  → rebuild API 相关行。

**L382** `        if not _EVAL_SAMPLE.is_file():`

**L383** `            raise HTTPException(status_code=500, detail="评估样例文档缺失")`

**L384** `        report = rebuild_with_best_config(`
  → rebuild API 相关行。

**L385** `            store,`

**L386** `            eval_sample_path=_EVAL_SAMPLE,`

**L387** `            include_sample_docs=body.include_sample_docs,`

**L388** `        )`

**L389** `    else:`

**L390** `        report = rebuild_store(store, include_sample_docs=body.include_sample_docs)`
  → rebuild API 相关行。

**L391** ``

**L392** `    cleared = session_manager.clear_all()`
  → rebuild API 相关行。

**L393** `    data = report.to_dict()`

**L394** `    data["sessions_cleared"] = cleared`

**L395** `    return RebuildResponse(**data)`

**L396** ``

**L397** ``

**L398** `@router.get("/status", response_model=KnowledgeStatusResponse)`

**L399** `def knowledge_status() -> KnowledgeStatusResponse:`

**L400** `    """返回知识库文档数、分块数、支持格式与文档列表"""`

**L401** `    store = get_knowledge_store()`

**L402** `    data = store.status_dict()`

**L403** `    return KnowledgeStatusResponse(**data)`

**L404** ``

**L405** ``

**L406** `@router.post("/upload", response_model=KnowledgeUploadResponse)`

**L407** `async def upload_document(`

**L408** `    file: UploadFile = File(..., description="企业文档 (.txt / .md / .pdf)"),`

**L409** `) -> KnowledgeUploadResponse:`

**L410** `    """`

**L411** `    上传企业文档到知识库：解析 → 落盘 → 分块 → Chroma 向量索引 → 持久化。`

**L412** ``

**L413** `    Day 26 起支持 Markdown 与 PDF。上传成功后清除服务端会话。`

**L414** `    """`

**L415** `    if not file.filename:`

**L416** `        raise HTTPException(status_code=422, detail="缺少文件名")`

**L417** ``

**L418** `    data = await file.read()`

**L419** `    if not data:`

**L420** `        raise HTTPException(status_code=422, detail="文件内容为空")`

**L421** `    if len(data) > MAX_UPLOAD_BYTES:`

**L422** `        raise HTTPException(status_code=413, detail="文件超过 500KB 上限")`

**L423** ``

**L424** `    try:`

**L425** `        meta = ingest_upload(data, file.filename)`

**L426** `    except ValueError as exc:`

**L427** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L428** `    except NexusError as exc:`

**L429** `        status = 400 if exc.code in (`

**L430** `            "PDF_PARSE_ERROR", "PDF_EMPTY", "UNSUPPORTED_FORMAT", "EMPTY_FILE"`

**L431** `        ) else 500`

**L432** `        raise HTTPException(status_code=status, detail=exc.message) from exc`

**L433** `    except StorageError as exc:`

**L434** `        raise HTTPException(status_code=500, detail=exc.message) from exc`

**L435** `    except UnicodeDecodeError as exc:`

**L436** `        raise HTTPException(status_code=400, detail="文本文件须为 UTF-8 编码") from exc`

**L437** ``

**L438** `    cleared = session_manager.clear_all()`
  → rebuild API 相关行。

**L439** `    store = get_knowledge_store()`

**L440** ``

**L441** `    return KnowledgeUploadResponse(`

**L442** `        filename=meta.name,`

**L443** `        format=meta.format,`

**L444** `        chunk_count=meta.chunk_count,`

**L445** `        document_count=store.document_count,`

**L446** `        total_chunks=store.chunk_count,`

**L447** `        sessions_cleared=cleared,`

**L448** `        index_mode=store.index_mode,`

**L449** `        message=f"文档已入库（{meta.format}），增量索引已更新",`

**L450** `    )`


## 6. 失败恢复

重建失败 mid-loop 可能导致空库——教学环境可重新 bootstrap；生产应事务化或先写临时 store。
