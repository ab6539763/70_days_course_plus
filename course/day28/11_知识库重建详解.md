# 知识库重建详解

**Day 28 深度专题** | ZL-NA-REQ-028

## 1. 问题定义

设源文件集合 \(S\)，当前配置 \(	heta\)。生产索引 \(I = 	ext{Index}(	ext{Parse}(S), 	heta_{	ext{old}})\)。配置更新为 \(	heta_{	ext{new}}\) 后，需计算 \(I' = 	ext{Index}(	ext{Parse}(S), 	heta_{	ext{new}})\)。

**无法**通过修改 \(	heta\) 元数据把 \(I\) 就地变为 \(I'\)，因 chunk 文本边界已固定。

## 2. rebuild_store 不变式

- 重建后 \(orall d \in 	ext{documents}\)，\(d\) 来自 \(S\) 中某个源文件  
- \(	ext{chunk\_count}\) 仅取决于 \(|S|\) 与 \(	heta_{	ext{new}}\)  
- `last_rebuilt_at` 单调更新（同进程连续 rebuild）

## 3. 与 evaluate 协作

`rebuild_with_best_config`：

```python
results = run_ab_experiment(doc, PRESET_CONFIGS, queries)
best = pick_best_config(results)
store.set_chunk_config(best.config)
return rebuild_store(store, ...)
```

**注意**：evaluate 样例是 `product_notice.md`，全库 rebuild 包含所有源文件——最优配置未必对所有文档全局最优。

## 4. 清空顺序为何重要

必须先 `documents.clear()` 再逐文件 `_append_chunks`，最后单次 `_rebuild_index()`。若中途 `_rebuild_index()` 会导致空索引窗口内 chat 失败——教学单进程可接受。

## 5. 运维 Runbook（摘要）

1. 备份 `data/knowledge/store.json`  
2. 确认 uploads 文档版本  
3. POST evaluate 存档 JSON  
4. POST rebuild（或 apply_best_config）  
5. 检查 last_rebuilt_at、chunk_count  
6. chat 抽测 + 监控错误率  



## 附录：knowledge_store 持久化走读

### `nexus-agent-platform/src/rag/knowledge_store.py` 逐行走读

共 **663** 行。

**L1** `"""`

**L2** `知识库存储 — 文档 ingestion、分块索引与 JSON 持久化`

**L3** ``

**L4** `将 Day 19–20 的 RAG 管线升级为可写入、可落盘的企业知识库 MVP。`

**L5** ``

**L6** `需求：ZL-NA-REQ-025`

**L7** `"""`

**L8** ``

**L9** `from __future__ import annotations`

**L10** ``

**L11** `import threading`

**L12** `from dataclasses import dataclass, field`

**L13** `from datetime import datetime, timezone`

**L14** `from pathlib import Path`

**L15** `from typing import Any`

**L16** ``

**L17** `from core.paths import get_path`

**L18** `from rag.chunker import TextChunk, chunk_documents, chunk_text`

**L19** `from rag.chunk_config import DEFAULT_CHUNK_CONFIG, ChunkConfig`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L20** `from rag.chunk_strategies import chunk_from_parsed`

**L21** `from rag.context import DocumentIndex, RAGContextService`

**L22** `from rag.chroma_retriever import ChromaEmbeddingRetriever`

**L23** `from rag.chroma_store import VECTOR_BACKEND, ChromaVectorIndex`

**L24** `from rag.citation_config import CitationConfig`

**L25** `from rag.expanding_retriever import ExpandingRetriever`

**L26** `from rag.expansion_config import ExpansionConfig`

**L27** `from rag.hybrid_retriever import HybridRetriever`

**L28** `from rag.rerank_config import RerankConfig`

**L29** `from rag.reranker import MockCrossEncoderReranker`

**L30** `from rag.reranking_retriever import RerankingRetriever`

**L31** `from rag.retrieval_config import RetrievalConfig`

**L32** `from rag.rewrite_config import RewriteConfig`

**L33** `from rag.rewriting_retriever import RewritingRetriever`

**L34** `from tools.doc_reader import DocumentRecord, read_text_file`

**L35** `from tools.parsers.base import ParsedDocument`

**L36** `from utils.json_utils import load_json, save_json`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L37** `from utils.text_utils import clean_text`

**L38** ``

**L39** `STORE_VERSION = "1.1"`

**L40** `PLATFORM_VERSION = "0.35.0"`

**L41** `INDEX_MODE_INCREMENTAL = "incremental"`

**L42** `INDEX_MODE_FULL = "full"`

**L43** ``

**L44** ``

**L45** `@dataclass`

**L46** `class KnowledgeDocument:`

**L47** `    """已入库文档元数据"""`

**L48** ``

**L49** `    name: str`

**L50** `    ingested_at: str`

**L51** `    size_bytes: int = 0`

**L52** `    chunk_count: int = 0`

**L53** `    format: str = "txt"`

**L54** ``

**L55** `    def to_dict(self) -> dict[str, Any]:`

**L56** `        return {`

**L57** `            "name": self.name,`

**L58** `            "ingested_at": self.ingested_at,`

**L59** `            "size_bytes": self.size_bytes,`

**L60** `            "chunk_count": self.chunk_count,`

**L61** `            "format": self.format,`

**L62** `        }`

**L63** ``

**L64** `    @classmethod`

**L65** `    def from_dict(cls, data: dict[str, Any]) -> KnowledgeDocument:`

**L66** `        return cls(`

**L67** `            name=str(data.get("name", "")),`

**L68** `            ingested_at=str(data.get("ingested_at", "")),`

**L69** `            size_bytes=int(data.get("size_bytes", 0)),`

**L70** `            chunk_count=int(data.get("chunk_count", 0)),`

**L71** `            format=str(data.get("format", "txt")),`

**L72** `        )`

**L73** ``

**L74** ``

**L75** `@dataclass`

**L76** `class KnowledgeStore:`

**L77** `    """`

**L78** `    企业知识库 — 分块 + Chroma 向量索引 + JSON 元数据持久化`

**L79** ``

**L80** `    典型用法：`

**L81** `        store = KnowledgeStore.load_or_bootstrap()`

**L82** `        store.ingest_text("新产品说明…", filename="notice.txt")`

**L83** `        rag = store.as_rag_service()`

**L84** `    """`

**L85** ``

**L86** `    documents: list[KnowledgeDocument] = field(default_factory=list)`

**L87** `    chunks: list[TextChunk] = field(default_factory=list)`

**L88** `    embedding_state: dict[str, Any] = field(default_factory=dict)`

**L89** `    vector_backend: str = VECTOR_BACKEND`

**L90** `    chunk_config: ChunkConfig = field(default_factory=ChunkConfig)`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L91** `    last_rebuilt_at: str | None = None`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L92** `    last_incremental_at: str | None = None`

**L93** `    index_mode: str = INDEX_MODE_FULL`

**L94** `    retrieval_config: RetrievalConfig = field(default_factory=RetrievalConfig)`

**L95** `    rerank_config: RerankConfig = field(default_factory=RerankConfig)`

**L96** `    rewrite_config: RewriteConfig = field(default_factory=RewriteConfig)`

**L97** `    citation_config: CitationConfig = field(default_factory=CitationConfig)`

**L98** `    expansion_config: ExpansionConfig = field(default_factory=ExpansionConfig)`

**L99** `    store_path: Path | None = None`

**L100** `    chroma_path: Path | None = None`

**L101** `    _rag_service: RAGContextService | None = field(default=None, repr=False)`

**L102** ``

**L103** `    @property`

**L104** `    def chunk_count(self) -> int:`

**L105** `        return len(self.chunks)`

**L106** ``

**L107** `    @property`

**L108** `    def document_count(self) -> int:`

**L109** `        return len(self.documents)`

**L110** ``

**L111** `    def as_rag_service(self) -> RAGContextService:`

**L112** `        """构建或返回缓存的 RAGContextService"""`

**L113** `        if self._rag_service is None:`

**L114** `            self._rag_service = self._build_rag_service()`

**L115** `        return self._rag_service`

**L116** ``

**L117** `    def invalidate_cache(self) -> None:`

**L118** `        self._rag_service = None`

**L119** ``

**L120** `    def get_chunk_config(self) -> ChunkConfig:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L121** `        return ChunkConfig.from_dict(self.chunk_config.to_dict())`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L122** ``

**L123** `    def set_chunk_config(self, config: ChunkConfig) -> ChunkConfig:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L124** `        config.validate()`

**L125** `        self.chunk_config = ChunkConfig.from_dict(config.to_dict())`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L126** `        return self.chunk_config`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L127** ``

**L128** `    def get_retrieval_config(self) -> RetrievalConfig:`

**L129** `        return RetrievalConfig.from_dict(self.retrieval_config.to_dict())`

**L130** ``

**L131** `    def set_retrieval_config(self, config: RetrievalConfig) -> RetrievalConfig:`

**L132** `        config.validate()`

**L133** `        self.retrieval_config = RetrievalConfig.from_dict(config.to_dict())`

**L134** `        self.invalidate_cache()`

**L135** `        return self.retrieval_config`

**L136** ``

**L137** `    def get_rerank_config(self) -> RerankConfig:`

**L138** `        return RerankConfig.from_dict(self.rerank_config.to_dict())`

**L139** ``

**L140** `    def set_rerank_config(self, config: RerankConfig) -> RerankConfig:`

**L141** `        config.validate()`

**L142** `        self.rerank_config = RerankConfig.from_dict(config.to_dict())`

**L143** `        self.invalidate_cache()`

**L144** `        return self.rerank_config`

**L145** ``

**L146** `    def get_rewrite_config(self) -> RewriteConfig:`

**L147** `        return RewriteConfig.from_dict(self.rewrite_config.to_dict())`

**L148** ``

**L149** `    def set_rewrite_config(self, config: RewriteConfig) -> RewriteConfig:`

**L150** `        config.validate()`

**L151** `        self.rewrite_config = RewriteConfig.from_dict(config.to_dict())`

**L152** `        self.invalidate_cache()`

**L153** `        return self.rewrite_config`

**L154** ``

**L155** `    def get_citation_config(self) -> CitationConfig:`

**L156** `        return CitationConfig.from_dict(self.citation_config.to_dict())`

**L157** ``

**L158** `    def set_citation_config(self, config: CitationConfig) -> CitationConfig:`

**L159** `        config.validate()`

**L160** `        self.citation_config = CitationConfig.from_dict(config.to_dict())`

**L161** `        return self.citation_config`

**L162** ``

**L163** `    def get_expansion_config(self) -> ExpansionConfig:`

**L164** `        return ExpansionConfig.from_dict(self.expansion_config.to_dict())`

**L165** ``

**L166** `    def set_expansion_config(self, config: ExpansionConfig) -> ExpansionConfig:`

**L167** `        config.validate()`

**L168** `        self.expansion_config = ExpansionConfig.from_dict(config.to_dict())`

**L169** `        self.invalidate_cache()`

**L170** `        return self.expansion_config`

**L171** ``

**L172** `    def fetch_citations(self, query: str) -> dict[str, Any]:`

**L173** `        """按当前 citation_config 检索并返回引用包 dict"""`

**L174** `        cfg = self.get_citation_config()`

**L175** `        if not cfg.enabled:`

**L176** `            return {`

**L177** `                "query": query.strip(),`

**L178** `                "citations": [],`

**L179** `                "rewrite": None,`

**L180** `                "expansion": None,`

**L181** `            }`

**L182** `        rag = self.as_rag_service()`

**L183** `        bundle = rag.retrieve_citation_bundle(query, config=cfg)`

**L184** `        return bundle.to_dict()`

**L185** ``

**L186** `    def ingest_text(`

**L187** `        self,`

**L188** `        content: str,`

**L189** `        *,`

**L190** `        filename: str,`

**L191** `        clean: bool = True,`

**L192** `        chunk_size: int | None = None,`

**L193** `        overlap: int | None = None,`

**L194** `    ) -> KnowledgeDocument:`

**L195** `        """将文本写入知识库并重建索引"""`

**L196** `        cfg = self.get_chunk_config()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L197** `        cs = chunk_size if chunk_size is not None else cfg.chunk_size`

**L198** `        ov = overlap if overlap is not None else cfg.overlap`

**L199** `        text = (content or "").strip()`

**L200** `        if not text:`

**L201** `            raise ValueError("文档内容不能为空")`

**L202** `        if not filename.strip():`

**L203** `            raise ValueError("filename 不能为空")`

**L204** ``

**L205** `        cleaned = text`

**L206** `        if clean:`

**L207** `            cleaned, _ = clean_text(text)`

**L208** `        doc = DocumentRecord(`

**L209** `            path=Path(filename),`

**L210** `            content=text,`

**L211** `            encoding="utf-8",`

**L212** `            size_bytes=len(text.encode("utf-8")),`

**L213** `            cleaned=cleaned,`

**L214** `        )`

**L215** `        new_chunks = chunk_documents(`

**L216** `            [doc],`

**L217** `            chunk_size=cs,`

**L218** `            overlap=ov,`

**L219** `            use_cleaned=clean,`

**L220** `        )`

**L221** `        self._append_chunks(filename, new_chunks, size_bytes=doc.size_bytes)`

**L222** `        self._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L223** `        return self.documents[-1]`

**L224** ``

**L225** `    def ingest_file(`

**L226** `        self,`

**L227** `        path: Path,`

**L228** `        *,`

**L229** `        clean: bool = True,`

**L230** `        chunk_size: int = 200,`

**L231** `        overlap: int = 40,`

**L232** `    ) -> KnowledgeDocument:`

**L233** `        """从磁盘文件 ingestion"""`

**L234** `        content, encoding = read_text_file(path)`

**L235** `        cleaned = content`

**L236** `        if clean:`

**L237** `            cleaned, _ = clean_text(content)`

**L238** `        doc = DocumentRecord(`

**L239** `            path=path,`

**L240** `            content=content,`

**L241** `            encoding=encoding,`

**L242** `            size_bytes=path.stat().st_size,`

**L243** `            cleaned=cleaned,`

**L244** `        )`

**L245** `        new_chunks = chunk_documents(`

**L246** `            [doc],`

**L247** `            chunk_size=chunk_size,`

**L248** `            overlap=overlap,`

**L249** `            use_cleaned=clean,`

**L250** `        )`

**L251** `        self._append_chunks(path.name, new_chunks, size_bytes=doc.size_bytes)`

**L252** `        self._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L253** `        return self.documents[-1]`

**L254** ``

**L255** `    def ingest_bytes(`

**L256** `        self,`

**L257** `        data: bytes,`

**L258** `        *,`

**L259** `        filename: str,`

**L260** `        clean: bool = True,`

**L261** `        chunk_strategy: str = "auto",`

**L262** `        incremental: bool = True,`

**L263** `    ) -> KnowledgeDocument:`

**L264** `        """处理上传二进制 — Day 26 起委托 doc_parser"""`

**L265** `        from tools.doc_parser import parse_bytes`

**L266** ``

**L267** `        parsed = parse_bytes(data, filename)`

**L268** `        cfg = self.get_chunk_config()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L269** `        return self.ingest_parsed(`

**L270** `            parsed,`

**L271** `            clean=clean,`

**L272** `            chunk_strategy=chunk_strategy if chunk_strategy != "auto" else cfg.strategy,`

**L273** `            incremental=incremental,`

**L274** `        )`

**L275** ``

**L276** `    def ingest_parsed(`

**L277** `        self,`

**L278** `        parsed: ParsedDocument,`

**L279** `        *,`

**L280** `        clean: bool = True,`

**L281** `        chunk_strategy: str | None = None,`

**L282** `        chunk_size: int | None = None,`

**L283** `        overlap: int | None = None,`

**L284** `        incremental: bool = False,`

**L285** `    ) -> KnowledgeDocument:`

**L286** `        """将 ParsedDocument 写入知识库"""`

**L287** `        cfg = self.get_chunk_config()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L288** `        strategy = chunk_strategy if chunk_strategy is not None else cfg.strategy`

**L289** `        cs = chunk_size if chunk_size is not None else cfg.chunk_size`

**L290** `        ov = overlap if overlap is not None else cfg.overlap`

**L291** `        text = (parsed.plain_text or "").strip()`

**L292** `        if not text:`

**L293** `            raise ValueError("解析结果为空")`

**L294** ``

**L295** `        if clean:`

**L296** `            text, _ = clean_text(text)`

**L297** `            parsed.plain_text = text`

**L298** `            for section in parsed.sections:`

**L299** `                section.body, _ = clean_text(section.body)`

**L300** ``

**L301** `        new_chunks = chunk_from_parsed(`

**L302** `            parsed,`

**L303** `            strategy=strategy,`

**L304** `            chunk_size=cs,`

**L305** `            overlap=ov,`

**L306** `        )`

**L307** `        size_bytes = len(parsed.plain_text.encode("utf-8"))`

**L308** ``

**L309** `        if incremental:`

**L310** `            removed = self._remove_document_by_source(parsed.filename)`

**L311** `            self._append_chunks(`

**L312** `                parsed.filename,`

**L313** `                new_chunks,`

**L314** `                size_bytes=size_bytes,`

**L315** `                doc_format=parsed.format,`

**L316** `            )`

**L317** `            self._incremental_index(new_chunks, replaced_count=len(removed))`

**L318** `        else:`

**L319** `            self._append_chunks(`

**L320** `                parsed.filename,`

**L321** `                new_chunks,`

**L322** `                size_bytes=size_bytes,`

**L323** `                doc_format=parsed.format,`

**L324** `            )`

**L325** `            self._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L326** `        return self.documents[-1]`

**L327** ``

**L328** `    def save(self, path: Path | None = None) -> Path:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L329** `        """持久化到 JSON"""`

**L330** `        target = path or self.store_path or _default_store_path()`

**L331** `        self.store_path = target`

**L332** `        payload = {`

**L333** `            "version": STORE_VERSION,`

**L334** `            "platform_version": PLATFORM_VERSION,`

**L335** `            "documents": [d.to_dict() for d in self.documents],`

**L336** `            "chunks": [_chunk_to_dict(c) for c in self.chunks],`

**L337** `            "embedding": self.embedding_state,`

**L338** `            "vector_backend": self.vector_backend,`

**L339** `            "chunk_config": self.chunk_config.to_dict(),`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L340** `            "last_rebuilt_at": self.last_rebuilt_at,`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L341** `            "last_incremental_at": self.last_incremental_at,`

**L342** `            "index_mode": self.index_mode,`

**L343** `            "retrieval_config": self.retrieval_config.to_dict(),`

**L344** `            "rerank_config": self.rerank_config.to_dict(),`

**L345** `            "rewrite_config": self.rewrite_config.to_dict(),`

**L346** `            "citation_config": self.citation_config.to_dict(),`

**L347** `            "expansion_config": self.expansion_config.to_dict(),`

**L348** `        }`

**L349** `        save_json(target, payload)`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L350** `        return target`

**L351** ``

**L352** `    @classmethod`

**L353** `    def load(cls, path: Path) -> KnowledgeStore:`

**L354** `        """从 JSON 加载知识库"""`

**L355** `        raw = load_json(path, default=None)`

**L356** `        if not raw:`

**L357** `            return cls.bootstrap_from_sample_docs(store_path=path)`

**L358** ``

**L359** `        store = cls(store_path=path)`

**L360** `        store.documents = [`

**L361** `            KnowledgeDocument.from_dict(d) for d in raw.get("documents", [])`

**L362** `        ]`

**L363** `        store.chunks = [_chunk_from_dict(c) for c in raw.get("chunks", [])]`

**L364** `        store.embedding_state = dict(raw.get("embedding") or {})`

**L365** `        store.vector_backend = str(raw.get("vector_backend") or VECTOR_BACKEND)`

**L366** `        if raw.get("chunk_config"):`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L367** `            store.chunk_config = ChunkConfig.from_dict(raw["chunk_config"])`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L368** `        store.last_rebuilt_at = raw.get("last_rebuilt_at")`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L369** `        store.last_incremental_at = raw.get("last_incremental_at")`

**L370** `        store.index_mode = str(raw.get("index_mode") or INDEX_MODE_FULL)`

**L371** `        if raw.get("retrieval_config"):`

**L372** `            store.retrieval_config = RetrievalConfig.from_dict(raw["retrieval_config"])`

**L373** `        if raw.get("rerank_config"):`

**L374** `            store.rerank_config = RerankConfig.from_dict(raw["rerank_config"])`

**L375** `        if raw.get("rewrite_config"):`

**L376** `            store.rewrite_config = RewriteConfig.from_dict(raw["rewrite_config"])`

**L377** `        if raw.get("citation_config"):`

**L378** `            store.citation_config = CitationConfig.from_dict(raw["citation_config"])`

**L379** `        if raw.get("expansion_config"):`

**L380** `            store.expansion_config = ExpansionConfig.from_dict(raw["expansion_config"])`

**L381** `        store._sync_chroma_from_json()`

**L382** `        store._rag_service = store._build_rag_service()`

**L383** `        return store`

**L384** ``

**L385** `    @classmethod`

**L386** `    def load_or_bootstrap(cls, path: Path | None = None) -> KnowledgeStore:`

**L387** `        """加载已有库，不存在则从 sample_docs 引导"""`

**L388** `        target = path or _default_store_path()`

**L389** `        if target.is_file():`

**L390** `            return cls.load(target)`

**L391** `        store = cls.bootstrap_from_sample_docs(store_path=target)`

**L392** `        store.save(target)`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L393** `        return store`

**L394** ``

**L395** `    @classmethod`

**L396** `    def bootstrap_from_sample_docs(cls, *, store_path: Path | None = None) -> KnowledgeStore:`

**L397** `        """用内置 sample_docs 初始化知识库"""`

**L398** `        rag = RAGContextService.from_sample_docs(use_embedding=True)`

**L399** `        store = cls(store_path=store_path)`

**L400** `        now = _utc_now()`

**L401** ``

**L402** `        by_source: dict[str, list[TextChunk]] = {}`

**L403** `        for chunk in rag.index.chunks:`

**L404** `            by_source.setdefault(chunk.source, []).append(chunk)`

**L405** ``

**L406** `        for source, chunks in sorted(by_source.items()):`

**L407** `            store.documents.append(`

**L408** `                KnowledgeDocument(`

**L409** `                    name=source,`

**L410** `                    ingested_at=now,`

**L411** `                    size_bytes=sum(len(c.text) for c in chunks),`

**L412** `                    chunk_count=len(chunks),`

**L413** `                )`

**L414** `            )`

**L415** `        store.chunks = list(rag.index.chunks)`

**L416** `        retriever = rag.index.retriever`

**L417** `        from rag.embedding_retriever import EmbeddingRetriever`

**L418** ``

**L419** `        if isinstance(retriever, EmbeddingRetriever):`

**L420** `            store.embedding_state = retriever._client.model.export_state()`

**L421** `        store._rebuild_index()`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L422** `        return store`

**L423** ``

**L424** `    def status_dict(self) -> dict[str, Any]:`

**L425** `        from tools.doc_parser import supported_formats`

**L426** ``

**L427** `        return {`

**L428** `            "document_count": self.document_count,`

**L429** `            "chunk_count": self.chunk_count,`

**L430** `            "documents": [d.to_dict() for d in self.documents],`

**L431** `            "store_path": str(self.store_path) if self.store_path else None,`

**L432** `            "platform_version": PLATFORM_VERSION,`

**L433** `            "supported_formats": supported_formats(),`

**L434** `            "chunk_config": self.chunk_config.to_dict(),`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L435** `            "last_rebuilt_at": self.last_rebuilt_at,`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L436** `            "last_incremental_at": self.last_incremental_at,`

**L437** `            "index_mode": self.index_mode,`

**L438** `            "retrieval_config": self.retrieval_config.to_dict(),`

**L439** `            "rerank_config": self.rerank_config.to_dict(),`

**L440** `            "rewrite_config": self.rewrite_config.to_dict(),`

**L441** `            "citation_config": self.citation_config.to_dict(),`

**L442** `            "expansion_config": self.expansion_config.to_dict(),`

**L443** `            "vector_backend": self.vector_backend,`

**L444** `            "chroma_path": str(self._resolve_chroma_path()),`

**L445** `            "chroma_count": self._chroma_index().count() if self.chunks else 0,`

**L446** `        }`

**L447** ``

**L448** `    def _append_chunks(`

**L449** `        self,`

**L450** `        filename: str,`

**L451** `        new_chunks: list[TextChunk],`

**L452** `        *,`

**L453** `        size_bytes: int,`

**L454** `        doc_format: str = "txt",`

**L455** `    ) -> None:`

**L456** `        base_index = len(self.chunks)`

**L457** `        reindexed: list[TextChunk] = []`

**L458** `        for i, chunk in enumerate(new_chunks):`

**L459** `            reindexed.append(`

**L460** `                TextChunk(`

**L461** `                    chunk_id=chunk.chunk_id,`

**L462** `                    text=chunk.text,`

**L463** `                    source=filename,`

**L464** `                    index=base_index + i,`

**L465** `                    start_char=chunk.start_char,`

**L466** `                    end_char=chunk.end_char,`

**L467** `                )`

**L468** `            )`

**L469** `        self.chunks.extend(reindexed)`

**L470** `        self.documents.append(`

**L471** `            KnowledgeDocument(`

**L472** `                name=filename,`

**L473** `                ingested_at=_utc_now(),`

**L474** `                size_bytes=size_bytes,`

**L475** `                chunk_count=len(reindexed),`

**L476** `                format=doc_format,`

**L477** `            )`

**L478** `        )`

**L479** ``

**L480** `    def _remove_document_by_source(self, filename: str) -> list[str]:`

**L481** `        """移除同名文档及其 chunks，并从 Chroma 删除旧向量"""`

**L482** `        removed_ids = [c.chunk_id for c in self.chunks if c.source == filename]`

**L483** `        if not removed_ids:`

**L484** `            return []`

**L485** ``

**L486** `        self._chroma_index().delete_by_ids(removed_ids)`

**L487** `        self.documents = [d for d in self.documents if d.name != filename]`

**L488** `        self.chunks = [c for c in self.chunks if c.source != filename]`

**L489** `        self.chunks = [`

**L490** `            TextChunk(`

**L491** `                chunk_id=c.chunk_id,`

**L492** `                text=c.text,`

**L493** `                source=c.source,`

**L494** `                index=i,`

**L495** `                start_char=c.start_char,`

**L496** `                end_char=c.end_char,`

**L497** `            )`

**L498** `            for i, c in enumerate(self.chunks)`

**L499** `        ]`

**L500** `        return removed_ids`

**L501** ``

**L502** `    def _resolve_chroma_path(self) -> Path:`

**L503** `        if self.chroma_path is not None:`

**L504** `            return self.chroma_path`

**L505** `        if self.store_path is not None:`

**L506** `            return self.store_path.parent / "chroma"`

**L507** `        return get_path("knowledge_chroma")`

**L508** ``

**L509** `    def _chroma_index(self) -> ChromaVectorIndex:`

**L510** `        return ChromaVectorIndex(self._resolve_chroma_path())`

**L511** ``

**L512** `    def _sync_chroma_from_json(self) -> None:`

**L513** `        """从 JSON 元数据恢复 Chroma（迁移或冷启动）"""`

**L514** `        if not self.chunks or not self.embedding_state:`

**L515** `            return`

**L516** `        chroma = self._chroma_index()`

**L517** `        if chroma.count() > 0:`

**L518** `            return`

**L519** `        from rag.embedding import EmbeddingClient`

**L520** ``

**L521** `        client = EmbeddingClient()`

**L522** `        client.model.load_state(self.embedding_state)`

**L523** `        vectors = client.embed_batch([c.text for c in self.chunks])`

**L524** `        chroma.upsert_chunks(self.chunks, vectors)`

**L525** ``

**L526** `    def _rebuild_index(self) -> None:`
  → 与 Day 28 rebuild 及 last_rebuilt_at 相关。

**L527** `        from rag.embedding_retriever import EmbeddingRetriever`

**L528** ``

**L529** `        if not self.chunks:`

**L530** `            self.embedding_state = {}`

**L531** `            self._chroma_index().reset()`

**L532** `            self.invalidate_cache()`

**L533** `            return`

**L534** ``

**L535** `        retriever = EmbeddingRetriever(self.chunks)`

**L536** `        self.embedding_state = retriever._client.model.export_state()`

**L537** `        vectors = retriever._client.embed_batch([c.text for c in self.chunks])`

**L538** `        chroma = self._chroma_index()`

**L539** `        chroma.reset()`

**L540** `        chroma.upsert_chunks(self.chunks, vectors)`

**L541** `        self.index_mode = INDEX_MODE_FULL`

**L542** `        self.invalidate_cache()`

**L543** ``

**L544** `    def _incremental_index(`

**L545** `        self,`

**L546** `        affected_chunks: list[TextChunk],`

**L547** `        *,`

**L548** `        replaced_count: int = 0,`

**L549** `    ) -> bool:`

**L550** `        """`

**L551** `        增量更新 Chroma：不 reset collection，仅 upsert 受影响 chunk。`

**L552** ``

**L553** `        若 TF-IDF 词表扩张，则回退为全量 upsert（仍不 reset）。`

**L554** `        返回是否发生词表扩张。`

**L555** `        """`

**L556** `        from rag.embedding_retriever import EmbeddingRetriever`

**L557** ``

**L558** `        if not self.chunks:`

**L559** `            self.embedding_state = {}`

**L560** `            self._chroma_index().reset()`

**L561** `            self.last_incremental_at = _utc_now()`

**L562** `            self.index_mode = INDEX_MODE_INCREMENTAL`

**L563** `            self.invalidate_cache()`

**L564** `            return False`

**L565** ``

**L566** `        old_vocab = dict(self.embedding_state.get("vocab") or {})`

**L567** `        retriever = EmbeddingRetriever(self.chunks)`

**L568** `        self.embedding_state = retriever._client.model.export_state()`

**L569** `        new_vocab = dict(self.embedding_state.get("vocab") or {})`

**L570** `        vocab_expanded = len(new_vocab) > len(old_vocab) and bool(old_vocab)`

**L571** ``

**L572** `        if vocab_expanded:`

**L573** `            affected_chunks = list(self.chunks)`

**L574** ``

**L575** `        client = retriever._client`

**L576** `        vectors = client.embed_batch([c.text for c in affected_chunks])`

**L577** `        chroma = self._chroma_index()`

**L578** `        if vocab_expanded:`

**L579** `            # TF-IDF 维度变化时 Chroma collection 须重建`

**L580** `            chroma.reset()`

**L581** `        chroma.upsert_chunks(affected_chunks, vectors)`

**L582** `        self.last_incremental_at = _utc_now()`

**L583** `        self.index_mode = INDEX_MODE_INCREMENTAL`

**L584** `        self.invalidate_cache()`

**L585** `        return vocab_expanded`

**L586** ``

**L587** `    def _build_rag_service(self) -> RAGContextService:`

**L588** `        if not self.chunks:`

**L589** `            return RAGContextService()`

**L590** ``

**L591** `        from rag.embedding import EmbeddingClient`

**L592** `        from rag.retriever import KeywordRetriever`

**L593** ``

**L594** `        client = EmbeddingClient()`

**L595** `        client.model.load_state(self.embedding_state)`

**L596** `        self._sync_chroma_from_json()`

**L597** `        chroma = self._chroma_index()`

**L598** `        vector = ChromaEmbeddingRetriever(self.chunks, chroma, client=client)`

**L599** `        keyword = KeywordRetriever(self.chunks)`

**L600** `        cfg = self.get_retrieval_config()`

**L601** `        hybrid = HybridRetriever(self.chunks, keyword, vector, config=cfg)`

**L602** `        rerank_cfg = self.get_rerank_config()`

**L603** `        reranking = RerankingRetriever(`

**L604** `            hybrid,`

**L605** `            reranker=MockCrossEncoderReranker(),`

**L606** `            config=rerank_cfg,`

**L607** `        )`

**L608** `        rewrite_cfg = self.get_rewrite_config()`

**L609** `        rewriting = RewritingRetriever(reranking, config=rewrite_cfg)`

**L610** `        expansion_cfg = self.get_expansion_config()`

**L611** `        retriever = ExpandingRetriever(rewriting, config=expansion_cfg)`

**L612** `        index = DocumentIndex(chunks=self.chunks, retriever=retriever)`

**L613** `        return RAGContextService(index)`

**L614** ``

**L615** ``

**L616** `_store: KnowledgeStore | None = None`

**L617** `_store_lock = threading.Lock()`

**L618** ``

**L619** ``

**L620** `def get_knowledge_store(*, reload: bool = False) -> KnowledgeStore:`

**L621** `    """全局知识库单例（API 与 factory 共享）"""`

**L622** `    global _store`

**L623** `    with _store_lock:`

**L624** `        if _store is None or reload:`

**L625** `            _store = KnowledgeStore.load_or_bootstrap()`

**L626** `        return _store`

**L627** ``

**L628** ``

**L629** `def set_knowledge_store(store: KnowledgeStore) -> None:`

**L630** `    """测试注入用"""`

**L631** `    global _store`

**L632** `    with _store_lock:`

**L633** `        _store = store`

**L634** ``

**L635** ``

**L636** `def _default_store_path() -> Path:`

**L637** `    return get_path("knowledge_store")`

**L638** ``

**L639** ``

**L640** `def _utc_now() -> str:`

**L641** `    return datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")`

**L642** ``

**L643** ``

**L644** `def _chunk_to_dict(chunk: TextChunk) -> dict[str, Any]:`

**L645** `    return {`

**L646** `        "chunk_id": chunk.chunk_id,`

**L647** `        "text": chunk.text,`

**L648** `        "source": chunk.source,`

**L649** `        "index": chunk.index,`

**L650** `        "start_char": chunk.start_char,`

**L651** `        "end_char": chunk.end_char,`

**L652** `    }`

**L653** ``

**L654** ``

**L655** `def _chunk_from_dict(data: dict[str, Any]) -> TextChunk:`

**L656** `    return TextChunk(`

**L657** `        chunk_id=str(data.get("chunk_id", "")),`

**L658** `        text=str(data.get("text", "")),`

**L659** `        source=str(data.get("source", "")),`

**L660** `        index=int(data.get("index", 0)),`

**L661** `        start_char=int(data.get("start_char", 0)),`

**L662** `        end_char=int(data.get("end_char", 0)),`

**L663** `    )`


## 附录：api/knowledge.py 重建端点走读

### `nexus-agent-platform/src/api/knowledge.py` 逐行走读

共 **331** 行。

**L1** `"""`

**L2** `知识库 REST API — 文档上传、分块调参与检索评估`

**L3** ``

**L4** `需求：ZL-NA-REQ-025 / ZL-NA-REQ-026 / ZL-NA-REQ-027 / ZL-NA-REQ-028 / ZL-NA-REQ-029 / ZL-NA-REQ-030 / ZL-NA-REQ-031 / ZL-NA-REQ-032 / ZL-NA-REQ-033 / ZL-NA-REQ-034 / ZL-NA-REQ-035`

**L5** `"""`

**L6** ``

**L7** `from __future__ import annotations`

**L8** ``

**L9** `from pathlib import Path`

**L10** ``

**L11** `from fastapi import APIRouter, File, HTTPException, UploadFile`

**L12** ``

**L13** `from api.schemas import (`

**L14** `    ChunkConfigRequest,`

**L15** `    ChunkConfigResponse,`

**L16** `    CitationConfigRequest,`

**L17** `    CitationConfigResponse,`

**L18** `    CitationPreviewRequest,`

**L19** `    CitationPreviewResponse,`

**L20** `    ExpansionConfigRequest,`

**L21** `    ExpansionConfigResponse,`

**L22** `    ExpansionPreviewRequest,`

**L23** `    ExpansionPreviewResponse,`

**L24** `    EvaluateRequest,`

**L25** `    EvaluateResponse,`

**L26** `    KnowledgeStatusResponse,`

**L27** `    KnowledgeUploadResponse,`

**L28** `    RebuildRequest,`

**L29** `    RebuildResponse,`

**L30** `    RerankConfigRequest,`

**L31** `    RerankConfigResponse,`

**L32** `    RewriteConfigRequest,`

**L33** `    RewriteConfigResponse,`

**L34** `    RewritePreviewRequest,`

**L35** `    RewritePreviewResponse,`

**L36** `    RetrievalConfigRequest,`

**L37** `    RetrievalConfigResponse,`

**L38** `)`

**L39** `from api.sessions import session_manager`
  → rebuild API 相关行。

**L40** `from core.exceptions import NexusError, StorageError`

**L41** `from rag.chunk_config import PRESET_CONFIGS, ChunkConfig`

**L42** `from rag.ingestion import ingest_upload`

**L43** `from rag.knowledge_rebuild import rebuild_store, rebuild_with_best_config`
  → rebuild API 相关行。

**L44** `from rag.knowledge_store import get_knowledge_store`

**L45** `from rag.citation_config import CitationConfig`

**L46** `from rag.expansion_config import ExpansionConfig`

**L47** `from rag.query_expander import build_expander`

**L48** `from rag.query_rewriter import RuleBasedQueryRewriter`

**L49** `from rag.rerank_config import RerankConfig`

**L50** `from rag.rewrite_config import RewriteConfig`

**L51** `from rag.retrieval_config import RetrievalConfig`

**L52** `from rag.retrieval_eval import EvalQuery, pick_best_config, run_ab_experiment`

**L53** `from tools.doc_parser import parse_bytes`

**L54** ``

**L55** `router = APIRouter(prefix="/api/knowledge", tags=["knowledge"])`

**L56** ``

**L57** `MAX_UPLOAD_BYTES = 512_000  # 500 KB 教学上限`

**L58** `_EVAL_SAMPLE = (`

**L59** `    Path(__file__).resolve().parent.parent / "day26" / "sample_docs" / "product_notice.md"`

**L60** `)`

**L61** ``

**L62** ``

**L63** `@router.get("/retrieval-config", response_model=RetrievalConfigResponse)`

**L64** `def get_retrieval_config() -> RetrievalConfigResponse:`

**L65** `    """返回当前检索模式（vector / keyword / hybrid）与融合参数"""`

**L66** `    cfg = get_knowledge_store().get_retrieval_config()`

**L67** `    return RetrievalConfigResponse(**cfg.to_dict())`

**L68** ``

**L69** ``

**L70** `@router.put("/retrieval-config", response_model=RetrievalConfigResponse)`

**L71** `def update_retrieval_config(body: RetrievalConfigRequest) -> RetrievalConfigResponse:`

**L72** `    """更新检索策略；变更后清除 RAG 缓存"""`

**L73** `    store = get_knowledge_store()`

**L74** `    try:`

**L75** `        cfg = RetrievalConfig.from_dict(body.model_dump())`

**L76** `        cfg.validate()`

**L77** `        store.set_retrieval_config(cfg)`

**L78** `        store.save()`

**L79** `    except ValueError as exc:`

**L80** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L81** `    return RetrievalConfigResponse(**cfg.to_dict())`

**L82** ``

**L83** ``

**L84** `@router.get("/rerank-config", response_model=RerankConfigResponse)`

**L85** `def get_rerank_config() -> RerankConfigResponse:`

**L86** `    """返回 rerank 开关、候选池大小与模型标识"""`

**L87** `    cfg = get_knowledge_store().get_rerank_config()`

**L88** `    return RerankConfigResponse(**cfg.to_dict())`

**L89** ``

**L90** ``

**L91** `@router.put("/rerank-config", response_model=RerankConfigResponse)`

**L92** `def update_rerank_config(body: RerankConfigRequest) -> RerankConfigResponse:`

**L93** `    """更新 rerank 策略；变更后清除 RAG 缓存"""`

**L94** `    store = get_knowledge_store()`

**L95** `    try:`

**L96** `        cfg = RerankConfig.from_dict(body.model_dump())`

**L97** `        cfg.validate()`

**L98** `        store.set_rerank_config(cfg)`

**L99** `        store.save()`

**L100** `    except ValueError as exc:`

**L101** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L102** `    return RerankConfigResponse(**cfg.to_dict())`

**L103** ``

**L104** ``

**L105** `@router.get("/rewrite-config", response_model=RewriteConfigResponse)`

**L106** `def get_rewrite_config() -> RewriteConfigResponse:`

**L107** `    """返回查询改写开关与规则模式"""`

**L108** `    cfg = get_knowledge_store().get_rewrite_config()`

**L109** `    return RewriteConfigResponse(**cfg.to_dict())`

**L110** ``

**L111** ``

**L112** `@router.put("/rewrite-config", response_model=RewriteConfigResponse)`

**L113** `def update_rewrite_config(body: RewriteConfigRequest) -> RewriteConfigResponse:`

**L114** `    """更新查询改写策略；变更后清除 RAG 缓存"""`

**L115** `    store = get_knowledge_store()`

**L116** `    try:`

**L117** `        cfg = RewriteConfig.from_dict(body.model_dump())`

**L118** `        cfg.validate()`

**L119** `        store.set_rewrite_config(cfg)`

**L120** `        store.save()`

**L121** `    except ValueError as exc:`

**L122** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L123** `    return RewriteConfigResponse(**cfg.to_dict())`

**L124** ``

**L125** ``

**L126** `@router.post("/rewrite-preview", response_model=RewritePreviewResponse)`

**L127** `def rewrite_preview(body: RewritePreviewRequest) -> RewritePreviewResponse:`

**L128** `    """预览单条 query 的规则改写结果（不触发检索）"""`

**L129** `    store = get_knowledge_store()`

**L130** `    cfg = store.get_rewrite_config()`

**L131** `    rewriter = RuleBasedQueryRewriter(config=cfg)`

**L132** `    result = rewriter.rewrite(body.query)`

**L133** `    return RewritePreviewResponse(**result.to_dict())`

**L134** ``

**L135** ``

**L136** `@router.get("/citation-config", response_model=CitationConfigResponse)`

**L137** `def get_citation_config() -> CitationConfigResponse:`

**L138** `    """返回引用溯源开关与展示参数"""`

**L139** `    cfg = get_knowledge_store().get_citation_config()`

**L140** `    return CitationConfigResponse(**cfg.to_dict())`

**L141** ``

**L142** ``

**L143** `@router.put("/citation-config", response_model=CitationConfigResponse)`

**L144** `def update_citation_config(body: CitationConfigRequest) -> CitationConfigResponse:`

**L145** `    """更新引用溯源策略并持久化"""`

**L146** `    store = get_knowledge_store()`

**L147** `    try:`

**L148** `        cfg = CitationConfig.from_dict(body.model_dump())`

**L149** `        cfg.validate()`

**L150** `        store.set_citation_config(cfg)`

**L151** `        store.save()`

**L152** `    except ValueError as exc:`

**L153** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L154** `    return CitationConfigResponse(**cfg.to_dict())`

**L155** ``

**L156** ``

**L157** `@router.post("/citation-preview", response_model=CitationPreviewResponse)`

**L158** `def citation_preview(body: CitationPreviewRequest) -> CitationPreviewResponse:`

**L159** `    """预览单条 query 的检索引用（含 rewrite / expansion 审计）"""`

**L160** `    store = get_knowledge_store()`

**L161** `    data = store.fetch_citations(body.query)`

**L162** `    return CitationPreviewResponse(**data)`

**L163** ``

**L164** ``

**L165** `@router.get("/expansion-config", response_model=ExpansionConfigResponse)`

**L166** `def get_expansion_config() -> ExpansionConfigResponse:`

**L167** `    """返回多 query 扩展开关与参数"""`

**L168** `    cfg = get_knowledge_store().get_expansion_config()`

**L169** `    return ExpansionConfigResponse(**cfg.to_dict())`

**L170** ``

**L171** ``

**L172** `@router.put("/expansion-config", response_model=ExpansionConfigResponse)`

**L173** `def update_expansion_config(body: ExpansionConfigRequest) -> ExpansionConfigResponse:`

**L174** `    """更新多 query 扩展策略并持久化"""`

**L175** `    store = get_knowledge_store()`

**L176** `    try:`

**L177** `        cfg = ExpansionConfig.from_dict(body.model_dump())`

**L178** `        cfg.validate()`

**L179** `        store.set_expansion_config(cfg)`

**L180** `        store.save()`

**L181** `    except ValueError as exc:`

**L182** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L183** `    return ExpansionConfigResponse(**cfg.to_dict())`

**L184** ``

**L185** ``

**L186** `@router.post("/expansion-preview", response_model=ExpansionPreviewResponse)`

**L187** `def expansion_preview(body: ExpansionPreviewRequest) -> ExpansionPreviewResponse:`

**L188** `    """预览单条 query 的多路扩展结果"""`

**L189** `    store = get_knowledge_store()`

**L190** `    cfg = store.get_expansion_config()`

**L191** `    expander = build_expander(cfg)`

**L192** `    result = expander.expand(body.query)`

**L193** `    return ExpansionPreviewResponse(**result.to_dict())`

**L194** ``

**L195** ``

**L196** `@router.get("/chunk-config", response_model=ChunkConfigResponse)`

**L197** `def get_chunk_config() -> ChunkConfigResponse:`

**L198** `    """返回当前知识库默认分块参数"""`

**L199** `    cfg = get_knowledge_store().get_chunk_config()`

**L200** `    return ChunkConfigResponse(**cfg.to_dict())`

**L201** ``

**L202** ``

**L203** `@router.put("/chunk-config", response_model=ChunkConfigResponse)`

**L204** `def update_chunk_config(body: ChunkConfigRequest) -> ChunkConfigResponse:`

**L205** `    """更新默认分块参数（影响后续上传）"""`

**L206** `    store = get_knowledge_store()`

**L207** `    try:`

**L208** `        cfg = ChunkConfig.from_dict(body.model_dump())`

**L209** `        cfg.validate()`

**L210** `        store.set_chunk_config(cfg)`

**L211** `        store.save()`

**L212** `    except ValueError as exc:`

**L213** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L214** `    return ChunkConfigResponse(**cfg.to_dict())`

**L215** ``

**L216** ``

**L217** `@router.post("/evaluate", response_model=EvaluateResponse)`

**L218** `def evaluate_chunk_configs(body: EvaluateRequest | None = None) -> EvaluateResponse:`

**L219** `    """`

**L220** `    对内置样例文档运行 A/B 分块评估，返回 hit@1 与推荐配置。`

**L221** ``

**L222** `    默认使用 PRESET_CONFIGS 四套预设与 day27 评估问句集。`

**L223** `    """`

**L224** `    body = body or EvaluateRequest()`

**L225** `    if not _EVAL_SAMPLE.is_file():`

**L226** `        raise HTTPException(status_code=500, detail="评估样例文档缺失")`

**L227** ``

**L228** `    doc = parse_bytes(_EVAL_SAMPLE.read_bytes(), _EVAL_SAMPLE.name)`

**L229** `    from day27.constants import EVAL_QUERIES`

**L230** ``

**L231** `    queries = [EvalQuery.from_dict(q) for q in EVAL_QUERIES]`

**L232** ``

**L233** `    if body.use_presets and not body.configs:`

**L234** `        configs = list(PRESET_CONFIGS)`

**L235** `    else:`

**L236** `        configs = [ChunkConfig.from_dict(c.model_dump()) for c in body.configs]`

**L237** `        for c in configs:`

**L238** `            c.validate()`

**L239** ``

**L240** `    results = run_ab_experiment(doc, configs, queries)`

**L241** `    best = pick_best_config(results)`

**L242** `    if not best:`

**L243** `        raise HTTPException(status_code=500, detail="评估未产生结果")`

**L244** ``

**L245** `    return EvaluateResponse(`

**L246** `        best_config=best.to_dict()["config"],`

**L247** `        results=[r.to_dict() for r in results],`

**L248** `        eval_query_count=len(queries),`

**L249** `    )`

**L250** ``

**L251** ``

**L252** `@router.post("/rebuild", response_model=RebuildResponse)`
  → rebuild API 相关行。

**L253** `def rebuild_knowledge_base(body: RebuildRequest | None = None) -> RebuildResponse:`
  → rebuild API 相关行。

**L254** `    """`

**L255** `    按当前 chunk_config（或 evaluate 最优配置）全量重建知识库。`

**L256** ``

**L257** `    重扫 sample_docs + knowledge_uploads，清空后重新分块与索引。`

**L258** `    """`

**L259** `    body = body or RebuildRequest()`

**L260** `    store = get_knowledge_store()`

**L261** ``

**L262** `    if body.apply_best_config:`
  → rebuild API 相关行。

**L263** `        if not _EVAL_SAMPLE.is_file():`

**L264** `            raise HTTPException(status_code=500, detail="评估样例文档缺失")`

**L265** `        report = rebuild_with_best_config(`
  → rebuild API 相关行。

**L266** `            store,`

**L267** `            eval_sample_path=_EVAL_SAMPLE,`

**L268** `            include_sample_docs=body.include_sample_docs,`

**L269** `        )`

**L270** `    else:`

**L271** `        report = rebuild_store(store, include_sample_docs=body.include_sample_docs)`
  → rebuild API 相关行。

**L272** ``

**L273** `    cleared = session_manager.clear_all()`
  → rebuild API 相关行。

**L274** `    data = report.to_dict()`

**L275** `    data["sessions_cleared"] = cleared`

**L276** `    return RebuildResponse(**data)`

**L277** ``

**L278** ``

**L279** `@router.get("/status", response_model=KnowledgeStatusResponse)`

**L280** `def knowledge_status() -> KnowledgeStatusResponse:`

**L281** `    """返回知识库文档数、分块数、支持格式与文档列表"""`

**L282** `    store = get_knowledge_store()`

**L283** `    data = store.status_dict()`

**L284** `    return KnowledgeStatusResponse(**data)`

**L285** ``

**L286** ``

**L287** `@router.post("/upload", response_model=KnowledgeUploadResponse)`

**L288** `async def upload_document(`

**L289** `    file: UploadFile = File(..., description="企业文档 (.txt / .md / .pdf)"),`

**L290** `) -> KnowledgeUploadResponse:`

**L291** `    """`

**L292** `    上传企业文档到知识库：解析 → 落盘 → 分块 → Chroma 向量索引 → 持久化。`

**L293** ``

**L294** `    Day 26 起支持 Markdown 与 PDF。上传成功后清除服务端会话。`

**L295** `    """`

**L296** `    if not file.filename:`

**L297** `        raise HTTPException(status_code=422, detail="缺少文件名")`

**L298** ``

**L299** `    data = await file.read()`

**L300** `    if not data:`

**L301** `        raise HTTPException(status_code=422, detail="文件内容为空")`

**L302** `    if len(data) > MAX_UPLOAD_BYTES:`

**L303** `        raise HTTPException(status_code=413, detail="文件超过 500KB 上限")`

**L304** ``

**L305** `    try:`

**L306** `        meta = ingest_upload(data, file.filename)`

**L307** `    except ValueError as exc:`

**L308** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L309** `    except NexusError as exc:`

**L310** `        status = 400 if exc.code in (`

**L311** `            "PDF_PARSE_ERROR", "PDF_EMPTY", "UNSUPPORTED_FORMAT", "EMPTY_FILE"`

**L312** `        ) else 500`

**L313** `        raise HTTPException(status_code=status, detail=exc.message) from exc`

**L314** `    except StorageError as exc:`

**L315** `        raise HTTPException(status_code=500, detail=exc.message) from exc`

**L316** `    except UnicodeDecodeError as exc:`

**L317** `        raise HTTPException(status_code=400, detail="文本文件须为 UTF-8 编码") from exc`

**L318** ``

**L319** `    cleared = session_manager.clear_all()`
  → rebuild API 相关行。

**L320** `    store = get_knowledge_store()`

**L321** ``

**L322** `    return KnowledgeUploadResponse(`

**L323** `        filename=meta.name,`

**L324** `        format=meta.format,`

**L325** `        chunk_count=meta.chunk_count,`

**L326** `        document_count=store.document_count,`

**L327** `        total_chunks=store.chunk_count,`

**L328** `        sessions_cleared=cleared,`

**L329** `        index_mode=store.index_mode,`

**L330** `        message=f"文档已入库（{meta.format}），增量索引已更新",`

**L331** `    )`


## 6. 失败恢复

重建失败 mid-loop 可能导致空库——教学环境可重新 bootstrap；生产应事务化或先写临时 store。
