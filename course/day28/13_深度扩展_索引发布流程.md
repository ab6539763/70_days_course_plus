## 附录：doc_parser.py（rebuild 解析链）

### `nexus-agent-platform/src/tools/doc_parser.py` 逐行走读

共 **64** 行。

**L1** `"""`

**L2** `统一文档解析入口 — 按扩展名分发`

**L3** ``

**L4** `需求：ZL-NA-REQ-026`

**L5** `"""`

**L6** ``

**L7** `from __future__ import annotations`

**L8** ``

**L9** `from pathlib import Path`

**L10** ``

**L11** `from core.exceptions import NexusError, StorageError`

**L12** `from tools.parsers.base import FORMAT_LABELS, SUPPORTED_EXTENSIONS, ParsedDocument`
  → rebuild 循环内 parse_bytes 入口。

**L13** `from tools.parsers.markdown_parser import parse_markdown`
  → rebuild 循环内 parse_bytes 入口。

**L14** `from tools.parsers.pdf_parser import parse_pdf`
  → rebuild 循环内 parse_bytes 入口。

**L15** `from tools.parsers.text_parser import parse_plain_text`
  → rebuild 循环内 parse_bytes 入口。

**L16** ``

**L17** ``

**L18** `def detect_format(filename: str) -> str:`
  → rebuild 循环内 parse_bytes 入口。

**L19** `    ext = Path(filename).suffix.lower()`

**L20** `    if ext not in SUPPORTED_EXTENSIONS:`

**L21** `        raise NexusError(`

**L22** `            f"不支持的文件格式: {ext}，支持: {sorted(SUPPORTED_EXTENSIONS)}",`

**L23** `            code="UNSUPPORTED_FORMAT",`

**L24** `        )`

**L25** `    return ext`

**L26** ``

**L27** ``

**L28** `def parse_bytes(data: bytes, filename: str) -> ParsedDocument:`
  → rebuild 循环内 parse_bytes 入口。

**L29** `    """`

**L30** `    将上传字节解析为统一 ParsedDocument。`

**L31** ``

**L32** `    Raises:`

**L33** `        StorageError: 格式不支持或解析失败`

**L34** `        UnicodeDecodeError: 文本文件非 UTF-8`

**L35** `    """`

**L36** `    if not data:`

**L37** `        raise NexusError("文件内容为空", code="EMPTY_FILE")`

**L38** ``

**L39** `    ext = detect_format(filename)`

**L40** `    safe_name = Path(filename).name`

**L41** ``

**L42** `    if ext == ".txt":`

**L43** `        doc = parse_plain_text(data, safe_name)`
  → rebuild 循环内 parse_bytes 入口。

**L44** `    elif ext in (".md", ".markdown"):`

**L45** `        doc = parse_markdown(data, safe_name)`
  → rebuild 循环内 parse_bytes 入口。

**L46** `    elif ext == ".pdf":`

**L47** `        doc = parse_pdf(data, safe_name)`
  → rebuild 循环内 parse_bytes 入口。

**L48** `    else:`

**L49** `        raise NexusError(f"未实现的格式: {ext}", code="UNSUPPORTED_FORMAT")`

**L50** ``

**L51** `    doc.metadata.setdefault("format_label", FORMAT_LABELS.get(ext, ext))`

**L52** `    return doc`

**L53** ``

**L54** ``

**L55** `def supported_formats() -> list[dict[str, str]]:`
  → rebuild 循环内 parse_bytes 入口。

**L56** `    """API 返回的支持格式列表"""`

**L57** `    seen: set[str] = set()`

**L58** `    items: list[dict[str, str]] = []`

**L59** `    for ext in sorted(SUPPORTED_EXTENSIONS):`

**L60** `        if ext in seen:`

**L61** `            continue`

**L62** `        items.append({"extension": ext, "label": FORMAT_LABELS.get(ext, ext)})`

**L63** `        seen.add(ext)`

**L64** `    return items`


## 附录：chunk_strategies.py

### `nexus-agent-platform/src/rag/chunk_strategies.py` 逐行走读

共 **132** 行。

**L1** `"""`

**L2** `分块策略 — 固定窗口 vs Markdown 章节`

**L3** ``

**L4** `需求：ZL-NA-REQ-026`

**L5** `"""`

**L6** ``

**L7** `from __future__ import annotations`

**L8** ``

**L9** `from dataclasses import dataclass`

**L10** ``

**L11** `from rag.chunker import TextChunk, chunk_text`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L12** `from tools.parsers.base import DocumentSection, ParsedDocument`

**L13** ``

**L14** ``

**L15** `@dataclass`

**L16** `class ChunkStrategyResult:`

**L17** `    """策略对比结果"""`

**L18** ``

**L19** `    strategy: str`

**L20** `    chunk_count: int`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L21** `    chunks: list[TextChunk]`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L22** `    preview: str`

**L23** ``

**L24** ``

**L25** `def chunk_fixed_window(`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L26** `    text: str,`

**L27** `    *,`

**L28** `    source: str,`

**L29** `    chunk_size: int = 200,`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L30** `    overlap: int = 40,`

**L31** `) -> list[TextChunk]:`

**L32** `    """Day 19 默认滑动窗口策略"""`

**L33** `    return chunk_text(text, chunk_size=chunk_size, overlap=overlap, source=source)`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L34** ``

**L35** ``

**L36** `def chunk_markdown_sections(`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L37** `    doc: ParsedDocument,`

**L38** `    *,`

**L39** `    max_chars: int = 400,`

**L40** `    overlap: int = 40,`

**L41** `) -> list[TextChunk]:`

**L42** `    """`

**L43** `    按 Markdown 章节分块：每节独立成块，过长再滑动切分。`

**L44** `    """`

**L45** `    if not doc.sections:`

**L46** `        return chunk_fixed_window(doc.plain_text, source=doc.filename)`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L47** ``

**L48** `    chunks: list[TextChunk] = []`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L49** `    global_index = 0`

**L50** ``

**L51** `    for section in doc.sections:`

**L52** `        header = f"[{section.title}] "`

**L53** `        body = section.body.strip()`

**L54** `        piece = f"{header}{body}" if body else section.title`

**L55** ``

**L56** `        if len(piece) <= max_chars:`

**L57** `            chunks.append(`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L58** `                TextChunk(`

**L59** `                    chunk_id=f"{doc.filename}:sec:{section.index}",`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L60** `                    text=piece,`

**L61** `                    source=doc.filename,`

**L62** `                    index=global_index,`

**L63** `                    start_char=0,`

**L64** `                    end_char=len(piece),`

**L65** `                )`

**L66** `            )`

**L67** `            global_index += 1`

**L68** `            continue`

**L69** ``

**L70** `        sub_chunks = chunk_text(`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L71** `            piece,`

**L72** `            chunk_size=max_chars,`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L73** `            overlap=overlap,`

**L74** `            source=doc.filename,`

**L75** `        )`

**L76** `        for sub in sub_chunks:`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L77** `            chunks.append(`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L78** `                TextChunk(`

**L79** `                    chunk_id=f"{doc.filename}:sec:{section.index}:{sub.index}",`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L80** `                    text=sub.text,`

**L81** `                    source=doc.filename,`

**L82** `                    index=global_index,`

**L83** `                    start_char=sub.start_char,`

**L84** `                    end_char=sub.end_char,`

**L85** `                )`

**L86** `            )`

**L87** `            global_index += 1`

**L88** ``

**L89** `    return chunks`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L90** ``

**L91** ``

**L92** `def chunk_from_parsed(`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L93** `    doc: ParsedDocument,`

**L94** `    *,`

**L95** `    strategy: str = "auto",`

**L96** `    chunk_size: int = 200,`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L97** `    overlap: int = 40,`

**L98** `) -> list[TextChunk]:`

**L99** `    """根据文档类型与策略选择分块方式"""`

**L100** `    chosen = strategy`

**L101** `    if strategy == "auto":`

**L102** `        chosen = "markdown" if doc.format == "markdown" and doc.sections else "fixed"`

**L103** ``

**L104** `    if chosen == "markdown":`

**L105** `        return chunk_markdown_sections(doc, max_chars=chunk_size, overlap=overlap)`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L106** `    return chunk_fixed_window(`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L107** `        doc.plain_text,`

**L108** `        source=doc.filename,`

**L109** `        chunk_size=chunk_size,`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L110** `        overlap=overlap,`

**L111** `    )`

**L112** ``

**L113** ``

**L114** `def compare_strategies(doc: ParsedDocument) -> list[ChunkStrategyResult]:`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L115** `    """对比固定窗口与章节策略（教学演示用）"""`

**L116** `    fixed = chunk_fixed_window(doc.plain_text, source=doc.filename)`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L117** `    section = chunk_markdown_sections(doc) if doc.sections else fixed`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L118** ``

**L119** `    return [`

**L120** `        ChunkStrategyResult(`

**L121** `            strategy="fixed",`

**L122** `            chunk_count=len(fixed),`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L123** `            chunks=fixed,`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L124** `            preview=fixed[0].text[:80] if fixed else "",`

**L125** `        ),`

**L126** `        ChunkStrategyResult(`

**L127** `            strategy="markdown",`

**L128** `            chunk_count=len(section),`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L129** `            chunks=section,`
  → rebuild 使用 chunk_from_parsed 应用 chunk_config。

**L130** `            preview=section[0].text[:80] if section else "",`

**L131** `        ),`

**L132** `    ]`


# 深度扩展：索引发布流程

## 蓝绿发布类比

| 蓝绿 | NexusAgent 教学 |
|------|-----------------|
| 绿环境新版本 | rebuild 后新 store |
| 切流量 | clear sessions + 新检索 |
| 回滚 | 恢复备份 store.json + rebuild |

## 变更窗口

企业实践常在低峰期 rebuild，配合：

- 只读模式 flag（未实现）  
- 双 store 切换（未实现）  

## 与 CI/CD

```yaml
- name: Knowledge rebuild smoke
  run: |
    PYTHONPATH=src pytest tests/day28/ -q
    python3 src/day28/rebuild_api_demo.py
```

## 审计

`last_rebuilt_at` + Git 中 uploads 版本 + evaluate JSON = 完整追溯链。

## Day 29 展望

rebuild 调用 `_rebuild_index` 会 reset Chroma；Day 29 学习向量落盘细节。
