# Day 8 课堂练习册

## 练习 1

```python
class A:
    def __init__(self, x):
        self.x = x
a = A(10)
```

`a.x` 是多少？**10**

## 练习 2

`msg.validate()` 返回 None 表示？**校验通过**

## 练习 3

`ChatMessage.from_dict` 是实例方法还是类方法？**类方法**（@classmethod）

## 练习 4

`print(msg)` 调用哪个魔术方法？**`__str__`**

## 练习 5

VALID_ROLES 有几个合法值？**3**（system, user, assistant）

## 练习 6

`to_api_message()` 返回几个键？**2**

## 练习 7

MessageHistory 的 `len(history)` 依赖哪个魔术方法？**`__len__`**

## 练习 8

dict `{"role":"user","content":"hi"}` 用一行构造 ChatMessage：

```python
ChatMessage.from_dict({"role": "user", "content": "hi"})
```

## 练习 9

`history.add_user("")` 成功还是失败？**失败**（空内容）

## 练习 10

ChatMessage 定义在哪个目录？**src/models/message.py**

---

## 练习 11（代码追踪）

阅读 `message_cli.py` 中 `handle_import_sample` 函数，回答：

1. 样例 JSON 路径是什么？
2. 从 JSON 到 ChatMessage 经过哪两个关键步骤？

**答案**：

1. `day05/sample_data/chat_completion.json`
2. `choices[0]["message"]` → `ChatMessage.from_dict()` → `history.add()`

## 练习 12（设计题）

为何 `MessageHistory.add()` 在追加前调用 `message.validate()`，而不是假设调用方已校验？

**答**：防御性编程；单一入口保证历史记录中不存在非法消息；CLI、测试、未来 API 层都受益。

## 练习 13

写出运行 OOP 演示与消息 CLI 的两条命令。

```bash
python3 src/day08/oop_demos.py
python3 src/day08/message_cli.py
```

（需在 `nexus-agent-platform` 目录下执行。）

---
