# Day 8 授课实录（节选）

**讲师**：陈默  
**日期**：2026-07-13

---

**陈默**（9:00）：Sprint 2 第一天，面向对象。你们已经会 dict 了，class 不是新数据，是**组织代码的方式**。

**学员**：self 能不能改名？  
**陈默**：语法上可以，改了会被所有 Python 程序员打。约定俗成 self。

**学员**：ChatMessage 和 dict 能混用吗？  
**陈默**：边界上 `from_dict` / `to_dict` 转换。函数内部尽量统一用 ChatMessage，减少 `.get` 链条。

**林晓**（15:00）：写 `message_cli` 时先测 `ChatMessage.validate`，再测 `History.add`，最后才拼菜单——**自底向上**。

**陈默**（17:00）：谁把 `models/message.py` 写进 `day08/` 了？models 是平台资产，不是作业草稿。

---

*整理：培训组*
