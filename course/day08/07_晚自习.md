# Day 8 晚自习

1. 运行 `oop_demos.py` 五次，每次修改一个参数观察输出
2. 用 `message_cli` 手动构建 5 轮对话并保存
3. 阅读 `models/message.py` 全文，标注每个方法的一行注释
4. 预习：什么是「抽象类」？`abc` 模块做什么？

```bash
python3 -c "import abc; help(abc.ABC)"
```

---

*Day 9 将实现 BaseModel 抽象基类。*
