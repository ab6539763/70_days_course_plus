# Day 8 课后作业

## 题目一：手写 ChatMessage（20 分）

在不看源码情况下，手写 `ChatMessage` 的 `__init__`、`validate`、`to_dict` 三个方法（可纸笔或 `exercises/day08/`）。

---

## 题目二：from_dict 练习（15 分）

给定：

```python
raw = {"role": "ASSISTANT", "content": "  你好  ", "created_at": "2026-01-01T00:00:00Z"}
```

`ChatMessage.from_dict(raw)` 后，`role` 和 `content` 分别是什么？为什么？

---

## 题目三：MessageHistory 扩展（25 分）

在 `MessageHistory` 添加 `clear_non_system()` 方法：删除所有非 system 消息，保留 system 提示。

编写至少 2 个 pytest 用例。

---

## 题目四：Contact 类迁移（20 分）

阅读 `contact_class.py`，写 200 字说明：若用 `Contact` 类重写 `contacts_manager`，结构会如何变化？

---

## 题目五：API 格式对照（20 分）

1. 画出 `to_dict()` 与 `to_api_message()` 字段差异表
2. 说明为何 API 请求不需要 `created_at`

---

## 加分题（+10 分）

实现 `ChatMessage.merge(other)` 实例方法：若 role 相同则拼接 content（用 `\n`），否则返回新 ChatMessage 列表 `[self, other]`。

---

*答案：[09_作业答案.md](09_作业答案.md)*
