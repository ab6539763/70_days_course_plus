# Day 39 授课实录

**09:05** 讲师展示上一轮 Sprint 遗留问题：客服机器人遇到「查电话」「查收益」混合问句时只能走固定分支，新增场景要改分支代码，运营等不起。
**09:30** 白板画出核心方案：引入 ReAct 循环：模型先 Thought（判断该用哪个工具），再 Action 调用 ToolExecutor，观察 Observation 后决定继续或给出 Final Answer。
**10:10** `ReActAgent` live coding。
**10:55** 走读 `agent/react_agent.py` 配置类。
**11:20** 第一次跑通 demo，全班确认 `agent_trace` 输出正常。

**14:05** demo 案例逐条讲解：用户先问「客服电话」再问「收益率」。
**14:50** 讲 `agent_mode=false` 时的兼容路径。
**15:15** `PUT /api/agent/react-config` 现场演示配置变更。
**15:58** pytest 第 17 个全绿。
**16:42** 预告 Day 40：AgentExecutor 框架工具链。

---

## 问答实录

**15:30 学员**：`agent_trace` 会不会让响应体过大？
**讲师**：默认返回，但可以通过配置关闭详细 trace，只保留最终 reply。

**16:05 学员**：关闭 `agent_mode` 最快的方式？
**讲师**：`PUT /api/agent/react-config` 把 `enabled` 设为 `false`，无需改代码。

---

## 讲师自评

- ReActAgent live coding 时间刚好
- Lab 环节学员普遍能独立完成
- Day 40 预告已铺垫

---

## 课后作业布置原话

「tests/day39/ 必须全绿再提交，作业 A 的案例脚本周日 23:59 前 push。」
