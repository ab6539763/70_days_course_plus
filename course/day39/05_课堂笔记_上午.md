# Day 39 课堂笔记（上午）

**09:00–09:40** 第一节：客服机器人遇到「查电话」「查收益」混合问句时只能走固定分支，...
**09:40–10:30** 第二节：agent/react_agent.py
**10:30–11:20** 第三节：ReActAgent 核心循环
**11:20–12:00** 第四节：与 Day 38 对比走读

---

## 第一节：问题背景（40 min）

客服机器人遇到「查电话」「查收益」混合问句时只能走固定分支，新增场景要改分支代码，运营等不起。

引入 ReAct 循环：模型先 Thought（判断该用哪个工具），再 Action 调用 ToolExecutor，观察 Observation 后决定继续或给出 Final Answer。

---

## 第二节：ReActAgent — Thought/Action/Observation 循环（50 min）

`agent/react_agent.py` 是本日配置的单一真相源，字段随日递增但结构一致：`enabled`、若干阈值/开关、`validate()` 校验。

**10:15** 课堂练习：写出三组非法配置，预测 `validate()` 抛出的异常信息。

---

## 第三节：ReActAgent（50 min）

`ReActAgent` 是今天的核心调度类，负责把「输入 query → 决策 → 调用工具 → 汇总」串起来，并把每一步记录进 `agent_trace`，便于教学演示与审计。

**10:55** 板书：Day38 让校验能重试；Day39 让 Agent 能自己选工具。

---

## 第四节：与 Day 38 对比（40 min）

agent/react_config.py — ReactConfig — max_steps / use_session_history

**11:40** 强调：Day 38（多轮 Self-RAG 校验重试）的能力仍然保留，今天是在其基础上新增一层。
