# Day 5 FAQ

## Q1: JSONDecodeError

检查：双引号、尾随逗号、单引号、BOM 头。

## Q2: 中文变成 \u4e2d

`json.dumps(data, ensure_ascii=False)`

## Q3: datetime 无法序列化

```python
import json
from datetime import datetime
json.dumps({"t": datetime.now().isoformat()})
```

## Q4: KeyError 链式访问

用 `.get("k", {})` 代替 `["k"]`。
