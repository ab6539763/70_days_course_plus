# Day 5 企业案例集：JSON 在 NexusAgent 中的 6 个场景

## 案例 1：待办持久化（今日）

`data/todos.json` — 本地配置存储模式。

## 案例 2：Chat API 请求体（Day 12）

```json
{
  "model": "deepseek-chat",
  "messages": [{"role": "user", "content": "你好"}]
}
```

## 案例 3：RAG 检索结果（Day 30）

```python
chunks = [
    {"text": "...", "score": 0.92, "source": "doc1.pdf"},
]
```

## 案例 4：Function Calling（Day 19）

```json
{
  "tool_calls": [{
    "function": {"name": "search", "arguments": "{\"q\":\"天气\"}"}
  }]
}
```

注意 `arguments` 是 **JSON 字符串**，需二次 `json.loads`。

## 案例 5：评估报告（Day 34）

Ragas 输出 JSON 报告存入文件。

## 案例 6：Docker 环境变量（Day 56）

`.env` 与 `config.json` 配合部署。

---

## 讨论：为何 arguments 要 JSON 字符串？

历史兼容性；模型输出的是文本，嵌套 JSON 作为字符串传递更安全。

---

*Day 5 企业案例集完。*
