# Day 5 课堂练习册

## 练习 A：dict 基础

```python
config = {"model": "deepseek-chat", "temperature": 0.7}
config["max_tokens"] = 1024
print(config["model"])
print(config.get("top_p", 1.0))
del config["temperature"]
```

## 练习 B：嵌套 API 访问

```python
resp = {"choices": [{"message": {"role": "assistant", "content": "Hi"}}]}
role = resp["choices"][0]["message"]["role"]
# 安全版
role = resp.get("choices", [{}])[0].get("message", {}).get("role", "")
```

## 练习 C：json 四种方法

```python
import json
s = '{"a": 1}'
d = json.loads(s)
s2 = json.dumps(d)
with open("t.json", "w") as f:
    json.dump(d, f)
with open("t.json") as f:
    d2 = json.load(f)
```

## 练习 D：合并 dict

```python
defaults = {"temperature": 0.7, "max_tokens": 512}
overrides = {"temperature": 0.9}
merged = {**defaults, **overrides}
```

## 练习 E：从 API 提取 token

```python
def get_total_tokens(resp):
    return resp.get("usage", {}).get("total_tokens", 0)
```

## 练习 F：list of dict 过滤

```python
todos = [{"id": 1, "done": True}, {"id": 2, "done": False}]
pending = [t for t in todos if not t["done"]]
```

---

*完成后再实现 todo_manager_v2 与 api_response_parser。*
