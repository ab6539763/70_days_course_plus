# Day 5 课后作业

**总分**：100 分

## 题目一：dict 操作（20 分）

`exercises/exercise_01_dict_ops.py`：给定用户 dict，完成 get、增删改、items 遍历、推导式生成 `{name: len(name)}`。

## 题目二：嵌套访问（20 分）

`exercise_02_nested.py`：从嵌套 dict 安全提取 `response["data"]["user"]["email"]`，不存在返回 `"N/A"`。

## 题目三：JSON 文件（25 分）

`exercise_03_json_file.py`：手动创建 `my_config.json`，含 name/version/features list，再 load 打印。

## 题目四：扩展 API 解析器（25 分）

支持解析 `error_response.json`，提取 `error.message` 和 `error.code`。

## 题目五：思考题（10 分）

1. dict 和 JSON object 区别？
2. 为什么 API 响应用 JSON 不用 XML？
3. todos 从 list of list 改 list of dict 的好处？

## 加分题（+10 分）

合并两个 JSON 文件中的 todos 列表并去重（按 id）。

---

*答案：[09_作业答案.md](09_作业答案.md)*
