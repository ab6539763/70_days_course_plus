# Day 16 流程图与示意图

---

## 1. 端到端数据流

```mermaid
flowchart LR
    A[用户输入] --> B[ChatMessage 列表]
    B --> C[build_stream_request_body]
    C --> D["POST stream=true"]
    D --> E[SSE 字节流]
    E --> F[逐行 decode UTF-8]
    F --> G[parse_sse_line]
    G --> H{done?}
    H -->|是| Z[StreamCompletionResult]
    H -->|否| I[parse_stream_chunk]
    I --> J[StreamAccumulator]
    J --> K[on_delta 回调]
    K --> F
    I --> L[记录 usage / finish_reason]
    L --> F
```

## 2. SSE 报文结构（stream_mock.sse）

```
┌─────────────────────────────────────────────┐
│ data: {"choices":[{"delta":{"role":...}}]}  │  ← 首包常含 role，content 可能为空
├─────────────────────────────────────────────┤
│ data: {"choices":[{"delta":{"content":"你"}}]} │
│ data: {"choices":[{"delta":{"content":"好"}}]} │
│ ... 若干 content delta ...                    │
├─────────────────────────────────────────────┤
│ data: {..., "finish_reason":"stop",          │
│        "usage":{"prompt_tokens":32,...}}     │  ← 末包带 usage
├─────────────────────────────────────────────┤
│ data: [DONE]                                  │  ← 协议终止
└─────────────────────────────────────────────┘
```

## 3. parse_sse_line 决策树

```mermaid
flowchart TD
    Start[输入 line] --> Strip[strip 空白]
    Strip --> Empty{空行?}
    Empty -->|是| None1[返回 None]
    Strip --> Comment{以 : 开头?}
    Comment -->|是| None2[返回 None]
    Strip --> Prefix{以 data: 开头?}
    Prefix -->|否| None3[返回 None]
    Prefix -->|是| Extract[取 data: 后 payload]
    Extract --> DoneCheck{payload == [DONE]?}
    DoneCheck -->|是| Done[返回 done True]
    DoneCheck -->|否| JSON[json.loads]
    JSON -->|失败| Err[APIError]
    JSON -->|成功| Dict[返回 chunk dict]
```

## 4. Day 12 vs Day 16 读取对比

```mermaid
sequenceDiagram
    participant C as Client
    participant S as Server

    Note over C,S: Day 12 阻塞
    C->>S: POST stream=false
    S-->>C: 完整 JSON body
    C->>C: resp.read() 一次
    C->>C: json.loads 一次

    Note over C,S: Day 16 流式
    C->>S: POST stream=true
    loop 直到 [DONE]
        S-->>C: 一行 data: ...
        C->>C: parse_sse_line
        C->>C: on_delta
    end
```

## 5. on_delta 与 Accumulator 分工

```
         ┌──────────────┐
 chunk ──►│ parse_stream │──► delta_content
         └──────┬───────┘
                │
       ┌────────┴────────┐
       ▼                 ▼
┌─────────────┐   ┌──────────────┐
│ on_delta    │   │ StreamAccum. │
│ 打字机/UI   │   │ 完整字符串    │
└─────────────┘   └──────┬───────┘
                         │
                         ▼
                  ChatMessage assistant
```

## 6. Mock 模式路径

```mermaid
flowchart TD
    Env{NEXUS_LLM_MOCK=1?}
    Env -->|是| File[stream_mock.sse]
    Env -->|否| Net[default_stream_transport]
    File --> Split[splitlines]
    Split --> Iter[iter_sse_events]
    Net --> UrlOpen[urlopen POST]
    UrlOpen --> LineIter[for raw_line in resp]
    LineIter --> Parse[parse_sse_line]
    Iter --> Parse
```

## 7. 错误传播路径

```mermaid
flowchart TD
    Req[urllib POST] --> OK{HTTP 状态}
    OK -->|2xx| Read[逐行读取]
    OK -->|4xx/5xx| HE[HTTPError]
    HE --> Body[exc.read body]
    Body --> API1[APIError + code]
    Read --> Net{网络}
    Net -->|URLError| API2[APIError]
    Read --> Parse[JSON 解析]
    Parse -->|失败| API3[APIError]
```

## 8. 课堂白板示意（ASCII）

```
  messages + config
        │
        ▼
  ┌─────────────┐     stream=True
  │ build_body  │──────────────────► API
  └─────────────┘
        ▲
        │         data: {chunk}
        │         data: {chunk}
        │         data: [DONE]
        └──────── transport 逐行
```

## 9. 与 Day 17 衔接示意

```mermaid
flowchart LR
    PT[PromptTemplate Day17] --> SP[system_prompt 字符串]
    SP --> SC[stream_chat]
    SC --> ST[stream_complete]
    ST --> OD[on_delta 显示]
```

流式模块消费的是 **已组装好的 messages**；Prompt 工程在 messages 组装阶段介入。
