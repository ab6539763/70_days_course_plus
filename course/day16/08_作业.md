# Day 16 作业

**截止**：次日上午课前  
**提交**：仓库 `homework/day16/` 或学习平台指定路径

---

## 作业 A：SSE 解析基础（必做，30 分）

### 要求

在 `homework/day16/sse_lab.py` 中实现：

1. 函数 `count_content_deltas(sse_text: str) -> int`：统计会产生非空 `delta_content` 的事件数  
2. 函数 `reconstruct_text(sse_text: str) -> str`：拼接完整助手回复  
3. 必须使用项目内的 `iter_sse_events` 与 `parse_stream_chunk`

### 验收

```bash
export PYTHONPATH=src
python3 homework/day16/sse_lab.py
# 对 stream_mock.sse 输出 delta 数与完整句子
```

---

## 作业 B：打字机回调（必做，30 分）

### 要求

编写 `homework/day16/typewriter_demo.py`：

- 使用 `StreamingLLMClient` + `NEXUS_LLM_MOCK=1`
- `on_delta` 实现每字延迟 50ms（`time.sleep`）的打字机效果
- 结束后打印 `usage_summary()`

### 提示

```python
import time
def slow_delta(ch: str) -> None:
    print(ch, end="", flush=True)
    time.sleep(0.05)
```

---

## 作业 C：对照分析（必做，20 分）

阅读 `llm/client.py` 与 `llm/streaming.py`，填写表格（提交 `homework/day16/day12_vs_day16.md`）：

| 对比项 | Day 12 complete | Day 16 stream_complete |
|--------|-----------------|------------------------|
| 请求体关键字段 | | |
| HTTP 读取方式 | | |
| 解析入口函数 | | |
| 用户可见输出时机 | | |
| usage 来源 | | |

（可参考课件 18，但须用自己的话补充至少 2 条「踩坑」观察）

---

## 作业 D：思考题（必做，20 分）

提交 `homework/day16/essay.md`，每题 ≥80 字：

1. 解释 `data: [DONE]` 在协议层与代码层分别起什么作用。  
2. 为何 `on_delta` 不应执行耗时网络请求？  
3. Day 15 的 token 计量如何与流式最后一 chunk 衔接？  
4. 预习设想：Day 17 Prompt 模板如何与 `stream_chat` 组合？（可画简图）

---

## 加分项（+10 分）

实现 `homework/day16/custom_mock.py`：

- 使用 `mock_stream_from_text` 自定义回复「流式作业已完成」
- 注入 `StreamingLLMClient(stream_transport=...)`
- 附运行截图或终端复制文本

---

## 评分标准

| 等级 | 标准 |
|------|------|
| 优秀 | A+B+C+D 全过，代码风格与项目一致，思考题有深度 |
| 良好 | 必做全过，个别边界未处理 |
| 及格 | A 或 B 有一项缺陷，思考题略简 |
| 不及格 | 未使用项目 API 或抄袭 SSE 解析逻辑 |

---

## 学术诚信

允许讨论协议与思路，**禁止**复制粘贴他人 `parse_sse_line` 实现。必须 import 项目模块。
