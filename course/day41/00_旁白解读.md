# Day 41 旁白解读

Day40 AgentExecutor 用一个线性 while 循环调度工具；今天要解决的问题是：产品要求「先规划、再执行工具、最后单独一步生成答案」三个阶段要能分别插桩、分别测试，AgentExecutor 的单一 while 循环耦合太紧。

赵岩在白板上画了一条演进线：

```
Day 40 AgentExecutor 框架工具链  →  Day 41 StateGraph 状态图编排
```

**今日目标**：引入轻量 StateGraph：add_node 注册 planner/tool_runner/answer 三个节点，add_conditional_edges 控制何时回到 planner、何时进入 answer，node_path 记录实际走过的节点序列。

**行动**：Day42 预习 人工审批工作流。

---

## 为什么是今天

Day40 AgentExecutor 是线性 invoke；Day41 用 StateGraph 显式声明节点与边，为后续加审批节点铺路。团队复盘上一轮迭代时发现，光靠 Day40 的能力还不足以支撑运营的新需求，于是把「StateGraph 状态图编排」排进了今天的 Sprint。

## 一句话总结

Day40 AgentExecutor 是线性 invoke；Day41 用 StateGraph 显式声明节点与边，为后续加审批节点铺路。
