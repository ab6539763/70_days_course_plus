# Day 41 课堂笔记（下午）

**14:00–14:30** 第五节：day41/graph_demo.py 现场
**14:30–15:20** 第六节：RAGAgentGraph 决策逻辑源码
**15:20–16:00** 第七节：/api/agent/graph-config API
**16:00–16:45** 第八节：pytest + chat 回归

---

## 第五节：demo 现场（30 min）

```bash
PYTHONPATH=src python3 src/day41/graph_demo.py
```

记录每个案例的委派/决策路径是否符合预期。

---

## 第六节：核心决策逻辑（50 min）

课堂上逐行读 `RAGAgentGraph` 的主循环，重点讲：何时判定「已经拿到答案」、何时继续下一步、`graph_trace` 每一步记录哪些字段。

---

## 第七节：API（40 min）

```bash
curl -s http://127.0.0.1:8000/api/agent/graph-config | jq .
curl -s -X PUT http://127.0.0.1:8000/api/agent/graph-config \
  -H 'Content-Type: application/json' -d '{"enabled": true}' | jq .
curl -s -X POST http://127.0.0.1:8000/api/agent/graph-preview \
  -H 'Content-Type: application/json' -d '{"query": "测试问题"}' | jq .

```

---

## 第八节：测试与 chat（45 min）

```bash
pytest tests/day41/ -v
```

**16:30** 里程碑：17 passed。

**16:40** 现场跑一次 `POST /api/chat` + `graph_mode: true`，对照响应里的 `graph_trace`。
