# Day 41 课堂笔记（上午）

**09:00–09:40** 第一节：产品要求「先规划、再执行工具、最后单独一步生成答案」三个阶段...
**09:40–10:30** 第二节：agent/state_graph.py
**10:30–11:20** 第三节：RAGAgentGraph 核心循环
**11:20–12:00** 第四节：与 Day 40 对比走读

---

## 第一节：问题背景（40 min）

产品要求「先规划、再执行工具、最后单独一步生成答案」三个阶段要能分别插桩、分别测试，AgentExecutor 的单一 while 循环耦合太紧。

引入轻量 StateGraph：add_node 注册 planner/tool_runner/answer 三个节点，add_conditional_edges 控制何时回到 planner、何时进入 answer，node_path 记录实际走过的节点序列。

---

## 第二节：StateGraph — add_node/add_edge/compile/invoke（50 min）

`agent/state_graph.py` 是本日配置的单一真相源，字段随日递增但结构一致：`enabled`、若干阈值/开关、`validate()` 校验。

**10:15** 课堂练习：写出三组非法配置，预测 `validate()` 抛出的异常信息。

---

## 第三节：RAGAgentGraph（50 min）

`RAGAgentGraph` 是今天的核心调度类，负责把「输入 query → 决策 → 调用工具 → 汇总」串起来，并把每一步记录进 `graph_trace`，便于教学演示与审计。

**10:55** 板书：Day40 AgentExecutor 是线性 invoke；Day41 用 StateGraph 显式声明节点与边，为后续加审批节点铺路。

---

## 第四节：与 Day 40 对比（40 min）

agent/graph_config.py — GraphConfig — max_iterations

**11:40** 强调：Day 40（AgentExecutor 框架工具链）的能力仍然保留，今天是在其基础上新增一层。
