# ZL-NA-REQ-041 需求扩展 — 用户故事

## US-41-01 插桩审计

**场景**：合规要求记录每一步决策节点

**期望**：node_path 直接给出 planner→tool_runner→answer 的完整轨迹

## US-41-02 提前收敛

**场景**：简单问题不需要工具

**期望**：planner 判定后条件边直接跳到 answer，跳过 tool_runner

## US-41-03 为审批铺路

**场景**：Day42 需要在工具执行后插入人工审批

**期望**：只需在 tool_runner 和 answer 之间插入新节点，无需重写整个循环

---

## 边界：空 query

`invoke("")` / `run("")` → 返回「请输入有效问题。」，不进入决策循环。

## 与上一日关系

Day40 AgentExecutor 是线性 invoke；Day41 用 StateGraph 显式声明节点与边，为后续加审批节点铺路。

## 风险登记

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| mock 决策命中率不足 | 中 | 演示效果打折 | 文档标注局限，预留真实模型接口 |
| 配置项过多导致运维困惑 | 低 | 排障变慢 | status 接口统一暴露当前配置 |

## 发布检查清单（扩展）

- [ ] `tests/day41/` 全绿
- [ ] `day41/graph_demo.py` / `day41/graph_api_demo.py` 输出正常
- [ ] PACKAGE_STRUCTURE.md 更新
- [ ] CI Day 41 job 添加
- [ ] 课件 regenerate ≥100k
- [ ] PR 关联 ZL-NA-REQ-041
