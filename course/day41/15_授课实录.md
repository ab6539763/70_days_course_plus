# Day 41 授课实录

**09:05** 讲师展示上一轮 Sprint 遗留问题：产品要求「先规划、再执行工具、最后单独一步生成答案」三个阶段要能分别插桩、分别测试，AgentExecutor 的单一 while 循环耦合太紧。
**09:30** 白板画出核心方案：引入轻量 StateGraph：add_node 注册 planner/tool_runner/answer 三个节点，add_conditional_edges 控制何时回到 planner、何时进入 answer，node_path 记录实际走过的节点序列。
**10:10** `RAGAgentGraph` live coding。
**10:55** 走读 `agent/state_graph.py` 配置类。
**11:20** 第一次跑通 demo，全班确认 `graph_trace` 输出正常。

**14:05** demo 案例逐条讲解：合规要求记录每一步决策节点。
**14:50** 讲 `graph_mode=false` 时的兼容路径。
**15:15** `PUT /api/agent/graph-config` 现场演示配置变更。
**15:58** pytest 第 17 个全绿。
**16:42** 预告 Day 42：人工审批工作流。

---

## 问答实录

**15:30 学员**：`graph_trace` 会不会让响应体过大？
**讲师**：默认返回，但可以通过配置关闭详细 trace，只保留最终 reply。

**16:05 学员**：关闭 `graph_mode` 最快的方式？
**讲师**：`PUT /api/agent/graph-config` 把 `enabled` 设为 `false`，无需改代码。

---

## 讲师自评

- RAGAgentGraph live coding 时间刚好
- Lab 环节学员普遍能独立完成
- Day 42 预告已铺垫

---

## 课后作业布置原话

「tests/day41/ 必须全绿再提交，作业 A 的案例脚本周日 23:59 前 push。」
