# Day 41 作业答案

## A 参考答案要点

| query | 预期委派/决策结果 |
|-------|-------------------|
| 合规要求记录每一步决策节点 | node_path 直接给出 planner→tool_runner→answer 的完整轨迹 |
| 简单问题不需要工具 | planner 判定后条件边直接跳到 answer，跳过 tool_runner |
| Day42 需要在工具执行后插入人工审批 | 只需在 tool_runner 和 answer 之间插入新节点，无需重写整个循环 |

## B 答案

1. 拿到 Final Answer，或达到 `max_iterations`/`max_steps` 上限。
2. `/api/agent/graph-config` 读写持久化配置；`/api/agent/graph-preview` 是无状态一次性预览，不落盘。
3. 行为与升级前完全一致——新逻辑默认不介入。

## C Lab 要点

必须体现一次真实 curl/TestClient 调用与其响应截图。

## D 示例输出

```
graph: enabled=true
```

## E bonus

```python
def test_empty_query_returns_placeholder():
    outcome = runner.invoke("")
    assert "请输入有效问题" in outcome.reply
```

---

## F 参考答案

关闭 `graph_mode` 的场景：新逻辑出现异常、需要临时回退、A/B 测试对照组。

---

## 讲评要点（讲师用）

作业 A 最常见错误：忘记结合 `history` 参数测试多轮场景。
作业 B 第 2 问：容易把 preview 和 config 接口混淆，需要强调「预览不落盘」。
