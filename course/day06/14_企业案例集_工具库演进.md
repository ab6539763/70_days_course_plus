# Day 6 企业案例集：工具库演进

## 案例 1：requests 库

业务代码 `requests.get(url)` — HTTP 细节封装在库内，业务只关心「拿 JSON」。

## 案例 2：灵犀 formatters

`format_box_report` 统一 CLI 输出样式，未来 Web 可换 HTML formatter，接口类似。

## 案例 3：validators 集中校验

API 入参校验与 CLI 共用 `parse_priority`，避免「Web 能传 urgent、CLI 不能」的不一致。

## 案例 4：技术债偿还

Day 5 在 todo_manager_v2 内重复了 Day 2 文本函数；Day 6 偿还债务，DRY。

## 案例 5：递归在 Agent 中的影子

ReAct 循环：思考 → 行动 → 观察 → 再思考，可建模为递归或 while；深度限制防死循环（明天 while 复习）。

## 案例 6：lambda 在排序中的局限

复杂 key 应具名函数：

```python
def todo_sort_key(todo):
    return (todo.get("done"), -priority_rank(todo.get("priority")))
```

可读性优于嵌套 lambda。

## 案例 7：包结构预告

```
src/
  utils/
  day06/
  services/   # Day 20+
  api/        # Day 25+
```

`utils` 是横切关注点，全项目共享。
