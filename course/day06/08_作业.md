# Day 6 课后作业

## 题目一：函数基础（20 分）

在 `nexus-agent-platform/exercises/day06/exercise_01_functions.py` 中实现：

1. `calc_bmi(weight: float, height: float) -> float` — BMI = 体重(kg) / 身高(m)²
2. `bmi_category(bmi: float) -> str` — 返回 `"偏瘦"` / `"正常"` / `"偏胖"` / `"肥胖"`（标准可自定并写在注释里）

**验收**：`main` 中打印两个测试用例结果。

---

## 题目二：*args 与 **kwargs（20 分）

`exercise_02_args.py`：

```python
def build_prompt(template: str, **variables) -> str:
    """用 template.format(**variables) 或等价方式填充占位符"""
```

示例：`build_prompt("你好 {name}，今日任务：{task}", name="张三", task="utils")`  
→ `"你好 张三，今日任务：utils"`

---

## 题目三：lambda 排序（15 分）

给定：

```python
todos = [
    {"id": 1, "priority": 2, "done": True},
    {"id": 2, "priority": 1, "done": False},
    {"id": 3, "priority": 3, "done": False},
]
```

用**一行** `sorted(...)` + lambda：先按 `done`（未完成在前），再按 `priority` 升序。

---

## 题目四：阅读与扩展 utils（25 分）

1. 阅读 `src/utils/validators.py`，说明 `require_non_empty` 为何返回 `str | None` 而不是 `bool`。（5 分）
2. 为 `formatters.format_todo_line` 写 2 个单元测试用例（可追加到 `tests/test_utils.py`）。（10 分）
3. 在 `todo_manager_v3.py` 中标出所有 `from utils` 的 import 行，并说明各自用途。（10 分）

---

## 题目五：思考题（20 分）

1. 默认参数为什么不能用 `list` 或 `dict`？用代码示例说明。（7 分）
2. `utils` 和 `day06` 业务代码如何划分？各举 2 个函数例子。（7 分）
3. 什么情况下用 `lambda`，什么情况下必须用 `def`？（6 分）

---

## 加分题（+10 分）

递归实现斐波那契第 n 项，并加**记忆化**（`dict` 缓存）避免重复计算：

```python
def fib(n: int, memo: dict | None = None) -> int:
    ...
```

`fib(50)` 应在秒级完成。

---

## 提交要求

1. 代码提交到个人分支，commit message：`feat(day06): homework functions and utils review`
2. 截图：`pytest tests/test_utils.py -v` 全绿
3. 可选：对比 v2 与 v3 运行同一 `todos.json` 的列表截图

---

*答案：[09_作业答案.md](09_作业答案.md)*
