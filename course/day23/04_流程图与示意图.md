# Day 23 流程图与示意图

**需求**：ZL-NA-REQ-023  
**用途**：授课投屏、学员笔记、作业参考

---

## 1. 端到端数据流

```mermaid
flowchart LR
    U[用户输入] --> F[chat-form submit]
    F --> D[dispatchMessage]
    D --> M{"useMock?"}
    M -->|是| MK[mock.js sendMessage]
    M -->|否| API["fetch POST /api/chat"]
    API --> CH[chat.py]
    CH --> HM[handle_message]
    HM --> CR[classify_reply]
    CR --> JSON[ChatResponse]
    JSON --> PR["parseReply / kind"]
    PR --> R[appendMessage 渲染]
```

## 2. FastAPI 应用启动

```mermaid
flowchart TD
    START[run_server.py] --> UV[uvicorn api.app:app]
    UV --> LOAD[加载 app 模块]
    LOAD --> CREATE[create_app]
    CREATE --> CORS[CORSMiddleware]
    CREATE --> ROUTER[include_router chat]
    CREATE --> EXC[exception_handlers]
    CREATE --> STATIC[mount frontend]
    STATIC --> READY[监听 8000]
```

## 3. 会话生命周期

```mermaid
stateDiagram-v2
    [*] --> 无会话: 首次 POST 无 session_id
    无会话 --> 默认会话: sid = default
    无会话 --> 自定义会话: sid = 客户端 UUID
    默认会话 --> 复用: 同 sid 再次 POST
    自定义会话 --> 复用: 同 sid 再次 POST
    复用 --> 同一Orchestrator: MessageHistory 延续
```

## 4. classify_reply 决策树

```mermaid
flowchart TD
    IN[reply 字符串] --> E{"空?"}
    E -->|是| SYS1["system / 空回复"]
    E -->|否| F{"startswith FAQ?"}
    F -->|是| FAQ["faq / FAQ 直答"]
    F -->|否| R{"匹配路由正则?"}
    R -->|是| RT["route / 路由·template"]
    R -->|否| I{"startswith 请输入?"}
    I -->|是| SYS2["system / 系统提示"]
    I -->|否| ER{"配置错误 or API失败?"}
    ER -->|是| ERR["error / 错误"]
    ER -->|否| LLM["llm / LLM 回复"]
```

## 5. 测试覆盖示意图

```mermaid
mindmap
  root((test_api_chat))
    HTTP
      health
      chat FAQ
      chat route
      empty 422
      session preserved
      frontend served
      config.js
    单元
      classify faq
      classify route
      classify llm
      session isolation
```

## 6. Day 22 → Day 23 迁移

```mermaid
flowchart TB
    subgraph d22[Day 22]
        HS[http.server:8080]
        UM[useMock true 默认]
    end
    subgraph d23[Day 23]
        UV[uvicorn:8000]
        CF[config.js useMock false]
    end
    HS -. 替换主路径 .-> UV
    UM -. "config.js" .-> CF
```

## 7. 四人组协作流（演示日）

```mermaid
sequenceDiagram
    participant 林 as 林晓
    participant 陈 as 陈默
    participant 赵 as 赵岩
    participant 周 as 周航

    周->>周: pytest 12 green
    陈->>陈: api_chat_demo 对照
    林->>林: 浏览器三问句
    赵->>赵: 截图写验收报告
    林-->>赵: 提交演示链接
    赵-->>陈: 确认 kind 来自 API
```

## 8. CORS 与同域对比

```mermaid
flowchart LR
    subgraph 同域[Day 23 推荐]
        A1[origin: localhost:8000]
        A2["fetch /api/chat"]
        A1 --> A2
    end
    subgraph 跨域[分离部署]
        B1[origin: localhost:8080]
        B2[API: localhost:8000]
        B1 -->|需 CORS| B2
    end
```

## 9. 异常传播

```mermaid
flowchart TD
    HM[handle_message] --> NX{"NexusError?"}
    NX -->|ConfigError| E500[HTTP 500]
    NX -->|APIError| E502[HTTP 502]
    NX -->|其他 NexusError| E500b[HTTP 500]
    NX -->|无| OK[200 ChatResponse]
```

流程图文档完。

---

## 10. 投屏用大图说明（讲师备注）

第 1 节端到端数据流图建议全屏讲解五分钟，手指沿 useMock 分支说明 Day 23 主路径是 API 分支。第 3 节会话状态图配合 `test_chat_session_id_preserved` _live 演示。第 6 节迁移图强调：8080 并非废弃，而是 Mock 专练端口；8000 是整合主端口。学员常混，须重复三次。

## 11. 学员手绘作业（可选）

要求学员不看文档手绘「POST /api/chat 序列图」，与 03_ 架构第 3 节对照。培训部发现手绘过关者，pytest 通过率高出百分之十八。林晓队全员手绘过关。

流程图扩展完。

**投屏提示**：第四节 classify 决策树建议彩色打印发放学员，辅助竞赛复习。四号文件终稿。智链科技培训部制图，2026-07-28。流程图册完。END。
