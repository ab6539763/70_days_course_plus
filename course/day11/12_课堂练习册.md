# Day 11 课堂练习册

**说明**：每题先独立思考 5 分钟，再对照 [09_作业答案.md](09_作业答案.md) 或源码。

---

## 基础题（1-5）

**1.** `Path("a") / "b" / "c.txt"` 的结果类型是？  
<details><summary>提示</summary>Path 对象，值为 a/b/c.txt</details>

**2.** `get_path("sample_docs")` 注册在 `core/paths.py` 的哪个键？  
<details><summary>答案</summary>`"sample_docs"` → `SRC_ROOT / "day02" / "sample_docs"`</details>

**3.** `read_text_file` 全部编码失败时抛什么异常？  
<details><summary>答案</summary>StorageError，code=STORAGE_ERROR</details>

**4.** `DocumentRecord.cleaned` 默认值为？  
<details><summary>答案</summary>None（未清洗）</details>

**5.** `DEFAULT_ENCODINGS` 第一个元素是？  
<details><summary>答案</summary>utf-8</details>

---

## 进阶题（6-10）

**6.** 写一行代码列出 `sample_docs` 下所有 txt 文件名（用项目 API）。  
```python
# 你的答案
[p.name for p in iter_text_files(get_path("sample_docs"))]
```

**7.** `mask_phone_numbers("联系13812345678")` 返回的 count 是多少？  
<details><summary>答案</summary>1</details>

**8.** `batch_clean_directory` 默认 `mask_phone` 参数是？  
<details><summary>答案</summary>True</details>

**9.** `iter_text_files(d, recursive=True)` 内部 glob 模式变成？  
<details><summary>答案</summary>**/*.txt（pattern 默认时）</details>

**10.** 运行 `python3 -m pytest tests/day11/ -v` 大约有多少条测试？  
<details><summary>答案</summary>12 条</details>

---

## 实操练习（需动手）

### 练习 A：压缩率计算

阅读 `doc_reader_demo.py` 第 40-42 行，手算某文件 `raw_len=100`, `clean_len=85` 的压缩率。  
**答案**：15.0%

### 练习 B：对比 Day 3 输出

分别运行 Day 3 批量清洗（若环境可用）与 `doc_reader_demo.py`，对比 `cleaned_` 文件 diff。

### 练习 C：encoding_demos

运行 `encoding_demos.py`，记录 `gbk.txt` 检测到的 encoding 字段。

---

## 快问快答卡片

| # | 问题 | 答案 |
|---|------|------|
| 1 | tools 层首个模块？ | doc_reader.py |
| 2 | 需求编号？ | ZL-NA-REQ-011 |
| 3 | 写出函数前缀？ | cleaned_ |
| 4 | 手机脱敏占位符？ | ***PHONE*** |
| 5 | 清洗函数来源？ | utils.text_utils.clean_text |

---

*共 10 道核心题 + 3 道实操。晚自习见 [07_晚自习.md](07_晚自习.md)。*
