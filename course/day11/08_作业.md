# Day 11 课后作业

**关联需求**：ZL-NA-REQ-011  
**提交**：Git commit `feat(day11): homework <姓名>` + `tests/day11/` 全绿

---

## 题目一：pathlib 基础（15 分）

编写脚本 `homework_path.py`（可放 `src/day11/` 或作业目录）：

1. 使用 `get_path("doc_output")` 获取输出目录  
2. 若目录不存在则创建  
3. 写入 `homework_probe.txt`，内容为学号 + 姓名  
4. 打印 `stat().st_size` 与 `read_text()` 结果  

---

## 题目二：编码回退实验（20 分）

在临时目录创建两个文件：

- `utf8_only.txt` — UTF-8 中文  
- `gbk_only.txt` — GBK 编码字节写入  

用 `read_text_file` 分别读取，断言返回的 `encoding` 字段正确。写成 pytest 函数 `test_homework_encoding`。

---

## 题目三：扩展 iter_text_files（25 分）

为 `iter_text_files` 编写包装函数：

```python
def count_lines_in_directory(directory: Path, pattern: str = "*.txt") -> dict[str, int]:
    """返回 {文件名: 行数}，使用 read_document(clean=False)"""
```

要求：空目录返回 `{}`；目录不存在抛 `StorageError`（透传）。

---

## 题目四：手机号脱敏边界（20 分）

`mask_phone_numbers` 对以下输入的行为是什么？用 pytest 参数化写 4 个用例并注释业务含义：

| 输入 | 预期 |
|------|------|
| `"无号码"` | count=0 |
| `"13812345678"` | count=1，含 `***PHONE***` |
| `"12345678901"`（非 1 开头） | 不脱敏 |
| `"13812345678和13900001111"` | count=2 |

---

## 题目五：架构思考题（20 分）

300 字以内回答：

> 为何 `batch_clean_directory` 放在 `tools/doc_reader.py` 而不是继续扩展 `day03/platform_cli.py`？  
> 结合 Day 10 包结构、Day 28 RAG、可测试性三点论述。

---

## 加分题（+10 分）

实现 `read_documents` 的 `recursive=True` 版本集成测试：在 `tmp_path` 建 `sub/a.txt` 与 `b.txt`，断言递归读到 2 个文件、非递归只读到 1 个。

---

## 评分标准

| 项 | 分值 |
|----|------|
| 代码可运行 | 30% |
| pytest 通过 | 30% |
| 符合 pathlib / StorageError 规范 | 20% |
| 题目五论述 | 20% |

---

*答案：[09_作业答案.md](09_作业答案.md)*
