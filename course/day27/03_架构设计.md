# Day 27 架构设计

**需求**：ZL-NA-REQ-027

## 分层视图

```
┌─────────────────────────────────────────┐
│  frontend/knowledge.js  (#kb-eval-btn)  │
└──────────────────┬──────────────────────┘
                   │ POST /evaluate
┌──────────────────▼──────────────────────┐
│  api/knowledge.py                       │
│  get/put chunk-config · evaluate        │
└───────┬─────────────────────┬───────────┘
        │                     │
        ▼                     ▼
┌───────────────┐    ┌────────────────────┐
│ KnowledgeStore│    │ retrieval_eval     │
│ chunk_config  │    │ (内存临时 retriever)│
│ persist       │    │ 不写 chunks        │
└───────────────┘    └─────────┬──────────┘
                               │
                               ▼
                    ┌──────────────────────┐
                    │ chunk_config         │
                    │ chunk_strategies     │
                    │ embedding_retriever  │
                    └──────────────────────┘
```

## 核心隔离原则

**评估路径**调用 `build_retriever_for_doc`：在内存中对单份 `ParsedDocument` 分块并训练 TF-IDF，**从不**调用 `store._append_chunks` 或 `save()`。

**生产路径**由 `upload` / `ingest` 使用 `store.get_chunk_config()` 分块并写入索引。

## ChunkConfig 源码锚点

```python
"""
分块参数配置 — chunk_size / overlap / strategy

需求：ZL-NA-REQ-027
"""

from __future__ import annotations

from dataclasses import asdict, dataclass, field
from typing import Any


@dataclass
class ChunkConfig:
    """知识库分块参数（可持久化、可 A/B 对比）"""

    chunk_size: int = 200
    overlap: int = 40
    strategy: str = "auto"  # auto | fixed | markdown
    name: str = "default"

    def validate(self) -> None:
        if self.chunk_size <= 0:
            raise ValueError("chunk_size 必须为正整数")
        if self.overlap < 0 or self.overlap >= self.chunk_size:
            raise ValueError("overlap 必须满足 0 <= overlap < chunk_size")
        if self.strategy not in ("auto", "fixed", "markdown"):
            raise ValueError("strategy 须为 auto / fixed / markdown")

    def to_dict(self) -> dict[str, Any]:
        return asdict(self)

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ChunkConfig:
        return cls(
            chunk_size=int(data.get("chunk_size", 200)),
            overlap=int(data.get("overlap", 40)),
            strategy=str(data.get("strategy", "auto")),
            name=str(data.get("name", "default")),
        )


DEFAULT_CHUNK_CONFIG = ChunkConfig()

PRESET_CONFIGS: tuple[ChunkConfig, ...] = (
    ChunkConfig(name="compact", chunk_size=120, overlap=20, strategy="auto"),
    ChunkConfig(name="default", chunk_size=200, overlap=40, strategy="auto"),
    ChunkConfig(name="wide", chunk_size=400, overlap=60, strategy="auto"),
    ChunkConfig(name="markdown_wide", chunk_size=500, overlap=50, strategy="markdown"),
)
```


## evaluate 端点伪代码

```python
doc = parse_bytes(_EVAL_SAMPLE)
queries = [EvalQuery.from_dict(q) for q in EVAL_QUERIES]
configs = PRESET_CONFIGS if body.use_presets else body.configs
results = run_ab_experiment(doc, configs, queries)
return EvaluateResponse(best_config=results[0], ...)
```

## 持久化字段

`store.json` 片段：

```json
{
  "chunk_config": {
    "chunk_size": 200,
    "overlap": 40,
    "strategy": "auto",
    "name": "default"
  }
}
```



## 附录 A：knowledge_store.py 走读

### `nexus-agent-platform/src/rag/knowledge_store.py` 逐行走读

共 **710** 行。

**L1** `"""`

**L2** `知识库存储 — 文档 ingestion、分块索引与 JSON 持久化`

**L3** ``

**L4** `将 Day 19–20 的 RAG 管线升级为可写入、可落盘的企业知识库 MVP。`

**L5** ``

**L6** `需求：ZL-NA-REQ-025`

**L7** `"""`

**L8** ``

**L9** `from __future__ import annotations`

**L10** ``

**L11** `import threading`

**L12** `from dataclasses import dataclass, field`

**L13** `from datetime import datetime, timezone`

**L14** `from pathlib import Path`

**L15** `from typing import Any`

**L16** ``

**L17** `from core.paths import get_path`

**L18** `from rag.chunker import TextChunk, chunk_documents, chunk_text`

**L19** `from rag.chunk_config import DEFAULT_CHUNK_CONFIG, ChunkConfig`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L20** `from rag.chunk_strategies import chunk_from_parsed`

**L21** `from rag.context import DocumentIndex, RAGContextService`

**L22** `from rag.chroma_retriever import ChromaEmbeddingRetriever`

**L23** `from rag.chroma_store import VECTOR_BACKEND, ChromaVectorIndex`

**L24** `from rag.citation_config import CitationConfig`

**L25** `from rag.expanding_retriever import ExpandingRetriever`

**L26** `from rag.expansion_config import ExpansionConfig`

**L27** `from rag.answer_validator import RuleBasedAnswerValidator, ValidationResult`

**L28** `from rag.route_config import RouteConfig`

**L29** `from rag.routing_retriever import RoutingRetriever`

**L30** `from rag.validation_config import ValidationConfig`

**L31** `from rag.hybrid_retriever import HybridRetriever`

**L32** `from rag.rerank_config import RerankConfig`

**L33** `from rag.reranker import MockCrossEncoderReranker`

**L34** `from rag.reranking_retriever import RerankingRetriever`

**L35** `from rag.retrieval_config import RetrievalConfig`

**L36** `from rag.rewrite_config import RewriteConfig`

**L37** `from rag.rewriting_retriever import RewritingRetriever`

**L38** `from tools.doc_reader import DocumentRecord, read_text_file`

**L39** `from tools.parsers.base import ParsedDocument`

**L40** `from utils.json_utils import load_json, save_json`

**L41** `from utils.text_utils import clean_text`

**L42** ``

**L43** `STORE_VERSION = "1.1"`

**L44** `PLATFORM_VERSION = "0.37.0"`

**L45** `INDEX_MODE_INCREMENTAL = "incremental"`

**L46** `INDEX_MODE_FULL = "full"`

**L47** ``

**L48** ``

**L49** `@dataclass`

**L50** `class KnowledgeDocument:`

**L51** `    """已入库文档元数据"""`

**L52** ``

**L53** `    name: str`

**L54** `    ingested_at: str`

**L55** `    size_bytes: int = 0`

**L56** `    chunk_count: int = 0`

**L57** `    format: str = "txt"`

**L58** ``

**L59** `    def to_dict(self) -> dict[str, Any]:`

**L60** `        return {`

**L61** `            "name": self.name,`

**L62** `            "ingested_at": self.ingested_at,`

**L63** `            "size_bytes": self.size_bytes,`

**L64** `            "chunk_count": self.chunk_count,`

**L65** `            "format": self.format,`

**L66** `        }`

**L67** ``

**L68** `    @classmethod`

**L69** `    def from_dict(cls, data: dict[str, Any]) -> KnowledgeDocument:`

**L70** `        return cls(`

**L71** `            name=str(data.get("name", "")),`

**L72** `            ingested_at=str(data.get("ingested_at", "")),`

**L73** `            size_bytes=int(data.get("size_bytes", 0)),`

**L74** `            chunk_count=int(data.get("chunk_count", 0)),`

**L75** `            format=str(data.get("format", "txt")),`

**L76** `        )`

**L77** ``

**L78** ``

**L79** `@dataclass`

**L80** `class KnowledgeStore:`

**L81** `    """`

**L82** `    企业知识库 — 分块 + Chroma 向量索引 + JSON 元数据持久化`

**L83** ``

**L84** `    典型用法：`

**L85** `        store = KnowledgeStore.load_or_bootstrap()`

**L86** `        store.ingest_text("新产品说明…", filename="notice.txt")`

**L87** `        rag = store.as_rag_service()`

**L88** `    """`

**L89** ``

**L90** `    documents: list[KnowledgeDocument] = field(default_factory=list)`

**L91** `    chunks: list[TextChunk] = field(default_factory=list)`

**L92** `    embedding_state: dict[str, Any] = field(default_factory=dict)`

**L93** `    vector_backend: str = VECTOR_BACKEND`

**L94** `    chunk_config: ChunkConfig = field(default_factory=ChunkConfig)`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L95** `    last_rebuilt_at: str | None = None`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L96** `    last_incremental_at: str | None = None`

**L97** `    index_mode: str = INDEX_MODE_FULL`

**L98** `    retrieval_config: RetrievalConfig = field(default_factory=RetrievalConfig)`

**L99** `    rerank_config: RerankConfig = field(default_factory=RerankConfig)`

**L100** `    rewrite_config: RewriteConfig = field(default_factory=RewriteConfig)`

**L101** `    citation_config: CitationConfig = field(default_factory=CitationConfig)`

**L102** `    expansion_config: ExpansionConfig = field(default_factory=ExpansionConfig)`

**L103** `    route_config: RouteConfig = field(default_factory=RouteConfig)`

**L104** `    validation_config: ValidationConfig = field(default_factory=ValidationConfig)`

**L105** `    store_path: Path | None = None`

**L106** `    chroma_path: Path | None = None`

**L107** `    _rag_service: RAGContextService | None = field(default=None, repr=False)`

**L108** ``

**L109** `    @property`

**L110** `    def chunk_count(self) -> int:`

**L111** `        return len(self.chunks)`

**L112** ``

**L113** `    @property`

**L114** `    def document_count(self) -> int:`

**L115** `        return len(self.documents)`

**L116** ``

**L117** `    def as_rag_service(self) -> RAGContextService:`

**L118** `        """构建或返回缓存的 RAGContextService"""`

**L119** `        if self._rag_service is None:`

**L120** `            self._rag_service = self._build_rag_service()`

**L121** `        return self._rag_service`

**L122** ``

**L123** `    def invalidate_cache(self) -> None:`

**L124** `        self._rag_service = None`

**L125** ``

**L126** `    def get_chunk_config(self) -> ChunkConfig:`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L127** `        return ChunkConfig.from_dict(self.chunk_config.to_dict())`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L128** ``

**L129** `    def set_chunk_config(self, config: ChunkConfig) -> ChunkConfig:`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L130** `        config.validate()`

**L131** `        self.chunk_config = ChunkConfig.from_dict(config.to_dict())`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L132** `        return self.chunk_config`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L133** ``

**L134** `    def get_retrieval_config(self) -> RetrievalConfig:`

**L135** `        return RetrievalConfig.from_dict(self.retrieval_config.to_dict())`

**L136** ``

**L137** `    def set_retrieval_config(self, config: RetrievalConfig) -> RetrievalConfig:`

**L138** `        config.validate()`

**L139** `        self.retrieval_config = RetrievalConfig.from_dict(config.to_dict())`

**L140** `        self.invalidate_cache()`

**L141** `        return self.retrieval_config`

**L142** ``

**L143** `    def get_rerank_config(self) -> RerankConfig:`

**L144** `        return RerankConfig.from_dict(self.rerank_config.to_dict())`

**L145** ``

**L146** `    def set_rerank_config(self, config: RerankConfig) -> RerankConfig:`

**L147** `        config.validate()`

**L148** `        self.rerank_config = RerankConfig.from_dict(config.to_dict())`

**L149** `        self.invalidate_cache()`

**L150** `        return self.rerank_config`

**L151** ``

**L152** `    def get_rewrite_config(self) -> RewriteConfig:`

**L153** `        return RewriteConfig.from_dict(self.rewrite_config.to_dict())`

**L154** ``

**L155** `    def set_rewrite_config(self, config: RewriteConfig) -> RewriteConfig:`

**L156** `        config.validate()`

**L157** `        self.rewrite_config = RewriteConfig.from_dict(config.to_dict())`

**L158** `        self.invalidate_cache()`

**L159** `        return self.rewrite_config`

**L160** ``

**L161** `    def get_citation_config(self) -> CitationConfig:`

**L162** `        return CitationConfig.from_dict(self.citation_config.to_dict())`

**L163** ``

**L164** `    def set_citation_config(self, config: CitationConfig) -> CitationConfig:`

**L165** `        config.validate()`

**L166** `        self.citation_config = CitationConfig.from_dict(config.to_dict())`

**L167** `        return self.citation_config`

**L168** ``

**L169** `    def get_expansion_config(self) -> ExpansionConfig:`

**L170** `        return ExpansionConfig.from_dict(self.expansion_config.to_dict())`

**L171** ``

**L172** `    def set_expansion_config(self, config: ExpansionConfig) -> ExpansionConfig:`

**L173** `        config.validate()`

**L174** `        self.expansion_config = ExpansionConfig.from_dict(config.to_dict())`

**L175** `        self.invalidate_cache()`

**L176** `        return self.expansion_config`

**L177** ``

**L178** `    def get_route_config(self) -> RouteConfig:`

**L179** `        return RouteConfig.from_dict(self.route_config.to_dict())`

**L180** ``

**L181** `    def set_route_config(self, config: RouteConfig) -> RouteConfig:`

**L182** `        config.validate()`

**L183** `        self.route_config = RouteConfig.from_dict(config.to_dict())`

**L184** `        self.invalidate_cache()`

**L185** `        return self.route_config`

**L186** ``

**L187** `    def get_validation_config(self) -> ValidationConfig:`

**L188** `        return ValidationConfig.from_dict(self.validation_config.to_dict())`

**L189** ``

**L190** `    def set_validation_config(self, config: ValidationConfig) -> ValidationConfig:`

**L191** `        config.validate()`

**L192** `        self.validation_config = ValidationConfig.from_dict(config.to_dict())`

**L193** `        return self.validation_config`

**L194** ``

**L195** `    def validate_answer(`

**L196** `        self,`

**L197** `        query: str,`

**L198** `        reply: str,`

**L199** `        citations: list[dict[str, Any]],`

**L200** `    ) -> ValidationResult | None:`

**L201** `        """按当前 validation_config 校验 reply 与 citations 一致性"""`

**L202** `        cfg = self.get_validation_config()`

**L203** `        if not cfg.enabled:`

**L204** `            return None`

**L205** `        validator = RuleBasedAnswerValidator(config=cfg)`

**L206** `        return validator.validate(query, reply, citations)`

**L207** ``

**L208** `    def fetch_citations(self, query: str) -> dict[str, Any]:`

**L209** `        """按当前 citation_config 检索并返回引用包 dict"""`

**L210** `        cfg = self.get_citation_config()`

**L211** `        if not cfg.enabled:`

**L212** `            return {`

**L213** `                "query": query.strip(),`

**L214** `                "citations": [],`

**L215** `                "rewrite": None,`

**L216** `                "expansion": None,`

**L217** `                "route": None,`

**L218** `            }`

**L219** `        rag = self.as_rag_service()`

**L220** `        bundle = rag.retrieve_citation_bundle(query, config=cfg)`

**L221** `        return bundle.to_dict()`

**L222** ``

**L223** `    def ingest_text(`

**L224** `        self,`

**L225** `        content: str,`

**L226** `        *,`

**L227** `        filename: str,`

**L228** `        clean: bool = True,`

**L229** `        chunk_size: int | None = None,`

**L230** `        overlap: int | None = None,`

**L231** `    ) -> KnowledgeDocument:`

**L232** `        """将文本写入知识库并重建索引"""`

**L233** `        cfg = self.get_chunk_config()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L234** `        cs = chunk_size if chunk_size is not None else cfg.chunk_size`

**L235** `        ov = overlap if overlap is not None else cfg.overlap`

**L236** `        text = (content or "").strip()`

**L237** `        if not text:`

**L238** `            raise ValueError("文档内容不能为空")`

**L239** `        if not filename.strip():`

**L240** `            raise ValueError("filename 不能为空")`

**L241** ``

**L242** `        cleaned = text`

**L243** `        if clean:`

**L244** `            cleaned, _ = clean_text(text)`

**L245** `        doc = DocumentRecord(`

**L246** `            path=Path(filename),`

**L247** `            content=text,`

**L248** `            encoding="utf-8",`

**L249** `            size_bytes=len(text.encode("utf-8")),`

**L250** `            cleaned=cleaned,`

**L251** `        )`

**L252** `        new_chunks = chunk_documents(`

**L253** `            [doc],`

**L254** `            chunk_size=cs,`

**L255** `            overlap=ov,`

**L256** `            use_cleaned=clean,`

**L257** `        )`

**L258** `        self._append_chunks(filename, new_chunks, size_bytes=doc.size_bytes)`

**L259** `        self._rebuild_index()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L260** `        return self.documents[-1]`

**L261** ``

**L262** `    def ingest_file(`

**L263** `        self,`

**L264** `        path: Path,`

**L265** `        *,`

**L266** `        clean: bool = True,`

**L267** `        chunk_size: int = 200,`

**L268** `        overlap: int = 40,`

**L269** `    ) -> KnowledgeDocument:`

**L270** `        """从磁盘文件 ingestion"""`

**L271** `        content, encoding = read_text_file(path)`

**L272** `        cleaned = content`

**L273** `        if clean:`

**L274** `            cleaned, _ = clean_text(content)`

**L275** `        doc = DocumentRecord(`

**L276** `            path=path,`

**L277** `            content=content,`

**L278** `            encoding=encoding,`

**L279** `            size_bytes=path.stat().st_size,`

**L280** `            cleaned=cleaned,`

**L281** `        )`

**L282** `        new_chunks = chunk_documents(`

**L283** `            [doc],`

**L284** `            chunk_size=chunk_size,`

**L285** `            overlap=overlap,`

**L286** `            use_cleaned=clean,`

**L287** `        )`

**L288** `        self._append_chunks(path.name, new_chunks, size_bytes=doc.size_bytes)`

**L289** `        self._rebuild_index()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L290** `        return self.documents[-1]`

**L291** ``

**L292** `    def ingest_bytes(`

**L293** `        self,`

**L294** `        data: bytes,`

**L295** `        *,`

**L296** `        filename: str,`

**L297** `        clean: bool = True,`

**L298** `        chunk_strategy: str = "auto",`

**L299** `        incremental: bool = True,`

**L300** `    ) -> KnowledgeDocument:`

**L301** `        """处理上传二进制 — Day 26 起委托 doc_parser"""`

**L302** `        from tools.doc_parser import parse_bytes`

**L303** ``

**L304** `        parsed = parse_bytes(data, filename)`

**L305** `        cfg = self.get_chunk_config()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L306** `        return self.ingest_parsed(`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L307** `            parsed,`

**L308** `            clean=clean,`

**L309** `            chunk_strategy=chunk_strategy if chunk_strategy != "auto" else cfg.strategy,`

**L310** `            incremental=incremental,`

**L311** `        )`

**L312** ``

**L313** `    def ingest_parsed(`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L314** `        self,`

**L315** `        parsed: ParsedDocument,`

**L316** `        *,`

**L317** `        clean: bool = True,`

**L318** `        chunk_strategy: str | None = None,`

**L319** `        chunk_size: int | None = None,`

**L320** `        overlap: int | None = None,`

**L321** `        incremental: bool = False,`

**L322** `    ) -> KnowledgeDocument:`

**L323** `        """将 ParsedDocument 写入知识库"""`

**L324** `        cfg = self.get_chunk_config()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L325** `        strategy = chunk_strategy if chunk_strategy is not None else cfg.strategy`

**L326** `        cs = chunk_size if chunk_size is not None else cfg.chunk_size`

**L327** `        ov = overlap if overlap is not None else cfg.overlap`

**L328** `        text = (parsed.plain_text or "").strip()`

**L329** `        if not text:`

**L330** `            raise ValueError("解析结果为空")`

**L331** ``

**L332** `        if clean:`

**L333** `            text, _ = clean_text(text)`

**L334** `            parsed.plain_text = text`

**L335** `            for section in parsed.sections:`

**L336** `                section.body, _ = clean_text(section.body)`

**L337** ``

**L338** `        new_chunks = chunk_from_parsed(`

**L339** `            parsed,`

**L340** `            strategy=strategy,`

**L341** `            chunk_size=cs,`

**L342** `            overlap=ov,`

**L343** `        )`

**L344** `        size_bytes = len(parsed.plain_text.encode("utf-8"))`

**L345** ``

**L346** `        if incremental:`

**L347** `            removed = self._remove_document_by_source(parsed.filename)`

**L348** `            self._append_chunks(`

**L349** `                parsed.filename,`

**L350** `                new_chunks,`

**L351** `                size_bytes=size_bytes,`

**L352** `                doc_format=parsed.format,`

**L353** `            )`

**L354** `            self._incremental_index(new_chunks, replaced_count=len(removed))`

**L355** `        else:`

**L356** `            self._append_chunks(`

**L357** `                parsed.filename,`

**L358** `                new_chunks,`

**L359** `                size_bytes=size_bytes,`

**L360** `                doc_format=parsed.format,`

**L361** `            )`

**L362** `            self._rebuild_index()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L363** `        return self.documents[-1]`

**L364** ``

**L365** `    def save(self, path: Path | None = None) -> Path:`

**L366** `        """持久化到 JSON"""`

**L367** `        target = path or self.store_path or _default_store_path()`

**L368** `        self.store_path = target`

**L369** `        payload = {`

**L370** `            "version": STORE_VERSION,`

**L371** `            "platform_version": PLATFORM_VERSION,`

**L372** `            "documents": [d.to_dict() for d in self.documents],`

**L373** `            "chunks": [_chunk_to_dict(c) for c in self.chunks],`

**L374** `            "embedding": self.embedding_state,`

**L375** `            "vector_backend": self.vector_backend,`

**L376** `            "chunk_config": self.chunk_config.to_dict(),`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L377** `            "last_rebuilt_at": self.last_rebuilt_at,`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L378** `            "last_incremental_at": self.last_incremental_at,`

**L379** `            "index_mode": self.index_mode,`

**L380** `            "retrieval_config": self.retrieval_config.to_dict(),`

**L381** `            "rerank_config": self.rerank_config.to_dict(),`

**L382** `            "rewrite_config": self.rewrite_config.to_dict(),`

**L383** `            "citation_config": self.citation_config.to_dict(),`

**L384** `            "expansion_config": self.expansion_config.to_dict(),`

**L385** `            "route_config": self.route_config.to_dict(),`

**L386** `            "validation_config": self.validation_config.to_dict(),`

**L387** `        }`

**L388** `        save_json(target, payload)`

**L389** `        return target`

**L390** ``

**L391** `    @classmethod`

**L392** `    def load(cls, path: Path) -> KnowledgeStore:`

**L393** `        """从 JSON 加载知识库"""`

**L394** `        raw = load_json(path, default=None)`

**L395** `        if not raw:`

**L396** `            return cls.bootstrap_from_sample_docs(store_path=path)`

**L397** ``

**L398** `        store = cls(store_path=path)`

**L399** `        store.documents = [`

**L400** `            KnowledgeDocument.from_dict(d) for d in raw.get("documents", [])`

**L401** `        ]`

**L402** `        store.chunks = [_chunk_from_dict(c) for c in raw.get("chunks", [])]`

**L403** `        store.embedding_state = dict(raw.get("embedding") or {})`

**L404** `        store.vector_backend = str(raw.get("vector_backend") or VECTOR_BACKEND)`

**L405** `        if raw.get("chunk_config"):`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L406** `            store.chunk_config = ChunkConfig.from_dict(raw["chunk_config"])`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L407** `        store.last_rebuilt_at = raw.get("last_rebuilt_at")`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L408** `        store.last_incremental_at = raw.get("last_incremental_at")`

**L409** `        store.index_mode = str(raw.get("index_mode") or INDEX_MODE_FULL)`

**L410** `        if raw.get("retrieval_config"):`

**L411** `            store.retrieval_config = RetrievalConfig.from_dict(raw["retrieval_config"])`

**L412** `        if raw.get("rerank_config"):`

**L413** `            store.rerank_config = RerankConfig.from_dict(raw["rerank_config"])`

**L414** `        if raw.get("rewrite_config"):`

**L415** `            store.rewrite_config = RewriteConfig.from_dict(raw["rewrite_config"])`

**L416** `        if raw.get("citation_config"):`

**L417** `            store.citation_config = CitationConfig.from_dict(raw["citation_config"])`

**L418** `        if raw.get("expansion_config"):`

**L419** `            store.expansion_config = ExpansionConfig.from_dict(raw["expansion_config"])`

**L420** `        if raw.get("route_config"):`

**L421** `            store.route_config = RouteConfig.from_dict(raw["route_config"])`

**L422** `        if raw.get("validation_config"):`

**L423** `            store.validation_config = ValidationConfig.from_dict(raw["validation_config"])`

**L424** `        store._sync_chroma_from_json()`

**L425** `        store._rag_service = store._build_rag_service()`

**L426** `        return store`

**L427** ``

**L428** `    @classmethod`

**L429** `    def load_or_bootstrap(cls, path: Path | None = None) -> KnowledgeStore:`

**L430** `        """加载已有库，不存在则从 sample_docs 引导"""`

**L431** `        target = path or _default_store_path()`

**L432** `        if target.is_file():`

**L433** `            return cls.load(target)`

**L434** `        store = cls.bootstrap_from_sample_docs(store_path=target)`

**L435** `        store.save(target)`

**L436** `        return store`

**L437** ``

**L438** `    @classmethod`

**L439** `    def bootstrap_from_sample_docs(cls, *, store_path: Path | None = None) -> KnowledgeStore:`

**L440** `        """用内置 sample_docs 初始化知识库"""`

**L441** `        rag = RAGContextService.from_sample_docs(use_embedding=True)`

**L442** `        store = cls(store_path=store_path)`

**L443** `        now = _utc_now()`

**L444** ``

**L445** `        by_source: dict[str, list[TextChunk]] = {}`

**L446** `        for chunk in rag.index.chunks:`

**L447** `            by_source.setdefault(chunk.source, []).append(chunk)`

**L448** ``

**L449** `        for source, chunks in sorted(by_source.items()):`

**L450** `            store.documents.append(`

**L451** `                KnowledgeDocument(`

**L452** `                    name=source,`

**L453** `                    ingested_at=now,`

**L454** `                    size_bytes=sum(len(c.text) for c in chunks),`

**L455** `                    chunk_count=len(chunks),`

**L456** `                )`

**L457** `            )`

**L458** `        store.chunks = list(rag.index.chunks)`

**L459** `        retriever = rag.index.retriever`

**L460** `        from rag.embedding_retriever import EmbeddingRetriever`

**L461** ``

**L462** `        if isinstance(retriever, EmbeddingRetriever):`

**L463** `            store.embedding_state = retriever._client.model.export_state()`

**L464** `        store._rebuild_index()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L465** `        return store`

**L466** ``

**L467** `    def status_dict(self) -> dict[str, Any]:`

**L468** `        from tools.doc_parser import supported_formats`

**L469** ``

**L470** `        return {`

**L471** `            "document_count": self.document_count,`

**L472** `            "chunk_count": self.chunk_count,`

**L473** `            "documents": [d.to_dict() for d in self.documents],`

**L474** `            "store_path": str(self.store_path) if self.store_path else None,`

**L475** `            "platform_version": PLATFORM_VERSION,`

**L476** `            "supported_formats": supported_formats(),`

**L477** `            "chunk_config": self.chunk_config.to_dict(),`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L478** `            "last_rebuilt_at": self.last_rebuilt_at,`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L479** `            "last_incremental_at": self.last_incremental_at,`

**L480** `            "index_mode": self.index_mode,`

**L481** `            "retrieval_config": self.retrieval_config.to_dict(),`

**L482** `            "rerank_config": self.rerank_config.to_dict(),`

**L483** `            "rewrite_config": self.rewrite_config.to_dict(),`

**L484** `            "citation_config": self.citation_config.to_dict(),`

**L485** `            "expansion_config": self.expansion_config.to_dict(),`

**L486** `            "route_config": self.route_config.to_dict(),`

**L487** `            "validation_config": self.validation_config.to_dict(),`

**L488** `            "vector_backend": self.vector_backend,`

**L489** `            "chroma_path": str(self._resolve_chroma_path()),`

**L490** `            "chroma_count": self._chroma_index().count() if self.chunks else 0,`

**L491** `        }`

**L492** ``

**L493** `    def _append_chunks(`

**L494** `        self,`

**L495** `        filename: str,`

**L496** `        new_chunks: list[TextChunk],`

**L497** `        *,`

**L498** `        size_bytes: int,`

**L499** `        doc_format: str = "txt",`

**L500** `    ) -> None:`

**L501** `        base_index = len(self.chunks)`

**L502** `        reindexed: list[TextChunk] = []`

**L503** `        for i, chunk in enumerate(new_chunks):`

**L504** `            reindexed.append(`

**L505** `                TextChunk(`

**L506** `                    chunk_id=chunk.chunk_id,`

**L507** `                    text=chunk.text,`

**L508** `                    source=filename,`

**L509** `                    index=base_index + i,`

**L510** `                    start_char=chunk.start_char,`

**L511** `                    end_char=chunk.end_char,`

**L512** `                )`

**L513** `            )`

**L514** `        self.chunks.extend(reindexed)`

**L515** `        self.documents.append(`

**L516** `            KnowledgeDocument(`

**L517** `                name=filename,`

**L518** `                ingested_at=_utc_now(),`

**L519** `                size_bytes=size_bytes,`

**L520** `                chunk_count=len(reindexed),`

**L521** `                format=doc_format,`

**L522** `            )`

**L523** `        )`

**L524** ``

**L525** `    def _remove_document_by_source(self, filename: str) -> list[str]:`

**L526** `        """移除同名文档及其 chunks，并从 Chroma 删除旧向量"""`

**L527** `        removed_ids = [c.chunk_id for c in self.chunks if c.source == filename]`

**L528** `        if not removed_ids:`

**L529** `            return []`

**L530** ``

**L531** `        self._chroma_index().delete_by_ids(removed_ids)`

**L532** `        self.documents = [d for d in self.documents if d.name != filename]`

**L533** `        self.chunks = [c for c in self.chunks if c.source != filename]`

**L534** `        self.chunks = [`

**L535** `            TextChunk(`

**L536** `                chunk_id=c.chunk_id,`

**L537** `                text=c.text,`

**L538** `                source=c.source,`

**L539** `                index=i,`

**L540** `                start_char=c.start_char,`

**L541** `                end_char=c.end_char,`

**L542** `            )`

**L543** `            for i, c in enumerate(self.chunks)`

**L544** `        ]`

**L545** `        return removed_ids`

**L546** ``

**L547** `    def _resolve_chroma_path(self) -> Path:`

**L548** `        if self.chroma_path is not None:`

**L549** `            return self.chroma_path`

**L550** `        if self.store_path is not None:`

**L551** `            return self.store_path.parent / "chroma"`

**L552** `        return get_path("knowledge_chroma")`

**L553** ``

**L554** `    def _chroma_index(self) -> ChromaVectorIndex:`

**L555** `        return ChromaVectorIndex(self._resolve_chroma_path())`

**L556** ``

**L557** `    def _sync_chroma_from_json(self) -> None:`

**L558** `        """从 JSON 元数据恢复 Chroma（迁移或冷启动）"""`

**L559** `        if not self.chunks or not self.embedding_state:`

**L560** `            return`

**L561** `        chroma = self._chroma_index()`

**L562** `        if chroma.count() > 0:`

**L563** `            return`

**L564** `        from rag.embedding import EmbeddingClient`

**L565** ``

**L566** `        client = EmbeddingClient()`

**L567** `        client.model.load_state(self.embedding_state)`

**L568** `        vectors = client.embed_batch([c.text for c in self.chunks])`

**L569** `        chroma.upsert_chunks(self.chunks, vectors)`

**L570** ``

**L571** `    def _rebuild_index(self) -> None:`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L572** `        from rag.embedding_retriever import EmbeddingRetriever`

**L573** ``

**L574** `        if not self.chunks:`

**L575** `            self.embedding_state = {}`

**L576** `            self._chroma_index().reset()`

**L577** `            self.invalidate_cache()`

**L578** `            return`

**L579** ``

**L580** `        retriever = EmbeddingRetriever(self.chunks)`

**L581** `        self.embedding_state = retriever._client.model.export_state()`

**L582** `        vectors = retriever._client.embed_batch([c.text for c in self.chunks])`

**L583** `        chroma = self._chroma_index()`

**L584** `        chroma.reset()`

**L585** `        chroma.upsert_chunks(self.chunks, vectors)`

**L586** `        self.index_mode = INDEX_MODE_FULL`

**L587** `        self.invalidate_cache()`

**L588** ``

**L589** `    def _incremental_index(`

**L590** `        self,`

**L591** `        affected_chunks: list[TextChunk],`

**L592** `        *,`

**L593** `        replaced_count: int = 0,`

**L594** `    ) -> bool:`

**L595** `        """`

**L596** `        增量更新 Chroma：不 reset collection，仅 upsert 受影响 chunk。`

**L597** ``

**L598** `        若 TF-IDF 词表扩张，则回退为全量 upsert（仍不 reset）。`

**L599** `        返回是否发生词表扩张。`

**L600** `        """`

**L601** `        from rag.embedding_retriever import EmbeddingRetriever`

**L602** ``

**L603** `        if not self.chunks:`

**L604** `            self.embedding_state = {}`

**L605** `            self._chroma_index().reset()`

**L606** `            self.last_incremental_at = _utc_now()`

**L607** `            self.index_mode = INDEX_MODE_INCREMENTAL`

**L608** `            self.invalidate_cache()`

**L609** `            return False`

**L610** ``

**L611** `        old_vocab = dict(self.embedding_state.get("vocab") or {})`

**L612** `        retriever = EmbeddingRetriever(self.chunks)`

**L613** `        self.embedding_state = retriever._client.model.export_state()`

**L614** `        new_vocab = dict(self.embedding_state.get("vocab") or {})`

**L615** `        vocab_expanded = len(new_vocab) > len(old_vocab) and bool(old_vocab)`

**L616** ``

**L617** `        if vocab_expanded:`

**L618** `            affected_chunks = list(self.chunks)`

**L619** ``

**L620** `        client = retriever._client`

**L621** `        vectors = client.embed_batch([c.text for c in affected_chunks])`

**L622** `        chroma = self._chroma_index()`

**L623** `        if vocab_expanded:`

**L624** `            # TF-IDF 维度变化时 Chroma collection 须重建`

**L625** `            chroma.reset()`

**L626** `        chroma.upsert_chunks(affected_chunks, vectors)`

**L627** `        self.last_incremental_at = _utc_now()`

**L628** `        self.index_mode = INDEX_MODE_INCREMENTAL`

**L629** `        self.invalidate_cache()`

**L630** `        return vocab_expanded`

**L631** ``

**L632** `    def _build_rag_service(self) -> RAGContextService:`

**L633** `        if not self.chunks:`

**L634** `            return RAGContextService()`

**L635** ``

**L636** `        from rag.embedding import EmbeddingClient`

**L637** `        from rag.retriever import KeywordRetriever`

**L638** ``

**L639** `        client = EmbeddingClient()`

**L640** `        client.model.load_state(self.embedding_state)`

**L641** `        self._sync_chroma_from_json()`

**L642** `        chroma = self._chroma_index()`

**L643** `        vector = ChromaEmbeddingRetriever(self.chunks, chroma, client=client)`

**L644** `        keyword = KeywordRetriever(self.chunks)`

**L645** `        cfg = self.get_retrieval_config()`

**L646** `        hybrid = HybridRetriever(self.chunks, keyword, vector, config=cfg)`

**L647** `        rerank_cfg = self.get_rerank_config()`

**L648** `        reranking = RerankingRetriever(`

**L649** `            hybrid,`

**L650** `            reranker=MockCrossEncoderReranker(),`

**L651** `            config=rerank_cfg,`

**L652** `        )`

**L653** `        rewrite_cfg = self.get_rewrite_config()`

**L654** `        rewriting = RewritingRetriever(reranking, config=rewrite_cfg)`

**L655** `        expansion_cfg = self.get_expansion_config()`

**L656** `        expanding = ExpandingRetriever(rewriting, config=expansion_cfg)`

**L657** `        route_cfg = self.get_route_config()`

**L658** `        retriever = RoutingRetriever(expanding, config=route_cfg)`

**L659** `        index = DocumentIndex(chunks=self.chunks, retriever=retriever)`

**L660** `        return RAGContextService(index)`

**L661** ``

**L662** ``

**L663** `_store: KnowledgeStore | None = None`

**L664** `_store_lock = threading.Lock()`

**L665** ``

**L666** ``

**L667** `def get_knowledge_store(*, reload: bool = False) -> KnowledgeStore:`

**L668** `    """全局知识库单例（API 与 factory 共享）"""`

**L669** `    global _store`

**L670** `    with _store_lock:`

**L671** `        if _store is None or reload:`

**L672** `            _store = KnowledgeStore.load_or_bootstrap()`

**L673** `        return _store`

**L674** ``

**L675** ``

**L676** `def set_knowledge_store(store: KnowledgeStore) -> None:`

**L677** `    """测试注入用"""`

**L678** `    global _store`

**L679** `    with _store_lock:`

**L680** `        _store = store`

**L681** ``

**L682** ``

**L683** `def _default_store_path() -> Path:`

**L684** `    return get_path("knowledge_store")`

**L685** ``

**L686** ``

**L687** `def _utc_now() -> str:`

**L688** `    return datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")`

**L689** ``

**L690** ``

**L691** `def _chunk_to_dict(chunk: TextChunk) -> dict[str, Any]:`

**L692** `    return {`

**L693** `        "chunk_id": chunk.chunk_id,`

**L694** `        "text": chunk.text,`

**L695** `        "source": chunk.source,`

**L696** `        "index": chunk.index,`

**L697** `        "start_char": chunk.start_char,`

**L698** `        "end_char": chunk.end_char,`

**L699** `    }`

**L700** ``

**L701** ``

**L702** `def _chunk_from_dict(data: dict[str, Any]) -> TextChunk:`

**L703** `    return TextChunk(`

**L704** `        chunk_id=str(data.get("chunk_id", "")),`

**L705** `        text=str(data.get("text", "")),`

**L706** `        source=str(data.get("source", "")),`

**L707** `        index=int(data.get("index", 0)),`

**L708** `        start_char=int(data.get("start_char", 0)),`

**L709** `        end_char=int(data.get("end_char", 0)),`

**L710** `    )`


## 附录 B：api/knowledge.py 走读

### `nexus-agent-platform/src/api/knowledge.py` 逐行走读

共 **412** 行。

**L1** `"""`

**L2** `知识库 REST API — 文档上传、分块调参与检索评估`

**L3** ``

**L4** `需求：ZL-NA-REQ-025 / ZL-NA-REQ-026 / ZL-NA-REQ-027 / ZL-NA-REQ-028 / ZL-NA-REQ-029 / ZL-NA-REQ-030 / ZL-NA-REQ-031 / ZL-NA-REQ-032 / ZL-NA-REQ-033 / ZL-NA-REQ-034 / ZL-NA-REQ-035 / ZL-NA-REQ-036 / ZL-NA-REQ-037`

**L5** `"""`

**L6** ``

**L7** `from __future__ import annotations`

**L8** ``

**L9** `from pathlib import Path`

**L10** ``

**L11** `from fastapi import APIRouter, File, HTTPException, UploadFile`

**L12** ``

**L13** `from api.schemas import (`

**L14** `    ChunkConfigRequest,`

**L15** `    ChunkConfigResponse,`

**L16** `    CitationConfigRequest,`

**L17** `    CitationConfigResponse,`

**L18** `    CitationPreviewRequest,`

**L19** `    CitationPreviewResponse,`

**L20** `    ExpansionConfigRequest,`

**L21** `    ExpansionConfigResponse,`

**L22** `    ExpansionPreviewRequest,`

**L23** `    ExpansionPreviewResponse,`

**L24** `    EvaluateRequest,`

**L25** `    EvaluateResponse,`

**L26** `    KnowledgeStatusResponse,`

**L27** `    KnowledgeUploadResponse,`

**L28** `    RebuildRequest,`

**L29** `    RebuildResponse,`

**L30** `    RerankConfigRequest,`

**L31** `    RerankConfigResponse,`

**L32** `    RewriteConfigRequest,`

**L33** `    RewriteConfigResponse,`

**L34** `    RewritePreviewRequest,`

**L35** `    RewritePreviewResponse,`

**L36** `    RouteConfigRequest,`

**L37** `    RouteConfigResponse,`

**L38** `    RoutePreviewRequest,`

**L39** `    RoutePreviewResponse,`

**L40** `    ValidationConfigRequest,`

**L41** `    ValidationConfigResponse,`

**L42** `    ValidationPreviewRequest,`

**L43** `    ValidationPreviewResponse,`

**L44** `    RetrievalConfigRequest,`

**L45** `    RetrievalConfigResponse,`

**L46** `)`

**L47** `from api.sessions import session_manager`

**L48** `from core.exceptions import NexusError, StorageError`

**L49** `from rag.chunk_config import PRESET_CONFIGS, ChunkConfig`
  → evaluate / chunk-config API 相关行。

**L50** `from rag.ingestion import ingest_upload`

**L51** `from rag.knowledge_rebuild import rebuild_store, rebuild_with_best_config`
  → evaluate / chunk-config API 相关行。

**L52** `from rag.knowledge_store import get_knowledge_store`

**L53** `from rag.citation_config import CitationConfig`

**L54** `from rag.expansion_config import ExpansionConfig`

**L55** `from rag.query_expander import build_expander`

**L56** `from rag.query_rewriter import RuleBasedQueryRewriter`

**L57** `from rag.query_router import RuleBasedQueryRouter`

**L58** `from rag.route_config import RouteConfig`

**L59** `from rag.validation_config import ValidationConfig`

**L60** `from rag.rerank_config import RerankConfig`

**L61** `from rag.rewrite_config import RewriteConfig`

**L62** `from rag.retrieval_config import RetrievalConfig`

**L63** `from rag.retrieval_eval import EvalQuery, pick_best_config, run_ab_experiment`

**L64** `from tools.doc_parser import parse_bytes`

**L65** ``

**L66** `router = APIRouter(prefix="/api/knowledge", tags=["knowledge"])`

**L67** ``

**L68** `MAX_UPLOAD_BYTES = 512_000  # 500 KB 教学上限`

**L69** `_EVAL_SAMPLE = (`
  → evaluate / chunk-config API 相关行。

**L70** `    Path(__file__).resolve().parent.parent / "day26" / "sample_docs" / "product_notice.md"`

**L71** `)`

**L72** ``

**L73** ``

**L74** `@router.get("/retrieval-config", response_model=RetrievalConfigResponse)`

**L75** `def get_retrieval_config() -> RetrievalConfigResponse:`

**L76** `    """返回当前检索模式（vector / keyword / hybrid）与融合参数"""`

**L77** `    cfg = get_knowledge_store().get_retrieval_config()`

**L78** `    return RetrievalConfigResponse(**cfg.to_dict())`

**L79** ``

**L80** ``

**L81** `@router.put("/retrieval-config", response_model=RetrievalConfigResponse)`

**L82** `def update_retrieval_config(body: RetrievalConfigRequest) -> RetrievalConfigResponse:`

**L83** `    """更新检索策略；变更后清除 RAG 缓存"""`

**L84** `    store = get_knowledge_store()`

**L85** `    try:`

**L86** `        cfg = RetrievalConfig.from_dict(body.model_dump())`

**L87** `        cfg.validate()`

**L88** `        store.set_retrieval_config(cfg)`

**L89** `        store.save()`

**L90** `    except ValueError as exc:`

**L91** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L92** `    return RetrievalConfigResponse(**cfg.to_dict())`

**L93** ``

**L94** ``

**L95** `@router.get("/rerank-config", response_model=RerankConfigResponse)`

**L96** `def get_rerank_config() -> RerankConfigResponse:`

**L97** `    """返回 rerank 开关、候选池大小与模型标识"""`

**L98** `    cfg = get_knowledge_store().get_rerank_config()`

**L99** `    return RerankConfigResponse(**cfg.to_dict())`

**L100** ``

**L101** ``

**L102** `@router.put("/rerank-config", response_model=RerankConfigResponse)`

**L103** `def update_rerank_config(body: RerankConfigRequest) -> RerankConfigResponse:`

**L104** `    """更新 rerank 策略；变更后清除 RAG 缓存"""`

**L105** `    store = get_knowledge_store()`

**L106** `    try:`

**L107** `        cfg = RerankConfig.from_dict(body.model_dump())`

**L108** `        cfg.validate()`

**L109** `        store.set_rerank_config(cfg)`

**L110** `        store.save()`

**L111** `    except ValueError as exc:`

**L112** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L113** `    return RerankConfigResponse(**cfg.to_dict())`

**L114** ``

**L115** ``

**L116** `@router.get("/rewrite-config", response_model=RewriteConfigResponse)`

**L117** `def get_rewrite_config() -> RewriteConfigResponse:`

**L118** `    """返回查询改写开关与规则模式"""`

**L119** `    cfg = get_knowledge_store().get_rewrite_config()`

**L120** `    return RewriteConfigResponse(**cfg.to_dict())`

**L121** ``

**L122** ``

**L123** `@router.put("/rewrite-config", response_model=RewriteConfigResponse)`

**L124** `def update_rewrite_config(body: RewriteConfigRequest) -> RewriteConfigResponse:`

**L125** `    """更新查询改写策略；变更后清除 RAG 缓存"""`

**L126** `    store = get_knowledge_store()`

**L127** `    try:`

**L128** `        cfg = RewriteConfig.from_dict(body.model_dump())`

**L129** `        cfg.validate()`

**L130** `        store.set_rewrite_config(cfg)`

**L131** `        store.save()`

**L132** `    except ValueError as exc:`

**L133** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L134** `    return RewriteConfigResponse(**cfg.to_dict())`

**L135** ``

**L136** ``

**L137** `@router.post("/rewrite-preview", response_model=RewritePreviewResponse)`

**L138** `def rewrite_preview(body: RewritePreviewRequest) -> RewritePreviewResponse:`

**L139** `    """预览单条 query 的规则改写结果（不触发检索）"""`

**L140** `    store = get_knowledge_store()`

**L141** `    cfg = store.get_rewrite_config()`

**L142** `    rewriter = RuleBasedQueryRewriter(config=cfg)`

**L143** `    result = rewriter.rewrite(body.query)`

**L144** `    return RewritePreviewResponse(**result.to_dict())`

**L145** ``

**L146** ``

**L147** `@router.get("/citation-config", response_model=CitationConfigResponse)`

**L148** `def get_citation_config() -> CitationConfigResponse:`

**L149** `    """返回引用溯源开关与展示参数"""`

**L150** `    cfg = get_knowledge_store().get_citation_config()`

**L151** `    return CitationConfigResponse(**cfg.to_dict())`

**L152** ``

**L153** ``

**L154** `@router.put("/citation-config", response_model=CitationConfigResponse)`

**L155** `def update_citation_config(body: CitationConfigRequest) -> CitationConfigResponse:`

**L156** `    """更新引用溯源策略并持久化"""`

**L157** `    store = get_knowledge_store()`

**L158** `    try:`

**L159** `        cfg = CitationConfig.from_dict(body.model_dump())`

**L160** `        cfg.validate()`

**L161** `        store.set_citation_config(cfg)`

**L162** `        store.save()`

**L163** `    except ValueError as exc:`

**L164** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L165** `    return CitationConfigResponse(**cfg.to_dict())`

**L166** ``

**L167** ``

**L168** `@router.post("/citation-preview", response_model=CitationPreviewResponse)`

**L169** `def citation_preview(body: CitationPreviewRequest) -> CitationPreviewResponse:`

**L170** `    """预览单条 query 的检索引用（含 rewrite / expansion 审计）"""`

**L171** `    store = get_knowledge_store()`

**L172** `    data = store.fetch_citations(body.query)`

**L173** `    return CitationPreviewResponse(**data)`

**L174** ``

**L175** ``

**L176** `@router.get("/expansion-config", response_model=ExpansionConfigResponse)`

**L177** `def get_expansion_config() -> ExpansionConfigResponse:`

**L178** `    """返回多 query 扩展开关与参数"""`

**L179** `    cfg = get_knowledge_store().get_expansion_config()`

**L180** `    return ExpansionConfigResponse(**cfg.to_dict())`

**L181** ``

**L182** ``

**L183** `@router.put("/expansion-config", response_model=ExpansionConfigResponse)`

**L184** `def update_expansion_config(body: ExpansionConfigRequest) -> ExpansionConfigResponse:`

**L185** `    """更新多 query 扩展策略并持久化"""`

**L186** `    store = get_knowledge_store()`

**L187** `    try:`

**L188** `        cfg = ExpansionConfig.from_dict(body.model_dump())`

**L189** `        cfg.validate()`

**L190** `        store.set_expansion_config(cfg)`

**L191** `        store.save()`

**L192** `    except ValueError as exc:`

**L193** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L194** `    return ExpansionConfigResponse(**cfg.to_dict())`

**L195** ``

**L196** ``

**L197** `@router.post("/expansion-preview", response_model=ExpansionPreviewResponse)`

**L198** `def expansion_preview(body: ExpansionPreviewRequest) -> ExpansionPreviewResponse:`

**L199** `    """预览单条 query 的多路扩展结果"""`

**L200** `    store = get_knowledge_store()`

**L201** `    cfg = store.get_expansion_config()`

**L202** `    expander = build_expander(cfg)`

**L203** `    result = expander.expand(body.query)`

**L204** `    return ExpansionPreviewResponse(**result.to_dict())`

**L205** ``

**L206** ``

**L207** `@router.get("/route-config", response_model=RouteConfigResponse)`

**L208** `def get_route_config() -> RouteConfigResponse:`

**L209** `    """返回检索管线路由开关与默认意图"""`

**L210** `    cfg = get_knowledge_store().get_route_config()`

**L211** `    return RouteConfigResponse(**cfg.to_dict())`

**L212** ``

**L213** ``

**L214** `@router.put("/route-config", response_model=RouteConfigResponse)`

**L215** `def update_route_config(body: RouteConfigRequest) -> RouteConfigResponse:`

**L216** `    """更新检索管线路由策略并持久化"""`

**L217** `    store = get_knowledge_store()`

**L218** `    try:`

**L219** `        cfg = RouteConfig.from_dict(body.model_dump())`

**L220** `        cfg.validate()`

**L221** `        store.set_route_config(cfg)`

**L222** `        store.save()`

**L223** `    except ValueError as exc:`

**L224** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L225** `    return RouteConfigResponse(**cfg.to_dict())`

**L226** ``

**L227** ``

**L228** `@router.post("/route-preview", response_model=RoutePreviewResponse)`

**L229** `def route_preview(body: RoutePreviewRequest) -> RoutePreviewResponse:`

**L230** `    """预览单条 query 的路由决策（expand/rewrite 开关）"""`

**L231** `    store = get_knowledge_store()`

**L232** `    cfg = store.get_route_config()`

**L233** `    router = RuleBasedQueryRouter(config=cfg)`

**L234** `    result = router.route(body.query)`

**L235** `    return RoutePreviewResponse(**result.to_dict())`

**L236** ``

**L237** ``

**L238** `@router.get("/validation-config", response_model=ValidationConfigResponse)`

**L239** `def get_validation_config() -> ValidationConfigResponse:`

**L240** `    """返回答案校验开关与阈值"""`

**L241** `    cfg = get_knowledge_store().get_validation_config()`

**L242** `    return ValidationConfigResponse(**cfg.to_dict())`

**L243** ``

**L244** ``

**L245** `@router.put("/validation-config", response_model=ValidationConfigResponse)`

**L246** `def update_validation_config(body: ValidationConfigRequest) -> ValidationConfigResponse:`

**L247** `    """更新答案校验策略并持久化"""`

**L248** `    store = get_knowledge_store()`

**L249** `    try:`

**L250** `        cfg = ValidationConfig.from_dict(body.model_dump())`

**L251** `        cfg.validate()`

**L252** `        store.set_validation_config(cfg)`

**L253** `        store.save()`

**L254** `    except ValueError as exc:`

**L255** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L256** `    return ValidationConfigResponse(**cfg.to_dict())`

**L257** ``

**L258** ``

**L259** `@router.post("/validation-preview", response_model=ValidationPreviewResponse)`

**L260** `def validation_preview(body: ValidationPreviewRequest) -> ValidationPreviewResponse:`

**L261** `    """预览单条 query + reply 的引用一致性校验"""`

**L262** `    from rag.answer_validator import RuleBasedAnswerValidator`

**L263** ``

**L264** `    store = get_knowledge_store()`

**L265** `    citations = list(body.citations)`

**L266** `    if not citations:`

**L267** `        cite_data = store.fetch_citations(body.query)`

**L268** `        citations = cite_data.get("citations") or []`

**L269** `    cfg = store.get_validation_config()`

**L270** `    preview_cfg = ValidationConfig.from_dict({**cfg.to_dict(), "enabled": True})`

**L271** `    result = RuleBasedAnswerValidator(config=preview_cfg).validate(`

**L272** `        body.query, body.reply, citations`

**L273** `    )`

**L274** `    return ValidationPreviewResponse(**result.to_dict())`

**L275** ``

**L276** ``

**L277** `@router.get("/chunk-config", response_model=ChunkConfigResponse)`
  → evaluate / chunk-config API 相关行。

**L278** `def get_chunk_config() -> ChunkConfigResponse:`
  → evaluate / chunk-config API 相关行。

**L279** `    """返回当前知识库默认分块参数"""`

**L280** `    cfg = get_knowledge_store().get_chunk_config()`
  → evaluate / chunk-config API 相关行。

**L281** `    return ChunkConfigResponse(**cfg.to_dict())`

**L282** ``

**L283** ``

**L284** `@router.put("/chunk-config", response_model=ChunkConfigResponse)`
  → evaluate / chunk-config API 相关行。

**L285** `def update_chunk_config(body: ChunkConfigRequest) -> ChunkConfigResponse:`
  → evaluate / chunk-config API 相关行。

**L286** `    """更新默认分块参数（影响后续上传）"""`

**L287** `    store = get_knowledge_store()`

**L288** `    try:`

**L289** `        cfg = ChunkConfig.from_dict(body.model_dump())`

**L290** `        cfg.validate()`

**L291** `        store.set_chunk_config(cfg)`
  → evaluate / chunk-config API 相关行。

**L292** `        store.save()`

**L293** `    except ValueError as exc:`

**L294** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L295** `    return ChunkConfigResponse(**cfg.to_dict())`

**L296** ``

**L297** ``

**L298** `@router.post("/evaluate", response_model=EvaluateResponse)`
  → evaluate / chunk-config API 相关行。

**L299** `def evaluate_chunk_configs(body: EvaluateRequest | None = None) -> EvaluateResponse:`
  → evaluate / chunk-config API 相关行。

**L300** `    """`

**L301** `    对内置样例文档运行 A/B 分块评估，返回 hit@1 与推荐配置。`

**L302** ``

**L303** `    默认使用 PRESET_CONFIGS 四套预设与 day27 评估问句集。`
  → evaluate / chunk-config API 相关行。

**L304** `    """`

**L305** `    body = body or EvaluateRequest()`

**L306** `    if not _EVAL_SAMPLE.is_file():`
  → evaluate / chunk-config API 相关行。

**L307** `        raise HTTPException(status_code=500, detail="评估样例文档缺失")`

**L308** ``

**L309** `    doc = parse_bytes(_EVAL_SAMPLE.read_bytes(), _EVAL_SAMPLE.name)`
  → evaluate / chunk-config API 相关行。

**L310** `    from day27.constants import EVAL_QUERIES`
  → evaluate / chunk-config API 相关行。

**L311** ``

**L312** `    queries = [EvalQuery.from_dict(q) for q in EVAL_QUERIES]`
  → evaluate / chunk-config API 相关行。

**L313** ``

**L314** `    if body.use_presets and not body.configs:`

**L315** `        configs = list(PRESET_CONFIGS)`
  → evaluate / chunk-config API 相关行。

**L316** `    else:`

**L317** `        configs = [ChunkConfig.from_dict(c.model_dump()) for c in body.configs]`

**L318** `        for c in configs:`

**L319** `            c.validate()`

**L320** ``

**L321** `    results = run_ab_experiment(doc, configs, queries)`

**L322** `    best = pick_best_config(results)`

**L323** `    if not best:`

**L324** `        raise HTTPException(status_code=500, detail="评估未产生结果")`

**L325** ``

**L326** `    return EvaluateResponse(`

**L327** `        best_config=best.to_dict()["config"],`

**L328** `        results=[r.to_dict() for r in results],`

**L329** `        eval_query_count=len(queries),`

**L330** `    )`

**L331** ``

**L332** ``

**L333** `@router.post("/rebuild", response_model=RebuildResponse)`
  → evaluate / chunk-config API 相关行。

**L334** `def rebuild_knowledge_base(body: RebuildRequest | None = None) -> RebuildResponse:`
  → evaluate / chunk-config API 相关行。

**L335** `    """`

**L336** `    按当前 chunk_config（或 evaluate 最优配置）全量重建知识库。`
  → evaluate / chunk-config API 相关行。

**L337** ``

**L338** `    重扫 sample_docs + knowledge_uploads，清空后重新分块与索引。`

**L339** `    """`

**L340** `    body = body or RebuildRequest()`

**L341** `    store = get_knowledge_store()`

**L342** ``

**L343** `    if body.apply_best_config:`

**L344** `        if not _EVAL_SAMPLE.is_file():`
  → evaluate / chunk-config API 相关行。

**L345** `            raise HTTPException(status_code=500, detail="评估样例文档缺失")`

**L346** `        report = rebuild_with_best_config(`
  → evaluate / chunk-config API 相关行。

**L347** `            store,`

**L348** `            eval_sample_path=_EVAL_SAMPLE,`
  → evaluate / chunk-config API 相关行。

**L349** `            include_sample_docs=body.include_sample_docs,`

**L350** `        )`

**L351** `    else:`

**L352** `        report = rebuild_store(store, include_sample_docs=body.include_sample_docs)`
  → evaluate / chunk-config API 相关行。

**L353** ``

**L354** `    cleared = session_manager.clear_all()`

**L355** `    data = report.to_dict()`

**L356** `    data["sessions_cleared"] = cleared`

**L357** `    return RebuildResponse(**data)`

**L358** ``

**L359** ``

**L360** `@router.get("/status", response_model=KnowledgeStatusResponse)`

**L361** `def knowledge_status() -> KnowledgeStatusResponse:`

**L362** `    """返回知识库文档数、分块数、支持格式与文档列表"""`

**L363** `    store = get_knowledge_store()`

**L364** `    data = store.status_dict()`

**L365** `    return KnowledgeStatusResponse(**data)`

**L366** ``

**L367** ``

**L368** `@router.post("/upload", response_model=KnowledgeUploadResponse)`

**L369** `async def upload_document(`

**L370** `    file: UploadFile = File(..., description="企业文档 (.txt / .md / .pdf)"),`

**L371** `) -> KnowledgeUploadResponse:`

**L372** `    """`

**L373** `    上传企业文档到知识库：解析 → 落盘 → 分块 → Chroma 向量索引 → 持久化。`

**L374** ``

**L375** `    Day 26 起支持 Markdown 与 PDF。上传成功后清除服务端会话。`

**L376** `    """`

**L377** `    if not file.filename:`

**L378** `        raise HTTPException(status_code=422, detail="缺少文件名")`

**L379** ``

**L380** `    data = await file.read()`

**L381** `    if not data:`

**L382** `        raise HTTPException(status_code=422, detail="文件内容为空")`

**L383** `    if len(data) > MAX_UPLOAD_BYTES:`

**L384** `        raise HTTPException(status_code=413, detail="文件超过 500KB 上限")`

**L385** ``

**L386** `    try:`

**L387** `        meta = ingest_upload(data, file.filename)`

**L388** `    except ValueError as exc:`

**L389** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L390** `    except NexusError as exc:`

**L391** `        status = 400 if exc.code in (`

**L392** `            "PDF_PARSE_ERROR", "PDF_EMPTY", "UNSUPPORTED_FORMAT", "EMPTY_FILE"`

**L393** `        ) else 500`

**L394** `        raise HTTPException(status_code=status, detail=exc.message) from exc`

**L395** `    except StorageError as exc:`

**L396** `        raise HTTPException(status_code=500, detail=exc.message) from exc`

**L397** `    except UnicodeDecodeError as exc:`

**L398** `        raise HTTPException(status_code=400, detail="文本文件须为 UTF-8 编码") from exc`

**L399** ``

**L400** `    cleared = session_manager.clear_all()`

**L401** `    store = get_knowledge_store()`

**L402** ``

**L403** `    return KnowledgeUploadResponse(`

**L404** `        filename=meta.name,`

**L405** `        format=meta.format,`

**L406** `        chunk_count=meta.chunk_count,`
  → evaluate / chunk-config API 相关行。

**L407** `        document_count=store.document_count,`

**L408** `        total_chunks=store.chunk_count,`
  → evaluate / chunk-config API 相关行。

**L409** `        sessions_cleared=cleared,`

**L410** `        index_mode=store.index_mode,`

**L411** `        message=f"文档已入库（{meta.format}），增量索引已更新",`

**L412** `    )`


## 失败模式

| 失败 | 处理 |
|------|------|
| overlap 非法 | validate → 422 |
| 样例文件缺失 | 500 + detail |
| 空 configs 结果 | 500「评估未产生结果」 |
