# Day 27 架构设计

**需求**：ZL-NA-REQ-027

## 分层视图

```
┌─────────────────────────────────────────┐
│  frontend/knowledge.js  (#kb-eval-btn)  │
└──────────────────┬──────────────────────┘
                   │ POST /evaluate
┌──────────────────▼──────────────────────┐
│  api/knowledge.py                       │
│  get/put chunk-config · evaluate        │
└───────┬─────────────────────┬───────────┘
        │                     │
        ▼                     ▼
┌───────────────┐    ┌────────────────────┐
│ KnowledgeStore│    │ retrieval_eval     │
│ chunk_config  │    │ (内存临时 retriever)│
│ persist       │    │ 不写 chunks        │
└───────────────┘    └─────────┬──────────┘
                               │
                               ▼
                    ┌──────────────────────┐
                    │ chunk_config         │
                    │ chunk_strategies     │
                    │ embedding_retriever  │
                    └──────────────────────┘
```

## 核心隔离原则

**评估路径**调用 `build_retriever_for_doc`：在内存中对单份 `ParsedDocument` 分块并训练 TF-IDF，**从不**调用 `store._append_chunks` 或 `save()`。

**生产路径**由 `upload` / `ingest` 使用 `store.get_chunk_config()` 分块并写入索引。

## ChunkConfig 源码锚点

```python
"""
分块参数配置 — chunk_size / overlap / strategy

需求：ZL-NA-REQ-027
"""

from __future__ import annotations

from dataclasses import asdict, dataclass, field
from typing import Any


@dataclass
class ChunkConfig:
    """知识库分块参数（可持久化、可 A/B 对比）"""

    chunk_size: int = 200
    overlap: int = 40
    strategy: str = "auto"  # auto | fixed | markdown
    name: str = "default"

    def validate(self) -> None:
        if self.chunk_size <= 0:
            raise ValueError("chunk_size 必须为正整数")
        if self.overlap < 0 or self.overlap >= self.chunk_size:
            raise ValueError("overlap 必须满足 0 <= overlap < chunk_size")
        if self.strategy not in ("auto", "fixed", "markdown"):
            raise ValueError("strategy 须为 auto / fixed / markdown")

    def to_dict(self) -> dict[str, Any]:
        return asdict(self)

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ChunkConfig:
        return cls(
            chunk_size=int(data.get("chunk_size", 200)),
            overlap=int(data.get("overlap", 40)),
            strategy=str(data.get("strategy", "auto")),
            name=str(data.get("name", "default")),
        )


DEFAULT_CHUNK_CONFIG = ChunkConfig()

PRESET_CONFIGS: tuple[ChunkConfig, ...] = (
    ChunkConfig(name="compact", chunk_size=120, overlap=20, strategy="auto"),
    ChunkConfig(name="default", chunk_size=200, overlap=40, strategy="auto"),
    ChunkConfig(name="wide", chunk_size=400, overlap=60, strategy="auto"),
    ChunkConfig(name="markdown_wide", chunk_size=500, overlap=50, strategy="markdown"),
)
```


## evaluate 端点伪代码

```python
doc = parse_bytes(_EVAL_SAMPLE)
queries = [EvalQuery.from_dict(q) for q in EVAL_QUERIES]
configs = PRESET_CONFIGS if body.use_presets else body.configs
results = run_ab_experiment(doc, configs, queries)
return EvaluateResponse(best_config=results[0], ...)
```

## 持久化字段

`store.json` 片段：

```json
{
  "chunk_config": {
    "chunk_size": 200,
    "overlap": 40,
    "strategy": "auto",
    "name": "default"
  }
}
```



## 附录 A：knowledge_store.py 走读

### `nexus-agent-platform/src/rag/knowledge_store.py` 逐行走读

共 **833** 行。

**L1** `"""`

**L2** `知识库存储 — 文档 ingestion、分块索引与 JSON 持久化`

**L3** ``

**L4** `将 Day 19–20 的 RAG 管线升级为可写入、可落盘的企业知识库 MVP。`

**L5** ``

**L6** `需求：ZL-NA-REQ-025`

**L7** `"""`

**L8** ``

**L9** `from __future__ import annotations`

**L10** ``

**L11** `import threading`

**L12** `from dataclasses import dataclass, field`

**L13** `from datetime import datetime, timezone`

**L14** `from pathlib import Path`

**L15** `from typing import Any`

**L16** ``

**L17** `from core.paths import get_path`

**L18** `from agent.approval_config import ApprovalConfig`

**L19** `from agent.dify_config import DifyConfig`

**L20** `from agent.mcp_config import McpConfig`

**L21** `from agent.supervisor_config import SupervisorConfig`

**L22** `from agent.executor_config import ExecutorConfig`

**L23** `from agent.graph_config import GraphConfig`

**L24** `from agent.react_config import ReactConfig`

**L25** `from rag.chunker import TextChunk, chunk_documents, chunk_text`

**L26** `from rag.chunk_config import DEFAULT_CHUNK_CONFIG, ChunkConfig`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L27** `from rag.chunk_strategies import chunk_from_parsed`

**L28** `from rag.context import DocumentIndex, RAGContextService`

**L29** `from rag.chroma_retriever import ChromaEmbeddingRetriever`

**L30** `from rag.chroma_store import VECTOR_BACKEND, ChromaVectorIndex`

**L31** `from rag.citation_config import CitationConfig`

**L32** `from rag.expanding_retriever import ExpandingRetriever`

**L33** `from rag.expansion_config import ExpansionConfig`

**L34** `from rag.answer_validator import RuleBasedAnswerValidator, ValidationResult`

**L35** `from rag.route_config import RouteConfig`

**L36** `from rag.routing_retriever import RoutingRetriever`

**L37** `from rag.validation_config import ValidationConfig`

**L38** `from rag.hybrid_retriever import HybridRetriever`

**L39** `from rag.rerank_config import RerankConfig`

**L40** `from rag.reranker import MockCrossEncoderReranker`

**L41** `from rag.reranking_retriever import RerankingRetriever`

**L42** `from rag.retrieval_config import RetrievalConfig`

**L43** `from rag.rewrite_config import RewriteConfig`

**L44** `from rag.rewriting_retriever import RewritingRetriever`

**L45** `from tools.doc_reader import DocumentRecord, read_text_file`

**L46** `from tools.parsers.base import ParsedDocument`

**L47** `from utils.json_utils import load_json, save_json`

**L48** `from utils.text_utils import clean_text`

**L49** ``

**L50** `STORE_VERSION = "1.1"`

**L51** `PLATFORM_VERSION = "0.45.0"`

**L52** `INDEX_MODE_INCREMENTAL = "incremental"`

**L53** `INDEX_MODE_FULL = "full"`

**L54** ``

**L55** ``

**L56** `@dataclass`

**L57** `class KnowledgeDocument:`

**L58** `    """已入库文档元数据"""`

**L59** ``

**L60** `    name: str`

**L61** `    ingested_at: str`

**L62** `    size_bytes: int = 0`

**L63** `    chunk_count: int = 0`

**L64** `    format: str = "txt"`

**L65** ``

**L66** `    def to_dict(self) -> dict[str, Any]:`

**L67** `        return {`

**L68** `            "name": self.name,`

**L69** `            "ingested_at": self.ingested_at,`

**L70** `            "size_bytes": self.size_bytes,`

**L71** `            "chunk_count": self.chunk_count,`

**L72** `            "format": self.format,`

**L73** `        }`

**L74** ``

**L75** `    @classmethod`

**L76** `    def from_dict(cls, data: dict[str, Any]) -> KnowledgeDocument:`

**L77** `        return cls(`

**L78** `            name=str(data.get("name", "")),`

**L79** `            ingested_at=str(data.get("ingested_at", "")),`

**L80** `            size_bytes=int(data.get("size_bytes", 0)),`

**L81** `            chunk_count=int(data.get("chunk_count", 0)),`

**L82** `            format=str(data.get("format", "txt")),`

**L83** `        )`

**L84** ``

**L85** ``

**L86** `@dataclass`

**L87** `class KnowledgeStore:`

**L88** `    """`

**L89** `    企业知识库 — 分块 + Chroma 向量索引 + JSON 元数据持久化`

**L90** ``

**L91** `    典型用法：`

**L92** `        store = KnowledgeStore.load_or_bootstrap()`

**L93** `        store.ingest_text("新产品说明…", filename="notice.txt")`

**L94** `        rag = store.as_rag_service()`

**L95** `    """`

**L96** ``

**L97** `    documents: list[KnowledgeDocument] = field(default_factory=list)`

**L98** `    chunks: list[TextChunk] = field(default_factory=list)`

**L99** `    embedding_state: dict[str, Any] = field(default_factory=dict)`

**L100** `    vector_backend: str = VECTOR_BACKEND`

**L101** `    chunk_config: ChunkConfig = field(default_factory=ChunkConfig)`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L102** `    last_rebuilt_at: str | None = None`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L103** `    last_incremental_at: str | None = None`

**L104** `    index_mode: str = INDEX_MODE_FULL`

**L105** `    retrieval_config: RetrievalConfig = field(default_factory=RetrievalConfig)`

**L106** `    rerank_config: RerankConfig = field(default_factory=RerankConfig)`

**L107** `    rewrite_config: RewriteConfig = field(default_factory=RewriteConfig)`

**L108** `    citation_config: CitationConfig = field(default_factory=CitationConfig)`

**L109** `    expansion_config: ExpansionConfig = field(default_factory=ExpansionConfig)`

**L110** `    route_config: RouteConfig = field(default_factory=RouteConfig)`

**L111** `    validation_config: ValidationConfig = field(default_factory=ValidationConfig)`

**L112** `    react_config: ReactConfig = field(default_factory=ReactConfig)`

**L113** `    executor_config: ExecutorConfig = field(default_factory=ExecutorConfig)`

**L114** `    graph_config: GraphConfig = field(default_factory=GraphConfig)`

**L115** `    approval_config: ApprovalConfig = field(default_factory=ApprovalConfig)`

**L116** `    supervisor_config: SupervisorConfig = field(default_factory=SupervisorConfig)`

**L117** `    mcp_config: McpConfig = field(default_factory=McpConfig)`

**L118** `    dify_config: DifyConfig = field(default_factory=DifyConfig)`

**L119** `    store_path: Path | None = None`

**L120** `    chroma_path: Path | None = None`

**L121** `    _rag_service: RAGContextService | None = field(default=None, repr=False)`

**L122** ``

**L123** `    @property`

**L124** `    def chunk_count(self) -> int:`

**L125** `        return len(self.chunks)`

**L126** ``

**L127** `    @property`

**L128** `    def document_count(self) -> int:`

**L129** `        return len(self.documents)`

**L130** ``

**L131** `    def as_rag_service(self) -> RAGContextService:`

**L132** `        """构建或返回缓存的 RAGContextService"""`

**L133** `        if self._rag_service is None:`

**L134** `            self._rag_service = self._build_rag_service()`

**L135** `        return self._rag_service`

**L136** ``

**L137** `    def invalidate_cache(self) -> None:`

**L138** `        self._rag_service = None`

**L139** ``

**L140** `    def get_chunk_config(self) -> ChunkConfig:`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L141** `        return ChunkConfig.from_dict(self.chunk_config.to_dict())`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L142** ``

**L143** `    def set_chunk_config(self, config: ChunkConfig) -> ChunkConfig:`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L144** `        config.validate()`

**L145** `        self.chunk_config = ChunkConfig.from_dict(config.to_dict())`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L146** `        return self.chunk_config`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L147** ``

**L148** `    def get_retrieval_config(self) -> RetrievalConfig:`

**L149** `        return RetrievalConfig.from_dict(self.retrieval_config.to_dict())`

**L150** ``

**L151** `    def set_retrieval_config(self, config: RetrievalConfig) -> RetrievalConfig:`

**L152** `        config.validate()`

**L153** `        self.retrieval_config = RetrievalConfig.from_dict(config.to_dict())`

**L154** `        self.invalidate_cache()`

**L155** `        return self.retrieval_config`

**L156** ``

**L157** `    def get_rerank_config(self) -> RerankConfig:`

**L158** `        return RerankConfig.from_dict(self.rerank_config.to_dict())`

**L159** ``

**L160** `    def set_rerank_config(self, config: RerankConfig) -> RerankConfig:`

**L161** `        config.validate()`

**L162** `        self.rerank_config = RerankConfig.from_dict(config.to_dict())`

**L163** `        self.invalidate_cache()`

**L164** `        return self.rerank_config`

**L165** ``

**L166** `    def get_rewrite_config(self) -> RewriteConfig:`

**L167** `        return RewriteConfig.from_dict(self.rewrite_config.to_dict())`

**L168** ``

**L169** `    def set_rewrite_config(self, config: RewriteConfig) -> RewriteConfig:`

**L170** `        config.validate()`

**L171** `        self.rewrite_config = RewriteConfig.from_dict(config.to_dict())`

**L172** `        self.invalidate_cache()`

**L173** `        return self.rewrite_config`

**L174** ``

**L175** `    def get_citation_config(self) -> CitationConfig:`

**L176** `        return CitationConfig.from_dict(self.citation_config.to_dict())`

**L177** ``

**L178** `    def set_citation_config(self, config: CitationConfig) -> CitationConfig:`

**L179** `        config.validate()`

**L180** `        self.citation_config = CitationConfig.from_dict(config.to_dict())`

**L181** `        return self.citation_config`

**L182** ``

**L183** `    def get_expansion_config(self) -> ExpansionConfig:`

**L184** `        return ExpansionConfig.from_dict(self.expansion_config.to_dict())`

**L185** ``

**L186** `    def set_expansion_config(self, config: ExpansionConfig) -> ExpansionConfig:`

**L187** `        config.validate()`

**L188** `        self.expansion_config = ExpansionConfig.from_dict(config.to_dict())`

**L189** `        self.invalidate_cache()`

**L190** `        return self.expansion_config`

**L191** ``

**L192** `    def get_route_config(self) -> RouteConfig:`

**L193** `        return RouteConfig.from_dict(self.route_config.to_dict())`

**L194** ``

**L195** `    def set_route_config(self, config: RouteConfig) -> RouteConfig:`

**L196** `        config.validate()`

**L197** `        self.route_config = RouteConfig.from_dict(config.to_dict())`

**L198** `        self.invalidate_cache()`

**L199** `        return self.route_config`

**L200** ``

**L201** `    def get_validation_config(self) -> ValidationConfig:`

**L202** `        return ValidationConfig.from_dict(self.validation_config.to_dict())`

**L203** ``

**L204** `    def set_validation_config(self, config: ValidationConfig) -> ValidationConfig:`

**L205** `        config.validate()`

**L206** `        self.validation_config = ValidationConfig.from_dict(config.to_dict())`

**L207** `        return self.validation_config`

**L208** ``

**L209** `    def get_react_config(self) -> ReactConfig:`

**L210** `        return ReactConfig.from_dict(self.react_config.to_dict())`

**L211** ``

**L212** `    def set_react_config(self, config: ReactConfig) -> ReactConfig:`

**L213** `        config.validate()`

**L214** `        self.react_config = ReactConfig.from_dict(config.to_dict())`

**L215** `        return self.react_config`

**L216** ``

**L217** `    def get_executor_config(self) -> ExecutorConfig:`

**L218** `        return ExecutorConfig.from_dict(self.executor_config.to_dict())`

**L219** ``

**L220** `    def set_executor_config(self, config: ExecutorConfig) -> ExecutorConfig:`

**L221** `        config.validate()`

**L222** `        self.executor_config = ExecutorConfig.from_dict(config.to_dict())`

**L223** `        return self.executor_config`

**L224** ``

**L225** `    def get_graph_config(self) -> GraphConfig:`

**L226** `        return GraphConfig.from_dict(self.graph_config.to_dict())`

**L227** ``

**L228** `    def set_graph_config(self, config: GraphConfig) -> GraphConfig:`

**L229** `        config.validate()`

**L230** `        self.graph_config = GraphConfig.from_dict(config.to_dict())`

**L231** `        return self.graph_config`

**L232** ``

**L233** `    def get_approval_config(self) -> ApprovalConfig:`

**L234** `        return ApprovalConfig.from_dict(self.approval_config.to_dict())`

**L235** ``

**L236** `    def set_approval_config(self, config: ApprovalConfig) -> ApprovalConfig:`

**L237** `        config.validate()`

**L238** `        self.approval_config = ApprovalConfig.from_dict(config.to_dict())`

**L239** `        return self.approval_config`

**L240** ``

**L241** `    def get_supervisor_config(self) -> SupervisorConfig:`

**L242** `        return SupervisorConfig.from_dict(self.supervisor_config.to_dict())`

**L243** ``

**L244** `    def set_supervisor_config(self, config: SupervisorConfig) -> SupervisorConfig:`

**L245** `        config.validate()`

**L246** `        self.supervisor_config = SupervisorConfig.from_dict(config.to_dict())`

**L247** `        return self.supervisor_config`

**L248** ``

**L249** `    def get_mcp_config(self) -> McpConfig:`

**L250** `        return McpConfig.from_dict(self.mcp_config.to_dict())`

**L251** ``

**L252** `    def set_mcp_config(self, config: McpConfig) -> McpConfig:`

**L253** `        config.validate()`

**L254** `        self.mcp_config = McpConfig.from_dict(config.to_dict())`

**L255** `        return self.mcp_config`

**L256** ``

**L257** `    def get_dify_config(self) -> DifyConfig:`

**L258** `        return DifyConfig.from_dict(self.dify_config.to_dict())`

**L259** ``

**L260** `    def set_dify_config(self, config: DifyConfig) -> DifyConfig:`

**L261** `        config.validate()`

**L262** `        self.dify_config = DifyConfig.from_dict(config.to_dict())`

**L263** `        return self.dify_config`

**L264** ``

**L265** `    def validate_answer(`

**L266** `        self,`

**L267** `        query: str,`

**L268** `        reply: str,`

**L269** `        citations: list[dict[str, Any]],`

**L270** `    ) -> ValidationResult | None:`

**L271** `        """按当前 validation_config 校验 reply 与 citations 一致性"""`

**L272** `        cfg = self.get_validation_config()`

**L273** `        if not cfg.enabled:`

**L274** `            return None`

**L275** `        validator = RuleBasedAnswerValidator(config=cfg)`

**L276** `        return validator.validate(query, reply, citations)`

**L277** ``

**L278** `    def fetch_citations(self, query: str) -> dict[str, Any]:`

**L279** `        """按当前 citation_config 检索并返回引用包 dict"""`

**L280** `        cfg = self.get_citation_config()`

**L281** `        if not cfg.enabled:`

**L282** `            return {`

**L283** `                "query": query.strip(),`

**L284** `                "citations": [],`

**L285** `                "rewrite": None,`

**L286** `                "expansion": None,`

**L287** `                "route": None,`

**L288** `            }`

**L289** `        rag = self.as_rag_service()`

**L290** `        bundle = rag.retrieve_citation_bundle(query, config=cfg)`

**L291** `        return bundle.to_dict()`

**L292** ``

**L293** `    def fetch_citations_retry(self, query: str, *, attempt: int = 1) -> dict[str, Any]:`

**L294** `        """Self-RAG 重试 — 强制 rag_wide 并放大 citation pool"""`

**L295** `        from rag.citation_config import CitationConfig`

**L296** `        from rag.route_config import INTENT_RAG_WIDE`

**L297** ``

**L298** `        cfg = self.get_citation_config()`

**L299** `        if not cfg.enabled:`

**L300** `            return self.fetch_citations(query)`

**L301** ``

**L302** `        boosted = CitationConfig.from_dict(`

**L303** `            {`

**L304** `                **cfg.to_dict(),`

**L305** `                "max_citations": min(100, max(cfg.max_citations, 20) + attempt * 10),`

**L306** `            }`

**L307** `        )`

**L308** `        rag = self.as_rag_service()`

**L309** `        bundle = rag.retrieve_citation_bundle(`

**L310** `            query,`

**L311** `            config=boosted,`

**L312** `            intent_override=INTENT_RAG_WIDE,`

**L313** `        )`

**L314** `        data = bundle.to_dict()`

**L315** `        data["retry_attempt"] = attempt`

**L316** `        return data`

**L317** ``

**L318** `    def ingest_text(`

**L319** `        self,`

**L320** `        content: str,`

**L321** `        *,`

**L322** `        filename: str,`

**L323** `        clean: bool = True,`

**L324** `        chunk_size: int | None = None,`

**L325** `        overlap: int | None = None,`

**L326** `    ) -> KnowledgeDocument:`

**L327** `        """将文本写入知识库并重建索引"""`

**L328** `        cfg = self.get_chunk_config()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L329** `        cs = chunk_size if chunk_size is not None else cfg.chunk_size`

**L330** `        ov = overlap if overlap is not None else cfg.overlap`

**L331** `        text = (content or "").strip()`

**L332** `        if not text:`

**L333** `            raise ValueError("文档内容不能为空")`

**L334** `        if not filename.strip():`

**L335** `            raise ValueError("filename 不能为空")`

**L336** ``

**L337** `        cleaned = text`

**L338** `        if clean:`

**L339** `            cleaned, _ = clean_text(text)`

**L340** `        doc = DocumentRecord(`

**L341** `            path=Path(filename),`

**L342** `            content=text,`

**L343** `            encoding="utf-8",`

**L344** `            size_bytes=len(text.encode("utf-8")),`

**L345** `            cleaned=cleaned,`

**L346** `        )`

**L347** `        new_chunks = chunk_documents(`

**L348** `            [doc],`

**L349** `            chunk_size=cs,`

**L350** `            overlap=ov,`

**L351** `            use_cleaned=clean,`

**L352** `        )`

**L353** `        self._append_chunks(filename, new_chunks, size_bytes=doc.size_bytes)`

**L354** `        self._rebuild_index()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L355** `        return self.documents[-1]`

**L356** ``

**L357** `    def ingest_file(`

**L358** `        self,`

**L359** `        path: Path,`

**L360** `        *,`

**L361** `        clean: bool = True,`

**L362** `        chunk_size: int = 200,`

**L363** `        overlap: int = 40,`

**L364** `    ) -> KnowledgeDocument:`

**L365** `        """从磁盘文件 ingestion"""`

**L366** `        content, encoding = read_text_file(path)`

**L367** `        cleaned = content`

**L368** `        if clean:`

**L369** `            cleaned, _ = clean_text(content)`

**L370** `        doc = DocumentRecord(`

**L371** `            path=path,`

**L372** `            content=content,`

**L373** `            encoding=encoding,`

**L374** `            size_bytes=path.stat().st_size,`

**L375** `            cleaned=cleaned,`

**L376** `        )`

**L377** `        new_chunks = chunk_documents(`

**L378** `            [doc],`

**L379** `            chunk_size=chunk_size,`

**L380** `            overlap=overlap,`

**L381** `            use_cleaned=clean,`

**L382** `        )`

**L383** `        self._append_chunks(path.name, new_chunks, size_bytes=doc.size_bytes)`

**L384** `        self._rebuild_index()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L385** `        return self.documents[-1]`

**L386** ``

**L387** `    def ingest_bytes(`

**L388** `        self,`

**L389** `        data: bytes,`

**L390** `        *,`

**L391** `        filename: str,`

**L392** `        clean: bool = True,`

**L393** `        chunk_strategy: str = "auto",`

**L394** `        incremental: bool = True,`

**L395** `    ) -> KnowledgeDocument:`

**L396** `        """处理上传二进制 — Day 26 起委托 doc_parser"""`

**L397** `        from tools.doc_parser import parse_bytes`

**L398** ``

**L399** `        parsed = parse_bytes(data, filename)`

**L400** `        cfg = self.get_chunk_config()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L401** `        return self.ingest_parsed(`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L402** `            parsed,`

**L403** `            clean=clean,`

**L404** `            chunk_strategy=chunk_strategy if chunk_strategy != "auto" else cfg.strategy,`

**L405** `            incremental=incremental,`

**L406** `        )`

**L407** ``

**L408** `    def ingest_parsed(`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L409** `        self,`

**L410** `        parsed: ParsedDocument,`

**L411** `        *,`

**L412** `        clean: bool = True,`

**L413** `        chunk_strategy: str | None = None,`

**L414** `        chunk_size: int | None = None,`

**L415** `        overlap: int | None = None,`

**L416** `        incremental: bool = False,`

**L417** `    ) -> KnowledgeDocument:`

**L418** `        """将 ParsedDocument 写入知识库"""`

**L419** `        cfg = self.get_chunk_config()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L420** `        strategy = chunk_strategy if chunk_strategy is not None else cfg.strategy`

**L421** `        cs = chunk_size if chunk_size is not None else cfg.chunk_size`

**L422** `        ov = overlap if overlap is not None else cfg.overlap`

**L423** `        text = (parsed.plain_text or "").strip()`

**L424** `        if not text:`

**L425** `            raise ValueError("解析结果为空")`

**L426** ``

**L427** `        if clean:`

**L428** `            text, _ = clean_text(text)`

**L429** `            parsed.plain_text = text`

**L430** `            for section in parsed.sections:`

**L431** `                section.body, _ = clean_text(section.body)`

**L432** ``

**L433** `        new_chunks = chunk_from_parsed(`

**L434** `            parsed,`

**L435** `            strategy=strategy,`

**L436** `            chunk_size=cs,`

**L437** `            overlap=ov,`

**L438** `        )`

**L439** `        size_bytes = len(parsed.plain_text.encode("utf-8"))`

**L440** ``

**L441** `        if incremental:`

**L442** `            removed = self._remove_document_by_source(parsed.filename)`

**L443** `            self._append_chunks(`

**L444** `                parsed.filename,`

**L445** `                new_chunks,`

**L446** `                size_bytes=size_bytes,`

**L447** `                doc_format=parsed.format,`

**L448** `            )`

**L449** `            self._incremental_index(new_chunks, replaced_count=len(removed))`

**L450** `        else:`

**L451** `            self._append_chunks(`

**L452** `                parsed.filename,`

**L453** `                new_chunks,`

**L454** `                size_bytes=size_bytes,`

**L455** `                doc_format=parsed.format,`

**L456** `            )`

**L457** `            self._rebuild_index()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L458** `        return self.documents[-1]`

**L459** ``

**L460** `    def save(self, path: Path | None = None) -> Path:`

**L461** `        """持久化到 JSON"""`

**L462** `        target = path or self.store_path or _default_store_path()`

**L463** `        self.store_path = target`

**L464** `        payload = {`

**L465** `            "version": STORE_VERSION,`

**L466** `            "platform_version": PLATFORM_VERSION,`

**L467** `            "documents": [d.to_dict() for d in self.documents],`

**L468** `            "chunks": [_chunk_to_dict(c) for c in self.chunks],`

**L469** `            "embedding": self.embedding_state,`

**L470** `            "vector_backend": self.vector_backend,`

**L471** `            "chunk_config": self.chunk_config.to_dict(),`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L472** `            "last_rebuilt_at": self.last_rebuilt_at,`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L473** `            "last_incremental_at": self.last_incremental_at,`

**L474** `            "index_mode": self.index_mode,`

**L475** `            "retrieval_config": self.retrieval_config.to_dict(),`

**L476** `            "rerank_config": self.rerank_config.to_dict(),`

**L477** `            "rewrite_config": self.rewrite_config.to_dict(),`

**L478** `            "citation_config": self.citation_config.to_dict(),`

**L479** `            "expansion_config": self.expansion_config.to_dict(),`

**L480** `            "route_config": self.route_config.to_dict(),`

**L481** `            "validation_config": self.validation_config.to_dict(),`

**L482** `            "react_config": self.react_config.to_dict(),`

**L483** `            "executor_config": self.executor_config.to_dict(),`

**L484** `            "graph_config": self.graph_config.to_dict(),`

**L485** `            "approval_config": self.approval_config.to_dict(),`

**L486** `            "supervisor_config": self.supervisor_config.to_dict(),`

**L487** `            "mcp_config": self.mcp_config.to_dict(),`

**L488** `            "dify_config": self.dify_config.to_dict(),`

**L489** `        }`

**L490** `        save_json(target, payload)`

**L491** `        return target`

**L492** ``

**L493** `    @classmethod`

**L494** `    def load(cls, path: Path) -> KnowledgeStore:`

**L495** `        """从 JSON 加载知识库"""`

**L496** `        raw = load_json(path, default=None)`

**L497** `        if not raw:`

**L498** `            return cls.bootstrap_from_sample_docs(store_path=path)`

**L499** ``

**L500** `        store = cls(store_path=path)`

**L501** `        store.documents = [`

**L502** `            KnowledgeDocument.from_dict(d) for d in raw.get("documents", [])`

**L503** `        ]`

**L504** `        store.chunks = [_chunk_from_dict(c) for c in raw.get("chunks", [])]`

**L505** `        store.embedding_state = dict(raw.get("embedding") or {})`

**L506** `        store.vector_backend = str(raw.get("vector_backend") or VECTOR_BACKEND)`

**L507** `        if raw.get("chunk_config"):`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L508** `            store.chunk_config = ChunkConfig.from_dict(raw["chunk_config"])`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L509** `        store.last_rebuilt_at = raw.get("last_rebuilt_at")`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L510** `        store.last_incremental_at = raw.get("last_incremental_at")`

**L511** `        store.index_mode = str(raw.get("index_mode") or INDEX_MODE_FULL)`

**L512** `        if raw.get("retrieval_config"):`

**L513** `            store.retrieval_config = RetrievalConfig.from_dict(raw["retrieval_config"])`

**L514** `        if raw.get("rerank_config"):`

**L515** `            store.rerank_config = RerankConfig.from_dict(raw["rerank_config"])`

**L516** `        if raw.get("rewrite_config"):`

**L517** `            store.rewrite_config = RewriteConfig.from_dict(raw["rewrite_config"])`

**L518** `        if raw.get("citation_config"):`

**L519** `            store.citation_config = CitationConfig.from_dict(raw["citation_config"])`

**L520** `        if raw.get("expansion_config"):`

**L521** `            store.expansion_config = ExpansionConfig.from_dict(raw["expansion_config"])`

**L522** `        if raw.get("route_config"):`

**L523** `            store.route_config = RouteConfig.from_dict(raw["route_config"])`

**L524** `        if raw.get("validation_config"):`

**L525** `            store.validation_config = ValidationConfig.from_dict(raw["validation_config"])`

**L526** `        if raw.get("react_config"):`

**L527** `            store.react_config = ReactConfig.from_dict(raw["react_config"])`

**L528** `        if raw.get("executor_config"):`

**L529** `            store.executor_config = ExecutorConfig.from_dict(raw["executor_config"])`

**L530** `        if raw.get("graph_config"):`

**L531** `            store.graph_config = GraphConfig.from_dict(raw["graph_config"])`

**L532** `        if raw.get("approval_config"):`

**L533** `            store.approval_config = ApprovalConfig.from_dict(raw["approval_config"])`

**L534** `        if raw.get("supervisor_config"):`

**L535** `            store.supervisor_config = SupervisorConfig.from_dict(raw["supervisor_config"])`

**L536** `        if raw.get("mcp_config"):`

**L537** `            store.mcp_config = McpConfig.from_dict(raw["mcp_config"])`

**L538** `        if raw.get("dify_config"):`

**L539** `            store.dify_config = DifyConfig.from_dict(raw["dify_config"])`

**L540** `        store._sync_chroma_from_json()`

**L541** `        store._rag_service = store._build_rag_service()`

**L542** `        return store`

**L543** ``

**L544** `    @classmethod`

**L545** `    def load_or_bootstrap(cls, path: Path | None = None) -> KnowledgeStore:`

**L546** `        """加载已有库，不存在则从 sample_docs 引导"""`

**L547** `        target = path or _default_store_path()`

**L548** `        if target.is_file():`

**L549** `            return cls.load(target)`

**L550** `        store = cls.bootstrap_from_sample_docs(store_path=target)`

**L551** `        store.save(target)`

**L552** `        return store`

**L553** ``

**L554** `    @classmethod`

**L555** `    def bootstrap_from_sample_docs(cls, *, store_path: Path | None = None) -> KnowledgeStore:`

**L556** `        """用内置 sample_docs 初始化知识库"""`

**L557** `        rag = RAGContextService.from_sample_docs(use_embedding=True)`

**L558** `        store = cls(store_path=store_path)`

**L559** `        now = _utc_now()`

**L560** ``

**L561** `        by_source: dict[str, list[TextChunk]] = {}`

**L562** `        for chunk in rag.index.chunks:`

**L563** `            by_source.setdefault(chunk.source, []).append(chunk)`

**L564** ``

**L565** `        for source, chunks in sorted(by_source.items()):`

**L566** `            store.documents.append(`

**L567** `                KnowledgeDocument(`

**L568** `                    name=source,`

**L569** `                    ingested_at=now,`

**L570** `                    size_bytes=sum(len(c.text) for c in chunks),`

**L571** `                    chunk_count=len(chunks),`

**L572** `                )`

**L573** `            )`

**L574** `        store.chunks = list(rag.index.chunks)`

**L575** `        retriever = rag.index.retriever`

**L576** `        from rag.embedding_retriever import EmbeddingRetriever`

**L577** ``

**L578** `        if isinstance(retriever, EmbeddingRetriever):`

**L579** `            store.embedding_state = retriever._client.model.export_state()`

**L580** `        store._rebuild_index()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L581** `        return store`

**L582** ``

**L583** `    def status_dict(self) -> dict[str, Any]:`

**L584** `        from tools.doc_parser import supported_formats`

**L585** ``

**L586** `        return {`

**L587** `            "document_count": self.document_count,`

**L588** `            "chunk_count": self.chunk_count,`

**L589** `            "documents": [d.to_dict() for d in self.documents],`

**L590** `            "store_path": str(self.store_path) if self.store_path else None,`

**L591** `            "platform_version": PLATFORM_VERSION,`

**L592** `            "supported_formats": supported_formats(),`

**L593** `            "chunk_config": self.chunk_config.to_dict(),`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L594** `            "last_rebuilt_at": self.last_rebuilt_at,`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L595** `            "last_incremental_at": self.last_incremental_at,`

**L596** `            "index_mode": self.index_mode,`

**L597** `            "retrieval_config": self.retrieval_config.to_dict(),`

**L598** `            "rerank_config": self.rerank_config.to_dict(),`

**L599** `            "rewrite_config": self.rewrite_config.to_dict(),`

**L600** `            "citation_config": self.citation_config.to_dict(),`

**L601** `            "expansion_config": self.expansion_config.to_dict(),`

**L602** `            "route_config": self.route_config.to_dict(),`

**L603** `            "validation_config": self.validation_config.to_dict(),`

**L604** `            "react_config": self.react_config.to_dict(),`

**L605** `            "executor_config": self.executor_config.to_dict(),`

**L606** `            "graph_config": self.graph_config.to_dict(),`

**L607** `            "approval_config": self.approval_config.to_dict(),`

**L608** `            "supervisor_config": self.supervisor_config.to_dict(),`

**L609** `            "mcp_config": self.mcp_config.to_dict(),`

**L610** `            "dify_config": self.dify_config.to_dict(),`

**L611** `            "vector_backend": self.vector_backend,`

**L612** `            "chroma_path": str(self._resolve_chroma_path()),`

**L613** `            "chroma_count": self._chroma_index().count() if self.chunks else 0,`

**L614** `        }`

**L615** ``

**L616** `    def _append_chunks(`

**L617** `        self,`

**L618** `        filename: str,`

**L619** `        new_chunks: list[TextChunk],`

**L620** `        *,`

**L621** `        size_bytes: int,`

**L622** `        doc_format: str = "txt",`

**L623** `    ) -> None:`

**L624** `        base_index = len(self.chunks)`

**L625** `        reindexed: list[TextChunk] = []`

**L626** `        for i, chunk in enumerate(new_chunks):`

**L627** `            reindexed.append(`

**L628** `                TextChunk(`

**L629** `                    chunk_id=chunk.chunk_id,`

**L630** `                    text=chunk.text,`

**L631** `                    source=filename,`

**L632** `                    index=base_index + i,`

**L633** `                    start_char=chunk.start_char,`

**L634** `                    end_char=chunk.end_char,`

**L635** `                )`

**L636** `            )`

**L637** `        self.chunks.extend(reindexed)`

**L638** `        self.documents.append(`

**L639** `            KnowledgeDocument(`

**L640** `                name=filename,`

**L641** `                ingested_at=_utc_now(),`

**L642** `                size_bytes=size_bytes,`

**L643** `                chunk_count=len(reindexed),`

**L644** `                format=doc_format,`

**L645** `            )`

**L646** `        )`

**L647** ``

**L648** `    def _remove_document_by_source(self, filename: str) -> list[str]:`

**L649** `        """移除同名文档及其 chunks，并从 Chroma 删除旧向量"""`

**L650** `        removed_ids = [c.chunk_id for c in self.chunks if c.source == filename]`

**L651** `        if not removed_ids:`

**L652** `            return []`

**L653** ``

**L654** `        self._chroma_index().delete_by_ids(removed_ids)`

**L655** `        self.documents = [d for d in self.documents if d.name != filename]`

**L656** `        self.chunks = [c for c in self.chunks if c.source != filename]`

**L657** `        self.chunks = [`

**L658** `            TextChunk(`

**L659** `                chunk_id=c.chunk_id,`

**L660** `                text=c.text,`

**L661** `                source=c.source,`

**L662** `                index=i,`

**L663** `                start_char=c.start_char,`

**L664** `                end_char=c.end_char,`

**L665** `            )`

**L666** `            for i, c in enumerate(self.chunks)`

**L667** `        ]`

**L668** `        return removed_ids`

**L669** ``

**L670** `    def _resolve_chroma_path(self) -> Path:`

**L671** `        if self.chroma_path is not None:`

**L672** `            return self.chroma_path`

**L673** `        if self.store_path is not None:`

**L674** `            return self.store_path.parent / "chroma"`

**L675** `        return get_path("knowledge_chroma")`

**L676** ``

**L677** `    def _chroma_index(self) -> ChromaVectorIndex:`

**L678** `        return ChromaVectorIndex(self._resolve_chroma_path())`

**L679** ``

**L680** `    def _sync_chroma_from_json(self) -> None:`

**L681** `        """从 JSON 元数据恢复 Chroma（迁移或冷启动）"""`

**L682** `        if not self.chunks or not self.embedding_state:`

**L683** `            return`

**L684** `        chroma = self._chroma_index()`

**L685** `        if chroma.count() > 0:`

**L686** `            return`

**L687** `        from rag.embedding import EmbeddingClient`

**L688** ``

**L689** `        client = EmbeddingClient()`

**L690** `        client.model.load_state(self.embedding_state)`

**L691** `        vectors = client.embed_batch([c.text for c in self.chunks])`

**L692** `        chroma.upsert_chunks(self.chunks, vectors)`

**L693** ``

**L694** `    def _rebuild_index(self) -> None:`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L695** `        from rag.embedding_retriever import EmbeddingRetriever`

**L696** ``

**L697** `        if not self.chunks:`

**L698** `            self.embedding_state = {}`

**L699** `            self._chroma_index().reset()`

**L700** `            self.invalidate_cache()`

**L701** `            return`

**L702** ``

**L703** `        retriever = EmbeddingRetriever(self.chunks)`

**L704** `        self.embedding_state = retriever._client.model.export_state()`

**L705** `        vectors = retriever._client.embed_batch([c.text for c in self.chunks])`

**L706** `        chroma = self._chroma_index()`

**L707** `        chroma.reset()`

**L708** `        chroma.upsert_chunks(self.chunks, vectors)`

**L709** `        self.index_mode = INDEX_MODE_FULL`

**L710** `        self.invalidate_cache()`

**L711** ``

**L712** `    def _incremental_index(`

**L713** `        self,`

**L714** `        affected_chunks: list[TextChunk],`

**L715** `        *,`

**L716** `        replaced_count: int = 0,`

**L717** `    ) -> bool:`

**L718** `        """`

**L719** `        增量更新 Chroma：不 reset collection，仅 upsert 受影响 chunk。`

**L720** ``

**L721** `        若 TF-IDF 词表扩张，则回退为全量 upsert（仍不 reset）。`

**L722** `        返回是否发生词表扩张。`

**L723** `        """`

**L724** `        from rag.embedding_retriever import EmbeddingRetriever`

**L725** ``

**L726** `        if not self.chunks:`

**L727** `            self.embedding_state = {}`

**L728** `            self._chroma_index().reset()`

**L729** `            self.last_incremental_at = _utc_now()`

**L730** `            self.index_mode = INDEX_MODE_INCREMENTAL`

**L731** `            self.invalidate_cache()`

**L732** `            return False`

**L733** ``

**L734** `        old_vocab = dict(self.embedding_state.get("vocab") or {})`

**L735** `        retriever = EmbeddingRetriever(self.chunks)`

**L736** `        self.embedding_state = retriever._client.model.export_state()`

**L737** `        new_vocab = dict(self.embedding_state.get("vocab") or {})`

**L738** `        vocab_expanded = len(new_vocab) > len(old_vocab) and bool(old_vocab)`

**L739** ``

**L740** `        if vocab_expanded:`

**L741** `            affected_chunks = list(self.chunks)`

**L742** ``

**L743** `        client = retriever._client`

**L744** `        vectors = client.embed_batch([c.text for c in affected_chunks])`

**L745** `        chroma = self._chroma_index()`

**L746** `        if vocab_expanded:`

**L747** `            # TF-IDF 维度变化时 Chroma collection 须重建`

**L748** `            chroma.reset()`

**L749** `        chroma.upsert_chunks(affected_chunks, vectors)`

**L750** `        self.last_incremental_at = _utc_now()`

**L751** `        self.index_mode = INDEX_MODE_INCREMENTAL`

**L752** `        self.invalidate_cache()`

**L753** `        return vocab_expanded`

**L754** ``

**L755** `    def _build_rag_service(self) -> RAGContextService:`

**L756** `        if not self.chunks:`

**L757** `            return RAGContextService()`

**L758** ``

**L759** `        from rag.embedding import EmbeddingClient`

**L760** `        from rag.retriever import KeywordRetriever`

**L761** ``

**L762** `        client = EmbeddingClient()`

**L763** `        client.model.load_state(self.embedding_state)`

**L764** `        self._sync_chroma_from_json()`

**L765** `        chroma = self._chroma_index()`

**L766** `        vector = ChromaEmbeddingRetriever(self.chunks, chroma, client=client)`

**L767** `        keyword = KeywordRetriever(self.chunks)`

**L768** `        cfg = self.get_retrieval_config()`

**L769** `        hybrid = HybridRetriever(self.chunks, keyword, vector, config=cfg)`

**L770** `        rerank_cfg = self.get_rerank_config()`

**L771** `        reranking = RerankingRetriever(`

**L772** `            hybrid,`

**L773** `            reranker=MockCrossEncoderReranker(),`

**L774** `            config=rerank_cfg,`

**L775** `        )`

**L776** `        rewrite_cfg = self.get_rewrite_config()`

**L777** `        rewriting = RewritingRetriever(reranking, config=rewrite_cfg)`

**L778** `        expansion_cfg = self.get_expansion_config()`

**L779** `        expanding = ExpandingRetriever(rewriting, config=expansion_cfg)`

**L780** `        route_cfg = self.get_route_config()`

**L781** `        retriever = RoutingRetriever(expanding, config=route_cfg)`

**L782** `        index = DocumentIndex(chunks=self.chunks, retriever=retriever)`

**L783** `        return RAGContextService(index)`

**L784** ``

**L785** ``

**L786** `_store: KnowledgeStore | None = None`

**L787** `_store_lock = threading.Lock()`

**L788** ``

**L789** ``

**L790** `def get_knowledge_store(*, reload: bool = False) -> KnowledgeStore:`

**L791** `    """全局知识库单例（API 与 factory 共享）"""`

**L792** `    global _store`

**L793** `    with _store_lock:`

**L794** `        if _store is None or reload:`

**L795** `            _store = KnowledgeStore.load_or_bootstrap()`

**L796** `        return _store`

**L797** ``

**L798** ``

**L799** `def set_knowledge_store(store: KnowledgeStore) -> None:`

**L800** `    """测试注入用"""`

**L801** `    global _store`

**L802** `    with _store_lock:`

**L803** `        _store = store`

**L804** ``

**L805** ``

**L806** `def _default_store_path() -> Path:`

**L807** `    return get_path("knowledge_store")`

**L808** ``

**L809** ``

**L810** `def _utc_now() -> str:`

**L811** `    return datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")`

**L812** ``

**L813** ``

**L814** `def _chunk_to_dict(chunk: TextChunk) -> dict[str, Any]:`

**L815** `    return {`

**L816** `        "chunk_id": chunk.chunk_id,`

**L817** `        "text": chunk.text,`

**L818** `        "source": chunk.source,`

**L819** `        "index": chunk.index,`

**L820** `        "start_char": chunk.start_char,`

**L821** `        "end_char": chunk.end_char,`

**L822** `    }`

**L823** ``

**L824** ``

**L825** `def _chunk_from_dict(data: dict[str, Any]) -> TextChunk:`

**L826** `    return TextChunk(`

**L827** `        chunk_id=str(data.get("chunk_id", "")),`

**L828** `        text=str(data.get("text", "")),`

**L829** `        source=str(data.get("source", "")),`

**L830** `        index=int(data.get("index", 0)),`

**L831** `        start_char=int(data.get("start_char", 0)),`

**L832** `        end_char=int(data.get("end_char", 0)),`

**L833** `    )`


## 附录 B：api/knowledge.py 走读

### `nexus-agent-platform/src/api/knowledge.py` 逐行走读

共 **450** 行。

**L1** `"""`

**L2** `知识库 REST API — 文档上传、分块调参与检索评估`

**L3** ``

**L4** `需求：ZL-NA-REQ-025 / ZL-NA-REQ-026 / ZL-NA-REQ-027 / ZL-NA-REQ-028 / ZL-NA-REQ-029 / ZL-NA-REQ-030 / ZL-NA-REQ-031 / ZL-NA-REQ-032 / ZL-NA-REQ-033 / ZL-NA-REQ-034 / ZL-NA-REQ-035 / ZL-NA-REQ-036 / ZL-NA-REQ-037 / ZL-NA-REQ-038`

**L5** `"""`

**L6** ``

**L7** `from __future__ import annotations`

**L8** ``

**L9** `from pathlib import Path`

**L10** ``

**L11** `from fastapi import APIRouter, File, HTTPException, UploadFile`

**L12** ``

**L13** `from api.schemas import (`

**L14** `    ChunkConfigRequest,`

**L15** `    ChunkConfigResponse,`

**L16** `    CitationConfigRequest,`

**L17** `    CitationConfigResponse,`

**L18** `    CitationPreviewRequest,`

**L19** `    CitationPreviewResponse,`

**L20** `    ExpansionConfigRequest,`

**L21** `    ExpansionConfigResponse,`

**L22** `    ExpansionPreviewRequest,`

**L23** `    ExpansionPreviewResponse,`

**L24** `    EvaluateRequest,`

**L25** `    EvaluateResponse,`

**L26** `    KnowledgeStatusResponse,`

**L27** `    KnowledgeUploadResponse,`

**L28** `    RebuildRequest,`

**L29** `    RebuildResponse,`

**L30** `    RerankConfigRequest,`

**L31** `    RerankConfigResponse,`

**L32** `    RewriteConfigRequest,`

**L33** `    RewriteConfigResponse,`

**L34** `    RewritePreviewRequest,`

**L35** `    RewritePreviewResponse,`

**L36** `    RouteConfigRequest,`

**L37** `    RouteConfigResponse,`

**L38** `    RoutePreviewRequest,`

**L39** `    RoutePreviewResponse,`

**L40** `    ValidationConfigRequest,`

**L41** `    ValidationConfigResponse,`

**L42** `    ValidationPreviewRequest,`

**L43** `    ValidationPreviewResponse,`

**L44** `    ValidationRetryPreviewRequest,`

**L45** `    ValidationRetryPreviewResponse,`

**L46** `    RetrievalConfigRequest,`

**L47** `    RetrievalConfigResponse,`

**L48** `)`

**L49** `from api.sessions import session_manager`

**L50** `from core.exceptions import NexusError, StorageError`

**L51** `from rag.chunk_config import PRESET_CONFIGS, ChunkConfig`
  → evaluate / chunk-config API 相关行。

**L52** `from rag.ingestion import ingest_upload`

**L53** `from rag.knowledge_rebuild import rebuild_store, rebuild_with_best_config`
  → evaluate / chunk-config API 相关行。

**L54** `from rag.knowledge_store import get_knowledge_store`

**L55** `from rag.citation_config import CitationConfig`

**L56** `from rag.expansion_config import ExpansionConfig`

**L57** `from rag.query_expander import build_expander`

**L58** `from rag.query_rewriter import RuleBasedQueryRewriter`

**L59** `from rag.query_router import RuleBasedQueryRouter`

**L60** `from rag.route_config import RouteConfig`

**L61** `from rag.validation_config import ValidationConfig`

**L62** `from rag.rerank_config import RerankConfig`

**L63** `from rag.rewrite_config import RewriteConfig`

**L64** `from rag.retrieval_config import RetrievalConfig`

**L65** `from rag.retrieval_eval import EvalQuery, pick_best_config, run_ab_experiment`

**L66** `from tools.doc_parser import parse_bytes`

**L67** ``

**L68** `router = APIRouter(prefix="/api/knowledge", tags=["knowledge"])`

**L69** ``

**L70** `MAX_UPLOAD_BYTES = 512_000  # 500 KB 教学上限`

**L71** `_EVAL_SAMPLE = (`
  → evaluate / chunk-config API 相关行。

**L72** `    Path(__file__).resolve().parent.parent / "day26" / "sample_docs" / "product_notice.md"`

**L73** `)`

**L74** ``

**L75** ``

**L76** `@router.get("/retrieval-config", response_model=RetrievalConfigResponse)`

**L77** `def get_retrieval_config() -> RetrievalConfigResponse:`

**L78** `    """返回当前检索模式（vector / keyword / hybrid）与融合参数"""`

**L79** `    cfg = get_knowledge_store().get_retrieval_config()`

**L80** `    return RetrievalConfigResponse(**cfg.to_dict())`

**L81** ``

**L82** ``

**L83** `@router.put("/retrieval-config", response_model=RetrievalConfigResponse)`

**L84** `def update_retrieval_config(body: RetrievalConfigRequest) -> RetrievalConfigResponse:`

**L85** `    """更新检索策略；变更后清除 RAG 缓存"""`

**L86** `    store = get_knowledge_store()`

**L87** `    try:`

**L88** `        cfg = RetrievalConfig.from_dict(body.model_dump())`

**L89** `        cfg.validate()`

**L90** `        store.set_retrieval_config(cfg)`

**L91** `        store.save()`

**L92** `    except ValueError as exc:`

**L93** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L94** `    return RetrievalConfigResponse(**cfg.to_dict())`

**L95** ``

**L96** ``

**L97** `@router.get("/rerank-config", response_model=RerankConfigResponse)`

**L98** `def get_rerank_config() -> RerankConfigResponse:`

**L99** `    """返回 rerank 开关、候选池大小与模型标识"""`

**L100** `    cfg = get_knowledge_store().get_rerank_config()`

**L101** `    return RerankConfigResponse(**cfg.to_dict())`

**L102** ``

**L103** ``

**L104** `@router.put("/rerank-config", response_model=RerankConfigResponse)`

**L105** `def update_rerank_config(body: RerankConfigRequest) -> RerankConfigResponse:`

**L106** `    """更新 rerank 策略；变更后清除 RAG 缓存"""`

**L107** `    store = get_knowledge_store()`

**L108** `    try:`

**L109** `        cfg = RerankConfig.from_dict(body.model_dump())`

**L110** `        cfg.validate()`

**L111** `        store.set_rerank_config(cfg)`

**L112** `        store.save()`

**L113** `    except ValueError as exc:`

**L114** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L115** `    return RerankConfigResponse(**cfg.to_dict())`

**L116** ``

**L117** ``

**L118** `@router.get("/rewrite-config", response_model=RewriteConfigResponse)`

**L119** `def get_rewrite_config() -> RewriteConfigResponse:`

**L120** `    """返回查询改写开关与规则模式"""`

**L121** `    cfg = get_knowledge_store().get_rewrite_config()`

**L122** `    return RewriteConfigResponse(**cfg.to_dict())`

**L123** ``

**L124** ``

**L125** `@router.put("/rewrite-config", response_model=RewriteConfigResponse)`

**L126** `def update_rewrite_config(body: RewriteConfigRequest) -> RewriteConfigResponse:`

**L127** `    """更新查询改写策略；变更后清除 RAG 缓存"""`

**L128** `    store = get_knowledge_store()`

**L129** `    try:`

**L130** `        cfg = RewriteConfig.from_dict(body.model_dump())`

**L131** `        cfg.validate()`

**L132** `        store.set_rewrite_config(cfg)`

**L133** `        store.save()`

**L134** `    except ValueError as exc:`

**L135** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L136** `    return RewriteConfigResponse(**cfg.to_dict())`

**L137** ``

**L138** ``

**L139** `@router.post("/rewrite-preview", response_model=RewritePreviewResponse)`

**L140** `def rewrite_preview(body: RewritePreviewRequest) -> RewritePreviewResponse:`

**L141** `    """预览单条 query 的规则改写结果（不触发检索）"""`

**L142** `    store = get_knowledge_store()`

**L143** `    cfg = store.get_rewrite_config()`

**L144** `    rewriter = RuleBasedQueryRewriter(config=cfg)`

**L145** `    result = rewriter.rewrite(body.query)`

**L146** `    return RewritePreviewResponse(**result.to_dict())`

**L147** ``

**L148** ``

**L149** `@router.get("/citation-config", response_model=CitationConfigResponse)`

**L150** `def get_citation_config() -> CitationConfigResponse:`

**L151** `    """返回引用溯源开关与展示参数"""`

**L152** `    cfg = get_knowledge_store().get_citation_config()`

**L153** `    return CitationConfigResponse(**cfg.to_dict())`

**L154** ``

**L155** ``

**L156** `@router.put("/citation-config", response_model=CitationConfigResponse)`

**L157** `def update_citation_config(body: CitationConfigRequest) -> CitationConfigResponse:`

**L158** `    """更新引用溯源策略并持久化"""`

**L159** `    store = get_knowledge_store()`

**L160** `    try:`

**L161** `        cfg = CitationConfig.from_dict(body.model_dump())`

**L162** `        cfg.validate()`

**L163** `        store.set_citation_config(cfg)`

**L164** `        store.save()`

**L165** `    except ValueError as exc:`

**L166** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L167** `    return CitationConfigResponse(**cfg.to_dict())`

**L168** ``

**L169** ``

**L170** `@router.post("/citation-preview", response_model=CitationPreviewResponse)`

**L171** `def citation_preview(body: CitationPreviewRequest) -> CitationPreviewResponse:`

**L172** `    """预览单条 query 的检索引用（含 rewrite / expansion 审计）"""`

**L173** `    store = get_knowledge_store()`

**L174** `    data = store.fetch_citations(body.query)`

**L175** `    return CitationPreviewResponse(**data)`

**L176** ``

**L177** ``

**L178** `@router.get("/expansion-config", response_model=ExpansionConfigResponse)`

**L179** `def get_expansion_config() -> ExpansionConfigResponse:`

**L180** `    """返回多 query 扩展开关与参数"""`

**L181** `    cfg = get_knowledge_store().get_expansion_config()`

**L182** `    return ExpansionConfigResponse(**cfg.to_dict())`

**L183** ``

**L184** ``

**L185** `@router.put("/expansion-config", response_model=ExpansionConfigResponse)`

**L186** `def update_expansion_config(body: ExpansionConfigRequest) -> ExpansionConfigResponse:`

**L187** `    """更新多 query 扩展策略并持久化"""`

**L188** `    store = get_knowledge_store()`

**L189** `    try:`

**L190** `        cfg = ExpansionConfig.from_dict(body.model_dump())`

**L191** `        cfg.validate()`

**L192** `        store.set_expansion_config(cfg)`

**L193** `        store.save()`

**L194** `    except ValueError as exc:`

**L195** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L196** `    return ExpansionConfigResponse(**cfg.to_dict())`

**L197** ``

**L198** ``

**L199** `@router.post("/expansion-preview", response_model=ExpansionPreviewResponse)`

**L200** `def expansion_preview(body: ExpansionPreviewRequest) -> ExpansionPreviewResponse:`

**L201** `    """预览单条 query 的多路扩展结果"""`

**L202** `    store = get_knowledge_store()`

**L203** `    cfg = store.get_expansion_config()`

**L204** `    expander = build_expander(cfg)`

**L205** `    result = expander.expand(body.query)`

**L206** `    return ExpansionPreviewResponse(**result.to_dict())`

**L207** ``

**L208** ``

**L209** `@router.get("/route-config", response_model=RouteConfigResponse)`

**L210** `def get_route_config() -> RouteConfigResponse:`

**L211** `    """返回检索管线路由开关与默认意图"""`

**L212** `    cfg = get_knowledge_store().get_route_config()`

**L213** `    return RouteConfigResponse(**cfg.to_dict())`

**L214** ``

**L215** ``

**L216** `@router.put("/route-config", response_model=RouteConfigResponse)`

**L217** `def update_route_config(body: RouteConfigRequest) -> RouteConfigResponse:`

**L218** `    """更新检索管线路由策略并持久化"""`

**L219** `    store = get_knowledge_store()`

**L220** `    try:`

**L221** `        cfg = RouteConfig.from_dict(body.model_dump())`

**L222** `        cfg.validate()`

**L223** `        store.set_route_config(cfg)`

**L224** `        store.save()`

**L225** `    except ValueError as exc:`

**L226** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L227** `    return RouteConfigResponse(**cfg.to_dict())`

**L228** ``

**L229** ``

**L230** `@router.post("/route-preview", response_model=RoutePreviewResponse)`

**L231** `def route_preview(body: RoutePreviewRequest) -> RoutePreviewResponse:`

**L232** `    """预览单条 query 的路由决策（expand/rewrite 开关）"""`

**L233** `    store = get_knowledge_store()`

**L234** `    cfg = store.get_route_config()`

**L235** `    router = RuleBasedQueryRouter(config=cfg)`

**L236** `    result = router.route(body.query)`

**L237** `    return RoutePreviewResponse(**result.to_dict())`

**L238** ``

**L239** ``

**L240** `@router.get("/validation-config", response_model=ValidationConfigResponse)`

**L241** `def get_validation_config() -> ValidationConfigResponse:`

**L242** `    """返回答案校验开关与阈值"""`

**L243** `    cfg = get_knowledge_store().get_validation_config()`

**L244** `    return ValidationConfigResponse(**cfg.to_dict())`

**L245** ``

**L246** ``

**L247** `@router.put("/validation-config", response_model=ValidationConfigResponse)`

**L248** `def update_validation_config(body: ValidationConfigRequest) -> ValidationConfigResponse:`

**L249** `    """更新答案校验策略并持久化"""`

**L250** `    store = get_knowledge_store()`

**L251** `    try:`

**L252** `        cfg = ValidationConfig.from_dict(body.model_dump())`

**L253** `        cfg.validate()`

**L254** `        store.set_validation_config(cfg)`

**L255** `        store.save()`

**L256** `    except ValueError as exc:`

**L257** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L258** `    return ValidationConfigResponse(**cfg.to_dict())`

**L259** ``

**L260** ``

**L261** `@router.post("/validation-preview", response_model=ValidationPreviewResponse)`

**L262** `def validation_preview(body: ValidationPreviewRequest) -> ValidationPreviewResponse:`

**L263** `    """预览单条 query + reply 的引用一致性校验"""`

**L264** `    from rag.answer_validator import RuleBasedAnswerValidator`

**L265** ``

**L266** `    store = get_knowledge_store()`

**L267** `    citations = list(body.citations)`

**L268** `    if not citations:`

**L269** `        cite_data = store.fetch_citations(body.query)`

**L270** `        citations = cite_data.get("citations") or []`

**L271** `    cfg = store.get_validation_config()`

**L272** `    preview_cfg = ValidationConfig.from_dict({**cfg.to_dict(), "enabled": True})`

**L273** `    result = RuleBasedAnswerValidator(config=preview_cfg).validate(`

**L274** `        body.query, body.reply, citations`

**L275** `    )`

**L276** `    return ValidationPreviewResponse(**result.to_dict())`

**L277** ``

**L278** ``

**L279** `@router.post("/validation-retry-preview", response_model=ValidationRetryPreviewResponse)`

**L280** `def validation_retry_preview(`

**L281** `    body: ValidationRetryPreviewRequest,`

**L282** `) -> ValidationRetryPreviewResponse:`

**L283** `    """模拟校验失败后的 rag_wide 重检索与再校验"""`

**L284** `    from rag.validation_retry import apply_validation_retry`

**L285** ``

**L286** `    store = get_knowledge_store()`

**L287** `    cite_data = store.fetch_citations(body.query)`

**L288** `    citations = list(body.citations) or cite_data.get("citations") or []`

**L289** `    if body.citations:`

**L290** `        cite_data = {**cite_data, "citations": citations}`

**L291** ``

**L292** `    saved = store.get_validation_config()`

**L293** `    retry_cfg = ValidationConfig.from_dict(`

**L294** `        {**saved.to_dict(), "enabled": True, "retry_on_fail": True, "max_retries": 1}`

**L295** `    )`

**L296** `    store.set_validation_config(retry_cfg)`

**L297** `    try:`

**L298** `        outcome = apply_validation_retry(`

**L299** `            store,`

**L300** `            body.query,`

**L301** `            body.reply,`

**L302** `            citations,`

**L303** `            cite_data,`

**L304** `        )`

**L305** `    finally:`

**L306** `        store.set_validation_config(saved)`

**L307** ``

**L308** `    if outcome is None:`

**L309** `        raise HTTPException(status_code=400, detail="校验已关闭")`

**L310** `    payload = outcome.validation.to_dict()`

**L311** `    payload["retry_route"] = outcome.cite_data.get("route")`

**L312** `    return ValidationRetryPreviewResponse(**payload)`

**L313** ``

**L314** ``

**L315** `@router.get("/chunk-config", response_model=ChunkConfigResponse)`
  → evaluate / chunk-config API 相关行。

**L316** `def get_chunk_config() -> ChunkConfigResponse:`
  → evaluate / chunk-config API 相关行。

**L317** `    """返回当前知识库默认分块参数"""`

**L318** `    cfg = get_knowledge_store().get_chunk_config()`
  → evaluate / chunk-config API 相关行。

**L319** `    return ChunkConfigResponse(**cfg.to_dict())`

**L320** ``

**L321** ``

**L322** `@router.put("/chunk-config", response_model=ChunkConfigResponse)`
  → evaluate / chunk-config API 相关行。

**L323** `def update_chunk_config(body: ChunkConfigRequest) -> ChunkConfigResponse:`
  → evaluate / chunk-config API 相关行。

**L324** `    """更新默认分块参数（影响后续上传）"""`

**L325** `    store = get_knowledge_store()`

**L326** `    try:`

**L327** `        cfg = ChunkConfig.from_dict(body.model_dump())`

**L328** `        cfg.validate()`

**L329** `        store.set_chunk_config(cfg)`
  → evaluate / chunk-config API 相关行。

**L330** `        store.save()`

**L331** `    except ValueError as exc:`

**L332** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L333** `    return ChunkConfigResponse(**cfg.to_dict())`

**L334** ``

**L335** ``

**L336** `@router.post("/evaluate", response_model=EvaluateResponse)`
  → evaluate / chunk-config API 相关行。

**L337** `def evaluate_chunk_configs(body: EvaluateRequest | None = None) -> EvaluateResponse:`
  → evaluate / chunk-config API 相关行。

**L338** `    """`

**L339** `    对内置样例文档运行 A/B 分块评估，返回 hit@1 与推荐配置。`

**L340** ``

**L341** `    默认使用 PRESET_CONFIGS 四套预设与 day27 评估问句集。`
  → evaluate / chunk-config API 相关行。

**L342** `    """`

**L343** `    body = body or EvaluateRequest()`

**L344** `    if not _EVAL_SAMPLE.is_file():`
  → evaluate / chunk-config API 相关行。

**L345** `        raise HTTPException(status_code=500, detail="评估样例文档缺失")`

**L346** ``

**L347** `    doc = parse_bytes(_EVAL_SAMPLE.read_bytes(), _EVAL_SAMPLE.name)`
  → evaluate / chunk-config API 相关行。

**L348** `    from day27.constants import EVAL_QUERIES`
  → evaluate / chunk-config API 相关行。

**L349** ``

**L350** `    queries = [EvalQuery.from_dict(q) for q in EVAL_QUERIES]`
  → evaluate / chunk-config API 相关行。

**L351** ``

**L352** `    if body.use_presets and not body.configs:`

**L353** `        configs = list(PRESET_CONFIGS)`
  → evaluate / chunk-config API 相关行。

**L354** `    else:`

**L355** `        configs = [ChunkConfig.from_dict(c.model_dump()) for c in body.configs]`

**L356** `        for c in configs:`

**L357** `            c.validate()`

**L358** ``

**L359** `    results = run_ab_experiment(doc, configs, queries)`

**L360** `    best = pick_best_config(results)`

**L361** `    if not best:`

**L362** `        raise HTTPException(status_code=500, detail="评估未产生结果")`

**L363** ``

**L364** `    return EvaluateResponse(`

**L365** `        best_config=best.to_dict()["config"],`

**L366** `        results=[r.to_dict() for r in results],`

**L367** `        eval_query_count=len(queries),`

**L368** `    )`

**L369** ``

**L370** ``

**L371** `@router.post("/rebuild", response_model=RebuildResponse)`
  → evaluate / chunk-config API 相关行。

**L372** `def rebuild_knowledge_base(body: RebuildRequest | None = None) -> RebuildResponse:`
  → evaluate / chunk-config API 相关行。

**L373** `    """`

**L374** `    按当前 chunk_config（或 evaluate 最优配置）全量重建知识库。`
  → evaluate / chunk-config API 相关行。

**L375** ``

**L376** `    重扫 sample_docs + knowledge_uploads，清空后重新分块与索引。`

**L377** `    """`

**L378** `    body = body or RebuildRequest()`

**L379** `    store = get_knowledge_store()`

**L380** ``

**L381** `    if body.apply_best_config:`

**L382** `        if not _EVAL_SAMPLE.is_file():`
  → evaluate / chunk-config API 相关行。

**L383** `            raise HTTPException(status_code=500, detail="评估样例文档缺失")`

**L384** `        report = rebuild_with_best_config(`
  → evaluate / chunk-config API 相关行。

**L385** `            store,`

**L386** `            eval_sample_path=_EVAL_SAMPLE,`
  → evaluate / chunk-config API 相关行。

**L387** `            include_sample_docs=body.include_sample_docs,`

**L388** `        )`

**L389** `    else:`

**L390** `        report = rebuild_store(store, include_sample_docs=body.include_sample_docs)`
  → evaluate / chunk-config API 相关行。

**L391** ``

**L392** `    cleared = session_manager.clear_all()`

**L393** `    data = report.to_dict()`

**L394** `    data["sessions_cleared"] = cleared`

**L395** `    return RebuildResponse(**data)`

**L396** ``

**L397** ``

**L398** `@router.get("/status", response_model=KnowledgeStatusResponse)`

**L399** `def knowledge_status() -> KnowledgeStatusResponse:`

**L400** `    """返回知识库文档数、分块数、支持格式与文档列表"""`

**L401** `    store = get_knowledge_store()`

**L402** `    data = store.status_dict()`

**L403** `    return KnowledgeStatusResponse(**data)`

**L404** ``

**L405** ``

**L406** `@router.post("/upload", response_model=KnowledgeUploadResponse)`

**L407** `async def upload_document(`

**L408** `    file: UploadFile = File(..., description="企业文档 (.txt / .md / .pdf)"),`

**L409** `) -> KnowledgeUploadResponse:`

**L410** `    """`

**L411** `    上传企业文档到知识库：解析 → 落盘 → 分块 → Chroma 向量索引 → 持久化。`

**L412** ``

**L413** `    Day 26 起支持 Markdown 与 PDF。上传成功后清除服务端会话。`

**L414** `    """`

**L415** `    if not file.filename:`

**L416** `        raise HTTPException(status_code=422, detail="缺少文件名")`

**L417** ``

**L418** `    data = await file.read()`

**L419** `    if not data:`

**L420** `        raise HTTPException(status_code=422, detail="文件内容为空")`

**L421** `    if len(data) > MAX_UPLOAD_BYTES:`

**L422** `        raise HTTPException(status_code=413, detail="文件超过 500KB 上限")`

**L423** ``

**L424** `    try:`

**L425** `        meta = ingest_upload(data, file.filename)`

**L426** `    except ValueError as exc:`

**L427** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L428** `    except NexusError as exc:`

**L429** `        status = 400 if exc.code in (`

**L430** `            "PDF_PARSE_ERROR", "PDF_EMPTY", "UNSUPPORTED_FORMAT", "EMPTY_FILE"`

**L431** `        ) else 500`

**L432** `        raise HTTPException(status_code=status, detail=exc.message) from exc`

**L433** `    except StorageError as exc:`

**L434** `        raise HTTPException(status_code=500, detail=exc.message) from exc`

**L435** `    except UnicodeDecodeError as exc:`

**L436** `        raise HTTPException(status_code=400, detail="文本文件须为 UTF-8 编码") from exc`

**L437** ``

**L438** `    cleared = session_manager.clear_all()`

**L439** `    store = get_knowledge_store()`

**L440** ``

**L441** `    return KnowledgeUploadResponse(`

**L442** `        filename=meta.name,`

**L443** `        format=meta.format,`

**L444** `        chunk_count=meta.chunk_count,`
  → evaluate / chunk-config API 相关行。

**L445** `        document_count=store.document_count,`

**L446** `        total_chunks=store.chunk_count,`
  → evaluate / chunk-config API 相关行。

**L447** `        sessions_cleared=cleared,`

**L448** `        index_mode=store.index_mode,`

**L449** `        message=f"文档已入库（{meta.format}），增量索引已更新",`

**L450** `    )`


## 失败模式

| 失败 | 处理 |
|------|------|
| overlap 非法 | validate → 422 |
| 样例文件缺失 | 500 + detail |
| 空 configs 结果 | 500「评估未产生结果」 |
