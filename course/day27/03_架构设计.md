# Day 27 架构设计

**需求**：ZL-NA-REQ-027

## 分层视图

```
┌─────────────────────────────────────────┐
│  frontend/knowledge.js  (#kb-eval-btn)  │
└──────────────────┬──────────────────────┘
                   │ POST /evaluate
┌──────────────────▼──────────────────────┐
│  api/knowledge.py                       │
│  get/put chunk-config · evaluate        │
└───────┬─────────────────────┬───────────┘
        │                     │
        ▼                     ▼
┌───────────────┐    ┌────────────────────┐
│ KnowledgeStore│    │ retrieval_eval     │
│ chunk_config  │    │ (内存临时 retriever)│
│ persist       │    │ 不写 chunks        │
└───────────────┘    └─────────┬──────────┘
                               │
                               ▼
                    ┌──────────────────────┐
                    │ chunk_config         │
                    │ chunk_strategies     │
                    │ embedding_retriever  │
                    └──────────────────────┘
```

## 核心隔离原则

**评估路径**调用 `build_retriever_for_doc`：在内存中对单份 `ParsedDocument` 分块并训练 TF-IDF，**从不**调用 `store._append_chunks` 或 `save()`。

**生产路径**由 `upload` / `ingest` 使用 `store.get_chunk_config()` 分块并写入索引。

## ChunkConfig 源码锚点

```python
"""
分块参数配置 — chunk_size / overlap / strategy

需求：ZL-NA-REQ-027
"""

from __future__ import annotations

from dataclasses import asdict, dataclass, field
from typing import Any


@dataclass
class ChunkConfig:
    """知识库分块参数（可持久化、可 A/B 对比）"""

    chunk_size: int = 200
    overlap: int = 40
    strategy: str = "auto"  # auto | fixed | markdown
    name: str = "default"

    def validate(self) -> None:
        if self.chunk_size <= 0:
            raise ValueError("chunk_size 必须为正整数")
        if self.overlap < 0 or self.overlap >= self.chunk_size:
            raise ValueError("overlap 必须满足 0 <= overlap < chunk_size")
        if self.strategy not in ("auto", "fixed", "markdown"):
            raise ValueError("strategy 须为 auto / fixed / markdown")

    def to_dict(self) -> dict[str, Any]:
        return asdict(self)

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ChunkConfig:
        return cls(
            chunk_size=int(data.get("chunk_size", 200)),
            overlap=int(data.get("overlap", 40)),
            strategy=str(data.get("strategy", "auto")),
            name=str(data.get("name", "default")),
        )


DEFAULT_CHUNK_CONFIG = ChunkConfig()

PRESET_CONFIGS: tuple[ChunkConfig, ...] = (
    ChunkConfig(name="compact", chunk_size=120, overlap=20, strategy="auto"),
    ChunkConfig(name="default", chunk_size=200, overlap=40, strategy="auto"),
    ChunkConfig(name="wide", chunk_size=400, overlap=60, strategy="auto"),
    ChunkConfig(name="markdown_wide", chunk_size=500, overlap=50, strategy="markdown"),
)
```


## evaluate 端点伪代码

```python
doc = parse_bytes(_EVAL_SAMPLE)
queries = [EvalQuery.from_dict(q) for q in EVAL_QUERIES]
configs = PRESET_CONFIGS if body.use_presets else body.configs
results = run_ab_experiment(doc, configs, queries)
return EvaluateResponse(best_config=results[0], ...)
```

## 持久化字段

`store.json` 片段：

```json
{
  "chunk_config": {
    "chunk_size": 200,
    "overlap": 40,
    "strategy": "auto",
    "name": "default"
  }
}
```



## 附录 A：knowledge_store.py 走读

### `nexus-agent-platform/src/rag/knowledge_store.py` 逐行走读

共 **556** 行。

**L1** `"""`

**L2** `知识库存储 — 文档 ingestion、分块索引与 JSON 持久化`

**L3** ``

**L4** `将 Day 19–20 的 RAG 管线升级为可写入、可落盘的企业知识库 MVP。`

**L5** ``

**L6** `需求：ZL-NA-REQ-025`

**L7** `"""`

**L8** ``

**L9** `from __future__ import annotations`

**L10** ``

**L11** `import threading`

**L12** `from dataclasses import dataclass, field`

**L13** `from datetime import datetime, timezone`

**L14** `from pathlib import Path`

**L15** `from typing import Any`

**L16** ``

**L17** `from core.paths import get_path`

**L18** `from rag.chunker import TextChunk, chunk_documents, chunk_text`

**L19** `from rag.chunk_config import DEFAULT_CHUNK_CONFIG, ChunkConfig`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L20** `from rag.chunk_strategies import chunk_from_parsed`

**L21** `from rag.context import DocumentIndex, RAGContextService`

**L22** `from rag.chroma_retriever import ChromaEmbeddingRetriever`

**L23** `from rag.chroma_store import VECTOR_BACKEND, ChromaVectorIndex`

**L24** `from tools.doc_reader import DocumentRecord, read_text_file`

**L25** `from tools.parsers.base import ParsedDocument`

**L26** `from utils.json_utils import load_json, save_json`

**L27** `from utils.text_utils import clean_text`

**L28** ``

**L29** `STORE_VERSION = "1.1"`

**L30** `PLATFORM_VERSION = "0.30.0"`

**L31** `INDEX_MODE_INCREMENTAL = "incremental"`

**L32** `INDEX_MODE_FULL = "full"`

**L33** ``

**L34** ``

**L35** `@dataclass`

**L36** `class KnowledgeDocument:`

**L37** `    """已入库文档元数据"""`

**L38** ``

**L39** `    name: str`

**L40** `    ingested_at: str`

**L41** `    size_bytes: int = 0`

**L42** `    chunk_count: int = 0`

**L43** `    format: str = "txt"`

**L44** ``

**L45** `    def to_dict(self) -> dict[str, Any]:`

**L46** `        return {`

**L47** `            "name": self.name,`

**L48** `            "ingested_at": self.ingested_at,`

**L49** `            "size_bytes": self.size_bytes,`

**L50** `            "chunk_count": self.chunk_count,`

**L51** `            "format": self.format,`

**L52** `        }`

**L53** ``

**L54** `    @classmethod`

**L55** `    def from_dict(cls, data: dict[str, Any]) -> KnowledgeDocument:`

**L56** `        return cls(`

**L57** `            name=str(data.get("name", "")),`

**L58** `            ingested_at=str(data.get("ingested_at", "")),`

**L59** `            size_bytes=int(data.get("size_bytes", 0)),`

**L60** `            chunk_count=int(data.get("chunk_count", 0)),`

**L61** `            format=str(data.get("format", "txt")),`

**L62** `        )`

**L63** ``

**L64** ``

**L65** `@dataclass`

**L66** `class KnowledgeStore:`

**L67** `    """`

**L68** `    企业知识库 — 分块 + Chroma 向量索引 + JSON 元数据持久化`

**L69** ``

**L70** `    典型用法：`

**L71** `        store = KnowledgeStore.load_or_bootstrap()`

**L72** `        store.ingest_text("新产品说明…", filename="notice.txt")`

**L73** `        rag = store.as_rag_service()`

**L74** `    """`

**L75** ``

**L76** `    documents: list[KnowledgeDocument] = field(default_factory=list)`

**L77** `    chunks: list[TextChunk] = field(default_factory=list)`

**L78** `    embedding_state: dict[str, Any] = field(default_factory=dict)`

**L79** `    vector_backend: str = VECTOR_BACKEND`

**L80** `    chunk_config: ChunkConfig = field(default_factory=ChunkConfig)`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L81** `    last_rebuilt_at: str | None = None`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L82** `    last_incremental_at: str | None = None`

**L83** `    index_mode: str = INDEX_MODE_FULL`

**L84** `    store_path: Path | None = None`

**L85** `    chroma_path: Path | None = None`

**L86** `    _rag_service: RAGContextService | None = field(default=None, repr=False)`

**L87** ``

**L88** `    @property`

**L89** `    def chunk_count(self) -> int:`

**L90** `        return len(self.chunks)`

**L91** ``

**L92** `    @property`

**L93** `    def document_count(self) -> int:`

**L94** `        return len(self.documents)`

**L95** ``

**L96** `    def as_rag_service(self) -> RAGContextService:`

**L97** `        """构建或返回缓存的 RAGContextService"""`

**L98** `        if self._rag_service is None:`

**L99** `            self._rag_service = self._build_rag_service()`

**L100** `        return self._rag_service`

**L101** ``

**L102** `    def invalidate_cache(self) -> None:`

**L103** `        self._rag_service = None`

**L104** ``

**L105** `    def get_chunk_config(self) -> ChunkConfig:`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L106** `        return ChunkConfig.from_dict(self.chunk_config.to_dict())`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L107** ``

**L108** `    def set_chunk_config(self, config: ChunkConfig) -> ChunkConfig:`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L109** `        config.validate()`

**L110** `        self.chunk_config = ChunkConfig.from_dict(config.to_dict())`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L111** `        return self.chunk_config`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L112** ``

**L113** `    def ingest_text(`

**L114** `        self,`

**L115** `        content: str,`

**L116** `        *,`

**L117** `        filename: str,`

**L118** `        clean: bool = True,`

**L119** `        chunk_size: int | None = None,`

**L120** `        overlap: int | None = None,`

**L121** `    ) -> KnowledgeDocument:`

**L122** `        """将文本写入知识库并重建索引"""`

**L123** `        cfg = self.get_chunk_config()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L124** `        cs = chunk_size if chunk_size is not None else cfg.chunk_size`

**L125** `        ov = overlap if overlap is not None else cfg.overlap`

**L126** `        text = (content or "").strip()`

**L127** `        if not text:`

**L128** `            raise ValueError("文档内容不能为空")`

**L129** `        if not filename.strip():`

**L130** `            raise ValueError("filename 不能为空")`

**L131** ``

**L132** `        cleaned = text`

**L133** `        if clean:`

**L134** `            cleaned, _ = clean_text(text)`

**L135** `        doc = DocumentRecord(`

**L136** `            path=Path(filename),`

**L137** `            content=text,`

**L138** `            encoding="utf-8",`

**L139** `            size_bytes=len(text.encode("utf-8")),`

**L140** `            cleaned=cleaned,`

**L141** `        )`

**L142** `        new_chunks = chunk_documents(`

**L143** `            [doc],`

**L144** `            chunk_size=cs,`

**L145** `            overlap=ov,`

**L146** `            use_cleaned=clean,`

**L147** `        )`

**L148** `        self._append_chunks(filename, new_chunks, size_bytes=doc.size_bytes)`

**L149** `        self._rebuild_index()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L150** `        return self.documents[-1]`

**L151** ``

**L152** `    def ingest_file(`

**L153** `        self,`

**L154** `        path: Path,`

**L155** `        *,`

**L156** `        clean: bool = True,`

**L157** `        chunk_size: int = 200,`

**L158** `        overlap: int = 40,`

**L159** `    ) -> KnowledgeDocument:`

**L160** `        """从磁盘文件 ingestion"""`

**L161** `        content, encoding = read_text_file(path)`

**L162** `        cleaned = content`

**L163** `        if clean:`

**L164** `            cleaned, _ = clean_text(content)`

**L165** `        doc = DocumentRecord(`

**L166** `            path=path,`

**L167** `            content=content,`

**L168** `            encoding=encoding,`

**L169** `            size_bytes=path.stat().st_size,`

**L170** `            cleaned=cleaned,`

**L171** `        )`

**L172** `        new_chunks = chunk_documents(`

**L173** `            [doc],`

**L174** `            chunk_size=chunk_size,`

**L175** `            overlap=overlap,`

**L176** `            use_cleaned=clean,`

**L177** `        )`

**L178** `        self._append_chunks(path.name, new_chunks, size_bytes=doc.size_bytes)`

**L179** `        self._rebuild_index()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L180** `        return self.documents[-1]`

**L181** ``

**L182** `    def ingest_bytes(`

**L183** `        self,`

**L184** `        data: bytes,`

**L185** `        *,`

**L186** `        filename: str,`

**L187** `        clean: bool = True,`

**L188** `        chunk_strategy: str = "auto",`

**L189** `        incremental: bool = True,`

**L190** `    ) -> KnowledgeDocument:`

**L191** `        """处理上传二进制 — Day 26 起委托 doc_parser"""`

**L192** `        from tools.doc_parser import parse_bytes`

**L193** ``

**L194** `        parsed = parse_bytes(data, filename)`

**L195** `        cfg = self.get_chunk_config()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L196** `        return self.ingest_parsed(`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L197** `            parsed,`

**L198** `            clean=clean,`

**L199** `            chunk_strategy=chunk_strategy if chunk_strategy != "auto" else cfg.strategy,`

**L200** `            incremental=incremental,`

**L201** `        )`

**L202** ``

**L203** `    def ingest_parsed(`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L204** `        self,`

**L205** `        parsed: ParsedDocument,`

**L206** `        *,`

**L207** `        clean: bool = True,`

**L208** `        chunk_strategy: str | None = None,`

**L209** `        chunk_size: int | None = None,`

**L210** `        overlap: int | None = None,`

**L211** `        incremental: bool = False,`

**L212** `    ) -> KnowledgeDocument:`

**L213** `        """将 ParsedDocument 写入知识库"""`

**L214** `        cfg = self.get_chunk_config()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L215** `        strategy = chunk_strategy if chunk_strategy is not None else cfg.strategy`

**L216** `        cs = chunk_size if chunk_size is not None else cfg.chunk_size`

**L217** `        ov = overlap if overlap is not None else cfg.overlap`

**L218** `        text = (parsed.plain_text or "").strip()`

**L219** `        if not text:`

**L220** `            raise ValueError("解析结果为空")`

**L221** ``

**L222** `        if clean:`

**L223** `            text, _ = clean_text(text)`

**L224** `            parsed.plain_text = text`

**L225** `            for section in parsed.sections:`

**L226** `                section.body, _ = clean_text(section.body)`

**L227** ``

**L228** `        new_chunks = chunk_from_parsed(`

**L229** `            parsed,`

**L230** `            strategy=strategy,`

**L231** `            chunk_size=cs,`

**L232** `            overlap=ov,`

**L233** `        )`

**L234** `        size_bytes = len(parsed.plain_text.encode("utf-8"))`

**L235** ``

**L236** `        if incremental:`

**L237** `            removed = self._remove_document_by_source(parsed.filename)`

**L238** `            self._append_chunks(`

**L239** `                parsed.filename,`

**L240** `                new_chunks,`

**L241** `                size_bytes=size_bytes,`

**L242** `                doc_format=parsed.format,`

**L243** `            )`

**L244** `            self._incremental_index(new_chunks, replaced_count=len(removed))`

**L245** `        else:`

**L246** `            self._append_chunks(`

**L247** `                parsed.filename,`

**L248** `                new_chunks,`

**L249** `                size_bytes=size_bytes,`

**L250** `                doc_format=parsed.format,`

**L251** `            )`

**L252** `            self._rebuild_index()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L253** `        return self.documents[-1]`

**L254** ``

**L255** `    def save(self, path: Path | None = None) -> Path:`

**L256** `        """持久化到 JSON"""`

**L257** `        target = path or self.store_path or _default_store_path()`

**L258** `        self.store_path = target`

**L259** `        payload = {`

**L260** `            "version": STORE_VERSION,`

**L261** `            "platform_version": PLATFORM_VERSION,`

**L262** `            "documents": [d.to_dict() for d in self.documents],`

**L263** `            "chunks": [_chunk_to_dict(c) for c in self.chunks],`

**L264** `            "embedding": self.embedding_state,`

**L265** `            "vector_backend": self.vector_backend,`

**L266** `            "chunk_config": self.chunk_config.to_dict(),`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L267** `            "last_rebuilt_at": self.last_rebuilt_at,`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L268** `            "last_incremental_at": self.last_incremental_at,`

**L269** `            "index_mode": self.index_mode,`

**L270** `        }`

**L271** `        save_json(target, payload)`

**L272** `        return target`

**L273** ``

**L274** `    @classmethod`

**L275** `    def load(cls, path: Path) -> KnowledgeStore:`

**L276** `        """从 JSON 加载知识库"""`

**L277** `        raw = load_json(path, default=None)`

**L278** `        if not raw:`

**L279** `            return cls.bootstrap_from_sample_docs(store_path=path)`

**L280** ``

**L281** `        store = cls(store_path=path)`

**L282** `        store.documents = [`

**L283** `            KnowledgeDocument.from_dict(d) for d in raw.get("documents", [])`

**L284** `        ]`

**L285** `        store.chunks = [_chunk_from_dict(c) for c in raw.get("chunks", [])]`

**L286** `        store.embedding_state = dict(raw.get("embedding") or {})`

**L287** `        store.vector_backend = str(raw.get("vector_backend") or VECTOR_BACKEND)`

**L288** `        if raw.get("chunk_config"):`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L289** `            store.chunk_config = ChunkConfig.from_dict(raw["chunk_config"])`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L290** `        store.last_rebuilt_at = raw.get("last_rebuilt_at")`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L291** `        store.last_incremental_at = raw.get("last_incremental_at")`

**L292** `        store.index_mode = str(raw.get("index_mode") or INDEX_MODE_FULL)`

**L293** `        store._sync_chroma_from_json()`

**L294** `        store._rag_service = store._build_rag_service()`

**L295** `        return store`

**L296** ``

**L297** `    @classmethod`

**L298** `    def load_or_bootstrap(cls, path: Path | None = None) -> KnowledgeStore:`

**L299** `        """加载已有库，不存在则从 sample_docs 引导"""`

**L300** `        target = path or _default_store_path()`

**L301** `        if target.is_file():`

**L302** `            return cls.load(target)`

**L303** `        store = cls.bootstrap_from_sample_docs(store_path=target)`

**L304** `        store.save(target)`

**L305** `        return store`

**L306** ``

**L307** `    @classmethod`

**L308** `    def bootstrap_from_sample_docs(cls, *, store_path: Path | None = None) -> KnowledgeStore:`

**L309** `        """用内置 sample_docs 初始化知识库"""`

**L310** `        rag = RAGContextService.from_sample_docs(use_embedding=True)`

**L311** `        store = cls(store_path=store_path)`

**L312** `        now = _utc_now()`

**L313** ``

**L314** `        by_source: dict[str, list[TextChunk]] = {}`

**L315** `        for chunk in rag.index.chunks:`

**L316** `            by_source.setdefault(chunk.source, []).append(chunk)`

**L317** ``

**L318** `        for source, chunks in sorted(by_source.items()):`

**L319** `            store.documents.append(`

**L320** `                KnowledgeDocument(`

**L321** `                    name=source,`

**L322** `                    ingested_at=now,`

**L323** `                    size_bytes=sum(len(c.text) for c in chunks),`

**L324** `                    chunk_count=len(chunks),`

**L325** `                )`

**L326** `            )`

**L327** `        store.chunks = list(rag.index.chunks)`

**L328** `        retriever = rag.index.retriever`

**L329** `        from rag.embedding_retriever import EmbeddingRetriever`

**L330** ``

**L331** `        if isinstance(retriever, EmbeddingRetriever):`

**L332** `            store.embedding_state = retriever._client.model.export_state()`

**L333** `        store._rebuild_index()`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L334** `        return store`

**L335** ``

**L336** `    def status_dict(self) -> dict[str, Any]:`

**L337** `        from tools.doc_parser import supported_formats`

**L338** ``

**L339** `        return {`

**L340** `            "document_count": self.document_count,`

**L341** `            "chunk_count": self.chunk_count,`

**L342** `            "documents": [d.to_dict() for d in self.documents],`

**L343** `            "store_path": str(self.store_path) if self.store_path else None,`

**L344** `            "platform_version": PLATFORM_VERSION,`

**L345** `            "supported_formats": supported_formats(),`

**L346** `            "chunk_config": self.chunk_config.to_dict(),`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L347** `            "last_rebuilt_at": self.last_rebuilt_at,`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L348** `            "last_incremental_at": self.last_incremental_at,`

**L349** `            "index_mode": self.index_mode,`

**L350** `            "vector_backend": self.vector_backend,`

**L351** `            "chroma_path": str(self._resolve_chroma_path()),`

**L352** `            "chroma_count": self._chroma_index().count() if self.chunks else 0,`

**L353** `        }`

**L354** ``

**L355** `    def _append_chunks(`

**L356** `        self,`

**L357** `        filename: str,`

**L358** `        new_chunks: list[TextChunk],`

**L359** `        *,`

**L360** `        size_bytes: int,`

**L361** `        doc_format: str = "txt",`

**L362** `    ) -> None:`

**L363** `        base_index = len(self.chunks)`

**L364** `        reindexed: list[TextChunk] = []`

**L365** `        for i, chunk in enumerate(new_chunks):`

**L366** `            reindexed.append(`

**L367** `                TextChunk(`

**L368** `                    chunk_id=chunk.chunk_id,`

**L369** `                    text=chunk.text,`

**L370** `                    source=filename,`

**L371** `                    index=base_index + i,`

**L372** `                    start_char=chunk.start_char,`

**L373** `                    end_char=chunk.end_char,`

**L374** `                )`

**L375** `            )`

**L376** `        self.chunks.extend(reindexed)`

**L377** `        self.documents.append(`

**L378** `            KnowledgeDocument(`

**L379** `                name=filename,`

**L380** `                ingested_at=_utc_now(),`

**L381** `                size_bytes=size_bytes,`

**L382** `                chunk_count=len(reindexed),`

**L383** `                format=doc_format,`

**L384** `            )`

**L385** `        )`

**L386** ``

**L387** `    def _remove_document_by_source(self, filename: str) -> list[str]:`

**L388** `        """移除同名文档及其 chunks，并从 Chroma 删除旧向量"""`

**L389** `        removed_ids = [c.chunk_id for c in self.chunks if c.source == filename]`

**L390** `        if not removed_ids:`

**L391** `            return []`

**L392** ``

**L393** `        self._chroma_index().delete_by_ids(removed_ids)`

**L394** `        self.documents = [d for d in self.documents if d.name != filename]`

**L395** `        self.chunks = [c for c in self.chunks if c.source != filename]`

**L396** `        self.chunks = [`

**L397** `            TextChunk(`

**L398** `                chunk_id=c.chunk_id,`

**L399** `                text=c.text,`

**L400** `                source=c.source,`

**L401** `                index=i,`

**L402** `                start_char=c.start_char,`

**L403** `                end_char=c.end_char,`

**L404** `            )`

**L405** `            for i, c in enumerate(self.chunks)`

**L406** `        ]`

**L407** `        return removed_ids`

**L408** ``

**L409** `    def _resolve_chroma_path(self) -> Path:`

**L410** `        if self.chroma_path is not None:`

**L411** `            return self.chroma_path`

**L412** `        if self.store_path is not None:`

**L413** `            return self.store_path.parent / "chroma"`

**L414** `        return get_path("knowledge_chroma")`

**L415** ``

**L416** `    def _chroma_index(self) -> ChromaVectorIndex:`

**L417** `        return ChromaVectorIndex(self._resolve_chroma_path())`

**L418** ``

**L419** `    def _sync_chroma_from_json(self) -> None:`

**L420** `        """从 JSON 元数据恢复 Chroma（迁移或冷启动）"""`

**L421** `        if not self.chunks or not self.embedding_state:`

**L422** `            return`

**L423** `        chroma = self._chroma_index()`

**L424** `        if chroma.count() > 0:`

**L425** `            return`

**L426** `        from rag.embedding import EmbeddingClient`

**L427** ``

**L428** `        client = EmbeddingClient()`

**L429** `        client.model.load_state(self.embedding_state)`

**L430** `        vectors = client.embed_batch([c.text for c in self.chunks])`

**L431** `        chroma.upsert_chunks(self.chunks, vectors)`

**L432** ``

**L433** `    def _rebuild_index(self) -> None:`
  → 与 Day 27 chunk_config 持久化及后续 rebuild 相关。

**L434** `        from rag.embedding_retriever import EmbeddingRetriever`

**L435** ``

**L436** `        if not self.chunks:`

**L437** `            self.embedding_state = {}`

**L438** `            self._chroma_index().reset()`

**L439** `            self.invalidate_cache()`

**L440** `            return`

**L441** ``

**L442** `        retriever = EmbeddingRetriever(self.chunks)`

**L443** `        self.embedding_state = retriever._client.model.export_state()`

**L444** `        vectors = retriever._client.embed_batch([c.text for c in self.chunks])`

**L445** `        chroma = self._chroma_index()`

**L446** `        chroma.reset()`

**L447** `        chroma.upsert_chunks(self.chunks, vectors)`

**L448** `        self.index_mode = INDEX_MODE_FULL`

**L449** `        self.invalidate_cache()`

**L450** ``

**L451** `    def _incremental_index(`

**L452** `        self,`

**L453** `        affected_chunks: list[TextChunk],`

**L454** `        *,`

**L455** `        replaced_count: int = 0,`

**L456** `    ) -> bool:`

**L457** `        """`

**L458** `        增量更新 Chroma：不 reset collection，仅 upsert 受影响 chunk。`

**L459** ``

**L460** `        若 TF-IDF 词表扩张，则回退为全量 upsert（仍不 reset）。`

**L461** `        返回是否发生词表扩张。`

**L462** `        """`

**L463** `        from rag.embedding_retriever import EmbeddingRetriever`

**L464** ``

**L465** `        if not self.chunks:`

**L466** `            self.embedding_state = {}`

**L467** `            self._chroma_index().reset()`

**L468** `            self.last_incremental_at = _utc_now()`

**L469** `            self.index_mode = INDEX_MODE_INCREMENTAL`

**L470** `            self.invalidate_cache()`

**L471** `            return False`

**L472** ``

**L473** `        old_vocab = dict(self.embedding_state.get("vocab") or {})`

**L474** `        retriever = EmbeddingRetriever(self.chunks)`

**L475** `        self.embedding_state = retriever._client.model.export_state()`

**L476** `        new_vocab = dict(self.embedding_state.get("vocab") or {})`

**L477** `        vocab_expanded = len(new_vocab) > len(old_vocab) and bool(old_vocab)`

**L478** ``

**L479** `        if vocab_expanded:`

**L480** `            affected_chunks = list(self.chunks)`

**L481** ``

**L482** `        client = retriever._client`

**L483** `        vectors = client.embed_batch([c.text for c in affected_chunks])`

**L484** `        chroma = self._chroma_index()`

**L485** `        if vocab_expanded:`

**L486** `            # TF-IDF 维度变化时 Chroma collection 须重建`

**L487** `            chroma.reset()`

**L488** `        chroma.upsert_chunks(affected_chunks, vectors)`

**L489** `        self.last_incremental_at = _utc_now()`

**L490** `        self.index_mode = INDEX_MODE_INCREMENTAL`

**L491** `        self.invalidate_cache()`

**L492** `        return vocab_expanded`

**L493** ``

**L494** `    def _build_rag_service(self) -> RAGContextService:`

**L495** `        if not self.chunks:`

**L496** `            return RAGContextService()`

**L497** ``

**L498** `        from rag.embedding import EmbeddingClient`

**L499** ``

**L500** `        client = EmbeddingClient()`

**L501** `        client.model.load_state(self.embedding_state)`

**L502** `        self._sync_chroma_from_json()`

**L503** `        chroma = self._chroma_index()`

**L504** `        retriever = ChromaEmbeddingRetriever(self.chunks, chroma, client=client)`

**L505** `        index = DocumentIndex(chunks=self.chunks, retriever=retriever)`

**L506** `        return RAGContextService(index)`

**L507** ``

**L508** ``

**L509** `_store: KnowledgeStore | None = None`

**L510** `_store_lock = threading.Lock()`

**L511** ``

**L512** ``

**L513** `def get_knowledge_store(*, reload: bool = False) -> KnowledgeStore:`

**L514** `    """全局知识库单例（API 与 factory 共享）"""`

**L515** `    global _store`

**L516** `    with _store_lock:`

**L517** `        if _store is None or reload:`

**L518** `            _store = KnowledgeStore.load_or_bootstrap()`

**L519** `        return _store`

**L520** ``

**L521** ``

**L522** `def set_knowledge_store(store: KnowledgeStore) -> None:`

**L523** `    """测试注入用"""`

**L524** `    global _store`

**L525** `    with _store_lock:`

**L526** `        _store = store`

**L527** ``

**L528** ``

**L529** `def _default_store_path() -> Path:`

**L530** `    return get_path("knowledge_store")`

**L531** ``

**L532** ``

**L533** `def _utc_now() -> str:`

**L534** `    return datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")`

**L535** ``

**L536** ``

**L537** `def _chunk_to_dict(chunk: TextChunk) -> dict[str, Any]:`

**L538** `    return {`

**L539** `        "chunk_id": chunk.chunk_id,`

**L540** `        "text": chunk.text,`

**L541** `        "source": chunk.source,`

**L542** `        "index": chunk.index,`

**L543** `        "start_char": chunk.start_char,`

**L544** `        "end_char": chunk.end_char,`

**L545** `    }`

**L546** ``

**L547** ``

**L548** `def _chunk_from_dict(data: dict[str, Any]) -> TextChunk:`

**L549** `    return TextChunk(`

**L550** `        chunk_id=str(data.get("chunk_id", "")),`

**L551** `        text=str(data.get("text", "")),`

**L552** `        source=str(data.get("source", "")),`

**L553** `        index=int(data.get("index", 0)),`

**L554** `        start_char=int(data.get("start_char", 0)),`

**L555** `        end_char=int(data.get("end_char", 0)),`

**L556** `    )`


## 附录 B：api/knowledge.py 走读

### `nexus-agent-platform/src/api/knowledge.py` 逐行走读

共 **175** 行。

**L1** `"""`

**L2** `知识库 REST API — 文档上传、分块调参与检索评估`

**L3** ``

**L4** `需求：ZL-NA-REQ-025 / ZL-NA-REQ-026 / ZL-NA-REQ-027 / ZL-NA-REQ-028 / ZL-NA-REQ-029 / ZL-NA-REQ-030`

**L5** `"""`

**L6** ``

**L7** `from __future__ import annotations`

**L8** ``

**L9** `from pathlib import Path`

**L10** ``

**L11** `from fastapi import APIRouter, File, HTTPException, UploadFile`

**L12** ``

**L13** `from api.schemas import (`

**L14** `    ChunkConfigRequest,`

**L15** `    ChunkConfigResponse,`

**L16** `    EvaluateRequest,`

**L17** `    EvaluateResponse,`

**L18** `    KnowledgeStatusResponse,`

**L19** `    KnowledgeUploadResponse,`

**L20** `    RebuildRequest,`

**L21** `    RebuildResponse,`

**L22** `)`

**L23** `from api.sessions import session_manager`

**L24** `from core.exceptions import NexusError, StorageError`

**L25** `from rag.chunk_config import PRESET_CONFIGS, ChunkConfig`
  → evaluate / chunk-config API 相关行。

**L26** `from rag.ingestion import ingest_upload`

**L27** `from rag.knowledge_rebuild import rebuild_store, rebuild_with_best_config`
  → evaluate / chunk-config API 相关行。

**L28** `from rag.knowledge_store import get_knowledge_store`

**L29** `from rag.retrieval_eval import EvalQuery, pick_best_config, run_ab_experiment`

**L30** `from tools.doc_parser import parse_bytes`

**L31** ``

**L32** `router = APIRouter(prefix="/api/knowledge", tags=["knowledge"])`

**L33** ``

**L34** `MAX_UPLOAD_BYTES = 512_000  # 500 KB 教学上限`

**L35** `_EVAL_SAMPLE = (`
  → evaluate / chunk-config API 相关行。

**L36** `    Path(__file__).resolve().parent.parent / "day26" / "sample_docs" / "product_notice.md"`

**L37** `)`

**L38** ``

**L39** ``

**L40** `@router.get("/chunk-config", response_model=ChunkConfigResponse)`
  → evaluate / chunk-config API 相关行。

**L41** `def get_chunk_config() -> ChunkConfigResponse:`
  → evaluate / chunk-config API 相关行。

**L42** `    """返回当前知识库默认分块参数"""`

**L43** `    cfg = get_knowledge_store().get_chunk_config()`
  → evaluate / chunk-config API 相关行。

**L44** `    return ChunkConfigResponse(**cfg.to_dict())`

**L45** ``

**L46** ``

**L47** `@router.put("/chunk-config", response_model=ChunkConfigResponse)`
  → evaluate / chunk-config API 相关行。

**L48** `def update_chunk_config(body: ChunkConfigRequest) -> ChunkConfigResponse:`
  → evaluate / chunk-config API 相关行。

**L49** `    """更新默认分块参数（影响后续上传）"""`

**L50** `    store = get_knowledge_store()`

**L51** `    try:`

**L52** `        cfg = ChunkConfig.from_dict(body.model_dump())`

**L53** `        cfg.validate()`

**L54** `        store.set_chunk_config(cfg)`
  → evaluate / chunk-config API 相关行。

**L55** `        store.save()`

**L56** `    except ValueError as exc:`

**L57** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L58** `    return ChunkConfigResponse(**cfg.to_dict())`

**L59** ``

**L60** ``

**L61** `@router.post("/evaluate", response_model=EvaluateResponse)`
  → evaluate / chunk-config API 相关行。

**L62** `def evaluate_chunk_configs(body: EvaluateRequest | None = None) -> EvaluateResponse:`
  → evaluate / chunk-config API 相关行。

**L63** `    """`

**L64** `    对内置样例文档运行 A/B 分块评估，返回 hit@1 与推荐配置。`

**L65** ``

**L66** `    默认使用 PRESET_CONFIGS 四套预设与 day27 评估问句集。`
  → evaluate / chunk-config API 相关行。

**L67** `    """`

**L68** `    body = body or EvaluateRequest()`

**L69** `    if not _EVAL_SAMPLE.is_file():`
  → evaluate / chunk-config API 相关行。

**L70** `        raise HTTPException(status_code=500, detail="评估样例文档缺失")`

**L71** ``

**L72** `    doc = parse_bytes(_EVAL_SAMPLE.read_bytes(), _EVAL_SAMPLE.name)`
  → evaluate / chunk-config API 相关行。

**L73** `    from day27.constants import EVAL_QUERIES`
  → evaluate / chunk-config API 相关行。

**L74** ``

**L75** `    queries = [EvalQuery.from_dict(q) for q in EVAL_QUERIES]`
  → evaluate / chunk-config API 相关行。

**L76** ``

**L77** `    if body.use_presets and not body.configs:`

**L78** `        configs = list(PRESET_CONFIGS)`
  → evaluate / chunk-config API 相关行。

**L79** `    else:`

**L80** `        configs = [ChunkConfig.from_dict(c.model_dump()) for c in body.configs]`

**L81** `        for c in configs:`

**L82** `            c.validate()`

**L83** ``

**L84** `    results = run_ab_experiment(doc, configs, queries)`

**L85** `    best = pick_best_config(results)`

**L86** `    if not best:`

**L87** `        raise HTTPException(status_code=500, detail="评估未产生结果")`

**L88** ``

**L89** `    return EvaluateResponse(`

**L90** `        best_config=best.to_dict()["config"],`

**L91** `        results=[r.to_dict() for r in results],`

**L92** `        eval_query_count=len(queries),`

**L93** `    )`

**L94** ``

**L95** ``

**L96** `@router.post("/rebuild", response_model=RebuildResponse)`
  → evaluate / chunk-config API 相关行。

**L97** `def rebuild_knowledge_base(body: RebuildRequest | None = None) -> RebuildResponse:`
  → evaluate / chunk-config API 相关行。

**L98** `    """`

**L99** `    按当前 chunk_config（或 evaluate 最优配置）全量重建知识库。`
  → evaluate / chunk-config API 相关行。

**L100** ``

**L101** `    重扫 sample_docs + knowledge_uploads，清空后重新分块与索引。`

**L102** `    """`

**L103** `    body = body or RebuildRequest()`

**L104** `    store = get_knowledge_store()`

**L105** ``

**L106** `    if body.apply_best_config:`

**L107** `        if not _EVAL_SAMPLE.is_file():`
  → evaluate / chunk-config API 相关行。

**L108** `            raise HTTPException(status_code=500, detail="评估样例文档缺失")`

**L109** `        report = rebuild_with_best_config(`
  → evaluate / chunk-config API 相关行。

**L110** `            store,`

**L111** `            eval_sample_path=_EVAL_SAMPLE,`
  → evaluate / chunk-config API 相关行。

**L112** `            include_sample_docs=body.include_sample_docs,`

**L113** `        )`

**L114** `    else:`

**L115** `        report = rebuild_store(store, include_sample_docs=body.include_sample_docs)`
  → evaluate / chunk-config API 相关行。

**L116** ``

**L117** `    cleared = session_manager.clear_all()`

**L118** `    data = report.to_dict()`

**L119** `    data["sessions_cleared"] = cleared`

**L120** `    return RebuildResponse(**data)`

**L121** ``

**L122** ``

**L123** `@router.get("/status", response_model=KnowledgeStatusResponse)`

**L124** `def knowledge_status() -> KnowledgeStatusResponse:`

**L125** `    """返回知识库文档数、分块数、支持格式与文档列表"""`

**L126** `    store = get_knowledge_store()`

**L127** `    data = store.status_dict()`

**L128** `    return KnowledgeStatusResponse(**data)`

**L129** ``

**L130** ``

**L131** `@router.post("/upload", response_model=KnowledgeUploadResponse)`

**L132** `async def upload_document(`

**L133** `    file: UploadFile = File(..., description="企业文档 (.txt / .md / .pdf)"),`

**L134** `) -> KnowledgeUploadResponse:`

**L135** `    """`

**L136** `    上传企业文档到知识库：解析 → 落盘 → 分块 → Chroma 向量索引 → 持久化。`

**L137** ``

**L138** `    Day 26 起支持 Markdown 与 PDF。上传成功后清除服务端会话。`

**L139** `    """`

**L140** `    if not file.filename:`

**L141** `        raise HTTPException(status_code=422, detail="缺少文件名")`

**L142** ``

**L143** `    data = await file.read()`

**L144** `    if not data:`

**L145** `        raise HTTPException(status_code=422, detail="文件内容为空")`

**L146** `    if len(data) > MAX_UPLOAD_BYTES:`

**L147** `        raise HTTPException(status_code=413, detail="文件超过 500KB 上限")`

**L148** ``

**L149** `    try:`

**L150** `        meta = ingest_upload(data, file.filename)`

**L151** `    except ValueError as exc:`

**L152** `        raise HTTPException(status_code=422, detail=str(exc)) from exc`

**L153** `    except NexusError as exc:`

**L154** `        status = 400 if exc.code in (`

**L155** `            "PDF_PARSE_ERROR", "PDF_EMPTY", "UNSUPPORTED_FORMAT", "EMPTY_FILE"`

**L156** `        ) else 500`

**L157** `        raise HTTPException(status_code=status, detail=exc.message) from exc`

**L158** `    except StorageError as exc:`

**L159** `        raise HTTPException(status_code=500, detail=exc.message) from exc`

**L160** `    except UnicodeDecodeError as exc:`

**L161** `        raise HTTPException(status_code=400, detail="文本文件须为 UTF-8 编码") from exc`

**L162** ``

**L163** `    cleared = session_manager.clear_all()`

**L164** `    store = get_knowledge_store()`

**L165** ``

**L166** `    return KnowledgeUploadResponse(`

**L167** `        filename=meta.name,`

**L168** `        format=meta.format,`

**L169** `        chunk_count=meta.chunk_count,`
  → evaluate / chunk-config API 相关行。

**L170** `        document_count=store.document_count,`

**L171** `        total_chunks=store.chunk_count,`
  → evaluate / chunk-config API 相关行。

**L172** `        sessions_cleared=cleared,`

**L173** `        index_mode=store.index_mode,`

**L174** `        message=f"文档已入库（{meta.format}），增量索引已更新",`

**L175** `    )`


## 失败模式

| 失败 | 处理 |
|------|------|
| overlap 非法 | validate → 422 |
| 样例文件缺失 | 500 + detail |
| 空 configs 结果 | 500「评估未产生结果」 |
