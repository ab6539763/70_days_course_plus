# Day 12 讲师授课实录（节选）

**讲师**：陈默  
**日期**：2026-07-17  
**教室**：智链科技实训室 B

---

## 开场（09:05）

「上周 Day 5 大家解析过一份假的 API 响应 JSON。今天那份 JSON **真的从网线回来**。」

板书：

```
Day 5  json.load(file)
Day 12 urllib → json.loads(response)
```

---

## 第一节 HTTP（09:35）

运行 `http_demos.py`。有同学笔记本无 Wi-Fi，GET 失败——

「看，我们 catch 了异常，演示继续。但 `llm_client_demo` **不能**静默失败，那是核心业务。」

强调 `User-Agent`：部分 API 拒绝空 UA。

---

## 第二节 请求体（10:50）

现场改 `api_demos.py` 的 `temperature` 为 `2.0`，再改为 `3.0`：

```python
config.ensure_valid()  # ValidationError
```

「边界校验在**发出请求之前**，省的是真钱。」

---

## 第三节 urllib POST 手写（14:10）

带领在白板写 `default_transport` 四步：dumps → encode → Request → urlopen。

提问：「为什么 `method="POST"`？」

同学答：「默认可能是 GET。」

「对。显式优于隐式。」

---

## 第四节 Mock 与 CI（15:20）

周航投屏 GitHub Actions：

```yaml
env:
  NEXUS_LLM_MOCK: "1"
```

「没有 Secret，PR 来自 fork 也绿。」

打开 `chat_completion.json`：「Mock 不是瞎编，是和 Day 5 **同一份卷子**。」

---

## 第五节 Live 踩坑（16:40）

讲师 Live 调用，故意 Key 少一位 → 401。

```python
except APIError as e:
    print(e.status_code, e.response_body[:200])
```

「生产日志别打印完整 body，可能有敏感信息。课堂可以看。」

---

## 收尾（16:55）

「明天 Day 13：**装饰器**。给 `complete` 穿一件.retry 外套。今晚思考题：ConfigError 要不要重试？」

全场：「不要！」

「为什么？」

「重试也过不了，浪费四次。」

---

*练习册：[12_课堂练习册.md](12_课堂练习册.md)*
