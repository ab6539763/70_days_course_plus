# Day 12 课后作业

**截止**：次日上午课前  
**提交**：Git commit + 课堂仓库对应目录

---

## 作业 A：环境配置（必做）

1. 复制 `.env.example` 为 `.env`
2. 配置 `NEXUS_LLM_MOCK=1`，验证 demo 可运行
3. 在作业本记录 `load_llm_env()` 返回的三个字段含义

---

## 作业 B：请求体分析（必做）

运行 `api_demos.py`，回答：

1. 请求体中 `messages` 有几条？各是什么 `role`？
2. `temperature` 与 `max_tokens` 的值是多少？
3. 若把 `system` 消息去掉，对模型行为有何影响？（简述）

---

## 作业 C：代码阅读（必做）

阅读 `llm/client.py`，用文字描述：

1. `LLMClient.__init__` 如何选择 transport？
2. `default_transport` 在 HTTP 错误时如何构造 `APIError`？
3. `chat()` 与 `complete()` 的关系？

---

## 作业 D：手写练习（必做）

不查资料，手写函数签名（无需完整实现）：

```python
def build_request_body(messages, config): ...
def parse_chat_completion(response): ...
def load_llm_env(*, env_file=None): ...
```

---

## 作业 E：扩展挑战（选做，+10 分）

实现一个**自定义 transport**，对任意 `user` 消息返回固定字符串 `"Echo: " + user_content`（从 payload 的 messages 里取最后一条 user）。

编写 5 行测试脚本调用 `LLMClient(transport=your_transport)` 并打印 assistant 回复。

---

## 作业 F：预习（选做）

阅读 Python 官方文档「Decorators」一节，列出装饰器的三个用途。

---

## 评分标准

| 项目 | 分值 |
|------|------|
| A 环境 | 15 |
| B 分析 | 20 |
| C 阅读 | 25 |
| D 手写 | 20 |
| E 挑战 | 10（ bonus） |
| 课堂 pytest 全绿 | 20 |

---

*参考答案：[09_作业答案.md](09_作业答案.md)*
