# Day 12 企业背景与今日任务

**日期**：2026 年 7 月 17 日  
**Sprint**：Sprint 2 第 5 天 — 网络与 API 日

## 晨会纪要

### 赵岩（产品）

> 「理财知识库 MVP 的验收标准是：**用户提问 → 系统基于文档给出回答**。昨天解决了文档进内存，今天解决**模型出答案**。」

### 陈默（架构）

> 「Day 5 我们离线解析过 Chat Completion JSON。今天用 `urllib` 发 POST，响应结构不变。解析层复用，**换的是传输层**。」

### 周航（DevOps）

> 「CI 流水线禁止注入生产 Key。`NEXUS_LLM_MOCK=1` 必须能让 `pytest tests/day12/` 在无外网环境通过。`.env` 不进 Git，`.env.example` 只留占位符。」

## 任务分配

| ID | 任务 | 负责人 | 优先级 |
|----|------|--------|--------|
| ZL-NA-REQ-012 | 实现 `llm/client.py` | 全员 | P0 |
| ZL-NA-051 | `load_llm_env` 与 `.env` 解析 | 林晓 | P0 |
| ZL-NA-052 | `parse_chat_completion` 生产化 | 林晓 | P0 |
| ZL-NA-053 | Mock transport + CI 用例 | 周航 | P0 |
| ZL-NA-054 | `http_demos` / `api_demos` 演示 | 助教 | P1 |

## Definition of Done

- [ ] `NEXUS_LLM_MOCK=1 python3 src/day12/llm_client_demo.py` 退出码 0
- [ ] `pytest tests/day12/ -v` 全绿（Mock 模式）
- [ ] 能口述 `build_request_body` 输出结构与 OpenAI 文档一致
- [ ] 能解释 Mock 与 Live 模式下 `_headers()` 的差异
- [ ] 非 Mock 且无 Key 时抛出 `ConfigError`（非裸 `KeyError`）

## 今日节奏

| 时段 | 内容 |
|------|------|
| 09:00-10:30 | HTTP 基础、`urllib` GET/POST |
| 10:45-12:00 | OpenAI 兼容 API、`ChatMessage.to_api_message()` |
| 14:00-15:30 | `LLMClient` 实现与 Mock |
| 15:45-17:00 | 测试、排错、可选 Live 调用 |

## 环境准备

```bash
cd nexus-agent-platform
cp .env.example .env
# 编辑 .env，填入 DEEPSEEK_API_KEY（可选，Mock 可跳过）
# NEXUS_LLM_MOCK=1
```

---

*下一篇：[02_需求文档.md](02_需求文档.md)*
