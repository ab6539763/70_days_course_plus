# Day 12 课堂练习册

**说明**：课堂上随堂完成，约 45 分钟

---

## 练习 1：urllib GET（10 分钟）

运行 `http_demos.py`，填空：

1. 演示请求的 host 是 __________  
2. 查询参数 `demo` 的值是 __________  
3. 若离线跳过网络，哪一段仍会执行？__________

---

## 练习 2：请求头（5 分钟）

`http_demos.py` 的 `demo_request_headers` 打印了哪些头？Live 模式下 LLM 还需要哪个头？

<details><summary>答案</summary>Content-Type, Authorization (Bearer)</details>

---

## 练习 3：build_request_body（10 分钟）

在 REPL 中：

```python
from llm.client import build_request_body
from models import ChatMessage, ModelConfig
import json

body = build_request_body(
    [ChatMessage("user", "测试")],
    ModelConfig(max_tokens=100),
)
print(json.dumps(body, ensure_ascii=False, indent=2))
```

问题：

1. `body` 有几个顶层 key？  
2. `messages[0]` 有几个字段？  

---

## 练习 4：Mock 端到端（10 分钟）

```bash
NEXUS_LLM_MOCK=1 python3 src/day12/llm_client_demo.py
```

记录：

| 字段 | 你的输出 |
|------|----------|
| 模式 | |
| finish_reason | |
| total_tokens | |

---

## 练习 5：故意触发 ConfigError（5 分钟）

```bash
unset NEXUS_LLM_MOCK DEEPSEEK_API_KEY
python3 -c "
from llm.env import load_llm_env
load_llm_env()
"
```

预期异常类型：__________

---

## 练习 6：对照 Day 5（5 分钟）

打开 `day05/sample_data/chat_completion.json`，找到 `choices[0].message.content`，与 Mock 演示输出是否一致？

---

## 练习 7：预习思考（课堂收尾）

装饰器 `@retry` 加在 `complete` 上时，列举两个会 benefit、一个不应 retry 的异常类型。

| 应重试 | 不应重试 |
|--------|----------|
| | |
| | |

---

*扩展：[13_深度扩展_urllib与requests.md](13_深度扩展_urllib与requests.md)*
