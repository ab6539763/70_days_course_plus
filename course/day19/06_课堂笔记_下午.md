# Day 19 课堂笔记（下午）

**讲师**：陈默  
**记录**：林晓  
**时段**：14:00–17:00

---

## 14:00 RAGContextService

### 工厂方法链

```python
RAGContextService.from_sample_docs()
# ≡ from_directory(get_path("sample_docs"))
# ≡ from_documents(read_documents(...))
```

周航强调：`get_path("sample_docs")` 保证 CI 与本地路径一致。

### retrieve_context 三步

1. `index.search(query, top_k)`  
2. 遍历结果，加 header 格式化  
3. 累计字符，超 `max_chars` 截断  

林晓运行 `rag_context_demo.py`，context 长度 487 字符，含两段 `---` 分隔。

---

## 14:45 query_context_provider

### intent.py 变更

```python
def _resolve_rag_context(self, query: str) -> str:
    if self.query_context_provider:
        return self.query_context_provider(query)
    if self.context_provider:
        return self.context_provider()
    return "（暂无检索上下文）"
```

**优先级**：查询感知 > 静态 > 占位。

### 与 build_variables 衔接

`rag_qa` 分支传入 `match.user_text` 作为 query —— 用户整句「请根据文档说明收益率」都会参与检索。

---

## 15:30 ChatAssistant 扩展

### /retrieve 命令

```python
if cmd == "/retrieve":
    if not self.rag_service:
        return True, "RAG 检索未启用...", False
    query = args.strip() or "（未提供查询词）"
    return True, self.rag_service.retrieve_summary(query), False
```

林晓在 REPL 试验：

```
>>> /retrieve 客服电话
检索「客服电话」共 3 条：
  1. score=50% source=raw_notice.txt 命中=客服, 电话
```

### rag_service 与 intent_router 分工

| 组件 | 作用 |
|------|------|
| intent_router + query_context_provider | 自动对话时注入 context |
| rag_service | `/retrieve` 人工调试 |

---

## 16:15 routed_rag_demo 联调

```bash
NEXUS_LLM_MOCK=1 python3 src/day19/routed_rag_demo.py
```

脚本顺序：

1. `/retrieve 年化收益率` — 只看检索  
2. `根据文档，年化收益率是多少？` — auto_route + RAG + LLM  
3. `/route 请总结产品要点` — 意图切到 doc_summary  
4. `/exit`  

林晓发现：第二条回复含 `[路由: rag_qa]`，且 mock LLM 返回「好的」。

---

## 16:45 测试讲评

`pytest tests/day19/test_rag.py -v` — 12 passed。

陈默讲 `test_retrieve_context_respects_max_chars`：

> 「`max_chars=120` 时允许略超（header 计算），断言 `<= 130` 是工程容差。」

---

## 今日收获（林晓）

1. RAG 最小闭环 = 分块 + 检索 + 拼接  
2. `query_context_provider` 是 Day 18→19 的关键接缝  
3. `/retrieve` 是运维好友，上线前必查  

## Day 20 预习

陈默板书：**Embedding**、**余弦相似度**、**同义词召回**。
