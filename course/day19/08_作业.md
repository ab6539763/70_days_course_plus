# Day 19 作业

**截止**：次日上午课前  
**提交**：仓库 `homework/day19/` 或学习平台指定路径

---

## 作业 A：分块参数实验（必做，⭐ 基础，25 分）

### 要求

在 `homework/day19/chunk_lab.py` 中：

1. 读取 `sample_docs/raw_notice.txt`（`read_document` + `clean=True`）  
2. 对 `(chunk_size, overlap)` 三组参数调用 `chunk_text` 并打印块数  
   - (120, 20)、(200, 40)、(100, 30)  
3. 打印每组第一块的 `chunk_id` 与前 50 字符  

### 验收

```bash
export PYTHONPATH=src
python3 homework/day19/chunk_lab.py
```

---

## 作业 B：自定义检索查询（必做，⭐⭐ 进阶，25 分）

### 要求

编写 `homework/day19/my_retrieve.py`：

- 使用 `RAGContextService.from_sample_docs()`  
- 对 5 条自写查询调用 `retrieve_summary`  
- 至少 1 条应零命中（如「区块链挖矿教程」）  
- 至少 1 条应命中「风险」相关片段  

提交 `my_retrieve_output.txt`（运行重定向）。

---

## 作业 C：query_context_provider 对比（必做，⭐⭐ 进阶，20 分）

### 要求

提交 `homework/day19/provider_compare.md`：

1. 分别创建  
   - `IntentRouter(context_provider=lambda: "静态A")`  
   - `IntentRouter(query_context_provider=service.retrieve_context)`  
2. 对同一 query「根据文档查收益率」比较 `build_variables()["context"]` 前 100 字  
3. 说明生产环境应选哪种及理由  

---

## 作业 D：对照与反思（必做，⭐ 基础，20 分）

提交 `homework/day19/day18_vs_day19.md`：

| 对比项 | Day 18 context_provider | Day 19 query_context_provider |
|--------|-------------------------|------------------------------|
| 函数签名 | | |
| 是否感知 query | | |
| 典型数据源 | | |
| 调试命令 | | |

须补充至少 2 条个人踩坑（可参考课件 10、18）。

---

## 作业 E：max_chars 边界实验（选做，⭐⭐⭐ 挑战，+10 分）

编写 `homework/day19/max_chars_lab.py`：

- `retrieve_context("收益率 风险 客服", top_k=5, max_chars=50/120/500)`  
- 打印每次长度与是否含 `...`  
- 解释与 `test_retrieve_context_respects_max_chars` 的关系  

---

## 难度说明

| 等级 | 作业 | 说明 |
|------|------|------|
| ⭐ 基础 | A、D | 参数与文档，1–2 小时 |
| ⭐⭐ 进阶 | B、C | 需读源码与测试 |
| ⭐⭐⭐ 挑战 | E | 边界与测试对照 |

---

## 评分标准

| 等级 | 标准 |
|------|------|
| 优秀 | A+B+C+D 全过，对比分析有业务依据 |
| 良好 | 必做全过，E 未完成 |
| 及格 | 有一项未用项目 API |
| 不及格 | 抄袭或无法运行 |

---

## 学术诚信

允许讨论设计思路，须独立编写代码与文档。必须 `from rag import ...` 使用项目模块。
