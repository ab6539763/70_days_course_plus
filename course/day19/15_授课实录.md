# Day 19 授课实录（节选）

**日期**：2026-07-24  
**讲师**：陈默  
**记录**：助教  
**说明**：口语实录整理，供复习语气与重点

---

## 开场（09:05）

陈默：「昨天我们解决了选模板，今天解决 rag_qa 模板里 context 从哪来。一句话：**让检索听 query 的话。**」

赵岩投屏内测截图，用户问收益率，助手扯到客服流程。林晓小声：「因为 context 里根本没有收益率那段。」

陈默：「对。所以 Day 18 的 `context_provider` 是过渡，今天正式上 `query_context_provider`。」

---

## chunker 讲解（09:40）

陈默在白板画长条，切三刀，第二刀与第一刀重叠一笔：

「overlap 不是浪费存储，是买保险。金融文本里数字和免责经常挨在一起，切断就检索不到了。」

学员问：`chunk_size` 用字符不用 token？  
陈默：「Sprint 3 先用字符，简单可测。Token 计数 Day 15 学过，后面可换 token 窗口。」

---

## 检索打分（10:20）

陈默打开 `retriever.py`：

「这个 score 别当成概率。它是命中比例，方便人看。67% 就是查询词里大约三分之二在块里出现了。」

周航插话：「CI 不连网，所以咱们不能今天就用 Embedding。关键词是刻意选择。」

---

## 下午集成（14:10）

陈默 live coding（已提前准备 `rag_context_demo.py`）：

```python
context = service.retrieve_context(QUERY, top_k=2, max_chars=500)
```

「看这段输出，`[片段1·raw_notice.txt·67%]` 是给运营看的。模型也能读，但主要为了你们验收。」

林晓问：`user_text` 整句检索会不会噪音多？  
陈默：「会。比如『请帮我根据文档』也会进 query。Day 22 可以做 query 改写；今天先朴素实现。」

---

## /retrieve 演示（15:00）

陈默在 REPL：

```
>>> /retrieve 客服电话
```

「运维不爱看代码，爱看这个。上线前抽 20 个高频问题跑一遍 `/retrieve`。」

---

## 联调与收尾（16:50）

`routed_rag_demo.py` 跑完，陈默总结三板斧：

1. `/retrieve` — 材料找对没有  
2. `/route` — 意图对不对  
3. `chat_turn` — 端到端  

「明天 Embedding。今天先把管子焊死，别漏。」

林晓笔记边角画了个小泵，旁注「Day 20」。

---

## 学员问答摘录

**Q**：能否检索 PDF？  
**A**：`doc_reader` 本期 txt；PDF 是 Sprint 4 文档解析专题。

**Q**：`top_k=3` 谁定的？  
**A**：产品默认，作业可调，观察 context 长度与质量权衡。

**Q**：测试为什么 12 条？  
**A**：覆盖分块、检索、集成、命令各一层，和 Day 17、18 对齐。

---

## 讲师课后备注

- 晚自习强调 `query_context_provider` 与 `rag_service` 双配置  
- 明日晨会 10 分钟复习 `_tokenize` bigram  
- 提醒林晓组预习 Embedding 余弦公式  
