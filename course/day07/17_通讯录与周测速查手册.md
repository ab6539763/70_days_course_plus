# Day 7 通讯录与周测速查手册

## 一、运行命令

```bash
cd nexus-agent-platform
python3 src/day07/week1_quiz.py
python3 src/day07/contacts_manager.py
python3 -m pytest tests/day07/ tests/test_utils.py -v
```

## 二、数据文件

| 文件 | 用途 |
|------|------|
| `src/day07/data/contacts.json` | 运行时数据 |
| `src/day07/sample_data/contacts_seed.json` | 导入练习种子 |

## 三、菜单对照

| 键 | 功能 |
|----|------|
| 1 | 添加 |
| 2 | 查看全部 |
| 3 | 姓名搜索 |
| 4 | 分组筛选 |
| 5 | 编辑 |
| 6 | 删除 |
| 7 | 分组统计 |
| 8 | 保存 |
| 0 | 退出并保存 |

## 四、联系人 JSON 字段

| 字段 | 必填 | 校验 |
|------|------|------|
| name | 是 | 非空 |
| phone | 是 | 11 位 1 开头 |
| email | 是 | 含 @ |
| group | 否 | 默认未分组 |

## 五、utils 新增 API

```python
validate_phone("13800138000")  # -> None
format_contact_line(contact)   # -> 格式化行
```

## 六、service 返回值约定

```python
contact, err = add_contact(store, ...)
if err:
    print(err)
```

```python
err = update_contact(contact, name="新名")
```

## 七、周测及格线

60 分 / 100，10 题 × 10 分。

## 八、Sprint 1 交付清单

- [x] text_cleaner (D2)
- [x] platform_cli (D3)
- [x] todo_manager v1-v3 (D4-6)
- [x] api_response_parser (D5)
- [x] utils (D6)
- [x] contacts_manager (D7)

## 九、常见正则

| 用途 | 模式 |
|------|------|
| 手机 | `^1\d{10}$` |
| 邮箱 | `^[^@\s]+@[^@\s]+\.[^@\s]+$` |

## 十、Day 8 预习

阅读 `class` 语法：`__init__`, `self`, 实例方法。

---

*Sprint 1 完结，进入面向对象阶段。*
