# Day 7 课后作业

## 题目一：周测巩固（15 分）

将 `week1_quiz.py` 得分提升至 **80 分**以上，截图提交。错题抄写解析到笔记本。

---

## 题目二：通讯录手工测试（20 分）

按下列用例操作 `contacts_manager.py`，记录输入输出：

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 添加姓名空 | 提示姓名不能为空 |
| 2 | 手机 `12345` | 格式错误 |
| 3 | 正确添加一条 | 显示新 id |
| 4 | 搜索姓名关键词 | 至少 1 条 |
| 5 | 退出再进入 | 数据仍在 |

---

## 题目三：扩展 validate_phone（20 分）

支持可选的 `-` 分隔符输入，如 `138-0013-8000`，校验前自动去除 `-` 再匹配 `^1\d{10}$`。

提示：在 `validate_phone` 开头 `phone = phone.replace("-", "")`。

---

## 题目四：新增「按邮箱域名筛选」（25 分）

在 `contact_service.py` 添加：

```python
def search_by_email_domain(contacts: list, domain: str) -> list:
    """筛选邮箱以 @domain 结尾的联系人，如 domain='zhilian-tech.com'"""
```

在 `contacts_manager.py` 菜单增加选项 9（或并入搜索子菜单）。

---

## 题目五：Sprint 1 复盘短文（20 分）

300 字以上，回答：

1. 待办 v1→v2→v3 各解决了什么问题？
2. utils 带来了什么好处？举一个具体函数。
3. 通讯录与待办在工程模式上有哪些相同与不同？

---

## 加分题（+10 分）

从 `sample_data/contacts_seed.json` 编写脚本 `import_seed.py`，将种子数据合并到 `data/contacts.json`（跳过重复手机号）。

---

*答案：[09_作业答案.md](09_作业答案.md)*
