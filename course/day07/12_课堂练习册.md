# Day 7 课堂练习册

## 练习 1：dict 创建联系人

手写 dict 表示：id=5, 姓名「测试」, 手机 13900001111, 邮箱 t@e.com, 分组「QA」。

**答案**：

```python
{"id": 5, "name": "测试", "phone": "13900001111", "email": "t@e.com", "group": "QA"}
```

---

## 练习 2：列表推导筛选

从 `contacts` 中筛出 `group == "研发部"` 的列表，一行代码。

```python
[r for r in contacts if r.get("group") == "研发部"]
```

---

## 练习 3：校验顺序

`validate_contact_fields` 先校验姓名还是手机？为什么？  
**答**：姓名（require_non_empty）→ 手机 → 邮箱；尽早失败，减少无效输入处理。

---

## 练习 4：next_id

store 有 3 条联系人，最大 id=3，next_id 应为？**4**

---

## 练习 5：JSON 路径

默认数据文件相对 `src/` 的路径？**day07/data/contacts.json**

---

## 练习 6：函数分层

下列放哪一层？

| 代码 | 层 |
|------|-----|
| `input("姓名")` | contacts_manager |
| `add_contact(...)` | contact_service |
| `validate_phone` | utils/validators |
| `format_contact_line` | utils/formatters |

---

## 练习 7：周测第 4 题

默认参数用 `[]` 的后果？**多次调用共享同一列表。**

---

## 练习 8：搜索

`search_by_name(contacts, "")` 返回？**全部联系人**（空关键词）。

---

## 练习 9：统计

3 人研发部、1 人产品部，`group_stats` 返回？`{"研发部": 3, "产品部": 1}`

---

## 练习 10：Sprint 1

待办管理器最后一个版本号？**v3（Day 6 utils 版）**
