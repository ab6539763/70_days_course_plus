# Day 22 课堂笔记（上午）

**日期**：2026-07-27  
**讲师**：陈默  
**记录**：培训助教

---

## 09:00 HTML 速成

- 文档类型 `<!DOCTYPE html>`，语言 `lang="zh-CN"`
- 语义标签：`header` / `main` / `footer` 优于全 `div`
- 表单：`form#chat-form` + `input#user-input` + `button#send-btn`
- **林晓问**：为何 `mock.js` 在 `app.js` 之前？**答**：`app.js` 依赖 `window.NexusMock`

## 09:30 必选元素 id

陈默投屏 `constants.py`：

```python
REQUIRED_ELEMENT_IDS = (
    "chat-container", "messages", "user-input",
    "send-btn", "chat-form", "loading",
)
```

缺任一 id → `frontend_audit.py` 报错 → CI 失败。

## 10:00 CSS 布局要点

- `:root` CSS 变量统一品牌色  
- Flex 纵向 `.app`；消息区 `overflow-y: auto`  
- BEM：`msg`、`msg--user`、`msg__bubble`、`msg__tag--faq`  
- `fadeIn` 动画提升体验，非 FR 硬性要求  

## 10:45 app.js 走读

1. `handleSubmit` 阻止默认表单跳转  
2. `appendMessage("user", text)` 立即反馈  
3. `await dispatchMessage` 异步  
4. `parseReply` → `appendMessage("bot", parsed, meta)`  

**赵岩**：FAQ 标签文案用「FAQ 直答」与后端日志一致。

## 11:30 mock.js 规则表

- `DELAY_MS = 350` — 模拟思考，避免「秒回」不真实  
- FAQ 优先于 ROUTE（与编排器一致：先 FAQ 再路由）  
- `sendMessageApi` 已写 `fetch('/api/chat')` — Day 23 启用  

## 11:50 浏览器演示

```bash
cd frontend && python3 -m http.server 8080
```

全班测试三条问句：
1. 投资有风险吗 → FAQ  
2. 总结要点 → doc_summary  
3. 你好 → Mock 默认  

---

## 上午小结

静态页三件套：**结构 id 稳定、样式区分角色、JS 解析契约**。下午 Lab 对照 `mock_bridge_demo.py`。

笔记完。
