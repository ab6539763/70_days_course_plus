# Day 22 课堂练习册

**课时**：90 分钟  
**形式**：随堂练 + 同伴互评

---

## 练习 1：HTML id 寻宝（10 分钟）

打开 `frontend/index.html`，填写：

| id | 所在标签 | 作用 |
|----|----------|------|
| messages | | |
| user-input | | |
| loading | | |

对照 `constants.REQUIRED_ELEMENT_IDS` 自检。

---

## 练习 2：CSS 变量实验（15 分钟）

1. 修改 `--brand` 为 `#dc2626`  
2. 刷新页面，观察用户气泡与按钮  
3. 恢复默认色  

**思考**：为何用变量而非硬编码 hex？

---

## 练习 3：Console 调用 Mock（15 分钟）

浏览器 Console 输入：

```javascript
NexusMock.sendMessage("投资有风险吗").then(console.log)
NexusMock.sendMessage("总结要点").then(console.log)
```

记录返回的 `kind` 字段。

---

## 练习 4：parseReply 跟读（20 分钟）

抄写 `app.js` 中 `parseReply`，对以下字符串手写预期 kind：

1. `[FAQ 直答·80%] 测试`  
2. `[路由: rag_qa] 正文`  
3. `普通回复`  

---

## 练习 5：pytest 单测跟跑（15 分钟）

```bash
cd nexus-agent-platform
export PYTHONPATH=src
python3 -m pytest tests/day22/test_frontend.py -v -k "faq"
```

解释 `test_mock_faq_pattern` 断言什么。

---

## 练习 6：结对 Review（15 分钟）

交换座位，按 [10_前端验收清单.md](10_前端验收清单.md) 勾检同伴 `homework/day22` 草稿。

练习册完。
