# Day 22 课堂笔记（下午）

**日期**：2026-07-27  
**讲师**：陈默 + 周航  
**记录**：培训助教

---

## 14:00 mock_bridge_demo 对照

```bash
cd nexus-agent-platform
export PYTHONPATH=src NEXUS_LLM_MOCK=1
python3 src/day22/mock_bridge_demo.py
```

输出示例：
- Q: 投资有风险吗 → 后端(FAQ): `[FAQ 直答·…]`  
- Q: 根据资料，年化收益率是多少？ → 后端(路由)  
- Q: 帮我总结要点 → 后端(路由)  

林晓发现：浏览器 Mock 与后端措辞略有不同，但**前缀类型**一致。陈默：「Day 23 接真后端后以前端显示后端为准。」

## 14:45 frontend_audit

```bash
python3 src/day22/frontend_audit.py
```

检查项：五文件、六 id、脚本引入、mock FAQ 字符串、appendMessage、CSS 气泡。

## 15:15 pytest 十二项

```bash
python3 -m pytest tests/day22/test_frontend.py -v
```

周航讲解参数化测试 `@pytest.mark.parametrize("filename", REQUIRED_FILES)` — 与 `constants.py` 单源真相。

## 15:45 Lab 自由练习

任务：
1. 修改 `--brand` 颜色观察全局变化  
2. 在 FAQ_RULES 增加「赎回」规则  
3. DevTools 查看 `msg--user`  computed style  

## 16:30 无障碍快讲

- `aria-live="polite"` 于 `#messages`  
- `visually-hidden` 的 label 关联 input  
- `aria-hidden` 于装饰性 avatar  

## 17:00 答疑精选

| 问题 | 答案 |
|------|------|
| 能否用 Vue？ | Day 22 范围外，作业不加分 |
| CORS 报错？ | Day 22 纯 Mock 无跨域；Day 23 同域部署 |
| 消息能存 localStorage 吗？ | 选做作业 E |

下午完。晚自习见 [07_晚自习.md](07_晚自习.md)。
