# Day 3 课后作业

**截止时间**：Day 4 上午 9:00  
**总分**：100 分

---

## 题目一：成绩等级判断（15 分）

`exercises/exercise_01_grade.py`：输入成绩（0-100），用 if-elif-else 输出等级 A/B/C/D/F（90/80/70/60 分界）。非法输入提示重试（while 循环）。

---

## 题目二：打印三角形（20 分）

`exercises/exercise_02_triangle.py`：用嵌套 for 循环打印：

```
*
**
***
****
*****
```

---

## 题目三：扩展 platform_cli（30 分）

在 `platform_cli.py` 增加菜单项 **7. 统计 output 目录**：

- 用 `for` 遍历 `output/` 下所有文件
- 统计文件数、总字符数
- f-string 输出报告

---

## 题目四：猜数字增强（20 分）

`exercises/exercise_04_guess_hard.py`：

- 增加难度选择：简单(1-50)、困难(1-200)
- 用 if-elif 处理难度
- 记录并显示历史猜测列表（list 预告，可用字符串拼接暂代）

---

## 题目五：思考题（15 分）

`exercises/exercise_05_thinking.md`：

1. `while True` 会不会死循环？如何避免？
2. 批量清洗为什么用 `for` 而不是 `while`？
3. `platform_cli` 和 Day 24 的 Web API 有什么相似性？
4. 猜数字里 `continue` 和 `attempts += 1` 的顺序为什么重要？

---

## 加分题（+10 分）

`exercises/exercise_bonus_fizzbuzz.py`：打印 1-100，能被 3 整除输出 Fizz，被 5 整除 Buzz，两者整除 FizzBuzz。

---

## 评分标准

| 等级 | 分数 | 标准 |
|------|------|------|
| A | 90-100 | 全部完成 + 加分题 |
| B | 75-89 | 完成 1-4 题，platform_cli 菜单 7 可用 |
| C | 60-74 | 完成 1-3 题 |
| D | < 60 | 未完成 platform_cli |

## 提交说明

```bash
git checkout -b feature/day03-platform-cli
git add nexus-agent-platform/src/day03/
git commit -m "feat(day03): 实现平台 CLI 菜单与流程控制练习"
git push -u origin feature/day03-platform-cli
```

PR 标题：`[Day03] 平台 CLI 骨架 platform_cli`

---

*答案：[09_作业答案.md](09_作业答案.md)*
