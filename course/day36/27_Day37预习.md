# Day 37 预习：Self-RAG 与答案校验

**预告**：路由让管线更省，但 LLM 仍可能「答非所问」。Day 37 将引入 **Self-RAG / 答案校验** — 生成后自检引用是否支撑 reply。

陈默：「有 citations 不够，还要判断 citations 是否真的回答了问题。」

## 预习问

1. route 与 Self-RAG 校验分别在管线哪一段？  
2. 校验失败时应重检索还是拒答？  

## Day37 路线图（预期）

| 模块 | 说明 |
|------|------|
| answer_validator.py | 引用-回复一致性打分 |
| pipeline | route → … → LLM → validate → retry? |
| 评估 | 幻觉率 vs 延迟 |

## 与 Day36 关系

```
route → expand? → … → citations → LLM → [validate] → 用户
```

## 预习阅读

浏览 `rag/citation_builder.py` 的 `CitationBundle`，思考如何用 citations 校验 reply。

## 一句话

Day36 让管线**更省**；Day37 让回答**更准**。

---

## 预习作业

1. 列出 2 条「有引用但答非所问」的反例。  
2. 说明校验应放在 chat API 还是 orchestrator。  
3. 阅读 Self-RAG 论文摘要（可选）。
