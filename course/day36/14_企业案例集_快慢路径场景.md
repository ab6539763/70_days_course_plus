# 企业案例集：自适应路由（Query Router）

## 案例 1：FAQ 快路径

**场景**：客服电话多少
**处理**：路由到 faq_fast，跳过 expand + rewrite，直接检索

## 案例 2：常规检索

**场景**：年化收益怎么样
**处理**：路由到 rag_standard，走默认管线

## 案例 3：宽召回慢路径

**场景**：理财安全吗
**处理**：路由到 rag_wide，开启 expand + rewrite 全增强

## 案例复盘模板

| 日期 | 变更 | 影响 | 备注 |
|------|------|------|------|
| — | 上线本日新增能力 | — | 配合灰度开关 |

## Incident 降级预案

怀疑新逻辑引入回归时，`PUT /api/knowledge/route-config` 将 `enabled` 置为 `false`，立即回退到 Day 35 行为。
