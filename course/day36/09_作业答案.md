# Day 36 作业答案

## A 参考答案要点

| query | 预期处理结果 |
|-------|-------------|
| 客服电话多少 | 路由到 faq_fast，跳过 expand + rewrite，直接检索 |
| 年化收益怎么样 | 路由到 rag_standard，走默认管线 |
| 理财安全吗 | 路由到 rag_wide，开启 expand + rewrite 全增强 |

## B 答案

1. 见 `rag/query_router.py` 的核心方法实现。
2. `/api/knowledge/route-config` 读写持久化配置；`/api/knowledge/route-preview` 是无状态一次性预览，不落盘。
3. 行为回退到 Day 35 的既有逻辑——新增能力默认不介入。

## C Lab 要点

必须体现一次真实 curl/TestClient 调用与其响应截图。

## D 示例输出

```
route_config: enabled=true
```

## E bonus

```python
def test_empty_query_returns_empty():
    result = core.process("")
    assert not result
```

---

## F 参考答案

关闭本日新增能力的场景：新逻辑出现异常、需要临时回退、A/B 测试对照组。

---

## 讲评要点（讲师用）

作业 A 最常见错误：混淆本日新增字段与 Day 35 遗留字段。
作业 B 第 2 问：容易把 preview 和 config 接口混淆，需要强调「预览不落盘」。
