# Day 36 作业

## A（35 分）：RoutingRetriever 案例扩展脚本

编写脚本，对 `ROUTE_QUERIES` 中每条 case 打印处理结果摘要：

- 客服电话多少
- 年化收益怎么样
- 理财安全吗

**评分**：可运行 20 分；输出含每条结果 10 分；结论 5 分。

## B（25 分）：问答

1. `RoutingRetriever` 的核心处理逻辑是什么？
2. `/api/knowledge/route-config` 与 `/api/knowledge/route-preview` 的区别？
3. `RouteConfig.enabled=false` 时行为与之前有何不同？

## C（25 分）：Lab 报告

完成 `26_实操Lab手册.md` 全部 Lab，含至少一次真实 API 调用截图。

## D（15 分）：配置审计

读取 `store.json` 的 `route_config`，输出人类可读摘要。

## E（bonus 10 分）：单元测试

为 `RoutingRetriever` 补一条边界测试（例如空 query 或配置关闭时的分支）。

## F（课堂参与 10 分）

知识竞赛或 demo 现场 1 分钟：解释「什么时候该关闭本日新增能力」。

---

## 评分 Rubric 汇总

| 题 | 满分 | 及格线 |
|----|------|--------|
| A | 35 | 25 |
| B | 25 | 15 |
| C | 25 | 18 |
| D | 15 | 10 |
| E bonus | 10 | — |
| F | 10 | 6 |

---

## 学术诚信

允许讨论思路，禁止抄袭 Lab 报告。相似度 >80% 扣该题满分。
