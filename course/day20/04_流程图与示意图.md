# Day 20 流程图与示意图

**主题**：Embedding 向量检索、FAQ 匹配与 RAG 全链路

---

## 1. 主流程（auto_route + Embedding RAG + FAQ）

```mermaid
flowchart LR
    A[用户输入] --> B{"斜杠命令?"}
    B -->|/similar| C[faq_matcher.match]
    B -->|/retrieve| D[rag_service.retrieve_summary]
    B -->|/route| E[intent_router.classify]
    B -->|否| F{"auto_route?"}
    F -->|是| G[route_and_apply]
    G --> H[retrieve_context use_embedding]
    H --> I[apply_template rag_qa]
    I --> J[LLM complete]
    F -->|否| J
```

## 2. Embedding 索引构建（离线）

```mermaid
flowchart TD
    Dir[sample_docs] --> RD[read_documents]
    RD --> CD[chunk_documents]
    CD --> Chunks[TextChunk 列表]
    Chunks --> Fit[TfidfEmbeddingModel.fit]
    Fit --> Batch[embed_batch 预计算]
    Batch --> IDX[IndexedChunk 列表]
    IDX --> Ready[EmbeddingRetriever 就绪]
```

## 3. 余弦相似度计算

```mermaid
flowchart TD
    A[向量 a] --> Dot[dot_product a,b]
    B[向量 b] --> Dot
    A --> Na[vector_norm a]
    B --> Nb[vector_norm b]
    Dot --> Div["除以 na * nb"]
    Na --> Div
    Nb --> Div
    Div --> Sim[cosine_similarity]
    Z{"na=0 或 nb=0?"}
    Z -->|是| Zero[返回 0.0]
```

公式：

\[
\text{sim}(A, B) = \frac{A \cdot B}{\|A\| \|B\|}
\]

## 4. expand_tokens 同义词扩展

```
查询: "投资回报率是多少"

tokenize → [投资回报率, 是多少, 投资, 资回, 回报, 报率, ...]

TOKEN_SYNONYMS["回报率"] → 收益, 投资回报, 年化收益
PHRASE_GROUPS 命中 → 年化收益率, 投资回报率, 收益率 ...

expand 后词表更丰富 → TF-IDF 向量与「年化收益」文档更接近
```

## 5. 关键词 vs 向量对比示意

| 查询 | KeywordRetriever | EmbeddingRetriever |
|------|------------------|-------------------|
| 年化收益率是多少 | 命中（字面重叠） | 命中（高 sim） |
| 投资回报率是多少 | 常未命中 | 命中（同义词+向量） |
| 怎么打客服 | 可能命中 | 命中 |
| 量子纠缠原理 | 未命中 | 未命中 |

## 6. /similar 命令流程

```mermaid
sequenceDiagram
    participant User
    participant CLI as ChatAssistant
    participant FAQ as SimilarQuestionMatcher

    User->> "CLI: /similar 怎么联系客服"
    CLI->> "FAQ: match(query)"
    FAQ->>FAQ: embed + cosine vs FAQ 向量
    FAQ-->>CLI: FaqMatch
    CLI-->>User: summary + 标准答案
    Note over CLI: 不调用 LLM
```

## 7. retrieve_context 与 sim 标注

```mermaid
flowchart TD
    Q[query] --> S[EmbeddingRetriever.search]
    S --> Hit{"有结果?"}
    Hit -->|否| NF[未命中提示]
    Hit -->|是| Loop[遍历 results]
    Loop --> H["header: [片段N·source·sim=42%]"]
    H --> Body[正文 chunk.text]
    Body --> MC{"超过 max_chars?"}
    MC --> Join[separator 拼接]
    Join --> Out[context 字符串]
```

## 8. Day 19 → Day 20 切换点

```mermaid
flowchart LR
    subgraph Day19
        KR[KeywordRetriever]
    end
    subgraph Day20
        Flag["use_embedding=True"]
        ER[EmbeddingRetriever]
    end
    KR -->|默认| RCS[RAGContextService]
    Flag --> ER
    ER --> RCS
```

一行代码切换：`RAGContextService.from_sample_docs(use_embedding=True)`。
