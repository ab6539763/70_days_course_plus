# Day 20 作业

**截止**：次日上午课前（Day 21 周测前）  
**提交**：仓库 `homework/day20/` 或学习平台指定路径

---

## 作业 A：余弦相似度手算（必做，⭐ 基础，20 分）

### 要求

在 `homework/day20/cosine_lab.md` 中：

1. 手算向量 a=[3,4] 与 b=[4,3] 的余弦相似度（展示 dot、norm、除法步骤）  
2. 用 Python 验证：

```python
from rag import cosine_similarity
print(cosine_similarity([3.0, 4.0], [4.0, 3.0]))
```

3. 解释 `test_cosine_similarity_orthogonal` 为何期望 0.0  

---

## 作业 B：embedding_demos 扩展（必做，⭐ 基础，20 分）

### 要求

复制 `src/day20/embedding_demos.py` 为 `homework/day20/my_embedding_demos.py`，在 `PAIRS` 末尾新增 2 组：

- `("风险大不大", "投资有风险，入市需谨慎")` — 预期 sim 较高  
- `("今天天气", "理财产品说明书")` — 预期 sim 较低  

运行并提交 `my_embedding_output.txt`。

---

## 作业 C：关键词 vs 向量对比报告（必做，⭐⭐ 进阶，25 分）

### 要求

提交 `homework/day20/kw_vs_emb.md`：

1. 运行 `vector_search_demos.py`  
2. 自选 5 条查询，分别用  
   - `RAGContextService.from_sample_docs(use_embedding=False)`  
   - `RAGContextService.from_sample_docs(use_embedding=True)`  
   调用 `index.search(query, top_k=2)`  
3. 制表对比 top1 score/sim  
4. 至少 1 条查询体现「关键词未命中、向量命中」  

---

## 作业 D：FAQ 阈值实验（必做，⭐⭐ 进阶，20 分）

### 要求

编写 `homework/day20/faq_threshold.py`：

```python
from services import SimilarQuestionMatcher

for th in [0.2, 0.32, 0.5]:
    m = SimilarQuestionMatcher(threshold=th)
    r = m.match("投资回报率怎么算？")
    print(th, r.score if r else None)
```

说明 threshold 升高对误匹配与漏匹配的影响（200 字内）。

---

## 作业 E：use_embedding 集成反思（必做，⭐ 基础，15 分）

提交 `homework/day20/day19_vs_day20.md`：

| 对比项 | Day 19 KeywordRetriever | Day 20 EmbeddingRetriever |
|--------|-------------------------|---------------------------|
| 打分方式 | | |
| 同义词「投资回报率」 | | |
| context header | | |
| 新增 Chat 命令 | | |

须补充至少 2 条个人踩坑。

---

## 作业 F：同义词表扩展（选做，⭐⭐⭐ 挑战，+10 分）

在 `homework/day20/my_synonyms_patch.md` 中设计：

- 为 `TOKEN_SYNONYMS` 新增 3 个金融词条  
- 为 `PHRASE_GROUPS` 新增 1 组短语  
- 各附带 1 条预期提升的查询样例  

不要求改仓库源码，仅设计文档。

---

## 难度说明

| 等级 | 作业 | 说明 |
|------|------|------|
| ⭐ 基础 | A、B、E | 公式与演示，1–2 小时 |
| ⭐⭐ 进阶 | C、D | 需读源码与对比实验 |
| ⭐⭐⭐ 挑战 | F | 业务词汇设计 |

---

## 评分标准

| 等级 | 标准 |
|------|------|
| 优秀 | A–E 全过，对比分析有业务依据 |
| 良好 | 必做全过，F 未完成 |
| 及格 | 有一项未用项目 API |
| 不及格 | 抄袭或无法运行 |

---

## 学术诚信

允许讨论设计思路，须独立编写代码与文档。必须 `from rag import ...` / `from services import ...` 使用项目模块。

**提示**：Day 21 周测将覆盖今日作业 C、E 的知识点。
