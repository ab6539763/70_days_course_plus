# Day 20 授课实录（节选）

**日期**：2026-07-25  
**讲师**：陈默  
**记录**：助教

---

## 开场（09:05）

陈默：「昨天林晓跑 `retriever_demos.py`，『投资回报率』零命中。今天解决**语义鸿沟**。」

赵岩投屏用户反馈截图，强调这是 P0 体验问题。

---

## vector.py 讲解（09:40）

陈默板书余弦公式，现场问：「两个向量垂直，相似度多少？」

林晓：「零。」

陈默打开 `tests/day20/test_embedding.py` 的 `test_cosine_similarity_orthogonal`，现场跑绿。

> 「零向量也返回 0，别除零。这是工程细节。」

---

## TF-IDF 演示（10:20）

运行 `embedding_demos.py`：

```
Q: 投资回报率
D: 年化收益率可达 8%...
sim=0.312
```

陈默：「0.31 在 TF-IDF 里已经算高了。换 OpenAI embedding 可能 0.85，但接口一样。」

---

## 同义词表 walkthrough（11:00）

打开 `synonyms.py`，逐条解释 `PHRASE_GROUPS`：

> 「这是智链理财场景的**领域词典**。你们作业 F 可以提案扩展，但要合规审核。」

林晓问：「能否自动挖掘同义词？」

陈默：「可以，那是 Day 31 RAG 调优话题。今天先手工表 + 向量。」

---

## 下午联调（15:10）

全场运行 `routed_embedding_demo.py`。林晓机器输出：

```
>>> /similar 投资回报率怎么算
匹配 FAQ「年化收益率是多少？」| 相似度=38% | 分类=product
答案：请参考产品说明书，收益率以公告为准。
```

陈默：「注意 `/similar` 不调 LLM。产品以后可走**快捷通道**。」

---

## 测试讲评（16:30）

周航讲解 `test_keyword_vs_embedding_synonym`：

> 「允许关键词空，但向量必须有。这是今日核心回归。」

---

## 收尾预告 Day 21（16:55）

赵岩：「明天周测，不考偏题，考你们会不会整条链路跑通。」

陈默：「复习 `/route`、`/retrieve`、`/similar`，还有 Day 18 意图。」

---

## 学员提问精选

| 提问 | 讲师回答摘要 |
|------|--------------|
| sim 显示 42% 算高吗？ | TF-IDF 下 >30% 通常可信 |
| FAQ 和 RAG 冲突怎么办？ | 产品规则：高置信 FAQ 优先 |
| 能否同时开两种检索器？ | 可构造 Hybrid，今日未实现 |
