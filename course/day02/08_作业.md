# Day 2 课后作业

**截止时间**：Day 3 上午 9:00  
**总分**：100 分

---

## 题目一：运算符练习（15 分）

编写 `src/day02/exercises/exercise_01_operators.py`：

1. 定义 `a=17, b=5`，打印 `a+b`, `a-b`, `a*b`, `a/b`, `a//b`, `a%b`, `a**2`
2. 判断 `a` 是否为奇数（用 `%`）
3. 判断 `18 <= a <= 65`（用链式比较）
4. 用 f-string 一行输出：`17 除以 5 商 3 余 2`

---

## 题目二：字符串切片与方法（20 分）

编写 `src/day02/exercises/exercise_02_strings.py`：

给定 `s = "  NexusAgent-RAG-v0.1.0  "`，完成：

1. 去首尾空白
2. 转大写
3. 用 `replace` 把 `-` 换成 `_`
4. 用切片取出 `RAG`（不能用 replace）
5. 用 `split` 按 `_` 分割
6. f-string 输出：`原始长度 X，处理后长度 Y`

---

## 题目三：f-string 格式化报告（20 分）

编写 `src/day02/exercises/exercise_03_fstring.py`：

模拟清洗报告数据：

```python
source = "test.txt"
raw_len = 2500
clean_len = 2100
replace_count = 7
```

用 f-string 输出对齐的报告（压缩率百分比保留 1 位小数，数字千分位）。

---

## 题目四：扩展 text_cleaner（30 分）

在 `exercise_04_custom_cleaner.py` 中扩展清洗器：

1. 新增 `--mask-phone` 参数：将 11 位手机号（1 开头）替换为 `[手机号已脱敏]`
   - 提示：可用简单循环或 `re.sub(r'1\d{10}', '[手机号已脱敏]', text)`（若用正则需在注释说明）
2. 新增统计项：清洗前后**行数**变化
3. 对 `sample_docs/raw_policy.txt` 运行并截图/记录输出

---

## 题目五：思考题（15 分）

在 `exercise_05_thinking.md` 回答：

1. 字符串为什么设计成不可变？对清洗流水线有什么好处？
2. `split()` 和 `split("\n")` 处理 Windows 文本时有什么区别？
3. 为什么 RAG 入库前要做大小写统一？举一个在 NexusAgent 场景中的例子。
4. f-string 和 Prompt 模板的关系是什么？
5. 你今天写的清洗规则，哪一条对检索质量影响最大？为什么？

---

## 加分题（+10 分）

实现 `exercise_bonus_palindrome.py`：判断用户输入的字符串是否为回文（忽略大小写和空格）。

```
请输入：A man a plan a canal Panama
结果：是回文
```

提示：`s.lower().replace(" ", "")` 后比较 `s == s[::-1]`

---

*答案：[09_作业答案.md](09_作业答案.md)*
