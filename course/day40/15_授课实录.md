# Day 40 授课实录

**09:05** 讲师展示上一轮 Sprint 遗留问题：Day39 的 ReAct 循环里，每个工具的入参 schema 是手写字典，新增工具要改多处代码，容易漏字段。
**09:30** 白板画出核心方案：引入 LangChain 风格 @tool 装饰器 + StructuredTool：从函数签名自动推导 OpenAI function-calling schema，AgentExecutor.invoke 统一调度并记录 intermediate_steps。
**10:10** `AgentExecutor` live coding。
**10:55** 走读 `agent/structured_tool.py` 配置类。
**11:20** 第一次跑通 demo，全班确认 `executor_trace` 输出正常。

**14:05** demo 案例逐条讲解：运营要求新增「工单查询」工具。
**14:50** 讲 `executor_mode=false` 时的兼容路径。
**15:15** `PUT /api/agent/executor-config` 现场演示配置变更。
**15:58** pytest 第 17 个全绿。
**16:42** 预告 Day 41：StateGraph 状态图编排。

---

## 问答实录

**15:30 学员**：`executor_trace` 会不会让响应体过大？
**讲师**：默认返回，但可以通过配置关闭详细 trace，只保留最终 reply。

**16:05 学员**：关闭 `executor_mode` 最快的方式？
**讲师**：`PUT /api/agent/executor-config` 把 `enabled` 设为 `false`，无需改代码。

---

## 讲师自评

- AgentExecutor live coding 时间刚好
- Lab 环节学员普遍能独立完成
- Day 41 预告已铺垫

---

## 课后作业布置原话

「tests/day40/ 必须全绿再提交，作业 A 的案例脚本周日 23:59 前 push。」
