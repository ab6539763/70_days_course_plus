# ZL-NA-REQ-040 需求扩展 — 用户故事

## US-40-01 新增工具零改动

**场景**：运营要求新增「工单查询」工具

**期望**：写一个带类型标注的函数 + @tool 装饰器即可注册，无需改 AgentExecutor 代码

## US-40-02 schema 校验前置

**场景**：工具入参类型不对

**期望**：StructuredTool.to_openai_tool() 导出的 schema 可直接喂给真实模型做 function-calling 校验

## US-40-03 与 ReAct 对比

**场景**：同一问题分别跑 Day39/Day40

**期望**：intermediate_steps 与 agent_trace 结构一致，便于灰度切换

---

## 边界：空 query

`invoke("")` / `run("")` → 返回「请输入有效问题。」，不进入决策循环。

## 与上一日关系

Day39 手写 ReAct 验证思路；Day40 用 StructuredTool 把工具注册框架化。

## 风险登记

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| mock 决策命中率不足 | 中 | 演示效果打折 | 文档标注局限，预留真实模型接口 |
| 配置项过多导致运维困惑 | 低 | 排障变慢 | status 接口统一暴露当前配置 |

## 发布检查清单（扩展）

- [ ] `tests/day40/` 全绿
- [ ] `day40/executor_demo.py` / `day40/executor_api_demo.py` 输出正常
- [ ] PACKAGE_STRUCTURE.md 更新
- [ ] CI Day 40 job 添加
- [ ] 课件 regenerate ≥100k
- [ ] PR 关联 ZL-NA-REQ-040
