# Day 40 课堂笔记（上午）

**09:00–09:40** 第一节：Day39 的 ReAct 循环里，每个工具的入参 sche...
**09:40–10:30** 第二节：agent/structured_tool.py
**10:30–11:20** 第三节：AgentExecutor 核心循环
**11:20–12:00** 第四节：与 Day 39 对比走读

---

## 第一节：问题背景（40 min）

Day39 的 ReAct 循环里，每个工具的入参 schema 是手写字典，新增工具要改多处代码，容易漏字段。

引入 LangChain 风格 @tool 装饰器 + StructuredTool：从函数签名自动推导 OpenAI function-calling schema，AgentExecutor.invoke 统一调度并记录 intermediate_steps。

---

## 第二节：StructuredTool + @tool 装饰器 + OpenAI schema（50 min）

`agent/structured_tool.py` 是本日配置的单一真相源，字段随日递增但结构一致：`enabled`、若干阈值/开关、`validate()` 校验。

**10:15** 课堂练习：写出三组非法配置，预测 `validate()` 抛出的异常信息。

---

## 第三节：AgentExecutor（50 min）

`AgentExecutor` 是今天的核心调度类，负责把「输入 query → 决策 → 调用工具 → 汇总」串起来，并把每一步记录进 `executor_trace`，便于教学演示与审计。

**10:55** 板书：Day39 手写 ReAct 验证思路；Day40 用 StructuredTool 把工具注册框架化。

---

## 第四节：与 Day 39 对比（40 min）

agent/executor_config.py — ExecutorConfig — max_iterations

**11:40** 强调：Day 39（手写 ReAct Agent）的能力仍然保留，今天是在其基础上新增一层。
