# ZL-NA-REQ-044 产品需求文档（PRD）

**需求名称**：MCP 协议与工具生态
**优先级**：P0
**平台版本**：v0.44.0

---

## 1. 背景

SubAgent 的三个工具都是进程内直调，若要接入第三方团队独立维护的工具（甚至跨语言），Supervisor 现有委派方式无法复用；需要一套与厂商无关的协议边界。 自研一个符合 MCP（Model Context Protocol）语义的 Server：对外暴露 tools/list（发现工具 schema）与 tools/call（按名调用），McpRunner 按 discover→route→call→answer 四阶段驱动，未来把进程内 Server 换成真实外部 MCP 服务时业务代码无需改动。

## 2. 目标

- `agent/mcp_config.py` — McpConfig — server_name / mock_routing / max_tool_calls
- `agent/mcp_protocol.py` — JSON-RPC tools/list + tools/call 消息类型
- `agent/mcp_server.py` — NexusMcpServer — 暴露 ToolRegistry
- `agent/mcp_client.py` — McpClient — list/call 封装
- `agent/mcp_bridge.py` — MCP → StructuredTool 桥接
- `agent/mcp_runner.py` — discover → route → call → answer
- `GET/PUT /api/agent/mcp-config` 配置读写
- `POST /api/agent/mcp-preview` 无状态预览
- `/api/agent/mcp-list-tools` 额外接口
- `POST /api/chat` + `mcp_mode=true` → `mcp_trace`

## 3. 功能需求

### FR-001 McpConfig — server_name / mock_routing / max_tool_calls

- 实现位置：`agent/mcp_config.py`

### FR-002 JSON-RPC tools/list + tools/call 消息类型

- 实现位置：`agent/mcp_protocol.py`

### FR-003 NexusMcpServer — 暴露 ToolRegistry

- 实现位置：`agent/mcp_server.py`

### FR-004 McpClient — list/call 封装

- 实现位置：`agent/mcp_client.py`

### FR-005 MCP → StructuredTool 桥接

- 实现位置：`agent/mcp_bridge.py`

### FR-006 discover → route → call → answer

- 实现位置：`agent/mcp_runner.py`

### FR-007 持久化与 status

- `store.json` 新增对应 config 字段
- `GET /api/knowledge/status` 含该 config 字段
- `platform_version` 为 `v0.44.0`

### FR-008 Chat 集成

- `api/chat.py` 在 `mcp_mode=true` 且配置 `enabled` 时走本日管线
- `ChatResponse` 新增 `mcp_trace` 字段（及 mcp_tools, tools_used）

### FR-009 演示与测试

- `day44/mcp_demo.py` 打印决策/委派轨迹
- `day44/mcp_api_demo.py` 演示 REST API
- `tests/day44/` 20 项覆盖单元 + API + chat

## 4. 非功能需求

### NFR-001 可观测性

每次调用都返回结构化 `mcp_trace`，可用于审计与教学演示。

### NFR-002 兼容性

`mcp_mode=false`（默认）时行为与之前完全一致，不影响既有回归。

### NFR-003 可测试性

核心类可脱离 FastAPI 直接单测（构造 `ToolExecutor` 后 `invoke`/`run`）。

## 5. 非目标

- 接入真实大模型 function-calling（当前仍为规则/mock 决策）
- 跨 session 的长期记忆持久化

---

## 5.1 FR 追溯矩阵

| FR | 实现位置 | 测试 |
|----|----------|------|
| FR-001 | agent/mcp_config.py | tests/day44/ |
| FR-002 | agent/mcp_protocol.py | tests/day44/ |
| FR-003 | agent/mcp_server.py | tests/day44/ |
| FR-004 | agent/mcp_client.py | tests/day44/ |
| FR-005 | agent/mcp_bridge.py | tests/day44/ |
| FR-006 | agent/mcp_runner.py | tests/day44/ |

## 5.2 验收标准

| ID | 场景 | 预期 |
|----|------|------|
| AC-01 | 默认配置 GET /api/agent/mcp-config | enabled=true |
| AC-02 | chat mcp_mode=true | 响应含 `mcp_trace` |
| AC-03 | chat mcp_mode=false（默认） | 行为与之前一致 |
| AC-04 | tests/day44/ | 全绿 |

---

## 6. 详细验收步骤

```bash
PYTHONPATH=src NEXUS_LLM_MOCK=1 pytest tests/day44/ -v
PYTHONPATH=src python3 src/day44/mcp_demo.py
PYTHONPATH=src NEXUS_LLM_MOCK=1 python3 src/day44/mcp_api_demo.py
```

---

## 7. 风险登记

| 风险 | 缓解 |
|------|------|
| mock 决策与真实模型行为不一致 | 文档标注 mock_routing，预留切换真实模型的接口 |
| 新增字段影响既有前端解析 | `mcp_trace` 默认 null，向后兼容 |

---

## 8. 发布说明 v0.44.0

**新增**：agent/mcp_config.py、agent/mcp_protocol.py、agent/mcp_server.py、agent/mcp_client.py、agent/mcp_bridge.py、agent/mcp_runner.py
**变更**：`ChatResponse` 扩展 `mcp_trace` 字段
**注意**：mcp_mode 默认关闭，需显式开启才走新管线

---

## 9. 需求变更记录

| 版本 | 变更 |
|------|------|
| v0.44.0-draft | 仅核心类 |
| v0.44.0 | API + chat 集成 + 20 tests |

---

## 10. PRD 签字页

产品：________  研发：________  测试：________
