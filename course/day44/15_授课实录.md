# Day 44 授课实录

**09:05** 讲师展示上一轮 Sprint 遗留问题：SubAgent 的三个工具都是进程内直调，若要接入第三方团队独立维护的工具（甚至跨语言），Supervisor 现有委派方式无法复用；需要一套与厂商无关的协议边界。
**09:30** 白板画出核心方案：自研一个符合 MCP（Model Context Protocol）语义的 Server：对外暴露 tools/list（发现工具 schema）与 tools/call（按名调用），McpRunner 按 discover→route→call→answer 四阶段驱动，未来把进程内 Server 换成真实外部 MCP 服务时业务代码无需改动。
**10:10** `McpRunner` live coding。
**10:55** 走读 `agent/mcp_config.py` 配置类。
**11:20** 第一次跑通 demo，全班确认 `mcp_trace` 输出正常。

**14:05** demo 案例逐条讲解：「客服电话多少」。
**14:50** 讲 `mcp_mode=false` 时的兼容路径。
**15:15** `PUT /api/agent/mcp-config` 现场演示配置变更。
**15:58** pytest 第 20 个全绿。
**16:42** 预告 Day 45：Dify 工作流对接与周测。

---

## 问答实录

**15:30 学员**：`mcp_trace` 会不会让响应体过大？
**讲师**：默认返回，但可以通过配置关闭详细 trace，只保留最终 reply。

**16:05 学员**：关闭 `mcp_mode` 最快的方式？
**讲师**：`PUT /api/agent/mcp-config` 把 `enabled` 设为 `false`，无需改代码。

---

## 讲师自评

- McpRunner live coding 时间刚好
- Lab 环节学员普遍能独立完成
- Day 45 预告已铺垫

---

## 课后作业布置原话

「tests/day44/ 必须全绿再提交，作业 A 的案例脚本周日 23:59 前 push。」
