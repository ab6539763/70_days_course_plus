# Day 10 旁白解读：给平台立法，模块与异常

> **前置要求**：完成 Day 1-9

---

## 旁白

2026 年 7 月 15 日。Sprint 2 第三天，陈默没有讲新语法花活，而是打开仓库目录树：

```
src/
├── day01/ ... day10/   ← 教学实验（保留）
├── core/               ← 今日新建：宪法
├── models/             ← 昨日定型
├── services/           ← 今日新建：业务服务
├── llm/ chat/ tools/   ← 今日占位：未来领地
└── utils/
```

> 「Day 1-9 你们写了大量 `sys.path.insert` 和分散的异常处理。今天做**工程化整理**：异常进 `core.exceptions`，路径进 `core.paths`，消息历史进 `services`。**dayXX 文件夹不删**——那是教材；但新功能不准再往 day 里堆生产代码。」

赵岩：

> 「`ModelValidationError` 从 models 迁到 core，仍继承 ValueError，旧测试不用改。这就是**迁移要有兼容层**。」

---

## 今日结束后，你应该能够——

1. 理解 import / from / `__name__` / `__all__`
2. 使用 try/except 捕获分层异常
3. 说明 `core` / `models` / `services` / `dayXX` 职责
4. 运行 `structure_audit.py` 检查包结构
5. 使用 `get_path()` 获取数据文件路径

---

## 与 Day 11 的衔接

今日完成**包结构立法**。`tools/` 目录已占位；明日 Day 11 实现 `doc_reader.py` 批量读取企业文档，为 RAG 流水线做文件层准备。`llm/`、`chat/` 分别等待 Day 12、Day 14 填充。

---

*下一篇：[01_企业背景与今日任务.md](01_企业背景与今日任务.md)*
