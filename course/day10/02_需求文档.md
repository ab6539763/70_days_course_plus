# ZL-NA-REQ-010：包结构重组与异常体系

**版本**：v0.1.0  
**作者**：陈默 / 赵岩  
**状态**：已批准

## 1. 概述

建立 `core` 基础设施包、 `services` 服务层，预置 `llm`/`chat`/`tools` 骨架；统一异常与路径管理。

## 2. core 包

| 模块 | 职责 |
|------|------|
| exceptions.py | NexusError 层次 |
| paths.py | SRC_ROOT、PATHS、get_path |
| bootstrap.py | setup_python_path、ensure_importable |

## 3. 异常层次

```
NexusError
├── ConfigError
├── ModelError
│   └── ModelValidationError (兼 ValueError)
├── StorageError
│   └── JsonParseError
└── ImportPathError
```

## 4. services 迁移

`MessageHistory` → `services/message_history.py`  
`day08/message_history.py` 保留 re-export。

## 5. 骨架包

`llm/`、`chat/`、`tools/` 各含 `__init__.py`，Day 12/14/11 填充。

## 6. 文档

`nexus-agent-platform/PACKAGE_STRUCTURE.md`

## 7. 测试

`tests/day10/test_core.py` ≥ 10 用例；全量 pytest 回归。

---

*评审：陈默 ✅ 赵岩 ✅*
