# Day 10 课后作业

## 题目一：import 练习（15 分）

写脚本打印 `models.message` 模块的文件路径（提示：`import models.message as m; print(m.__file__)`）。

## 题目二：异常捕获（20 分）

写函数 `safe_ensure(model: BaseModel) -> str`，成功返回 `"OK"`，失败返回异常 message 字符串。

## 题目三：注册新路径（25 分）

在 `core/paths.PATHS` 添加 `"config": SRC_ROOT / "data" / "config.json"`，并写 pytest 验证 `get_path("config")`。

## 题目四：阅读 PACKAGE_STRUCTURE（20 分）

抄写依赖方向五条禁止规则，并各举一个违反例子。

## 题目五：迁移思考（20 分）

200 字：为何 MessageHistory 放 services 而非 models？

## 加分题（+10 分）

为 `structure_audit.py` 增加检查：每个生产包 `__init__.py` 定义了 `__all__`（llm/chat/tools 可为空列表）。

---

*答案：[09_作业答案.md](09_作业答案.md)*
