# Day 10 完整代码走查

## 一、core/exceptions.py

异常树见需求文档。重点：`NexusError.message` 与 `NexusError.code` 供日志使用。

## 二、core/paths.py

```python
PATHS = {
    "todos": SRC_ROOT / "day05" / "data" / "todos.json",
    "contacts": SRC_ROOT / "day07" / "data" / "contacts.json",
    "messages": SRC_ROOT / "day08" / "data" / "messages.json",
}
```

新增数据文件：在此注册 + 更新 PACKAGE_STRUCTURE.md。

## 三、core/bootstrap.py

- `setup_python_path()` — 脚本入口第一行调用
- `ensure_importable("core", "models")` — 自检脚本用

## 四、services/message_history.py

与 Day 8 差异：

| 方法 | Day 8 | Day 10 services |
|------|-------|-----------------|
| add | 返回 err\|None | 同（兼容） |
| load | 宽松 default | load 严格抛 JsonParseError |
| save | save_json | save + StorageError |

## 五、day08 兼容层

```python
from services.message_history import MessageHistory
```

旧 import 路径不变。

## 六、models/llm_base.py 变更

```python
from core.exceptions import ModelValidationError
```

类定义删除本地 ModelValidationError，改从 core 导入。

## 七、演示脚本

| 脚本 | 用途 |
|------|------|
| module_demos.py | import、__name__、bootstrap |
| exception_demos.py | 异常捕获层次 |
| structure_audit.py | 包结构 CI 门禁 |

## 八、阅读顺序

1. PACKAGE_STRUCTURE.md  
2. core/exceptions.py  
3. core/paths.py + bootstrap.py  
4. services/message_history.py  
5. day10 三个演示脚本  
6. tests/day10/test_core.py  

---

*约 1500 字，配合源码阅读。*
