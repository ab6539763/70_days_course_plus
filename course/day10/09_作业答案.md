# Day 10 作业答案

## 题目一

```python
import sys
from pathlib import Path
sys.path.insert(0, str(Path("src").resolve()))
import models.message as m
print(m.__file__)
```

## 题目二

```python
from models.llm_base import BaseModel

def safe_ensure(model: BaseModel) -> str:
    try:
        model.ensure_valid()
        return "OK"
    except Exception as e:
        return str(e)
```

## 题目三

在 paths.py 添加键值；测试：

```python
def test_config_path():
    p = get_path("config")
    assert p.name == "config.json"
```

## 题目四要点

- core 不 import dayXX  
- models 不 import services  
- utils 不 import dayXX  
- 示例：在 models/message.py 写 `from day08 import xxx` 即违反

## 题目五要点

MessageHistory 管理**集合**与 **IO**，不是单一领域实体；models 放 ChatMessage 单条消息；服务层编排多条消息与持久化。

---

*加分题：遍历 SRC_ROOT 下包目录检查 `__all__ in dir(module)`。*
