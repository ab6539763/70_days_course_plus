# Day 42 授课实录

**09:05** 讲师展示上一轮 Sprint 遗留问题：合规要求：涉及具体收益率数字的回答，必须有人工复核后才能发给客户，但当前 chat 接口是全自动直出。
**09:30** 白板画出核心方案：在 StateGraph 的 tool_runner 之后插入 human_approval 节点：命中 require_rag_approval 规则时中断执行并返回 checkpoint_id，人工调用 approval-resume 携带 approved/comment 后流程续跑。
**10:10** `ApprovalWorkflowGraph` live coding。
**10:55** 走读 `agent/approval_config.py` 配置类。
**11:20** 第一次跑通 demo，全班确认 `approval` 输出正常。

**14:05** demo 案例逐条讲解：用户问「年化收益率能到多少」。
**14:50** 讲 `approval_mode=false` 时的兼容路径。
**15:15** `PUT /api/agent/approval-config` 现场演示配置变更。
**15:58** pytest 第 16 个全绿。
**16:42** 预告 Day 43：Supervisor 多 Agent 委派。

---

## 问答实录

**15:30 学员**：`approval` 会不会让响应体过大？
**讲师**：默认返回，但可以通过配置关闭详细 trace，只保留最终 reply。

**16:05 学员**：关闭 `approval_mode` 最快的方式？
**讲师**：`PUT /api/agent/approval-config` 把 `enabled` 设为 `false`，无需改代码。

---

## 讲师自评

- ApprovalWorkflowGraph live coding 时间刚好
- Lab 环节学员普遍能独立完成
- Day 43 预告已铺垫

---

## 课后作业布置原话

「tests/day42/ 必须全绿再提交，作业 A 的案例脚本周日 23:59 前 push。」
