# Day 42 课堂笔记（上午）

**09:00–09:40** 第一节：合规要求：涉及具体收益率数字的回答，必须有人工复核后才能发给...
**09:40–10:30** 第二节：agent/approval_config.py
**10:30–11:20** 第三节：ApprovalWorkflowGraph 核心循环
**11:20–12:00** 第四节：与 Day 41 对比走读

---

## 第一节：问题背景（40 min）

合规要求：涉及具体收益率数字的回答，必须有人工复核后才能发给客户，但当前 chat 接口是全自动直出。

在 StateGraph 的 tool_runner 之后插入 human_approval 节点：命中 require_rag_approval 规则时中断执行并返回 checkpoint_id，人工调用 approval-resume 携带 approved/comment 后流程续跑。

---

## 第二节：ApprovalConfig — require_rag_approval / mock_auto_approve（50 min）

`agent/approval_config.py` 是本日配置的单一真相源，字段随日递增但结构一致：`enabled`、若干阈值/开关、`validate()` 校验。

**10:15** 课堂练习：写出三组非法配置，预测 `validate()` 抛出的异常信息。

---

## 第三节：ApprovalWorkflowGraph（50 min）

`ApprovalWorkflowGraph` 是今天的核心调度类，负责把「输入 query → 决策 → 调用工具 → 汇总」串起来，并把每一步记录进 `approval`，便于教学演示与审计。

**10:55** 板书：Day41 让节点图可编排；Day42 在图里加入人工审批卡点，管住高风险 RAG 输出。

---

## 第四节：与 Day 41 对比（40 min）

agent/approval_workflow_graph.py — human_approval 节点 + resume

**11:40** 强调：Day 41（StateGraph 状态图编排）的能力仍然保留，今天是在其基础上新增一层。
