# ZL-NA-REQ-042 需求扩展 — 用户故事

## US-42-01 高风险问句拦截

**场景**：用户问「年化收益率能到多少」

**期望**：human_approval 中断，chat 响应 interrupted=true + checkpoint_id

## US-42-02 人工通过

**场景**：审核员调用 approval-resume approved=true

**期望**：流程从中断点续跑，返回最终 reply

## US-42-03 人工驳回

**场景**：审核员 approved=false + comment

**期望**：流程终止并返回拒答说明，不发原始 RAG 结果

---

## 边界：空 query

`invoke("")` / `run("")` → 返回「请输入有效问题。」，不进入决策循环。

## 与上一日关系

Day41 让节点图可编排；Day42 在图里加入人工审批卡点，管住高风险 RAG 输出。

## 风险登记

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| mock 决策命中率不足 | 中 | 演示效果打折 | 文档标注局限，预留真实模型接口 |
| 配置项过多导致运维困惑 | 低 | 排障变慢 | status 接口统一暴露当前配置 |

## 发布检查清单（扩展）

- [ ] `tests/day42/` 全绿
- [ ] `day42/approval_demo.py` / `day42/approval_api_demo.py` 输出正常
- [ ] PACKAGE_STRUCTURE.md 更新
- [ ] CI Day 42 job 添加
- [ ] 课件 regenerate ≥100k
- [ ] PR 关联 ZL-NA-REQ-042
