# Day 42 课堂笔记（下午）

**14:00–14:30** 第五节：day42/approval_demo.py 现场
**14:30–15:20** 第六节：ApprovalWorkflowGraph 决策逻辑源码
**15:20–16:00** 第七节：/api/agent/approval-config API
**16:00–16:45** 第八节：pytest + chat 回归

---

## 第五节：demo 现场（30 min）

```bash
PYTHONPATH=src python3 src/day42/approval_demo.py
```

记录每个案例的委派/决策路径是否符合预期。

---

## 第六节：核心决策逻辑（50 min）

课堂上逐行读 `ApprovalWorkflowGraph` 的主循环，重点讲：何时判定「已经拿到答案」、何时继续下一步、`approval` 每一步记录哪些字段。

---

## 第七节：API（40 min）

```bash
curl -s http://127.0.0.1:8000/api/agent/approval-config | jq .
curl -s -X PUT http://127.0.0.1:8000/api/agent/approval-config \
  -H 'Content-Type: application/json' -d '{"enabled": true}' | jq .
curl -s -X POST http://127.0.0.1:8000/api/agent/approval-preview \
  -H 'Content-Type: application/json' -d '{"query": "测试问题"}' | jq .
curl -s -X POST http://127.0.0.1:8000/api/agent/approval-resume
```

---

## 第八节：测试与 chat（45 min）

```bash
pytest tests/day42/ -v
```

**16:30** 里程碑：16 passed。

**16:40** 现场跑一次 `POST /api/chat` + `approval_mode: true`，对照响应里的 `approval`。
