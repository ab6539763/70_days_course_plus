# ZL-NA-REQ-042 产品需求文档（PRD）

**需求名称**：人工审批工作流
**优先级**：P0
**平台版本**：v0.42.0

---

## 1. 背景

合规要求：涉及具体收益率数字的回答，必须有人工复核后才能发给客户，但当前 chat 接口是全自动直出。 在 StateGraph 的 tool_runner 之后插入 human_approval 节点：命中 require_rag_approval 规则时中断执行并返回 checkpoint_id，人工调用 approval-resume 携带 approved/comment 后流程续跑。

## 2. 目标

- `agent/approval_config.py` — ApprovalConfig — require_rag_approval / mock_auto_approve
- `agent/approval_checkpoint.py` — 中断检查点存取
- `agent/approval_workflow_graph.py` — human_approval 节点 + resume
- `GET/PUT /api/agent/approval-config` 配置读写
- `POST /api/agent/approval-preview` 无状态预览
- `/api/agent/approval-resume` 额外接口
- `POST /api/chat` + `approval_mode=true` → `approval`

## 3. 功能需求

### FR-001 ApprovalConfig — require_rag_approval / mock_auto_approve

- 实现位置：`agent/approval_config.py`

### FR-002 中断检查点存取

- 实现位置：`agent/approval_checkpoint.py`

### FR-003 human_approval 节点 + resume

- 实现位置：`agent/approval_workflow_graph.py`

### FR-004 持久化与 status

- `store.json` 新增对应 config 字段
- `GET /api/knowledge/status` 含该 config 字段
- `platform_version` 为 `v0.42.0`

### FR-005 Chat 集成

- `api/chat.py` 在 `approval_mode=true` 且配置 `enabled` 时走本日管线
- `ChatResponse` 新增 `approval` 字段（及 graph_trace）

### FR-006 演示与测试

- `day42/approval_demo.py` 打印决策/委派轨迹
- `day42/approval_api_demo.py` 演示 REST API
- `tests/day42/` 16 项覆盖单元 + API + chat

## 4. 非功能需求

### NFR-001 可观测性

每次调用都返回结构化 `approval`，可用于审计与教学演示。

### NFR-002 兼容性

`approval_mode=false`（默认）时行为与之前完全一致，不影响既有回归。

### NFR-003 可测试性

核心类可脱离 FastAPI 直接单测（构造 `ToolExecutor` 后 `invoke`/`run`）。

## 5. 非目标

- 接入真实大模型 function-calling（当前仍为规则/mock 决策）
- 跨 session 的长期记忆持久化

---

## 5.1 FR 追溯矩阵

| FR | 实现位置 | 测试 |
|----|----------|------|
| FR-001 | agent/approval_config.py | tests/day42/ |
| FR-002 | agent/approval_checkpoint.py | tests/day42/ |
| FR-003 | agent/approval_workflow_graph.py | tests/day42/ |

## 5.2 验收标准

| ID | 场景 | 预期 |
|----|------|------|
| AC-01 | 默认配置 GET /api/agent/approval-config | enabled=true |
| AC-02 | chat approval_mode=true | 响应含 `approval` |
| AC-03 | chat approval_mode=false（默认） | 行为与之前一致 |
| AC-04 | tests/day42/ | 全绿 |

---

## 6. 详细验收步骤

```bash
PYTHONPATH=src NEXUS_LLM_MOCK=1 pytest tests/day42/ -v
PYTHONPATH=src python3 src/day42/approval_demo.py
PYTHONPATH=src NEXUS_LLM_MOCK=1 python3 src/day42/approval_api_demo.py
```

---

## 7. 风险登记

| 风险 | 缓解 |
|------|------|
| mock 决策与真实模型行为不一致 | 文档标注 mock_routing，预留切换真实模型的接口 |
| 新增字段影响既有前端解析 | `approval` 默认 null，向后兼容 |

---

## 8. 发布说明 v0.42.0

**新增**：agent/approval_config.py、agent/approval_checkpoint.py、agent/approval_workflow_graph.py
**变更**：`ChatResponse` 扩展 `approval` 字段
**注意**：approval_mode 默认关闭，需显式开启才走新管线

---

## 9. 需求变更记录

| 版本 | 变更 |
|------|------|
| v0.42.0-draft | 仅核心类 |
| v0.42.0 | API + chat 集成 + 16 tests |

---

## 10. PRD 签字页

产品：________  研发：________  测试：________
