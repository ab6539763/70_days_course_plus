# Day 43 授课实录

**09:05** 讲师展示上一轮 Sprint 遗留问题：随着工具越来越多，一个 Agent 要同时懂 FAQ、RAG 检索、意图分类，Prompt 越写越长，维护成本飙升。
**09:30** 白板画出核心方案：引入 Supervisor 模式：supervisor_route 节点先判断问题类型，再委派给 faq_worker / rag_worker / intent_worker 三个专职子 Agent 之一执行，最后 synthesize 节点汇总成统一回复。
**10:10** `SupervisorGraph` live coding。
**10:55** 走读 `agent/supervisor_config.py` 配置类。
**11:20** 第一次跑通 demo，全班确认 `supervisor_trace` 输出正常。

**14:05** demo 案例逐条讲解：「客服电话多少」。
**14:50** 讲 `supervisor_mode=false` 时的兼容路径。
**15:15** `PUT /api/agent/supervisor-config` 现场演示配置变更。
**15:58** pytest 第 17 个全绿。
**16:42** 预告 Day 44：MCP 协议与工具生态。

---

## 问答实录

**15:30 学员**：`supervisor_trace` 会不会让响应体过大？
**讲师**：默认返回，但可以通过配置关闭详细 trace，只保留最终 reply。

**16:05 学员**：关闭 `supervisor_mode` 最快的方式？
**讲师**：`PUT /api/agent/supervisor-config` 把 `enabled` 设为 `false`，无需改代码。

---

## 讲师自评

- SupervisorGraph live coding 时间刚好
- Lab 环节学员普遍能独立完成
- Day 44 预告已铺垫

---

## 课后作业布置原话

「tests/day43/ 必须全绿再提交，作业 A 的案例脚本周日 23:59 前 push。」
