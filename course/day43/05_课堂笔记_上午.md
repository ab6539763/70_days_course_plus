# Day 43 课堂笔记（上午）

**09:00–09:40** 第一节：随着工具越来越多，一个 Agent 要同时懂 FAQ、RAG...
**09:40–10:30** 第二节：agent/supervisor_config.py
**10:30–11:20** 第三节：SupervisorGraph 核心循环
**11:20–12:00** 第四节：与 Day 42 对比走读

---

## 第一节：问题背景（40 min）

随着工具越来越多，一个 Agent 要同时懂 FAQ、RAG 检索、意图分类，Prompt 越写越长，维护成本飙升。

引入 Supervisor 模式：supervisor_route 节点先判断问题类型，再委派给 faq_worker / rag_worker / intent_worker 三个专职子 Agent 之一执行，最后 synthesize 节点汇总成统一回复。

---

## 第二节：SupervisorConfig — max_delegations（50 min）

`agent/supervisor_config.py` 是本日配置的单一真相源，字段随日递增但结构一致：`enabled`、若干阈值/开关、`validate()` 校验。

**10:15** 课堂练习：写出三组非法配置，预测 `validate()` 抛出的异常信息。

---

## 第三节：SupervisorGraph（50 min）

`SupervisorGraph` 是今天的核心调度类，负责把「输入 query → 决策 → 调用工具 → 汇总」串起来，并把每一步记录进 `supervisor_trace`，便于教学演示与审计。

**10:55** 板书：Day42 让单 Agent 可管控；Day43 让多个专职 Agent 协作委派。

---

## 第四节：与 Day 42 对比（40 min）

agent/supervisor_graph.py — supervisor_route → worker → synthesize

**11:40** 强调：Day 42（人工审批工作流）的能力仍然保留，今天是在其基础上新增一层。
